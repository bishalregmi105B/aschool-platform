slug: gps_tracking
min_core_version: \"1.0.0\"
name: GPS Bus Tracking
name_nepali: जीपीएस बस ट्र्याकिङ
category: premium
price_monthly: 299
price_yearly: 2990
coming_soon: false
emoji: 🚌
icon: Bus
version: 1.0.0
description: Live bus tracking via ESP32+NEO-6M, parent ETA alerts, route management,
  stop notifications, speed alerts
depends_on: []
conflicts_with: []
events:
  emits:
  listens: []
capabilities:
  api_blueprint: app.api.v1.transport
  models_module: app.models.transport
  services:
  - app.tasks.gps_firebase_poller
  tasks:
  - app.tasks.gps_processing
ui:
  nav:
    route: /dashboard/transport
    section: Operations
    label: Bus Tracking
    label_nepali: बस ट्र्याकिङ
    icon: Bus
    visible_to:
    - school_admin
    - parent
    - student
    subitems:
    - label: Live Map
      route: /dashboard/transport/map
      visible_to:
      - school_admin
    - label: Routes
      route: /dashboard/transport/routes
      visible_to:
      - school_admin
    - label: Buses
      route: /dashboard/transport/buses
      visible_to:
      - school_admin
    - label: Stops
      route: /dashboard/transport/stops
      visible_to:
      - school_admin
    - label: Trips
      route: /dashboard/transport/trips
      visible_to:
      - school_admin
    - label: Monitor
      route: /dashboard/transport/monitor
    - label: GPS Logs
      route: /dashboard/transport/logs
    - label: Reports
      route: /dashboard/transport/reports
      visible_to:
      - school_admin
  aos:
    about: "Track school buses live on a map with parent ETA alerts, route and stop management, trips, and speed alerts."
mobile:
  admin:
    feature_folder: transport
    tabs:
    - Live Map
    - Routes
    - Buses
  parent:
    feature_folder: bus_tracker
    tabs:
    - Track Bus
    - ETA
    - Stops
schema_version: 2
