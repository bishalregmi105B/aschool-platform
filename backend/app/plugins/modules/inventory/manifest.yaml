slug: inventory
name: "Inventory & Assets"
name_nepali: "सामग्री व्यवस्थापन"
category: growth
price_monthly: 149
price_yearly: 1490
emoji: "📦"
icon: "Package"
description: "QR asset tracking, procurement, depreciation"

api_blueprint: "app.api.v1.inventory"
models_module: "app.models.inventory"

depends_on: []
conflicts_with: []

frontend:
  route: "/dashboard/inventory"
  sidebar:
    section: "Operations"
    label: "Inventory"
    label_nepali: "सामग्री"
    icon: "Package"
    # Single page (asset registry). No subitems: /assets, /procurement and
    # /scan have no pages and rendered as dead sidebar links.
    visible_to: ["school_admin", "staff"]

flutter:
  admin_app:
    { feature_folder: "inventory", tabs: ["Assets", "Procurement", "Scan"] }
