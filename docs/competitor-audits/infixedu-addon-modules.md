# Competitor Audit — InfixEdu Add-On Modules (4 packages)

Audited: 2026-09-12. Source: `/home/bishal-regmi/Desktop/ASchool/Other Projects/InfixEdu School Modules/InfixEdu School Modules/` (inner module zips extracted to `/tmp/audit/` for reading; originals are `jitsi-meet-infixedu-module-1.4_extracted`, `razorpay-payment-gateway-for-infixedu-2.0_extracted`, `infixedu-zoom-live-class-module-2.0_extracted`, `parent-student-registration-for-infixedu-1.0.0_extracted`).

All four are nwidart/laravel-modules packages sold on the InfixEdu marketplace. Each ships `module.json` (service providers) plus a `{Module}.json` install manifest (marketplace `item_id`, explicit migration-file map, owned table `names`, `versions`, support `url`, `notes`). Compare against ASchool's manifest.yaml plugin system (`/home/bishal-regmi/Desktop/ASchool/backend/app/plugins/`).

---

## Deep re-audit (v2) — implementation-level (2026-09-12)

- **v2 status**: covered all four modules end-to-end (every route, every controller method, every migration read line-by-line: Jitsi 1.4 — 5 controllers, 5 migrations, 4 entities, 2 route files, 8 key blades, documentation.txt; Zoom 2.0 — 5 controllers, 6 migrations, 4 entities, 2 route files, config; RazorPay 2.0 — controller, installer migration, routes, demo blade, api stub, help folder; ParentRegistration 1.0.0 — controller (749 lines), 2 migrations, 2 entities, routes, form + approve-email blades, install doc); four end-to-end traces with exact rules; four comparison tables vs ASchool; three designs (live-class stack, public self-registration staging→approve→provision, addon manifest engineering); 25 new V2 findings. Remaining: none for this scope (Zoom `vendor/macsidigital` SDK internals and RazorPay bundled `razorpay/api` vendor lib treated as third-party black boxes).

Method note: Jitsi/RazorPay/ParentRegistration module zips were re-extracted read-only to `/tmp/audit/{jitsi,razorpay,parentreg}/` for this pass; Zoom was read from its already-extracted tree. All `/tmp` and competitor-tree content untouched otherwise; the only file written is this report.

### V2.1 Jitsi Meet v1.4 — line-by-line

Paths: `/tmp/audit/jitsi/Jitsi/` (from `…/jitsi-meet-infixedu-module-1.4_extracted/Jitsi_Meet_Package_InfixEdu/Jitsi_module_infixedu/Jitsi_v1.4.zip`).

**Routes** (`Routes/web.php`): all under `middleware('auth')` + `subdomain`. Gated by `userRolePermission:` — virtual-class store/update/destroy = 818/819/820 (L27-31), meetings store/update/destroy = 823/824/825 (L36-40), settings = 831/832 (L53-54), class reports = 827 (L57), meeting reports = 829 (L58). NOT gated: show/edit (L29-30, L37-38), the four join/start routes (L43-51), `user-list-user-type-wise` (L42). Two route bugs: L18 declares `JitsiController@index` but no such class exists anywhere in the module (dead route); L43 `virtual-class-room/{id}` is wired to `JitsiMeetingController@meetingStart` — the *virtual-class* room route points at the **meeting** controller, so a class join by that path looks up `jitsi_meetings` and 404s/nulls. API mirror (`Routes/api.php` L7-8): `jitsi/virtual-class/{record_id}` + `jitsi/meetings`, `auth:api` only.

**JitsiMeetingController** (`Http/Controllers/JitsiMeetingController.php`, 335 lines):
- `store()` L59-139: validation L87-95 = `member_type` required, `participate_ids` required|array, `topic`/`date`/`time`/`duration` required; `instructor_id`, `description`, `time_start_before` optional/unvalidated. Attached file L76-83: **no mimes/size validation** — `getClientOriginalExtension()` concatenated onto user-controlled `topic`; written to `public/uploads/jitsi-meeting/`. Room id L102: `'meeting_id' => date('ymdhmi')` (10-digit numeric, minute resolution, no school prefix — collides across schools on shared meet.jit.si). `start_time`/`end_time` derived L113-114 from date+time+duration. L120 `participates()->attach`; L121 notification with `member_type` passed as `role_id`. L122 `DB::commit()` with **no `DB::beginTransaction`**. L131-133 unreachable code after `return`. Catch L136 leaks `$e->getMessage()` to the UI.
- `show()` L141-147: `findOrFail` with **no ownership/school check** — any authenticated user renders any meeting's detail.
- `edit()` L149-179: ownership check only for `role_id != 1` (L171-176); L161 `$data['editdata']->participates[0]['role_id']` — **fatals when a meeting has zero participants**.
- `update()` L181-246: validation only topic/date/time (L197-203); **no ownership check at all** (the check lives only in `edit()`); only `role_id == 1` re-attaches participants (L236-239) so a teacher update silently keeps stale ones; L242 commit-without-begin; no try/catch.
- `destroy()` L249-262: returns at L254, making the pivot-cleanup block L256-259 **dead code** — `jitsi_meeting_users` rows orphaned on every delete.
- `meetingStart()`/`meetingJoin()` L264-278: byte-identical duplicates; look up by `meeting_id` string, no participant check, no status check.
- `setNotificaiton()` L280-322: direct `SmNotification::insert` rows per participant (message wording per updateStatus).
- `userWiseUserList()` L324-334: returns `id, full_name, school_id` for every user of a client-supplied `role_id` — no int validation, no throttle (user enumeration).

**JitsiVirtualClassController** (`Http/Controllers/JitsiVirtualClassController.php`, 559 lines):
- `index()` L27-106: teacher branch L65-71 fine; **parent (role 3) branch L74-84** overwrites `$data` with `$data=['virtual_classs']` (wiping `classes`/`teachers` for the view, L78), loops children and re-assigns per child (last child wins, L79-84); the query L82 `where('class_id',$c)->where('section_id',$s)->orwhere('section_id',null)` is **unbracketed and school-less** → `(class AND section) OR section IS NULL` = every null-section class of **every school**. Same leak for students (role 2) at L92.
- `store()` L125-264: teacher co-host limit check L130-136 runs **before** validation and reads `$request->teacher_ids` unconditionally (null-count fatal under PHP 8 if the field is absent); error message says "More Than 2 Person" while the check is `$count > 3` (allows exactly 3). Validation role-split L137-156. Room id L186: `date('ymd' . rand(0, 100))` — the rand is appended to the *format string* (`ymd55`), 1-in-101 same-day collision across the platform. Non-1/5/4 creators attach `Auth::user()` as teacher L203-207. L209-219: null section expands to all sections; L211 `->get('section_id','class_id')` — Laravel `get()` takes ONE columns arg, so `class_id` is silently ignored. Student+parent notification fan-out L221-225 / L491-557 (rows for the student **and** their parent). Teacher notifications L228-251 hardcode `->where('school_id', 1)` (L239) and `$notification->school_id = 1` (L247) — **co-teacher notifications only ever work for school 1** (single-tenant leftover). L255 commit-without-begin.
- `show()` L266-276: `catch (Exception $e)` — no leading backslash and no import, so **the catch never matches** and the body is empty either way (blank page on error).
- `edit()` L278-337: ownership check L285-290; L294 lists ALL virtual classes in the edit view regardless of role.
- `update()` L339-433: L385-402 `updateOrCreate` **regenerates `meeting_id` on every update** (L388) — every join link/route breaks after any edit; `created_by` overwritten to the updater (L400); only role 1 re-attaches teachers (L419-422); notification query reads `$request['class']` (L424) but the form field is `class_id` → **update notifications are silently always empty**; commit-without-begin L430.
- `destroy()` L436-463: file cleanup checks `file_exists($localMeeting->logo)` (L449) — the column is `attached_file`, so **files are never deleted**.
- `classStart()`/`classJoin()` L465-487: identical duplicates, no status/participant check.

**JitsiApiController** (`Http/Controllers/Api/JitsiApiController.php`, 250 lines): `index($id=null)` L16-146 — for roles 2/3 it does `StudentRecord::where('id', $id)->first()` (L80) with **no ownership check and no null check** (fatal on unknown id, then reads `->class_id`); the meeting query L83 repeats the `(class AND section) OR section IS NULL` cross-school leak. `meetings()` L149-249 — teacher branch L153-158 uses `whereHas(participates)->orWhere('created_by',…)`; inside every loop it queries `jitsi_virtual_class_teachers` for **JitsiMeeting** rows (L162, L190, L221 — wrong table, copy-paste) and the `role_id == 1` sub-check (L161 etc.) is unreachable inside role-4/1/else branches. ~150 lines of the status-derivation loop are pasted four times.

**JitsiReportController** (`Http/Controllers/JitsiReportController.php`, 201 lines): `index()` L21-45 — `catch (Exception $e)` unbackslashed + empty body (L41-43): any error yields a null view response. `meetingReport()` L48-72 — `role_id != 1` routes to `meetingSearchOthers()` (L54-55) which has **no self-scoping**: any non-admin can search all meetings by member_type/ids. Private search methods L73-180 duplicate each other with the misspelled `teachser_ids` param (L92, L117, L148, L171, L186); the teacher variant only applies a teacher constraint **if** the client sends `teachser_ids` (L148-152) — omitting it lists everyone's classes.

**JitsiSettingController** (42 lines): `settings()` L12-17, `updateSettings()` L19-39 — validation is only `jitsi_server => required` (L21-23); saved via `JitsiSetting::updateOrCreate(['id' => 1])` (L26-27): one **global** row, no school scoping, lowercased on save (L29). There is no app-id/secret/room-prefix — so "self-hosted" is just a URL swap, and the script tag below loads whatever URL an admin puts there.

**Entities** (`Entities/`): `JitsiMeeting::getCurrentStatusAttribute` L31-66 — reads `$this->is_recurring` / `$this->recurring_end_date` (L41) which **do not exist in the jitsi migrations** (copy-paste from Zoom; `null == 1` makes the branch dead); L36-37 `SmGeneralSettings::where('school_id', Auth::user()->school_id)` + `date_default_timezone_set(...)` — **mutates global PHP timezone on every read and fatals in console/queue context** (no auth user). Non-recurring window L52-64: started = `now ∈ [start − before, end]`, waiting = `now ≤ end + before`, else closed. `JitsiVirtualClass::getCurrentStatus` L65-95 is the same except waiting = `now ≤ end − before` (L82-84) — **inconsistent grace semantics between the two models**. `JitsiVirtualClass::getUrlAttribute` L97-103 returns `https://zoom.us/wc/...` — Zoom URLs inside the Jitsi module (dead/wrong code). `getMeetingDateTimeAttribute` L74-78 reads `date_of_meeting`/`time_of_meeting` — again Zoom column names that don't exist here.

**Migrations (column-by-column)**:
- `2021_03_29_055746_create_jitsi_virtual_classes_table.php` L17-35: `id`, `created_by` int default 1, `meeting_id` TEXT, `start_time`/`end_time` timestamps nullable, `class_id` int nullable, `section_id` **string** (not int), `subject_id` string (never used by the controller), `topic`/`description`/`time_start_before`/`date`/`time`/`datetime` text, `duration` int default 0 (comment "0 means unlimited"), `attached_file` text. No FKs, no unique. Plus L38-43: **mutates host data** — sets `InfixModuleManager('Jitsi')->version = "1.0"`.
- `2021_03_29_060954_jitsi_virtual_class_teachers.php` L17-22: `meeting_id`/`user_id` unsignedBigInteger nullable, timestamps — no FK/unique.
- `2021_03_29_070403_create_jitsi_settings_table.php` (the installer, 280 lines): creates `jitsi_settings` (`id`, `jitsi_server` string default `https://meet.jit.si/`, timestamps, L23-27) and seeds row 1 (L29-31). Then, wrapped in one `try` whose catch is `Log::info` only (L265-267) so every failure is silent:
  - L36-52: rewrites host rows — `InfixModuleStudentParentInfo` id 109 → route `jitsi/virtual-class/child/{id}`, id 110 → `jitsi/meetings/parent`.
  - L54-164: `InfixPermissionAssign` inserts using **hard-coded host ids 816-832** (`module_info` filled from `InfixModuleInfo::find($id)->name` — null-crash if the host id space differs): role 5 admin = 816-830; role 4 teacher = 816-830; roles 7/8/6 = [816, 822, 826]; students (2) = [816, 817, 821]; parents (3) = [108, 109, 110]. **Driver bug** L113-123: the guard checks `where('role_id', 8)` but saves `role_id = 9` — if a librarian row already exists, the driver row is skipped.
  - L167-204: 35 `SmLanguagePhrase` upsert rows (en/es/bn/fr) — including Esperanto `antaŭe` in the **Spanish** column for `before` (L196) and `avant que` in French, empty es/bn/fr for `delete_meetings` (L181), and typos preserved from the module (`meeting_durration`, `Meetting Durration`, `virtaul`).
  - L219-264: creates `Sidebar` rows from `InfixModuleInfo` ids 816-832 (module_id 30 path for non-SaaS, L236-242), deduped by `infix_module_id`.
  - Nothing here is ever reversed: `down()` only drops `jitsi_settings` (L276-279); the permission/language/sidebar rows and the two rewritten routes **leak on uninstall**, and `Jitsi.json` drops only the five tables.
- `2021_03_29_124902_create_jitsi_meetings_table.php` L16-36: `created_by`/`instructor_id` int default 1, `member_type` int, `meeting_id` text, `topic`/`description`/`file` text (file default `""`), `start_time`/`end_time` timestamps, `time_start_before`/`date`/`time`/`datetime` text, `duration` int default 0.
- `2021_03_31_114808_create_jitsi_meeting_users_table.php` L16-21: `meeting_id` int **default 1**, `user_id` int **default 1** — pivot rows default to bogus 1s.

**Join flow / identity spoofing** (`Resources/views/meeting/start.blade.php`, mirrored in `virtualClass/start.blade.php`): L11 loads `external_api.js` from `{{$setting->jitsi_server}}` (admin-controlled URL, no SRI); L17 `getDomainName($setting->jitsi_server)`; L19 `roomName: {{$meeting->meeting_id}}` — **unquoted** in JS (works only because ids are numeric); L26-29 `userInfo: {email, displayName}` injected client-side from `Auth::user()` — trivially edited in devtools since there is **no JWT/token anywhere**; L32-33 `subject`/`avatarUrl` commands. Moderator is whoever joins first (Jitsi default); no per-school room prefix, no lobby, no recording handling of any kind.

**Installer-migration host mutations + manual steps** (`documentation.txt`): permission tree SQL must be inserted by hand — 816 "Jitsi Virtual Class" → 817 "Virtual Class" → 818 Add / 819 Edit / 820 Delete / **821 Start Class**; 822 "Virtual Meeting" → 823 Add / 824 Edit / 825 Delete / 826 Start Meeting; 827 Class Report → 828 Search; 829 Meeting Report → 830 Search; 831 Settings → 832 Update; plus **833** a duplicate "Virtual Meeting" row used only for sidebar guards (`userPermission(833)`). Also: manual paste of `@include('jitsi::menu.jitsi_sidebar')` guarded by `moduleStatusCheck('Jitsi')` into host `sidebar`/`parent_sidebar`/`student_sidebar` blades, and manual creation of `public/uploads/jitsi-meeting`.

### V2.2 Zoom Live Class v2.0 — line-by-line

Paths: `…/infixedu-zoom-live-class-module-2.0_extracted/Zoom/`.

**Migrations (column-by-column)**:
- `2020_06_10_060128_create_zoom_meetings_table.php` L11-71: `meeting_id` string (Zoom's numeric id as string), `password` string (Zoom meeting password, plaintext), `start_time`/`end_time` timestamps, `topic`/`description`/`attached_file`/`date_of_meeting`/`time_of_meeting`/`meeting_duration` **all strings** (dates and duration stored as client-supplied text), `time_before_start` int, `join_before_host`/`host_video`/`participant_video`/`mute_upon_entry`/`waiting_room` booleans, `audio` default 'both', `auto_recording` default 'none' (comment: local|cloud|none), `approval_type` default 0 (0 auto, 1 manual, 2 no registration), `is_recurring` bool, `recurring_type`/`recurring_repect_day` tinyint, `weekly_days` string (comma-joined day ordinals), `recurring_end_date` string, `status` bool default 1, `local_video`/`vedio_link` text (the manual-recording columns), `created_by`/`updated_by`/`school_id` unsignedBigInteger. Installer mutations in the same file: parent-route rewrites for ids 101/103 (L84-99); `InfixPermissionAssign` — role 5 = 554-570, role 4 = 554-567, roles 7/8/6/**9** = [554,560,564] with the **same driver bug as Jitsi** (checks `role_id = 8` for role 9, L135-179), students = [554,555,559], parents = [100,101,103]; ~70 `SmLanguagePhrase` upserts (L232-303) again containing Esperanto in the Spanish column (`antaŭe`, `temo`, `Priskribo`, `Dato de Kunveno`) and wrong French (`sujette`); catch = `Log::info` (L305-308).
- `2020_06_10_134834_create_zoom_meeting_users_table.php`: bare `meeting_id`/`user_id` pivot, no FK/unique.
- `2020_06_16_051034_create_zoom_settings_table.php` L14-30: `package_id` tinyint default 1, five booleans, `audio`/`auto_recording`/`approval_type`, `api_use_for` tinyint default 0 (0 = system account, 1 = per-user keys), `api_key`/`secret_key` strings. **L44-46 seeds every install with the vendor's own JWT credential**: `api_key = 'GsF_U_fzQyuqQ7bMDWBL9A'`, `secret_key = 'l0B0jsyfAXSTAVkYIBF3Jg0DLhZG247ybhOG'` — a working-looking Zoom JWT key/secret shipped to every buyer.
- `2020_06_18_084210_create_zoom_virtual_class_table.php`: same shape + `class_id`/`section_id` strings.
- `2020_06_18_084255_create_zoom_virtual_class_teachers_table.php`: bare pivot.
- `2021_06_30_044055_zoom_update.php` (host-table mutation, irreversible): adds `users.zoom_api_key_of_user` TEXT and `users.zoom_api_serect_of_user` TEXT (**"serect" typo in the schema**, L14-27) and `sm_weekends.zoom_order` int (L28-34); then seeds weekend ordinals Sat=7, Sun=1, Mon=2 … Fri=6 via `SmWeekend::where('name', …)` (L36-72) — **no school_id filter**, so in SaaS it rewrites weekend rows for every tenant; `down()` is **empty** (L74-77).

**Config** (`Config/config.php`): `baseUrl https://api.zoom.us/v2/`, `authentication_method: jwt`, `token_life = 604800` (1 week), `max_api_calls_per_request = 5`. The SDK is `macsidigital/laravel-zoom` (`Zoom::` facade).

**SettingController** (`Http/Controllers/SettingController.php`, 104 lines): `settings()` L15-31 license-gates via `User::checkPermission('Zoom') != 100 → Moduleverify`. `updateSettings()` L33-79: validation L35-47 requires `package_id`, five booleans, `audio`, `auto_recording`, `approval_type`, `secret_key`, `api_key`; `api_use_for` checkbox → 1/0 (L48-52); updates the single global `zoom_settings` row (L54-68) **including re-saving the keys**, then writes the host `.env` (`ZOOM_CLIENT_KEY`/`ZOOM_CLIENT_SECRET`) via `putEnvConfigration()` (L70-71) and runs `Artisan::call('config:clear')` (L72). `putEnvConfigration()` L93-103: `file_put_contents(base_path('.env'), str_replace("KEY=" . env($key), "KEY=" . $value, ...))` — **HTTP-driven .env mutation**, no format validation (a value with a newline corrupts the env file; if the key is absent from `.env`, `env()` is null and the replace silently no-ops). `updateIndSettings()` L80-91: per-teacher keys saved onto the **host `users` row** (`zoom_api_key_of_user`/`zoom_api_serect_of_user`) with no license gate, no key-format check, and no test connection.

**MeetingController** (`Http/Controllers/MeetingController.php`, 665 lines):
- `about()` L28-51: license gate, then L36-39 — **live phone-home telemetry**: `if (date('d') <= 15)` it POSTs `$this->TYPE, $this->User, $this->SmGeneralSettings, $this->SmUserLog, $this->InfixModuleManager, $this->URL` to `User::$api` (Guzzle). Those instance properties are never set on this controller (undefined-property nulls under PHP 8), but the outbound call is executed. (In ParentRegistration the same code is commented out; here it is active.)
- `meetingStart()` L74-98: the **broken guards** — `if (!$meeting->currentStatus == 'started')` (L81) and `if (!$meeting->currentStatus == 'closed')` (L85) evaluate as `(!$status) == 'started'` → `false == 'string'` → always false, so **neither guard can ever fire**: any meeting is joinable at any time through this route (`redirect($meeting->url)` L92). Lookup is by `meeting_id` string with no null check (caught by the blanket catch).
- `store()` L105-246: full Zoom-options validation L109-131 (`durration` typo field; `days` required_if `recurring_type == 2`; `attached_file` mimes jpeg/png/jpg/doc/docx/pdf/xls/xlsx — extension-only check; `date` notably **not** validated though used); weekly recurrence builds `weekly_days` from checked day ids (L133-136, L177-192, Zoom recurrence type 2 = weekly); conflict check L139; **quota guard** L145 `ZoomMeeting::whereDate('created_at', now())->count('id') >= 100` — **global across all schools**; host resolution L151-152 `Zoom::user()->where('status','active')->setPaginate(false)->setPerPage(300)->get()['data'][0]` — always the **first** Zoom account (single-host limitation); `type` = `is_recurring == 1 ? 8 : 2` (L156; 8 = recurring fixed time, 2 = scheduled); remote create L193 → local `ZoomMeeting::create` L203-230 (stores `(string)$meeting_details->id`, password, start/end derived locally, `time_before_start` from `time_start_before` input) → attach participants → notify → commit.
- `show()` L253-283: renders Zoom API `results` (`Zoom::meeting()->find($id)->toArray()` L267) for any id — join URLs, settings, host info — with no ownership scoping.
- `edit()` L290-326: ownership check L306-311; L301 `$data['editdata']->participates[0]['role_id']` fatals on zero participants.
- `update()` L334-481: full validation L337-359; `findOrFail($id)` — no ownership check in the action (only in `edit`); remote update via `Zoom::meeting()->find($system_meeting->meeting_id)->make([...])` + `Zoom::user()->find($profile['id'])->meetings()->save($meeting)` (L376-416); local update L420-446 (both `'password' => $request['password']` L426 and `'password' => $meeting_details->password` L442 — the latter wins); participants re-attached only for role 1 (L461-464).
- `destroy()` L501-525: ownership check L505-510; deletes remote then local; `file_exists($localMeeting->attached_file)` unlink.
- `defaultPageData()` L634-665: teacher branch L646-651 `whereHas(participates)->orWhere('created_by')->where('status', 1)` — AND-precedence means `status=1` applies only to the created_by branch (participants see status-0 rows too).
- `isTimeAvailableForMeeting()` L589-632 (private, identical logic to the API controller — see below).

**ZoomApiController** (`Http/Controllers/ZoomApiController.php`, 1,158 lines — the mobile-app mirror):
- `isTimeAvailableForMeeting($request, $id)` L77-118 — **the exact overlap logic**: participants = `participate_ids` or `[creator]` (L80-84); candidates = `ZoomMeeting::where('date_of_meeting', Carbon::parse($request['date'])->format("m/d/Y"))` (string equality on the **raw client date format**, L87/L95), `school_id = creator's school`, excluding `id` on update (L86-93), `whereHas('participates', user_id IN list)`; then for each candidate `if ($new_time->between(Carbon::parse($meeting->start_time), Carbon::parse($meeting->end_time)))` (L109) — **only the new meeting's START point is tested** against existing windows: a meeting that starts before an existing one and overlaps it is not caught; boundaries are inclusive so back-to-back slots at exactly `end == start` are falsely rejected; if the client sends a different date format the `where` silently matches nothing and the whole check no-ops.
- `zoomStoreMeeting()` L167-303: same store flow as web (validation, conflict L212, quota L217 **global**, host `[0]` L222-223, type 8/2 L227, recurrence L245-252, remote save L253, local create L263-290, attach, notify, commit L293).
- `zoomUpdateMeeting()` L347-485: `ZoomMeeting::findOrFail($request->meeting_id)` (L389) — **no ownership check**; conflict check excludes itself correctly (L391); remote re-save under host `[0]` (L395-428); `created_by` overwritten from client `creator_id` (L457); participant re-attach only if `$creator_info->role_id == 1` (L473-476).
- `zoomDeleteMeeting()` L487-501: `findOrFail($id)`, remote `delete(true)`, local delete — **no ownership check**.
- `meetingStart($id, $user_id)` L503-553 — the second copy of the broken status logic, worse: early-join window **hardcoded at 10 minutes** (`addMinute(-10)` L516/L524, ignoring the stored `time_before_start`); the three `if` blocks are not chained — `'started'`, then `'waiting'`, then an **unconditional `$status = 'closed'`** (L522/L531) so `$status` is ALWAYS `'closed'`; then the dead guards `if (!$status == 'started')` (L534) / `if (!$status == 'closed')` (L537) never fire, so **the API returns the join URL for any meeting at any time**, and the returned `status` semantics are garbage. Start-vs-join URL is the only role check: creator or role 1 → `/start`, else `/join` (L540-544) — that is Zoom's *account-host* concept, not moderation.
- `makeVirtualClass()` L558-600: the ONLY license gate in this controller (`User::checkPermission('Zoom') != 100`, L560-563) — `zoomStoreMeeting`/`zoomUpdateMeeting`/`zoomDeleteMeeting` have none.
- `storeVirtualClass()` L623-784: `$creator_info = User::find($request->created_by)` (L625) — **the acting user is taken from the request body** (identity substitution: any authenticated user can create as anyone, and the school scoping follows that id); admin validation branch L630-652 omits `date` (only the else branch requires it, L661) so an admin-created class with a missing date silently schedules `Carbon::parse(null)` = now; conflict `isTimeAvailableForClass($request, 0)` L687; quota L691 (global); role 1 attaches `teacher_ids` else attaches the creator (L768-772); student+parent fan-out L773-777.
- `isTimeAvailableForClass()` L785-833: scoped to `class_id`+`section_id`+date+school with teacher constraint; L821-822 compute `$strat_time`/`$end_time` and **never use them** (dead), then L824 re-tests with stored `start_time`/`end_time` — so the class path and the meeting path disagree about which window is tested.
- `showClassInfo($id)` L902-911: `VirtualClass::where('meeting_id', $id)` + `Zoom::meeting()->find($id)->toArray()` — full Zoom object for any id, no scoping.
- `ClassEdit($id, $uid)` L913-938: `return $data;` at L923 — **debug leftover returns the raw array (all classes, all meetings, edit form data) before the ownership check below it**, which is therefore dead code.
- `ClassUpdate()` L950-1105: `findOrFail($request->id)` no ownership (L1002); conflict check called as `isTimeAvailableForClass($request, $id = 1)` (L1005) — **excludes meeting id 1 instead of itself**; L1073 writes `'updated_by'` (column exists here, unlike Jitsi).
- `zoomMakeMeeting($id)` L35-66: role-4 branch L46-51 `whereHas(participates)->orWhere('created_by')->where('status',1)` — same precedence bug; `zoomMemberList($user_role)` L67-75: `User::where('role_id', …)->select('id','full_name','username','email',…)` — **no school filter**: any authenticated mobile user enumerates every user (names, usernames, emails) across all tenants.

**VirtualClassController** (`Http/Controllers/VirtualClassController.php`, 858 lines):
- `index()` L34-125: teacher without personal keys when `api_use_for == 1` is force-redirected to settings (L42-47); parent branch L89-100 and student branch L104-112 repeat the `(class AND section) OR section IS NULL` unbracketed, school-less leak (L98/L109).
- `mychild($id)` L127-142: takes any student id (route-gated only by `userRolePermission:101`), no school filter, same leak (L133-137), empty catch.
- `meetingStart()` L143-169: the third copy of the dead guards (`!$meeting->currentStatus == 'started'` L151, `== 'closed'` L155) — join allowed any time.
- `store()` L176-384: admin path takes a single `teacher_ids` and resolves `SmStaff::where('user_id', …)->first()->email` (L207-209) to pass as Zoom `alternative_hosts` when the host account is not type 1/licensed (L273-300) — the one genuinely thoughtful Zoom feature here; null staff → fatal on `->email`. Quota L246; conflict L241 (class/section-scoped variant L811-858, where `teacherList = [$request['teacher_ids']]` L813-814 wraps a single id so **co-teachers are ignored** in the conflict check, and the dead `$strat_time`/`$end_time` vars recur L846-847).
- `update()` L479-653: no ownership check; conflict exclusion `$id = $id` correct here (L541) — the web and API versions disagree (API uses literal 1).
- `fileUpload($id)` L656-667 and **`updateVedio()` L668-708 (the manual recording attach)**: route `POST zoom/upload_document` carries **no permission middleware** (see routes). No ownership check on `$request->meeting_id`; switch on `$request->meetingupload` (`meetingUpload` → `ZoomMeeting`, `classUpload` → `VirtualClass`, L677-681); uploaded `vedio` file has **no mimes/size validation** (L683-692), filename = `$request['topic']` (not in the upload form → null) + time + client extension, saved to `public/uploads/zoom-meeting/`; old `local_video` unlinked; `$system_meeting->vedio_link = $request->link` stored **raw with no URL validation** (L693). The `auto_recording` setting (local/cloud/none) is only a Zoom-side flag — nothing is ever downloaded or synced; recording is 100% this manual attach.
- `destroy()` L714-733: ownership check present; no try/catch — a Zoom API failure 500s after the local row is already deleted? No: remote delete happens first (L724-726), so a remote failure aborts before local delete — orphaned local rows on remote failure, or vice versa if unlinked file fails.

**ReportController** (196 lines): `report()` L21-52 role-split searches; `meetingReport()` L61-71: `role_id != 1` → `meetingSearchOthers()` which has **no self-scoping** (searches all meetings by member_type/ids); `virtaulClassSearchTeacher()` L155-176 applies the teacher constraint only when the client passes `teachser_ids` (L167-171).

**Routes** (`Routes/web.php`): meetings 560/556/562/563 (+ parent :103); virtual-class 555/561 (+ parent :101); reports :565/:567; settings GET :569. **Ungated at route level**: `POST settings` (`updateSettings`, key overwrite), `POST ind/settings` (per-user key save), `POST upload_document` (recording attach) — they rely solely on the auth session. Two naming bugs: `Route::get('user/settings', 'SettingController@userSettings')` points at a **method that does not exist** in SettingController (404/action-not-found), and `meeting-room/{id}`/`virtual-class-room/{id}` are **cross-wired** (meeting-room → VirtualClassController@meetingStart, virtual-class-room → MeetingController@meetingStart). API (`Routes/api.php`): `zoom-make-meeting/user_id/{id}`, `zoom-store-meeting`, `zoom-update-meeting`, `zoom-delete-meeting/meeting_id/{meeting_id}/`, `zoom-meeting-room/…`, `zoom/create-virtual-class/user_id/{user_id}`, `zoom/virtual-class-store`, `zoom-class-info/…`, `zoom-class-update`, `zoom-class-room/…` — with the route file calling `ZoomApiController@zoomMemberLiszt` (**typo; method is `zoomMemberList`** — the member-list endpoint is dead), and `zoom-edit-meeting/.../{uesr_id}` typo param name.

**Entity layer** (`Entities/ZoomMeeting.php`, `VirtualClass.php`): both expose `getCurrentStatusAttribute` with the same timezone-mutating, Auth-dependent accessor pattern (ZoomMeeting L46-77, VirtualClass L49-86 — including commented-out per-occurrence logic for recurring classes) and `getUrlAttribute` (creator/role-1 → `/start`, else `/join`; VirtualClass variant uses role 4/1). Waiting window: `now ≤ end − before` (non-recurring) so there is **no post-end grace**, unlike the Jitsi entities.

### V2.3 RazorPay Payment Gateway v2.0 — line-by-line

Paths: `/tmp/audit/razorpay/RazorPay/` (from `…/razorpay-payment-gateway-for-infixedu-2.0_extracted/RazorPay_InfixEdu_Package/03_RazorPay_Module_v2.0/RazorPay.zip`); help folder `…/RazorPay_InfixEdu_Package/01_help/`.

**Routes** (`Routes/web.php`): `GET razorpay/` (marketing), `GET about`, `GET pay` (demo page), `POST dopayment`, `POST get-order-id` — controller-level `auth` + `PM` middleware only (constructor L23-28); **no license gate** on `dopayment`/`getOrderId` (only `index()` has it, L33-37). `Routes/api.php` is the framework stub — **no mobile flow exists at all**.

**RazorPayController** (`Http/Controllers/RazorPayController.php`, 142 lines):
- `create()`/`show()`/`edit()` L59-72 return `razorpay::create`/`show`/`edit` — **views that don't exist** (only `abc`, `about`, `index`, `layouts` ship) → fatal "view not found" if ever routed.
- `dopayment()` L89-113, the capture path, end to end: `$input = $request->all()`; creates `SmFeesPayment` with `student_id`/`fees_type_id` straight from the client; `discount_amount = 0`, `fine = 0`; **`amount = $request->amount / 100`** (L99 — assumes the client sends paise); `payment_mode = 'RP'`; saves (L102). Then resolves the master via a join on `fees_type_id` + `student_id` → `first()` (L104-106) **with no null check**, then `$fees_assign = SmFeesAssign::where('fees_master_id', …)->first()` (L108) — matched **by fees_master_id alone, not by student** (the join already ties them, but if masters share fees_type the `first()` row can belong to a different student's assignment). Then the accounting bug: **`$fees_assign->fees_amount -= $request->amount`** (L109) — subtracts the RAW paise value while the payment row recorded `amount / 100` rupees: a Rs 5,000 payment (500000 paise) decrements the student's balance by Rs 500,000. There is **no `verifyPaymentSignature`** (grep confirms the SDK's `utility.verifyPaymentSignature` is never called), no order-id check, no server-side amount recomputation, no school/user scoping, no transaction — and the method ends with **`print_r($input); exit;`** (L111-112), dumping the request and killing the response in the production payment path.
- `getOrderId()` L115-141, the order path: fetches per-school `gateway_publisher_key`/`gateway_secret_key` from `SmPaymentGatewaySetting` (L117-120); `$role = User::find($request->user_id)` — **the paying user comes from the request**, not `auth()->user()` (L122); role 2 → SmStudent, role 3 → SmParent, anything else leaves `$user` undefined (fatal, swallowed by the catch into a Toastr redirect); `receipt` = the student's `full_name` (PII as gateway receipt, L129); `amount = $request->razorAmount` raw from the client (L130); `currency = generalSetting()->currency` — the **global** setting in a SaaS context (L131). The order itself is created server-side with the official SDK (L134-135 — the one correct step), but the JSON response is `$razorpayOrder + ['user' => $user, 'secretKey' => $razorPayDetails, 'role' => $role]` (L136) — **the secret-key model row (gateway_secret_key) and the full user object are returned to the browser**. Catch L137-140 converts all errors into a web redirect, useless for any AJAX caller.

**Installer migration** (`Database/Migrations/2020_07_05_125524_create_razor_pays_table.php`, 111 lines): despite the name, **no table is ever created** — `Schema::create` is never called; `down()` drops a nonexistent `razor_pays` (L107-110). The `up()` seeds host rows: SaaS branch loops `SmSchool::all()` (L28-53) creating `SmPaymentMethhod(method='RazorPay', type='Module', active_status=1)` and `SmPaymentGatewaySetting(gateway_name='RazorPay', gateway_username='demo@gmail.com', gateway_password='123456')` **per school — demo credentials shipped to every tenant**; non-SaaS branch (L54-75) does the same but reads `Auth::user()->school_id` **inside a migration** (L56, L66) — under `php artisan migrate` there is no session, so it null-crashes, the outer `catch (\Throwable)` swallows it (L97-99, `Log::info` only), and **nothing is seeded silently**. One `SmLanguagePhrase` row: `pay` → "Pay" (L79-96). Class-import typo `App\SmSChool` (L3).

**Demo page** (`Resources/views/abc.blade.php`, served by `pay()`): Razorpay checkout.js + jQuery 3.3.1; a **TVS keyboard product** with Amazon affiliate image (`images-na.ssl-images-amazon.com/.../31tPpWGQWzL.jpg`, L20), hardcoded `amount=2475` (L24) / `247500` paise (L90), description 'TVS Keyboard', `key: "{{ env('RAZORPAY_KEY') }}"` rendered into the page (L89), and its success handler POSTs only `razorpay_payment_id` to `dopayment` (L75-78) — the live demo of the client-trusted capture.

**Help folder** (`01_help/`): `RazorPay Secreet Key.txt` — "add RAZORPAY_KEY= and RAZORPAY_SECRET= to your .env"; `Database phpMyadmin Screnshot/delete selected table form database.png` — **uninstall is documented as manual phpMyAdmin surgery**; `RazorPay Credetials Screnshot/` walks entering keys in host settings (the module itself has no settings page).

### V2.4 Parent-Student Registration v1.0.0 — line-by-line

Paths: `/tmp/audit/parentreg/ParentRegistration/` (from `…/parent-student-registration-for-infixedu-1.0.0_extracted/ParentRegistration.zip`).

**Routes** (`Routes/web.php` L16-51): one flat group, **no auth middleware anywhere** — public `GET /` (marketing), `/about`, `/registration`, `/get-class-academicyear`, `/get-section`, `/get-classes`, `POST /student-store`; admin `GET/POST /student-list`, `/saas-student-list`, `POST student-approve`, `GET student-view/{id}`, `POST student-delete`, `GET/POST settings`, and the four `check-*` endpoints. The only access control is the **controller constructor**: `$this->middleware('auth'); $this->middleware('PM');` (controller L44-45) applies to every method — so as shipped, the "public" registration form is **login-gated** (guests are bounced to login), while conversely every admin action is protected only by those constructor lines inside the same file as the public routes.

**Constructor** L42-53: also collects telemetry payloads on **every request** — `json_encode(User::find(1))`, `SmGeneralSettings::find(1)` (full school settings incl. mail config), `SmUserLog::find(1)` — into instance vars (L47-49). The Guzzle send is commented out here (unlike Zoom's `about()`).

**Public form chain**: `registration()` L136-149 loads `SmSchool::all()` + `SmClass::all()` (unfiltered), academic years hardcoded `school_id = 1` (L141), genders from `SmBaseSetup` base_group 1, and `SmRegistrationSetting::find(1)`. AJAX: `getClasAcademicyear()` L153-158 returns `[$classes, $academic_years]` with `$classes` **always `[]`** (dead var); `getClasses()` L177-186 matches classes by `created_at LIKE '%<year>%'` (fragile heuristic); `getSection()` L161-173 resolves `SmClassSection` → sections. Form fields (`registration.blade.php`): `school` (SaaS picker), `academic_year`, `class`, `section`, `gender`, `date_of_birth` (d/m/Y), first/last name, student email/mobile, `relationButton` radio F/M/O (L270-278), guardian name/email/mobile, `how_do_know_us`, age.

**`studentStore()`** L189-307 (staging write): validation L198-225 — `class`/`section`/`academic_year`/`first_name`/`gender`/`date_of_birth`/`guardian_name`/`relationButton`/`guardian_mobile` required, `guardian_email` required + **`different:student_email`** (the only cross-field rule); `last_name`, `student_email`, `student_mobile`, `age`, `how_do_know_us` unvalidated; `g-recaptcha-response => required|captcha` appended only when `SmRegistrationSetting::find(1)->recaptcha == 1` (L196-210). Staging row L236-258: every field set individually onto `SmStudentRegistration`; `school_id` written **only** when the Saas module is on (`$request->school`, client-chosen, L254-256); **no server-side duplicate check before save** (the check-* endpoints exist but nothing enforces uniqueness at store time — duplicates are trivially created); no rate limiting. Acknowledgment mail L264-296: `new_reg_email` blade to student + guardian when `registration_after_mail == 1`, sender from global `SmEmailSetting::find(1)`; the mail `catch` **returns the success message** (L292-295).

**Duplicate pre-checks** L609-664: `checkStudentEmail`/`checkGuardianEmail` (identical twins) check `User` (email + school) and staging (student_email OR guardian_email); `checkStudentMobile` checks `SmStudent.mobile` + staging; `checkGuardianMobile` checks `SmParent.guardians_mobile` + staging. All return bare `1`/`0` JSON, are **unthrottled and enumerable**, and take `school_id` from the query string (cross-tenant probing by parameter).

**`studentApprove()`** L391-590 (provisioning, one transaction):
- `DB::beginTransaction()` L395; then **`$request = SmStudentRegistration::find($request->id)`** (L400) — the request variable is overwritten by the model (original input lost; unknown id → null → fatal on `->school_id`, and the catch at L586-589 **never calls `DB::rollBack()`**).
- Number generation: `admission_no = SmStudent::where('school_id', …)->max('admission_no') + 1` (L403-414) and `roll_no = …max('roll_no') + 1` scoped to class+section+school (L405-420) — plain `max()+1` with **no lock/unique** → duplicate numbers under concurrent approvals; `admission_no` is a string column in InfixEdu, so `max()` is lexicographic ("9" > "10").
- Backdates `created_at` to `<academic year>-01-01 12:00:00` for all three rows (L423, L438, L464, L492, L525).
- Student `User` (L427-443): `role_id = 2`, `username = admission_no`, `email = student_email` (may be null), **`password = Hash::make(123456)`** — fixed for everyone.
- Parent `User` (L449-466): `role_id = 3`, `username = guardian_email` or fallback `par-{school_id}-{admission_no}`, same fixed password.
- `SmParent` (L471-494): `user_id`, guardians name/mobile/email, `relation` F→Father / M→Mother / else Other.
- `SmStudent` (L498-533): class/section from staging, `admission_date = today`, links `user_id`/`parent_id`, `admission_no`, `roll_no`, names, gender, DOB, email/mobile, `school_id`, and **`session_id = $request->academic_year`** (L529) — it writes `session_id`, not `academic_id`, so provisioned students may not land in the right academic session.
- Deletes the staging row (L536) and commits (L538) — approval is destructive: **there is no "rejected" state anywhere**; the only outcomes are "row exists (pending)" or "row deleted (done)".
- Credential mail L543-580: `approve_email` blade (subject "Login Credentials", body from host `SmsTemplate` placeholders incl. student/guardian credential templates) when `approve_after_mail == 1`; **mail catch still toasts success** (L576-579).
- No student-cap/seat check of any kind before provisioning.

**Admin surfaces**: `studentList()` L310-314 + `studentListSearch()` L317-344 (school-scoped); `saasStudentList(search)` L350-387 (any authenticated PM user can list **any** institution's applicants); `studentView($id)` L666-672 and `studentDelete()` L593-606 — **no school scoping** on either.

**`Updatesettings()`** L676-748: writes `NOCAPTCHA_SITEKEY`/`NOCAPTCHA_SECRET` into the host `.env` via the same `str_replace(env(…))` pattern (L681-706); settings saved to `SmRegistrationSetting::find(1)` — **one global row** despite `school_id`/`academic_id` columns (L711-715); `position`, `registration_permission`, `registration_after_mail`, `approve_after_mail`, `recaptcha` are only updated `if (isset($request->…))` (L717-735) — unchecked checkboxes are absent from POST, so **toggles can never be turned off** this way; sitekey/secret also stored in the DB row (L737-738).

**Migrations (column-by-column)**: `2020_04_27_061914_create_sm_student_registrations_table.php` L12-49: `first_name`/`last_name` nullable strings; `class_id` int unsigned **FK → sm_classes onDelete cascade**; `section_id` FK → sm_sections cascade; `date_of_birth` date; `age` **string**; `academic_year` int (stores the SmAcademicYear id under a misleading name); `gender_id` FK → sm_base_setups cascade; `student_email`/`student_mobile`/`guardian_name`/`guardian_mobile`/`guardian_email` strings; `guardian_relation` string comment "F father, M mother, O other"; `how_do_know_us` text; `created_by`/`updated_by`/`school_id` int default 1; `academic_id` FK → sm_academic_years cascade; timestamps. **No unique constraints on any email/mobile; no status column.** `2020_04_27_061915_create_sm_registration_settings_table.php` L16-38: `position` int default 1 (comment "1=Header, 2=Footer, 0=hide"), `registration_permission` int default 1 ("1=enable, 2=Disable"), `registration_after_mail`/`approve_after_mail`/`recaptcha` int default 1, `nocaptcha_sitekey`/`nocaptcha_secret` strings, `created_by`/`updated_by`/`school_id` default 1, `academic_id` FK cascade; seed sets `recaptcha = 2` (off, L42-44). **`down()` drops `sm_student_registrations` — the wrong table** (copy-paste, L47-49): the settings table is never dropped and the staging table gets a double-drop attempt.

### V2.5 End-to-end traces (exact rules)

**(a) Jitsi: create → participants → join → status → reports.** Create (meeting): `POST jitsi/meetings` (`userRolePermission:823`) → validate member_type/participate_ids/topic/date/time/duration → optional unvalidated upload to `public/uploads/jitsi-meeting/` → `meeting_id = date('ymdhmi')` → `start_time = date + time`, `end_time = start + duration` → attach participants → one `SmNotification` row per participant ("jitsi meeting is created by X with you"). Create (class): `POST jitsi/virtual-class/store` (:818) → co-host check (bug: >3 not >2, pre-validation) → `meeting_id = date('ymd'.rand(0,100))` → attach 1-3 teachers (hardcoded school-1 teacher notifications) → fan-out notifications to every student of class(+sections) **and** their parents. Join: `GET meeting-start|class-start|meeting-join|class-join/{meeting_id}` (auth only, no participant/status check) → Blade loads `external_api.js` from the admin-set `jitsi_server`, `roomName` = the numeric meeting_id (unquoted), `userInfo` = client-injected email/displayName — **no JWT, no moderator, first-joiner-wins**. Status: purely derived — `started` if `now ∈ [start − time_start_before(default 10), end]`, `waiting` otherwise until `end (+ before` in JitsiMeeting / `− before` in JitsiVirtualClass — the two models disagree)`, else `closed`; accessor mutates the global PHP timezone per read. Reports: `virtual-class-reports` (:827)/`meeting-reports` (:829) filter by class/section/teacher/dates with the `teachser_ids` typo param; non-admin meeting reports are unscoped; `catch (Exception)` (unbackslashed) swallows errors to a blank view. Installer: permission ids 816-832 (+ manual 833) documented column-by-column in V2.1, with the driver role-8-check bug and orphaned sidebar/language/permission rows on uninstall.

**(b) Zoom: credentials → create/conflict/quota/recurring → join → recording → status.** Credentials: migration seeds `zoom_settings` row 1 with vendor JWT key/secret (`GsF_U_fzQyuqQ7bMDWBL9A` / `l0B0…`); admin `POST zoom/settings` (route-ungated) re-saves keys to the global row **and** `.env` (`ZOOM_CLIENT_KEY/SECRET`) via `str_replace(env())` + `config:clear`; per-teacher keys via `POST ind/settings` (ungated) onto host `users.zoom_api_key_of_user`/`zoom_api_serect_of_user`; `api_use_for` selects system vs personal; teacher without personal keys is redirected to settings on the virtual-class index. Create (`zoomStoreMeeting`/`store`): validate the full Zoom options payload (`durration`, `days` for weekly) → conflict check (exact logic in V2.2: same-day string match on `m/d/Y` + start-point-only `between` test over existing `[start,end]`) → **global** 100-meetings-per-calendar-day quota → host = first active Zoom account → remote `type 8` (recurring: `recurrence(type, repeat_interval=recurring_repect_day, end_date_time=endOfDay(recurring_end_date), weekly_days=comma ids)` for type 2/weekly) or `type 2` → local row stores remote id/password/start/end. Join: `zoom-meeting-room/{meeting_id}/user_id/{user_id}` → the triple-broken status derivation (hardcoded −10 window; unconditional `$status='closed'`; `!$status == 'started'` dead guards) → **always** returns `https://zoom.us/wc/{id}/start|join` (start only for creator/role 1). Recording: no webhook/sync anywhere; `POST upload_document` (ungated) → `updateVedio()` attaches an **unvalidated** file to `local_video` or an **unvalidated** link to `vedio_link`; `auto_recording` is a Zoom-side flag only. Reports: `ReportController` searches with the unscoped non-admin path.

**(c) RazorPay: order → capture → ledger.** `POST razorpay/get-order-id` (auth+PM): per-school keys from `SmPaymentGatewaySetting` → `Api(key, secret)->order->create({receipt: full_name, amount: client razorAmount, currency: global})` → **responds with the order + the secret-key model row + the user object**. Browser checkout (host fee-panel or the leftover `abc.blade.php` demo) posts back to `POST razorpay/dopayment` with only `razorpay_payment_id`/amount → `SmFeesPayment` row (`amount/100`, mode `RP`, discount/fine 0) → `SmFeesAssign` (matched by fees_master_id) `fees_amount -= raw paise` → **no signature verification, no transaction, `print_r($input); exit;`**. Installer seeds per-school `SmPaymentMethhod` + demo `SmPaymentGatewaySetting(demo@gmail.com/123456)`; non-SaaS CLI installs seed nothing (silent Auth-in-migration crash); uninstall = phpMyAdmin screenshots; keys also expected in `.env` (`RAZORPAY_KEY/SECRET`) for host-template checkout.

**(d) ParentRegistration: form → staging → pre-checks → approve → settings.** `GET /parentregistration/registration` (constructor-gated despite being "public") → chained AJAX (school → academic year → class → section) → `POST /student-store`: 9 required fields + `guardian_email different:student_email` (+ reCAPTCHA when enabled) → row in `sm_student_registrations` (no uniqueness enforcement, no rate limit, school_id only under Saas) → ack mail (success even on mail failure). Pre-checks: `check-student-email|mobile`, `check-guardian-email|mobile` → bare `1`/`0`, enumerable, school_id from query string. `POST student-approve`: begin → `$request` overwritten by staging model → `admission_no = max+1` per school, `roll_no = max+1` per class-section (no locks) → backdated `created_at` → student User (role 2, username=admission_no, password **123456**) + parent User (role 3, username=guardian_email, 123456) + `SmParent` (F/M/O) + `SmStudent` (session_id=academic_year, admission_date today) → staging row deleted → commit (no rollback in catch) → credential email (success toast even on failure). Settings: `position` (1 header/2 footer/0 hide on the login page), `registration_permission`, both mail toggles, reCAPTCHA keys — global `find(1)` row + `.env` dual-writes, isset() pattern cannot clear toggles.

### V2.6 Comparison tables vs ASchool

**(a) Live-class stack** — competitor behavior | ASchool behavior | delta | verdict.

| Concern | InfixEdu (file:line) | ASchool (file:line) | Delta | Verdict |
|---|---|---|---|---|
| Room naming | `date('ymdhmi')` / `date('ymd'.rand(0,100))` — guessable, cross-school collisions (JitsiMeetingController.php:102, JitsiVirtualClassController.php:186) | `aschool-{school_id[:8]}-{uuid4[:8]}` (video_service.py:21) | ASchool already better (school-scoped + random) | Keep ours; add tenant prefix length review |
| Join auth | None — open rooms, client-injected identity (meeting/start.blade.php:26-29) | None either — only a room id + URL (video_service.py:21-22; conferences.py:115-119 documents the limitation) | Both open; competitor proves the failure mode | **Adopt JWT**: signed room-scoped Jitsi tokens (design V2.7a) |
| Conflict detection | Same-day + start-point-only `between` vs participants; date stored as raw client string (ZoomApiController.php:77-118) | None — `create_live_class` accepts any `scheduled_at` (video_service.py:14-44) | Whole concept missing in ASchool | **Adapt** proper interval-overlap vs timetable (fix their two bugs) |
| Early-join window | `time_start_before` (default 10), derived status (ZoomMeeting.php:52-56) | Manual `start_class`/`end_class` only (video_service.py:46-72) | Missing in ASchool | **Adopt** as `join_window_mins` + derived status |
| Recording round-trip | Manual attach only — unvalidated file or raw link (`updateVedio`, VirtualClassController.php:668-708); no sync | `recording_url` column exists (models/lms.py:104) but nothing writes it; `end_class` just flips status (video_service.py:61-72) | Column present, flow missing | **Adapt** their attach concept with validated upload/link + `recording_status` |
| Recurring series | Zoom type 8 + recurrence map (ZoomApiController.php:245-252); local `recurring_type/repect_day/weekly_days/recurring_end_date` (2020_06_10 migration L43-48) | Nothing | Missing in ASchool | **Adapt** as `series_id` + materialized occurrences |
| API-quota guard | Global 100/day (ZoomApiController.php:217, MeetingController.php:145, VirtualClassController.php:246) | None | Missing; theirs is wrongly scoped | **Adapt per-school** (theirs is global by mistake) |
| Per-user provider creds | `users.zoom_api_key_of_user/_serect_of_user` + `api_use_for` flag (2021_06_30 migration; SettingController.php:80-91) | Per-school gateway creds via config (`fee_config`-supplied per services/payments headers) | Flag concept missing | **Adapt the flag**, store per-teacher Jitsi/Zoom identity in plugin config, not host `users` columns |
| Participant fan-out notifications | Students + parents on create/update (JitsiVirtualClassController.php:491-557) | `events.py` exists but lms emits only class_started/ended (lms/manifest.yaml) | Missing create/update notifications | **Adapt** via `lms.class_scheduled` event |
| Role-granular actions | Separate permissions incl. "Start Class" (documentation.txt: 818-821, 826) | `role_required("superadmin","school_admin","teacher")` (lms.py:252 etc.) | Coarser but centralized | Keep ours; treat "start" as teacher-owned |

**(b) Public self-registration staging→approve→provision vs the admission plugin (A-09)**:

| Concern | ParentRegistration (file:line) | ASchool (file:line) | Delta | Verdict |
|---|---|---|---|---|
| Public form | `/parentregistration/registration` — but constructor `auth`+`PM` gates it as shipped (controller L44-45; web.php L16-51) | `POST /public/<slug>/admission-inquiry`, 4 fields, `@limiter.limit("5/hour;20/day")` (website.py:536-575) | Ours is thinner (4 fields) but genuinely public + rate-limited | **Adapt**: extend to full applicant fields via `AdmissionForm.form_fields` (models/admission.py:24) |
| Staging table | `sm_student_registrations` — FK-cascaded, no unique, no status (migration L12-49) | `AdmissionApplication` with 8-state pipeline `submitted→…→accepted→enrolled` (models/admission.py:35-73) + `inquiry_id` link | Ours has a real state machine; theirs has none (delete = done) | Keep ours; add public submission source flag |
| Duplicate pre-checks | 4 enumerable unthrottled `1/0` endpoints, school_id from query (controller L609-664) | Server-side dup checks at signup only (auth.py:436-441); none on public inquiry | Concept missing | **Adapt** as a rate-limited public endpoint returning a generic "already known" signal |
| Approve→provision | `studentApprove()` transaction: User(role2)+User(role3)+SmParent+SmStudent, max+1 numbers, fixed 123456 (controller L391-590) | `admission.accepted` event → listener auto-creates User+Student with idempotency guard, student-cap check (E2), placeholder-phone fallback, `ensure_student_numbers` under FOR UPDATE lock (listeners.py:691-830) | Ours already strictly better (locks vs race, cap, idempotency, no fixed password) | Keep ours; steal their **parent account** idea (we create only the student User) |
| Numbering | `max()+1` on string column, lexicographic (controller L403-420) | `ensure_student_numbers` serialized per school (listeners.py:798-800) | Ours correct | Keep |
| Credentials delivery | Email with template placeholders; success toast on mail failure (controller L543-580) | SMS says "password shared by administration" (listeners.py:808-820) | Ours avoids mailing secrets | Keep |
| Source analytics | `how_do_know_us` free-text column (migration L34) | `AdmissionLead.source` enum-ish string (models/admission.py:84); inquiry hardcodes `source="website"` (website.py:561) | Their field is on the public form; ours only on leads | **Adopt**: add optional source/how-heard field to the public inquiry |
| Form placement toggle | `position` 1=header/2=footer/0=hide on login page (settings migration L18) | No equivalent concept | UX nicety | **Adapt** as website-config flag, not plugin DB row |

**(c) Addon manifest engineering deltas**:

| Concern | InfixEdu modules | ASchool | Delta / action |
|---|---|---|---|
| Install manifest | `{Module}.json` with marketplace `item_id`, explicit migration-file list, owned table `names`, `versions`, support `url`, `notes` (Zoom.json, Jitsi.json) | `manifest.yaml` with `schema_version: 2`, `capabilities`, `depends_on/conflicts_with`, `owns_tables` (v2 normalization comment, loader.py:46); no migration list | **Adopt** a `migrations:` ordered-file map + ledger (design V2.7c) |
| Installer mutations | Migrations write host rows: routes (Zoom 101/103, Jitsi 109/110), permission rows 554-570 / 816-833, ~70+35 language rows, sidebar rows, `users`/`sm_weekends` columns (both installer migrations) | Hooks may run code (`_run_plugin_hook`, plugins.py:84-102) but manifests own only config rows; no host-table writes | Keep our rule; their failures prove why (silent `Log::info` catches, leaked rows) |
| Demo credentials in installer | RazorPay seeds `demo@gmail.com/123456` per school; Zoom seeds vendor API keys (razorpay migration L44-72; zoom settings migration L44-46) | config_schema `secret` type with encrypted envelope (config_schema.py:208-222) | Keep ours; ban seed secrets in review criteria |
| Uninstall | Drop tables from `{Module}.json` `names`; everything else leaks (orphaned permissions/sidebar/language/routes) | Soft-uninstall, config-row cleanup hook, data kept by design (plugins.py:737-762) | **Adopt `data_retention: keep\|purge`** honoring `owns_tables` on purge |
| Upgrade | Version string poked into host `InfixModuleManager` from a migration (Jitsi virtual-classes migration L38-43); manual SQL dumps for v4.5 | `schema_version` ratchet + `/plugins/<slug>/migrate-config` (loader.py:55-68; plugins.py:997) | Keep ours |
| Sideload / marketplace intake | Zip → drop into `Modules/` → host file edits → manual SQL | Catalog = on-disk `modules/` dir (loader.py docstring); `_catalog_entries()` merges manifest + mirror (plugins.py:127-201); **no upload endpoint** | **Adopt guarded zip sideload** (design V2.7c) |
| Licensing gate | `item_id` + `User::checkPermission($module) != 100` repeated per action, inconsistently (RazorPay: only `index()` gated; Zoom API: only `makeVirtualClass`) | `@plugin_required` + entitlements/billing (decorators.py; entitlements.py) | Keep ours — the competitor's inconsistency is the argument |

### V2.7 Designs for ASchool (following our conventions)

**(a) Live-class stack — `backend/app/services/lms/video_service.py` + `backend/app/models/lms.py` + `backend/app/api/v1/lms.py`.**

Model (extend `LiveClass`, keep `SchoolModel` conventions — UUID pk, `school_id`, `is_deleted`, timestamps):
- Fix the service/model drift first: add columns `ends_at` (DateTime), `started_at`/`ended_at` (DateTime), `join_url` is not needed as a column (derive from `jitsi_room_id`), rename service kwargs to `jitsi_room_id`/`duration_mins`.
- New columns: `join_window_mins` Integer default 10 (the competitor's `time_start_before`, ZoomApiController.php:87); `series_id` UUID nullable (groups recurrences); `recurrence_type` String(20) nullable (`daily|weekly|monthly`), `recurrence_interval` Integer nullable, `recurrence_byday` String(50) nullable (their `weekly_days`), `recurrence_until` DateTime nullable; `recording_status` Enum(`none`,`pending`,`attached`) default `none`; keep `recording_url`.
- lms `config_schema.yaml` (new — see §5.7): `jitsi_domain` (string, default `meet.jit.si`), `jitsi_app_id` + `jitsi_app_secret` (type `secret`, envelope-encrypted per config_schema.py:208-222), `join_window_mins` (number), `max_daily_classes` (number, default 100 — per school, fixing the competitor's global quota).
- Derived status helper (pure function, no global timezone mutation, no Auth dependency — the exact anti-pattern of ZoomMeeting.php:46-77): `scheduled` → `waiting` when `now ≥ scheduled_at − join_window_mins` and not started → `live` when teacher started or window open → `ended` at `scheduled_at + duration_mins`.

Endpoints (all `@jwt_required @school_required @plugin_required("lms")`, matching lms.py style):
- `POST /lms/live-classes` (superadmin/school_admin/teacher): body `title, course_id?, class_id?, section_id?, scheduled_at, duration_mins, join_window_mins?, recurrence{type,interval,byday,until}?`. Conflict rule: reject with 409 when `new.start < other.ends_at AND new.ends_at > other.start` (true interval overlap — fixing the competitor's start-point-only test, ZoomApiController.php:109) against (i) other `live_classes` of the same teacher or same class/section that day and (ii) the teacher's `timetable` plugin slots; quota guard `max_daily_classes` per school per UTC day.
- Recurrence: materialize N child `LiveClass` rows sharing `series_id` (simplest correct model for conflict checks and the mobile list — no rrule evaluation at read time).
- `POST /lms/live-classes/<id>/token` — the leapfrog move: server issues a Jitsi JWT (HS256, `iss = jitsi_app_id`, `aud = jitsi`, `room = jitsi_room_id`, `sub = g.current_user.id`, `name`/`email` from the DB user, `exp = min(scheduled end + 15m, now + 2h)`, `moderator: true` only for the owning teacher/admin). The Blade/Flutter client never receives app credentials; identity can no longer be spoofed via devtools (their start.blade.php:26-29).
- `POST /lms/live-classes/<id>/start` and `/end` (owning teacher): keep manual override, but the derived status above means students see `waiting/live` without any call (their best idea).
- `POST /lms/live-classes/<id>/recording` (owning teacher): multipart file (validate mimetype + size, stored via our file management) **or** `recording_url` (http(s)-validated, same honesty pattern as conferences.py:120-124) → sets `recording_url`/`recording_status="attached"`; emit `lms.recording_attached` so the "Recordings" tabs (lms/manifest.yaml) have data.
- `GET /lms/live-classes` already exists (lms.py:214-236) — extend `_live_class_dict` with `ends_at`, `join_window_mins`, `series_id`, `recording_status`, `derived_status`.

**(b) Parent self-registration staging→approve→provision (A-09).** Do not add an InfixEdu-style parallel table; extend the admission plugin, which already has the pipeline and a strictly better provisioner:
- `POST /public/<slug>/admission-applications` (public, `@limiter.limit`, mirrors website.py:536): validates against the school's active `AdmissionForm.form_fields` (dynamic fields, models/admission.py:24) plus fixed fields `student_name, dob, gender, guardian_name, guardian_phone, guardian_email, guardian_relation(F/M/O→string), class_applied, source`; writes `AdmissionApplication(status="submitted", inquiry_id=None, form_data={...})`; emits `admission.application_received`. No accounts, no credentials — staging with a real state machine (the competitor's staging row with no status/delete=done is the anti-pattern).
- `GET /public/<slug>/admission-check?email=&phone=` → rate-limited, returns a single generic `{"known": true|false}` for **both** student and guardian signals (not four distinct enumerable endpoints, ParentRegistrationController.php:609-664), scoped to the slug's school server-side (never a client `school_id`).
- Approval reuses the existing flow: admin moves application → `accepted` → the `admission.accepted` listener provisions (listeners.py:691-830). One genuine gap to close: we create only the student `User`; add an optional parent `User` with `role="parent"`, username = guardian email (their idea, ParentRegistrationController.php:449-466) — with `generate_default_password()` (never their fixed `123456`) and delivery via the existing admin channel, plus a per-school `registration_permission`-style toggle in the admission config_schema (their `position` header/footer/hide placement idea maps to `school.website_config`).
- Keep our advantages: `ensure_student_numbers` lock (vs their `max()+1` race), `assert_student_cap` (they have nothing), idempotency guard (their approve is destructive — no reject state).

**(c) Addon manifest engineering.** Three deltas to implement in `backend/app/plugins/`:
- `migrations:` map (optional, from `{Module}.json`): `migrations: [{version: 1, tables: [...], script: "migrations/v1.py"}]` under the manifest; the installer (plugins.py `install`) runs them in order and records applied pairs `(slug, version)` in a `plugin_migrations` ledger table — explicitly NOT allowed to touch tables outside `owns_tables` (the rule their installer migrations violate at scale: host routes, permission rows, `users`/`sm_weekends` columns).
- `data_retention: keep | purge` (top-level, default `keep`): `uninstall` (plugins.py:737-762) consults it — `keep` = current behavior; `purge` = drop exactly `owns_tables` + run a `cleanup` hook, closing the "orphaned rows forever" gap without RazorPay's phpMyAdmin surgery.
- Sideload: `POST /plugins/registry/sideload` (superadmin only) — multipart zip; enforce sha256 + size cap; verify an Ed25519 signature file against a pinned publisher key for third-party packages; validate `manifest.yaml` at archive root through the existing `validator.py` (must carry `schema_version: 2`, `owns_tables`, no `migrations` entries outside `owns_tables`); extract to `modules/{slug}/`; then the existing discovery/`refresh-registry` flow works unchanged. This adopts their "drop-in module" model (the only marketplace advantage they have) without host-file edits.

### V2.8 New v2 findings

- **V2-01** (Critical, RazorPay): balance-decrement math is wrong by 100× — `SmFeesPayment.amount = $request->amount / 100` (RazorPayController.php:99) but `SmFeesAssign->fees_amount -= $request->amount` uses raw paise (L109): a Rs 5,000 payment reduces the ledger by Rs 500,000.
- **V2-02** (High, RazorPay): `SmFeesAssign` matched by `fees_master_id` alone via `first()` (RazorPayController.php:104-108) — the payment row names the student, the decrement row may not.
- **V2-03** (Critical, RazorPay): `getOrderId` returns `secretKey => $razorPayDetails` (the secret-key model) and the full user object in the JSON response (RazorPayController.php:136).
- **V2-04** (Critical, RazorPay): no `verifyPaymentSignature` anywhere (grep-confirmed) + `print_r($input); exit;` shipped in the capture path (RazorPayController.php:111-112); capture trusts client `amount`/`student_id`/`fees_type_id` with no authorization that the caller owns the student.
- **V2-05** (High, RazorPay): installer migration creates **no table**, and the non-SaaS branch reads `Auth::user()` inside a migration (L56, L66) — CLI installs crash silently inside the `catch` (L97-99) and seed nothing; `down()` drops a nonexistent table (L107-110). Demo credentials (`demo@gmail.com`/`123456`) seeded per school (L44-52).
- **V2-06** (High, Zoom): route-level gating holes — `POST zoom/settings`, `POST zoom/settings` (updateSettings), `POST ind/settings`, `POST upload_document` carry **no** `userRolePermission` middleware (Zoom Routes/web.php), so any authenticated user can overwrite API keys/.env or attach recordings; `GET user/settings` targets the nonexistent `SettingController@userSettings`.
- **V2-07** (High, Zoom): mobile member-list leaks all users cross-tenant (`zoomMemberList` has no school filter, ZoomApiController.php:67-75) — and its route calls the misspelled `zoomMemberLiszt`, so the endpoint is dead either way (Zoom Routes/api.php).
- **V2-08** (High, Zoom): join-time guards are dead code in three places — `if (!$meeting->currentStatus == 'started')` / `== 'closed'` (MeetingController.php:81,85; VirtualClassController.php:151,155) and the API variant where `$status` is unconditionally overwritten to `'closed'` before the guards (ZoomApiController.php:522,531,534,537) — any meeting/class is joinable at any time via API or web.
- **V2-09** (High, Zoom): conflict detection is start-point-only and format-fragile — `date_of_meeting` compared as a raw `m/d/Y` client string (ZoomApiController.php:87,95) so any alternate date format silently disables the check, and only the new start point is tested `between` existing `[start,end]` (L109); boundaries inclusive (false rejections back-to-back).
- **V2-10** (Medium, Zoom): the 100-meetings/24h quota is **global across all schools**, not per tenant (ZoomApiController.php:217, VirtualClassController.php:246, MeetingController.php:145) — one busy school locks out every other.
- **V2-11** (High, Zoom): HTTP-driven `.env` writes (`putEnvConfigration`, SettingController.php:93-103) + `Artisan::call('config:clear')` from a request (L72); per-user keys saved to host `users` columns with a schema typo (`zoom_api_serect_of_user`) and `sm_weekends.zoom_order` seeded for **all** tenants with an empty `down()` (2021_06_30_044055_zoom_update.php).
- **V2-12** (Critical, Zoom): a working-looking Zoom JWT api key/secret is seeded into every install (`GsF_U_fzQyuqQ7bMDWBL9A` / `l0B0jsyfAXSTAVkYIBF3Jg0DLhZG247ybhOG`, 2020_06_16_051034_create_zoom_settings_table.php:44-46) — a shared credential shipped to all buyers.
- **V2-13** (High, Zoom): API identity substitution — `creator_id`/`created_by`/`user_id` are taken from the request body (ZoomApiController.php:210, 388, 625, 951): any authenticated app user can create/update/delete as (and school-scope to) another user; `zoomUpdateMeeting`/`zoomDeleteMeeting`/`ClassUpdate` have no ownership checks at all (L389, L489, L1002).
- **V2-14** (Medium, Zoom): debug leftovers — `ClassEdit` `return $data;` before the ownership check (ZoomApiController.php:923, making the check dead); `ClassUpdate` conflict check excludes literal id 1 (`$id = 1`, L1005); `showClassInfo` returns full Zoom API objects for any id (L902-911).
- **V2-15** (Medium, Zoom): active phone-home telemetry in `MeetingController::about()` — Guzzle POST of settings/user-log payloads to the vendor API whenever `date('d') <= 15` (MeetingController.php:36-39), distinct from ParentRegistration's commented-out copy.
- **V2-16** (Medium, Jitsi): installer permission seeding has the driver bug (checks `role_id=8`, saves `role_id=9`, settings migration L113-123); relies on hard-coded host ids 816-832 (`InfixModuleInfo::find($value)->name` null-crash if absent); nothing is cleaned on uninstall (down() drops one table; sidebar/language/permission rows and rewritten parent routes 109/110 leak); documentation.txt requires manual host SQL (816-833) and sidebar blade edits.
- **V2-17** (High, Jitsi): functional breakage cluster in `JitsiVirtualClassController` — `update()` regenerates `meeting_id` on every edit (L388), breaking all join links; update notifications read `$request['class']` instead of `class_id` so they are silently empty (L424); `destroy()` checks `file_exists($localMeeting->logo)` (wrong column, L449) so files are never removed; teacher notifications hardcoded `school_id = 1` (L239, L247); `store()` co-host check runs pre-validation on a possibly-null field and allows 3 while warning about 2 (L130-136).
- **V2-18** (Medium, Jitsi): routing bugs — dead route to a nonexistent `JitsiController` (web.php:18); `virtual-class-room/{id}` wired to `JitsiMeetingController@meetingStart` (web.php:43); API `index($id)` uses client `record_id` with no ownership/null check (JitsiApiController.php:80-82); `show()`/`edit()` views ungated.
- **V2-19** (Medium, Jitsi): entity layer is copy-paste from Zoom — accessors read non-existent columns (`is_recurring`, `recurring_end_date`, `date_of_meeting`, `time_of_meeting`), `getUrlAttribute` returns `https://zoom.us/...` inside the Jitsi module, and every status read calls `date_default_timezone_set()` using `Auth::user()` — console/queue fatal + global side effect (JitsiMeeting.php:31-66, JitsiVirtualClass.php:65-103).
- **V2-20** (High, Jitsi+Zoom): cross-tenant class visibility — student/parent listings use `(class_id = X AND section_id = Y) OR section_id IS NULL` with **no school filter** (JitsiVirtualClassController.php:82,92; JitsiApiController.php:83; Zoom VirtualClassController.php:98,109,133-137): every null-section class of every school is listed.
- **V2-21** (High, ParentRegistration): the "public" registration form is not public as shipped — the constructor's `auth` + `PM` middleware applies to every method including `/registration` and `/student-store` (ParentRegistrationController.php:44-45; no route-level exceptions in web.php), while the same constructor collects `User(1)`/`SmGeneralSettings(1)`/`SmUserLog(1)` JSON on every request (L47-49).
- **V2-22** (High, ParentRegistration): provisioning correctness — `studentApprove()` overwrites `$request` with the model (L400), the catch never calls `DB::rollBack()` (L586-589), `admission_no`/`roll_no` are `max()+1` with no lock on a string column (L403-420), `session_id` is written instead of `academic_id` (L529), and the destructive approve has no rejected state.
- **V2-23** (Medium, ParentRegistration): settings defects — `down()` drops the wrong table (`sm_student_registrations`, settings migration L47-49); `Updatesettings()` uses a global `find(1)` row despite per-school columns and an `isset()` pattern that cannot clear toggles (controller L711-735); four duplicate/existence endpoints return enumerable bare `1/0` with client-supplied `school_id` (L609-664).
- **V2-24** (High, ASchool — our own tree): `VideoService` is out of sync with its model — `create_live_class` passes `room_id/join_url/duration_minutes/ends_at` (video_service.py:24-35) but `LiveClass` defines `jitsi_room_id`/`duration_mins` and has no `join_url`/`ends_at`/`started_at`/`ended_at` columns (models/lms.py:93-112), and it sets `status="completed"` (L69) which is not in the enum `scheduled|live|ended|cancelled` (models/lms.py:105-108); no route calls `VideoService` at all (grep: zero callers), so class creation/start/end are unreachable via the API — only `GET /lms/live-classes` exists (lms.py:214-236). Any parity work must first repair this drift (design V2.7a).
- **V2-25** (Medium, ASchool): the lms manifest advertises mobile tabs "Start Live Class" and "Recordings" (lms/manifest.yaml) and conferences documents the open-Jitsi-room limitation (conferences.py:115-119), but there is no token-issuance or recording-attach endpoint anywhere in the backend to back them — the two flows in V2.7a close the promise the manifest already makes.

---

## 1. Per-module findings

### 1.1 Jitsi Meet module v1.4 (marketplace item_id 32973934)

Files: `/tmp/audit/jitsi/Jitsi/` (from `Jitsi_Meet_Package_InfixEdu/Jitsi_module_infixedu/Jitsi_v1.4.zip`).

**What it does.** Two live-class concepts: standalone *meetings* (invite participants by role) and *virtual classes* (scoped to host class+section, up to 2 co-teachers), plus class/meeting reports and a one-field settings page. Rooms are opened inline via `JitsiMeetExternalAPI` (`Resources/views/meeting/start.blade.php` loads `{{setting->jitsi_server}}external_api.js`).

**Host integration (hook points).**
- `Providers/JitsiServiceProvider.php`: standard registerConfig/views/translations + `loadMigrationsFrom`. Routes via `Providers/RouteServiceProvider.php` (web + api groups).
- `Database/Migrations/2021_03_29_070403_create_jitsi_settings_table.php` is really an *installer*: creates `jitsi_settings`, then (a) rewrites routes in host `InfixModuleStudentParentInfo` rows, (b) bulk-inserts `InfixPermissionAssign` rows per role (admin 5, teacher 4, receptionist 7, librarian 8, driver 9, accountant 6, student 2, parent 3) using hard-coded host permission IDs `[816..832]`, (c) inserts ~30 `SmLanguagePhrase` rows (en/es/bn/fr incl. Bengali), (d) creates `Sidebar` rows, (e) branches on `moduleStatusCheck('SaasRolePermission')`.
- `documentation.txt` documents **manual** host edits: paste `@include('jitsi::menu.jitsi_sidebar')` guarded by `moduleStatusCheck('Jitsi')` into host `sidebar`/`parent_sidebar`/`student_sidebar` blades, run the module-info SQL inserts by hand, and create `public/uploads/jitsi-meeting`.
- Uninstall = drop the five tables listed in `Jitsi.json` (`jitsi_settings`, `jitsi_meetings`, `jitsi_virtual_classes`, `jitsi_virtual_class_teachers`, `jitsi_meeting_users`); the sidebar/permission/language rows are never cleaned up.

**Room naming / auth / recording.**
- Room id = `date('ymdhmi')` for meetings and `date('ymd' . rand(0,100))` for classes (`Http/Controllers/JitsiMeetingController.php` `store()`, `JitsiVirtualClassController.php` `store()`). Numeric, guessable, collision-prone (rand 0–100!), no school prefix.
- **No JWT anywhere.** Rooms are open; `userInfo` (email, displayName, avatar) is injected from Blade on the client and trivially spoofable. Moderator = whoever joins first (Jitsi default), not role-based.
- **No recording handling at all.** Status gating is a time window: `started`/`waiting`/`closed` derived from `start_time` minus `time_start_before` minutes.
- Notifications: direct inserts into host `SmNotification` for participants *and their parents* on create/update (`setNotificaiton()`).

**Settings surface.** One field only: `jitsi_server` (default `https://meet.jit.si/`, lowercased on save) — so self-hosted Jitsi is supported by URL swap, but there is no app-id/secret, no room-prefix per school, no per-tenant isolation. The settings controller (`JitsiSettingController.php`) is 42 lines; `updateOrCreate(id=1)` makes the row global.

**Routes** (`Routes/web.php`): `jitsi/meetings` CRUD, `jitsi/virtual-class` CRUD, `virtual-class/child/{id}` (parent view), `meeting-room/{id}` / `virtual-class-room/{id}` join routes, `user-list-user-type-wise` (AJAX participant picker), `virtual-class-reports`, `meeting-reports`, `settings`. API routes cover the same set for the mobile app.

**Quality.** Functional but sloppy: near-duplicate meeting/class controllers, missing `DB::beginTransaction` (commit without begin), unbracketed `orWhere` scoping bugs (teachers can see all meetings), typo-ridden notifications ("Jtis virtual class room details udpated"), empty catch blocks in `JitsiReportController`.

### 1.2 Zoom Live Class module v2.0 (item_id 27623128)

Files: `/home/bishal-regmi/Desktop/ASchool/Other Projects/InfixEdu School Modules/InfixEdu School Modules/infixedu-zoom-live-class-module-2.0_extracted/Zoom/`.

**What it does.** Same meetings + virtual classes duality as Jitsi but backed by real Zoom meetings via the `macsidigital/laravel-zoom` SDK (`MacsiDigital\Zoom` facade; `Config/config.php`: `baseUrl https://api.zoom.us/v2/`, `authentication_method: jwt`, `token_life` 1 week). Adds per-user Zoom credentials, recurring meetings, and a manual recording-upload flow.

**Integration details worth noting.**
- `zoom_settings` table (migration `2020_06_16_051034_create_zoom_settings_table.php`) stores defaults: `host_video`, `participant_video`, `join_before_host`, `audio`, `auto_recording (local|cloud|none)`, `approval_type`, `mute_upon_entry`, `waiting_room`, `api_use_for`, `api_key`, `secret_key`. The migration **seeds a hard-coded API key/secret** (`GsF_U_fzQyuqQ7bMDWBL9A` / `l0B0jsyfAXSTAVkYIBF3Jg0DLhZG247ybhOG`) into every install.
- `Http/Controllers/SettingController.php`: admin settings save keys both to `zoom_settings` **and** to the host `.env` (`ZOOM_CLIENT_KEY`/`ZOOM_CLIENT_SECRET`) via `putEnvConfigration()` (`file_put_contents` + `str_replace`) followed by `Artisan::call('config:clear')`. Per-teacher credentials: `2021_06_30_044055_zoom_update.php` alters the **host** `users` table with `zoom_api_key_of_user` / `zoom_api_serect_of_user` (typo in schema), and also injects a `zoom_order` column into host `sm_weekends`; the `ind/settings` route lets each teacher connect a personal Zoom account (`api_use_for` flag chooses system vs personal).
- License gate: every controller action calls `User::checkPermission('Zoom') != 100` → redirect to host `Moduleverify` route; `about()` reads host `InfixModuleManager`.
- API surface mirrors the web one for the mobile app (`Routes/api.php`, `auth:api` + `XSS` + `json.response` middleware).

**Meeting creation UX (the good parts).** `Http/Controllers/ZoomApiController.php::zoomStoreMeeting()`:
- Validates the full Zoom options payload; maps `is_recurring` → Zoom meeting `type 8` with `recurrence()` (type, repeat_interval, end_date_time).
- **Conflict detection**: `isTimeAvailableForMeeting()` rejects a slot if any participant already has a meeting overlapping on that date.
- **API-quota guard**: refuses the 101st meeting created that day (`ZoomMeeting::whereDate('created_at', now())->count() >= 100`).
- Pulls the first active Zoom user as host (`Zoom::user()->...->get()['data'][0]`) — single-host limitation; creates the meeting remotely, then locally stores `meeting_id`, `password`, `start_time`, `end_time`, attaches participants, and bulk-inserts notification rows.
- Join: redirect to `https://zoom.us/wc/{meeting_id}/start` (creator/admin) or `/join`; `currentStatus` accessor implements the same started/waiting/closed window with `time_before_start` (default 10 min).

**Recording handling.** No webhooks, no cloud-recording sync. Post-class, a teacher can attach a recording manually: `VirtualClassController::updateVedio()` accepts an uploaded file **or** an external link (`vedio_link`, i.e., YouTube), stored under `public/uploads/zoom-meeting/` (`Resources/views/recorder_file_upload.blade.php`). So "cloud recording" setting exists only as a Zoom-side flag; nothing is ever downloaded.

**Routes** (`Routes/web.php`, all under `middleware('subscriptionAccessUrl')` and `userRolePermission:{id}`):
- Meetings: `zoom/meetings` CRUD (`userRolePermission:560/556/562/563`), `meetings/parent` variant (`:103`).
- Virtual classes: `zoom/virtual-class` CRUD (`:555/561`), parent child view (`:101`).
- Reports: `virtual-class-reports` (`:565`), `meeting-reports` (`:567`); settings `:569`.
- Recording upload: `POST upload_document` → `VirtualClassController@updateVedio`; upload pages `virtual-upload-vedio-file/{id}`, `meeting-upload-vedio-file/{id}`.
- API mirror for the mobile app (`Routes/api.php`, `auth:api` + `XSS` + `json.response`): `zoom-make-meeting/user_id/{id}`, `zoom-store-meeting`, `zoom-update-meeting`, `zoom-delete-meeting/meeting_id/{meeting_id}/user_id/{user_id}`, `zoom-class-room/...` — same flows as web, separately maintained.

**Quality.** Most complete of the four, but ~1,000 lines duplicated between `ZoomApiController`, `MeetingController`, `VirtualClassController`; the host-account picker always uses user[0]; scoping bugs (`orWhere('created_by', ...)` unbracketed in teacher queries); `meetingStart()` has broken guard logic (`if (!$meeting->currentStatus == 'started')` — always true-ish, so the "closed" branch is unreachable); SQL to views shows `$results['join_url']` from API responses.

### 1.3 RazorPay Payment Gateway module v2.0 (item_id 27721206)

Files: `/tmp/audit/razorpay/RazorPay/` (from `RazorPay_InfixEdu_Package/03_RazorPay_Module_v2.0/RazorPay.zip`), docs in `razorpay-payment-gateway-for-infixedu-2.0_extracted/RazorPay_InfixEdu_Package/01_help/`.

**What it does.** Adds RazorPay as a fee-payment method for students/parents (SaaS and non-SaaS), using the official `razorpay/api` PHP SDK (vendor bundled in the zip).

**Integration.** The migration `Database/Migrations/2020_07_05_125524_create_razor_pays_table.php` is the installer: when Saas is enabled it loops `SmSchool::all()` and registers `SmPaymentMethhod(method='RazorPay', type='Module')` plus a `SmPaymentGatewaySetting(gateway_name='RazorPay', gateway_username='demo@gmail.com', gateway_password='123456')` **per school**, seeding demo credentials. Keys are then expected in host settings UI (help screenshots `UpdateRazorPay.png`, `RazorPay Enable.png`) and two `.env` vars (`RAZORPAY_KEY`, `RAZORPAY_SECRET`) per `01_help/RazorPay Secreet Key.txt`. License gate identical to Zoom (`User::checkPermission('RazorPay')` → `Moduleverify`).

**Routes** (`Routes/web.php`, no extra middleware beyond the controller's `auth`+`PM`): `razorpay/` (marketing), `about`, `pay` (the demo blade), `POST dopayment`, `POST get-order-id`. `Routes/api.php` is an empty stub — **no mobile-app flow exists**, meaning student/parent app payments for RazorPay were never built (fee-panel checkout is host web code). Help folder confirms manual steps: `Online Doc Link.txt` points to the vendor helpdesk; `RazorPay Secreet Key.txt` instructs adding `RAZORPAY_KEY`/`RAZORPAY_SECRET` to `.env`.

**Security findings (all verified in `Http/Controllers/RazorPayController.php`).**
- `getOrderId()` creates a Razorpay order server-side using per-school `gateway_publisher_key`/`gateway_secret_key` — then returns `response()->json($order + ['user'=>..., 'secretKey'=>$razorPayDetails, 'role'=>...])`: **the secret key is sent to the browser**.
- `dopayment()` records an `SmFeesPayment` (mode `RP`) and decrements `SmFeesAssign->fees_amount` based entirely on client-submitted `amount`, with **no `verifyPaymentSignature`** — payments are trusted client-side.
- `dopayment()` ends with `print_r($input); exit;` — debug code shipped in the payment path.
- `Resources/views/abc.blade.php` is a leftover Razorpay demo page: Amazon affiliate link, hardcoded "Price: 2,475 INR", a TVS-keyboard image, and `key: "{{ env('RAZORPAY_KEY') }}"` rendered client-side.
- The real fee-panel checkout lives in host files (host template edits), which is why the help folder ships phpMyAdmin screenshots (`Database phpMyadmin Screnshot/delete selected table form database.png`) for manual uninstall surgery.

**Quality.** Worst of the four. The module-specific endpoints are unsafe; only `getOrderId` follows the correct order-based flow, and even that leaks credentials.

### 1.4 Parent-Student Registration module v1.0.0

Files: `/tmp/audit/parentreg/ParentRegistration/` (from `ParentRegistration.zip`), plus `Update Files in infix Edu v.4.5/sql/imported.sql` (a full phpMyAdmin dump of host `infix_module_infos` for "upgrading" to InfixEdu v4.5 — upgrades are manual DB imports).

**What it does.** Public, no-auth registration form → staging table → admin review/approve → full account provisioning.

**Flow.**
- Public form `Routes/web.php::/parentregistration/registration` → `Http/Controllers/ParentRegistrationController.php::studentStore()`. Fields: first/last name, class+section (chained AJAX `get-section`/`getClasses`/`get-class-academicyear`, SaaS school picker), DOB+age, gender (host `SmBaseSetup`), student email+mobile, guardian name+relation (`F|M|O`)+email+mobile, and a `how_do_know_us` marketing-source field.
- Optional Google reCAPTCHA (`g-recaptcha-response => required|captcha`) toggled in settings.
- Stored in `sm_student_registrations` (migration `2020_04_27_061914_...`) — no accounts created yet.
- Duplicate pre-checks: `check-student-email|mobile`, `check-guardian-email|mobile` compare against `User` + staging table (return `1/0`; enumerable, unthrottled).
- Acknowledgment email (`new_reg_email`) to student+guardian if `registration_after_mail` enabled.
- Admin reviews `studentList` / `saasStudentList` (institution filter), views a record, then `studentApprove()` in one DB transaction: creates student `User` (role 2, `username = admission_no` = max+1 school-wide, `password = Hash::make(123456)`), parent `User` (role 3, `username = guardian email`, same `123456`), `SmParent`, `SmStudent` (`admission_date = today`, `roll_no` = per class-section max+1), deletes the staging row, and emails credentials (`approve_email`) if `approve_after_mail` enabled.

**Settings** (`sm_registration_settings`, migration `2020_04_27_061915_...`): `registration_permission` on/off, `position` (1=header / 2=footer / 0=hide — controls where the registration widget renders on the login page), both mail toggles, reCAPTCHA keys. `Updatesettings()` dual-writes reCAPTCHA keys into `.env` (`NOCAPTCHA_SITEKEY`/`NOCAPTCHA_SECRET`) via the same `.env` str_replace pattern. Settings are global (`SmRegistrationSetting::find(1)`) despite per-school columns.

**No payment at registration**, no document upload, no email/SMS verification of the applicant. The constructor contains disabled phone-home telemetry (commented-out Guzzle POST of `User`/`SmGeneralSettings`/`SmUserLog` JSON to the vendor API) — worth knowing who you buy from.

**Routes** (`Routes/web.php`): public `GET /parentregistration` (marketing page), `GET /registration` (form), `POST /student-store`; admin `GET/POST /student-list` + `/saas-student-list` (search), `POST student-approve`, `POST student-delete`, `GET student-view/{id}`; the four `check-*` uniqueness endpoints; `GET/POST settings`. Note the admin routes sit in the same unauthenticated group — only the constructor's `auth` + `PM` middleware separates admin actions from public ones, and `studentApprove` reads `id` straight from a POST body.

**Data model** (`Database/Migrations/2020_04_27_061914_create_sm_student_registrations_table.php`): staging row keeps `first_name/last_name`, `class_id`/`section_id` (FK into host `sm_classes`/`sm_sections` with `onDelete cascade`), `date_of_birth` + string `age`, `gender_id` (FK `sm_base_setups`), student email/mobile, guardian name/mobile/email/relation (`F|M|O` comment), `how_do_know_us`, plus `school_id`/`academic_id` for SaaS. `sm_registration_settings` adds `position`, `registration_permission`, `registration_after_mail`, `approve_after_mail`, `recaptcha` + `nocaptcha_sitekey/secret`, seeded with reCAPTCHA off.

**Quality.** The most product-sensible module of the four; the approve→provision pipeline is genuinely useful. Weak spots: fixed `123456` passwords, no verification challenge, enumerable uniqueness endpoints, single global settings row (`find(1)` ignores its own per-school columns), `guardian_email 'different:student_email'` is the only cross-field sanity check, and approval emails ship credentials even when mail sending fails silently (the catch block still toasts success).

---

## 2. Add-on engineering lessons vs ASchool's manifest.yaml system

| Concern | InfixEdu modules | ASchool (`backend/app/plugins/`) |
|---|---|---|
| Packaging | Self-contained folder: routes, controllers, migrations, views, config per module (`Modules/{Name}/`) | Manifest + central code (`modules/{slug}/manifest.yaml`, code in `app/api/v1/`, `app/models/`, `app/services/`) |
| Install | `{Module}.json` migration-file map + host file edits + sometimes manual SQL (`parent-registration .../Update Files/sql/imported.sql`) | `POST /plugins/install` → `install_plugin()` + WP-style `activate` hook (`app/api/v1/plugins.py::install`, `_run_plugin_hook`) |
| Uninstall | Drop tables listed in `{Module}.json` `names`; sidebar/permission/language rows leak forever | `uninstall` hook removes only config rows, keeps data tables (documented in `plugins.py::uninstall`) |
| Upgrade | Version bumps in `{Module}.json` + manual SQL dumps | `schema_version` 1→2 normalization in `loader.py::_normalize_manifest`; `/plugins/<slug>/migrate-config` runner |
| Settings | Ad-hoc table + hand-built Blade page; secrets re-rendered, `.env` writes | Declarative `config_schema.yaml` (18 types, `visible_when`, redaction, migrations) in `config_schema.py` |
| Licensing | `item_id` + per-controller `User::checkPermission($module)` gate + `Moduleverify` route | Entitlement at request layer (`@plugin_required` decorator, `entitlements.py`, `billing.py` trials/subscribe) |
| Dependencies | `requires: []` always empty; migrations reach into host tables (`users`, `sm_weekends`) | `depends_on`/`conflicts_with` enforced in install (`install()` returns 409 on dependency/conflict) |
| Events | None; direct `SmNotification` inserts | `events.py` pub/sub with per-school gating; manifests declare `emits/listens` |

Lessons, concretely:

1. **The `{Module}.json` migration map is a genuinely good idea** — an explicit, machine-readable list of migrations the installer must run and tables the uninstaller may drop. ASchool's manifests have `owns_tables` (v2) but no explicit ordered migration list; adopt the idea under a `migrations:` key if we ever support data-bearing third-party add-ons.
2. **Self-containment is what makes something sellable as an "add-on"** — InfixEdu modules can be zipped, bought, and dropped in. ASchool "plugins" are compile-time modules of one app; that's fine for first-party (50 plugins) but blocks any third-party marketplace. See §5.
3. **Licensing gates must live in one place.** InfixEdu repeats `User::checkPermission(...)` in every action and still leaks (RazorPay endpoints aren't uniformly gated). ASchool's decorator approach is correct — keep secrets/config out of controllers entirely.
4. **Migrations must never mutate host tables or seed demo credentials.** InfixEdu adds columns to `users`/`sm_weekends`, seeds Zoom keys and `demo@gmail.com/123456` payment rows. Our equivalent rule: plugins own only `owns_tables` + config rows.
5. **Uninstall must be reversible.** InfixEdu leaves permission/language/sidebar rows orphaned; RazorPay's documented uninstall is phpMyAdmin. Our config-row-only uninstall is better but should be explicit about retention (see §5).
6. **Shipping quality is part of the product**: RazorPay's `abc.blade.php` demo page and `print_r($input); exit;` in the payment path shipped to paying customers. Review gates for add-ons matter.

---

## 3. Specific features worth stealing

**From Zoom module (highest value):**
- **Per-user gateway credentials**: teacher-owned Zoom accounts (`zoom_api_key_of_user`, `api_use_for` flag) — maps to per-school (or per-teacher) eSewa/Khalti merchant credentials, which we already do (`fee_config`-supplied, see `app/services/payments/esewa_gateway.py` header comment). The steal is the *flag* letting a school say "use system account vs personal".
- **Scheduling conflict detection**: `isTimeAvailableForMeeting()` refuses slots overlapping a participant's existing meeting (`ZoomApiController.php` lines ~85–118). Our `VideoService.create_live_class()` (`backend/app/services/lms/video_service.py:14-44`) does no overlap check — wire it to timetable.
- **Recurring meeting support** (type, interval, end date) for weekly class schedules.
- **API-quota guard** (100 meetings/24h) — we should have a per-school quota guard on live-class creation.
- **Manual recording attach**: end-of-class "upload video or paste link" attached to the meeting record (`VirtualClassController::updateVedio`). Our `Lesson.video_url` exists but is disconnected from `LiveClass`; adding a "attach recording" action after `end_class()` closes the loop for our "Recordings" tabs (lms manifest `mobile.teacher.tabs`).

**From Jitsi module:**
- **Early-join window + derived status** (`time_start_before`, started/waiting/closed): students can join N minutes before; status needs no explicit `start_class` call. We require manual `start_class()`/`end_class()` today.
- **Parent notification fan-out** on class create/update (participants + their parents, with "updated by X with you" wording).
- **Granular per-action permissions** (add/edit/delete/**start class**/search as separate permission rows) — a teacher's ability to *start* a class being its own permission is a nice touch.
- Counter-lesson: their open rooms + client-injected identity show exactly what *not* to do; we should leapfrog with **Jitsi JWT tokens** (moderator claim for teacher) — see §4.

**From Parent-Student Registration module:**
- **Staging table → approve → provision pipeline**: `sm_student_registrations` + `studentApprove()` transaction creating User+Parent+Student with auto `admission_no` (max+1) and per-section `roll_no`, then credential email. Our public admission inquiry (`app/api/v1/website.py:537-575`) stops at a 4-field inquiry row with no conversion to an application/account.
- **Form enable/disable + placement** (`position`: header/footer of login page, 0=hide) as a settings switch — an in-marketplace-config-friendly pattern.
- **Duplicate pre-check endpoints** (email/mobile already known?) with inline AJAX feedback on the form.
- **`how_do_know_us` source field** feeding admission funnel analytics (our admission manifest already emits `admission.lead_created`; we lack the source field on the public form).
- **reCAPTCHA toggle with key management in the module's own settings** — our public form has rate limiting (`@limiter.limit("5/hour;20/day")`, `website.py:538`) which is better enforcement, but no bot-challenge option.

---

## 4. What ASchool lacks vs these modules (with evidence)

1. **No Jitsi authentication or moderation.** `video_service.py` only computes `room_id = f"aschool-{school_id[:8]}-{uuid.uuid4().hex[:8]}"` and a `join_url`; `app/api/v1/lms.py:251` returns `jitsi_room_id`. There is no JWT signing, no moderator/vs-attendee distinction, no identity binding. InfixEdu is equally open (§1.1) — but room naming is the only thing they get right that we do too; everything else they lose on, and we can win on with a `POST /lms/live-classes/<id>/token` endpoint issuing Jitsi JWTs (moderator for teacher, attendee for students, room-scoped, expiring).
2. **No scheduling-conflict detection for live classes.** `create_live_class()` accepts any `scheduled_at`. InfixEdu checks participant overlap across existing meetings (§3).
3. **No early-join window or derived status.** Our status moves only via explicit `start_class`/`end_class` calls; no "students may join 10 minutes early" concept.
4. **No recording round-trip.** `end_class()` sets `status="completed"` and that's it — no upload/link attach, no Zoom cloud-recording webhook sync (InfixEdu doesn't have webhooks either; the Zoom module proves manual attach is the minimum, and this is a differentiator opportunity for us).
5. **No public self-registration → enrollment pipeline.** `auth.py /register` (line 419) registers *schools*; the only public admission surface is the 4-field inquiry (`website.py:537-575`: student_name, guardian_name, phone, class_applied). Missing vs InfixEdu: class/section/DOB/gender/guardian-relation fields, duplicate pre-checks, acknowledgment + approval emails, and the approve→account-provision step with auto admission numbers.
6. **No recurring live classes** (Zoom recurrence model) — weekly timetabled classes must be created one by one.
7. **No per-school live-class quota guard** (InfixEdu: 100/day API-protection limit).
8. **No zip-upload sideload in the marketplace.** `app/api/v1/plugins.py` has install/uninstall/activate/config-schema/migrate-config but nothing accepts an uploaded package; the catalog is strictly the on-disk `modules/` directory (`loader.py` docstring: "the plugins DIRECTORY is the catalog source of truth").
9. **Manifest-only modules carry no migration story.** `owns_tables` exists in v2 manifests, but there is no explicit per-plugin migration map analogous to `{Module}.json`, and the uninstall hook deliberately keeps data with no retention policy option.

---

## 5. How our marketplace could adopt the best bits

1. **Zip sideload (guarded).** Add `POST /plugins/registry/sideload` (admin-only) accepting a zip: validate `manifest.yaml` at archive root via the existing `validator.py`, require `owns_tables`, `schema_version: 2`, a sha256 + size cap, and (for third-party) an Ed25519 signature verified against a pinned publisher key. Extract to `modules/{slug}/` and reuse `PluginLoader` discovery unchanged. This adopts InfixEdu's "drop-in module" model without their host-file edits.
2. **Versioned add-on schema = our manifest v2 + a migration contract.** Add optional `migrations:` (ordered file list) and `data_retention: keep|purge` to the manifest; the `uninstall` hook then either keeps tables (current behavior, documented) or drops exactly `owns_tables`. This is InfixEdu's `{Module}.json` idea folded into YAML, with none of the host-table mutation.
3. **In-marketplace config is already 80% there.** We have `GET/PUT /plugins/<slug>/config`, `/config-schema`, `/migrate-config` (`app/api/v1/plugins.py:826-1042`) and an 18-type schema dialect with secret redaction (`config_schema.py`). Adopt InfixEdu's UX idea: a "Configure" CTA directly on the marketplace card after install (their Zoom/RazorPay settings pages are separate, and Zoom's re-renders `secret_key` in HTML — our redaction already beats this). Add a "test connection" action per plugin (RazorPay's order-creation flow shows why: config is useless until proven).
4. **Keep billing-based entitlement; add an offline key fallback.** InfixEdu's `item_id` + `Moduleverify` + repeated `checkPermission` shows what happens when licensing is scattered; ours is centralized. For self-hosted deployments, accept a signed license key that maps to entitled slugs — same decorator, different resolver.
5. **Marketplace listing metadata**: InfixEdu's `{Module}.json` carries `versions`, support `url`, and release `notes`. Our manifests have `description`/`price` but no changelog URL or screenshots; add `changelog_url`, `screenshots`, `publisher` to the marketplace payload (`_catalog_entries()` in `plugins.py`).
6. **Advertise the event system to add-on authors.** `events.py` per-school gated pub/sub + declared `emits/listens` is strictly better than InfixEdu's notification inserts — make it a documented extension contract (one reason to prefer our marketplace over InfixEdu's).
7. **Ship the live-class gap closers as lms plugin config, not new plugins**: conflict detection, early-join window, recording attach, Jitsi JWT — all fit `backend/app/services/lms/video_service.py` + the lms `config_schema.yaml` (which doesn't exist yet; only attendance/library/fees/hr_payroll/ai_teacher have one).

---

## 6. Cross-module security scorecard (what a vetting process would catch)

| Module | Critical | High | Notable |
|---|---|---|---|
| Jitsi 1.4 | Open rooms, no auth | Client-injected identity | Collision-prone room ids, orphaned install rows |
| Zoom 2.0 | Hard-coded API keys in migration | `.env` writes from HTTP requests; host-table mutation | Secret re-rendered in settings HTML; broken status guards |
| RazorPay 2.0 | Secret key returned to browser; unverified payment capture | Debug `print_r/exit` in prod path; demo page shipped | No mobile API; manual DB uninstall |
| ParentReg 1.0.0 | Fixed `123456` passwords at provisioning | Enumerable existence checks; no verification challenge | Commented-out vendor telemetry; global settings row |

Takeaway: none of the four would pass a basic marketplace review. The engineering bar for ASchool's marketplace should be (a) signed packages, (b) declarative settings with redaction, (c) no host-table writes, (d) verified payment callbacks — we already enforce (b) partially and (d) fully in first-party gateways (`app/services/payments/*`), so codifying it as published review criteria is cheap.

---

## Appendix: evidence index

- Jitsi room naming: `/tmp/audit/jitsi/Jitsi/Http/Controllers/JitsiMeetingController.php` (`'meeting_id' => date('ymdhmi')`), `JitsiVirtualClassController.php` (`date('ymd' . rand(0,100))`).
- Jitsi no-JWT embed: `/tmp/audit/jitsi/Jitsi/Resources/views/meeting/start.blade.php`.
- Jitsi installer-migration: `/tmp/audit/jitsi/Jitsi/Database/Migrations/2021_03_29_070403_create_jitsi_settings_table.php`; manual steps: `/tmp/audit/jitsi/Jitsi/documentation.txt`.
- Zoom settings/`.env`/per-user keys: `.../Zoom/Http/Controllers/SettingController.php`, `.../Zoom/Database/Migrations/2021_06_30_044055_zoom_update.php`, seeded keys in `2020_06_16_051034_create_zoom_settings_table.php`.
- Zoom conflict check + quota: `.../Zoom/Http/Controllers/ZoomApiController.php`.
- Zoom manual recording upload: `.../Zoom/Http/Controllers/VirtualClassController.php::updateVedio`, `.../Zoom/Resources/views/recorder_file_upload.blade.php`.
- RazorPay secret leak / unverified payment / debug print: `/tmp/audit/razorpay/RazorPay/Http/Controllers/RazorPayController.php`.
- RazorPay installer migration: `/tmp/audit/razorpay/RazorPay/Database/Migrations/2020_07_05_125524_create_razor_pays_table.php`; demo page: `/tmp/audit/razorpay/RazorPay/Resources/views/abc.blade.php`.
- Parent registration pipeline: `/tmp/audit/parentreg/ParentRegistration/Http/Controllers/ParentRegistrationController.php` (`studentStore`, `studentApprove`, `Updatesettings`); migrations `2020_04_27_061914/061915_*.php`; manual upgrade SQL: `.../parent-student-registration-for-infixedu-1.0.0_extracted/Update Files in infix Edu v.4.5/sql/imported.sql`.
- ASchool counterparts: `backend/app/services/lms/video_service.py`, `backend/app/api/v1/lms.py:251`, `backend/app/api/v1/plugins.py`, `backend/app/plugins/loader.py`, `backend/app/plugins/config_schema.py`, `backend/app/plugins/events.py`, `backend/app/services/payments/{esewa,khalti,fonepay}_gateway.py`, `backend/app/api/v1/website.py:537-575`, `backend/app/api/v1/auth.py:419`, `backend/app/plugins/modules/{lms,admission,conferences}/manifest.yaml`.
