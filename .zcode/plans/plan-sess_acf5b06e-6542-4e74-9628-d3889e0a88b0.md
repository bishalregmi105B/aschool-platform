Deep-research-then-implement, starting now (no more planning docs):

1. FRONTEND DEEP DIVE FIRST: enumerate every page in frontend/app (215 routes). For EACH page an agent reads the code and answers: what feature does this page claim to provide → does it actually perform it end-to-end (real fetches to live endpoints, real state handling, real actions working)? Fix/complete every page that fails this bar (backend route missing → implement it; wired wrong → rewire; stub → make real).
2. Same deep treatment for backend (every route file: does each endpoint actually compute/return real data with correct logic) and the Flutter apps (every screen: does it really work against the live backend).
3. Then cross-cutting: duplicated plugins merged/removed, Nepal-specific correctness (BS dates, NPR, NEB grading, Nepali SMS/IEMIS) everywhere, production hardening, e2e role journeys, full regression + final audit/scorecard.
Everything runs via small agents (one-by-one), every fix runtime-verified in the live docker stack, FIX_STATUS/coverage files kept current, conventional commits as waves complete.