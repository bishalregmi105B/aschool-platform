# FIX STATUS — 2026-08-28 (against current source; `git diff HEAD` uncommitted changes included)

Baseline: `audits/DISCOVERED_SYSTEM_INVENTORY.md` (E1-E9, G1-G2, M1-M12). HEAD = `df584be`.
Status legend: FIXED-COMMITTED / FIXED-UNCOMMITTED / PARTIAL / OPEN.

## 1. Entitlement (E)

| Finding | Status | Evidence (current source) | Note |
|---|---|---|---|
| E1 registration grants hardcoded 6 plugins for every plan | **FIXED-UNCOMMITTED** | `backend/app/plugins/entitlements.py` — `PLAN_PLUGIN_TIERS` map (free→core+add_on; starter→+starter; growth→+growth; enterprise→+premium) is the single source of truth; `auth.py:302-315` registration calls `grant_plan_plugins()` which iterates the Plugin catalog by tier category (no hardcoded list; failures logged, not swallowed) | Runtime ✅: fresh registrations free=13/starter=22/growth=43/enterprise=57 active installs, cumulative by tier (growth gets hr_payroll+lms, enterprise adds premium). |
| E1b free plan gets paid `fees` as trial | **FIXED-UNCOMMITTED** | `entitlements.py grant_plan_plugins()` creates SchoolPlugin rows with `is_trial=False, trial_ends_at=None, next_billing_date=None`; registration no longer calls `billing.install_plugin()` at all → zero trial rows at signup; free tier set contains no paid category | Runtime ✅: 0 trial rows across all 4 fresh test schools; free school's paid-granted list is empty. |
| E2 `School.plan` write-only decoration | **FIXED-UNCOMMITTED** | Plan now drives entitlements (`entitlements.py`, used by registration) + `School.max_students` enforced server-side: `students.py:126-129` create → 403; real bulk path `iemis_importer.py` `_import_students` pre-loop check raises `StudentCapExceededError` → 403 in `run_import()`; NULL/0 cap = unlimited; **third path capped too: `admission.accepted` auto-enrollment now calls `assert_student_cap()` before creating any User/Student row (see E11)** | Runtime ✅: cap=2 → 201,201,403 "Student limit reached…"; NULL and 0 → create OK; update-only bulk import with cap=1 not blocked; admission listener cap=1 + 1 enrolled → nothing created, cap=1 + 0 enrolled → student auto-created (E11 tests). Note: students.py has no bulk route — bulk enrollment lives in iemis import. **Full Student( site audit (backend, non-test): `students.py:145` capped ✅, `iemis_importer.py:421` capped ✅, `plugins/listeners.py` admission auto-enrollment capped ✅ (E11), `seed_test_data.py:475` dev seed script (not a production path) — no uncapped creation site remains.** |
| E3 alias leak elibrary→design_studio | **FIXED-UNCOMMITTED** | `backend/app/plugins/decorators.py:13-20` — `design_studio` alias entry removed; `_acceptable_plugin_slugs` (:28-46) now single-hop only (requested slug + its direct alias + direct reverse aliases; no chaining). Mirror updated in `frontend/lib/plugins.tsx:55-92` (entry removed + non-transitive resolver). Regression tests flipped in `backend/tests/test_plugin_aliases.py`: design_studio resolves to only itself; elibrary install → 403 on `/api/v1/design-studio/bulk/id-cards` (was 400-via-leak). | ✅ runtime (flask container): `_acceptable_plugin_slugs("design_studio")` = `{design_studio}`; elibrary-trial school gets 403 `GET /api/v1/design-studio/data-sources` and 200 `GET /api/v1/elibrary/books`. Remaining aliases verified against live `plugins` table: `communications/hr/transport/visitors` have no published row at the old slug (pure legacy renames → safe); `library`⇄`library_management` same name "Library Management", same price (clean rename); `digital_content`⇄`elibrary` same name "E-Library & Digital Content" but **price/tier-crossing (growth 499 vs starter 299) — kept (same feature, digital_content's manifest points at the elibrary blueprint so its installers need the alias) and flagged for pricing reconciliation**; `design_studio` (growth 499, "Design Studio") confirmed a distinct published plugin — mapping removed. |
| E4 trials never expire | **FIXED-UNCOMMITTED** | New `backend/app/tasks/trial_expiry.py` — `@celery.task(name="expire_trials")` deactivates `SchoolPlugin` rows with `active=True & is_trial & trial_ends_at < now` (sets `uninstalled_at`, deletes per-school plugin cache); imported in `app/tasks/__init__.py` for autodiscovery; beat entry `plugin-trial-expiry-hourly` (`crontab(minute=0)`, hourly) added to `celery.conf.beat_schedule` in `backend/app/__init__.py` (15 schedules now). Defense-in-depth in `_set_school_context` (`backend/app/__init__.py:298-331`): request path excludes trial-expired rows from `g.installed_plugins` even while `active=True` (tz-safe naive→UTC). Uncommitted blocklist changes in the same file preserved. | ✅ runtime (flask container): imports proven for `app.plugins.decorators`, `app.tasks.trial_expiry`, `app.tasks`, `app`; test school with active elibrary trial backdated `trial_ends_at` −1h, `active=True` → `GET /elibrary/books` 403 from request-path gate alone; `expire_trials.run()` → `{'expired': 1}`, row flipped `active=False` + `uninstalled_at` set; gated call still 403 after task. Beat+worker containers restarted: live worker `inspect registered` includes `expire_trials`; beat config has `plugin-trial-expiry-hourly → crontab 0 * * * *`. Test rows cleaned up. |
| E5 subscribe collects no money; Stripe secret undefined | **FIXED-UNCOMMITTED** | `config.py` now defines `STRIPE_WEBHOOK_SECRET` (env-sourced, default empty; uncommitted JWT/cookie changes preserved); `app/api/webhooks/__init__.py` returns **400 + logged error** when the secret or `stripe` package is missing (was 500) and marks Stripe-verified `checkout.session.completed` activations `is_trial=False` + billing_cycle from metadata; `app/api/v1/plugins.py subscribe` now returns **402 without payment proof** (`{"payment":{"provider","transaction_id"}}`) and stores the reference on `SchoolPlugin.config["last_payment"]` — never silently marks paid | Caller survey (backend, frontend/lib, aschool_shared, flutter apps): only `tests/test_marketplace_billing.py` called `/subscribe`, no checkout flow exists → chose require-proof + 402 (documented in docstring); test updated to pin 402-then-activate. No new Invoice model (per scope); `next_billing_date` still unchecked by any expiry logic (E4 territory). Runtime: unconfigured webhook → 400; billing suite 3 passed. |
| E6 seed.py pricing reads nonexistent nested keys | **FIXED-UNCOMMITTED** | `backend/seed.py` now reads the real flat manifest schema (`price_monthly`/`price_yearly`/`is_free`/`category`) and idempotently SYNCs existing Plugin rows' price/tier/category so a stale DB self-heals on every seed; `seed_full.py` untouched except a one-line comment pointing to manifests as source of truth | Runtime in `aschool-flask-1`: run 1 = `57 plugins (0 created, 19 synced)`; Plugin table now matches manifests — fees 399/3999 starter, admission 599/5999 growth, gps_tracking 1999/19999 premium, website_builder 1499/14999 premium, ai_tutor 1499/14999 premium; run 2 = `0 synced` (idempotent); `trial_days` not clobbered (ai_tools still 7). |
| E7 AI services bypass token hub | **PARTIAL** | Bypassing (direct anthropic): `services/ai/lesson_plan.py:14-15,72`, `question_paper.py:14-15,85,110-112,120`, `auto_grader.py:14-15,60`, `homework_helper.py:14-15,60`, `school_insights.py:20-21,112`, `website_designer.py:5,73-74,124-125` | `timetable_solver.py` no longer an AI bypasser — rewritten as pure constraint-satisfaction solver (no anthropic import). The other 6 still skip `AITokenHub` quota/usage accounting. |
| E8 Hostel/FAQ no tenancy | **FIXED-COMMITTED** | `models/hostel.py:8,13,27,32,54,59` — Hostel/HostelRoom/HostelAllocation each have `school_id` FK, nullable=False, indexed; `models/faq.py:11` same; queries scoped: `api/v1/hostel.py:68,99,126,162,190`, `api/v1/faqs.py:30,46,76,89` | Commit `0339893` (tenant isolation) + pinned by `0eb20ce` isolation tests. |
| E9 OTP promised but unsent; phone_verified unconditional | **PARTIAL** | `auth.py:283-286` — `phone_verified=True` still set unconditionally; uncommitted diff removed the fake OTP step: registration now returns tokens directly (`auth.py:310-322`), no docstring lie | Deliberate design change (direct login, optional OTP via `/send-otp`/`/verify-otp` at `auth.py:80,98`). Phone still marked verified with zero verification. |
| E10 IEMIS import creates `User(phone=None)` → NOT NULL violation, rows error out | **FIXED-UNCOMMITTED** | `users.phone` is `String(20) nullable=False` (`models/user.py:44`), no unique constraint (parent lookup is by `school_id`+`phone`). `iemis_importer.py` now has `_placeholder_phone(school_id, key)` — deterministic `9800000xxxx` (blake2b of school+source row; bumped until free within the school; reserved 9800000000–9800999999 block). Student user creation uses it instead of `None`; staff/teacher creation falls back to it when the row has no phone; users are marked `permissions={"placeholder_phone": True}` and keep `phone_verified=False`. Same fallback applied to the admission listener's `User(phone=parent_phone or None)` (`listeners.py`, owned file) which had the identical NOT NULL bug. | Runtime ✅ (flask container, dev DB + tests): phoneless fixture row → user created (`imported:1, errors:0`), phone `98000000035` matches `9800000\d{4}`, marked True, no collision with parent lookup; empty `parent_phone` admission → user created with marked placeholder. Tests: `tests/test_provisioning_and_caps.py` (3 phone tests). **Discovered, NOT fixed (out of scope): `_import_teachers` passes `User(settings={…})`/reads `existing.settings` but `User` has no `settings` column → TypeError on every staff row (caught at the per-row except → errors list). Staff import needs that fixed separately.** |
| E11 admission.accepted auto-enrollment bypassed the student cap | **FIXED-UNCOMMITTED** | `app/plugins/listeners.py` `on_admission_accepted` now imports `assert_student_cap`/`StudentCapExceededError` from `entitlements.py` (same helper as E2) and checks BEFORE creating the User/Student; on cap violation it logs a warning, posts an in-app notification to `school_admin` (category `admission`, data.type `student_cap`) and creates nothing. | Runtime ✅ (flask container): school cap=1 with 1 existing student → emit → still 1 student, no new User, warning "Student limit reached: … plan allows 1 students and 1 are already enrolled"; cap=1 with 0 enrolled → student auto-created with user_id + phone set. Pinned by `tests/test_provisioning_and_caps.py` (2 cap tests). Site audit folded into E2 row. |
| E12 new school got NO AI quota row → all hub AI calls 429 ("inactive") | **FIXED-UNCOMMITTED** | `AITokenHub._check_quota` treats a MISSING `ai_school_quotas` row as blocked (`QuotaExceededError("inactive", 0, 0)`) — there is no None=unlimited default; the only provisioning was the manual `POST /ai-usage/quota/init` (`ai_usage.py:76-84`). `auth.py` `register_school` now calls `AITokenHub.ensure_quota_exists(str(school.id))` eagerly after plugin grants (env defaults `AI_DEFAULT_DAILY_LIMIT=10,000` / `AI_DEFAULT_MONTHLY_LIMIT=100,000`, `config.py:91-92`), failure logged not fatal. `token_hub.py` docstrings on `_check_quota`/`ensure_quota_exists` document the blocked-by-default semantics + provisioning contract. **SMS: no equivalent needed — no per-school SMS-quota model exists; usage is derived from `SMSLog.cost` sums (`sms.py sms_stats`) and the balance is provider-side (`SMSGateway.check_credits()` → Sparrow account)** (documented in auth.py comment). Note: superadmin-created schools (`schools.py:92`) still rely on lazy `quota/init` — out of scope (auth.py hook only), flagged. | Runtime ✅ (flask container, dev DB): `POST /auth/register` 201 → `ai_school_quotas` row exists (daily 10000 / monthly 100000 / active) for the new school id; rows cleaned up after. Pinned by `tests/test_provisioning_and_caps.py::test_registration_provisions_ai_quota`. Suites: new file 6/6 passed; `test_ai_token_hub.py` + `test_auth.py` 19/19 passed (no regression). |
| E13 staff IEMIS import crashed on nonexistent `User.settings` column | **FIXED-UNCOMMITTED** | `_import_staff` (`backend/app/api/v1/iemis_importer.py`) passed `User(settings={…})` on create and read/wrote `existing.settings` on update — `User` has no `settings` column (`models/user.py`: only `permissions` JSONB + `address` JSONB), so every staff row errored into the per-row except → import always returned errors. Fixed: IEMIS staff metadata (`iemis_teacher_id`/`iemis_designation`/`iemis_teaching_subject`) now lives in `User.permissions` (the de-facto per-user settings bag — `auth.py:487-490,516-519` already stores `totp_secret`/`mfa_enabled` there and names the local var `settings`), merged with (not clobbering) the `placeholder_phone` marker from E10; update path merges into `existing.permissions`. **Second crash on the same site, found + fixed:** the "create a Staff record too" block imported `app.models.staff.Staff` which is now an ALIAS of `User` (`models/staff.py`), so `sa_inspect` saw User columns, built `User(school_id=…)` only (no `full_name`/`role` NOT NULL values) and the batch `commit()` would IntegrityError — the try/except can't catch a commit outside it. Guarded: the Staff-record block only runs when `StaffModel is not User`. | Runtime ✅ (flask container, dev DB): 2-row import (one phoneless) → `imported:2, errors:0`; phoneless row → user created, role=staff, deterministic placeholder phone `98000005236`, `permissions={'placeholder_phone': True, 'iemis_designation': 'Assistant', …}`; re-import with email → update path `skipped:1, errors:0`, permissions merged without losing keys; zero bare duplicate User rows (alias guard). `User.settings` grep = 0 remaining. Suites `test_plugin_aliases.py` + `test_marketplace_billing.py` + `test_provisioning_and_caps.py` 11/11 green; imports clean. |
| E14 `digital_content` ⇄ `elibrary` = same feature published twice at different prices | **FIXED-UNCOMMITTED** | Both manifests are named "E-Library & Digital Content", share `models_module: app.models.digital_content`, and the elibrary frontend (`/dashboard/elibrary/*`) calls `/elibrary/books` etc. — one feature, listed as growth NPR 499 (digital_content) AND starter NPR 299 (elibrary): pricing confusion + revenue leak. End-state: ONE published product = **elibrary** (299). Mechanism (loader.py stores manifest keys verbatim; catalog = `Plugin.is_published` rows): added top-level `published: false` + `deprecated: true` to `modules/digital_content/manifest.yaml` (module manifests take precedence over legacy flat ones in loader) + mirrored to `manifests/digital_content.yaml` for the fallback path; `seed.py` now syncs `published` → `Plugin.is_published` for new AND existing rows (self-heals a stale DB; seed run synced exactly 1 row). `digital_content`→`elibrary` alias in `decorators.py` PLUGIN_SLUG_ALIASES deliberately KEPT (rename compat): a school with an old digital_content install still passes `plugin_required("elibrary")`. `install_plugin` already refuses unpublished slugs, so no new installs. Not deleted: manifest stays so legacy installs keep sidebar/blueprint resolution. | Runtime ✅ (flask container): Plugin rows `digital_content.is_published=False, elibrary=True`; marketplace query excludes digital_content (56 listed); LIVE `GET /api/v1/plugins/marketplace` (superadmin JWT) → digital_content absent, elibrary present (299.0, starter); `install_plugin("digital_content")` → refused "not found or not available"; test school with only a digital_content install passes the elibrary gate (`_acceptable_plugin_slugs("elibrary")` = {digital_content, elibrary}; decorator probe 200) and `g.installed_plugins` includes the slug. Suites 11/11 green (alias, marketplace billing, provisioning). **Duplicate-pair scan verdicts:** `incidents` (premium 499, base reporting) vs `incident_management` (growth 399, "Full behavior management", `depends_on: [incidents]`, distinct escalation/reports frontend gated `PluginGate("incident_management")` while all API routes gate `incidents`) = **genuinely two tiers of one chain, NOT duplicates — both stay published**. `portfolio` (growth 399) vs `student_portfolio` (growth 299): same name, same `api_blueprint: app.api.v1.portfolio`, same models `app.models.portfolio` — **TRUE duplicate, RESOLVED this session (same unpublish+alias mechanism): canonical = `student_portfolio` (growth NPR 299)** because every consumer surface gates it: web `PluginGate slug="student_portfolio"` (`frontend/app/dashboard/portfolio/page.tsx`), `mobile.py` visibility rules gate `student_portfolio` (portfolio + achievements), and all four flutter apps + aschool_shared screens pass `pluginSlug: 'student_portfolio'` — backend was the only surface saying `portfolio` (split-brain: neither install alone fully worked). Reconciliation: all 6 routes in `api/v1/portfolio.py` renamed to `plugin_required("student_portfolio")` so the canonical gate covers the whole route set (they share one blueprint, so the union was already identical); `portfolio` manifests (`modules/portfolio/manifest.yaml` + legacy `manifests/portfolio.yaml`) got `published: false` + `deprecated: true` → seed.py sync maps to `Plugin.is_published=False` (marketplace lists only student_portfolio; `install_plugin` refuses unpublished). `portfolio`→`student_portfolio` alias KEPT in `decorators.py` PLUGIN_SLUG_ALIASES, mirrored in `frontend/lib/plugins.tsx`, so legacy portfolio installs keep passing. Runtime ✅ (flask container): Plugin rows `portfolio.is_published=False (399), student_portfolio=True (299)`, marketplace = 55 listed with portfolio absent; `_acceptable_plugin_slugs("student_portfolio")` = {portfolio, student_portfolio}; decorator probe with `g.installed_plugins={"portfolio"}` ONLY → 200 on a student_portfolio-gated route (canonical-only install also 200); seed run twice → `0 created, 0 synced` both times (idempotent; DB already self-healed); `pytest tests/test_plugins.py tests/test_plugin_aliases.py -q` = **19 passed** incl. `test_legacy_portfolio_install_passes_canonical_gate`. |

## 1f. PAYMENT-GATEWAY callback batch — verification log (2026-08-29)

Method: throwaway fixture school with real fee-collection + partial-payment state driven through the Flask test client in `aschool-flask-1` (`backend/tmp_payments_verify.py`, kept for reruns; self-sweeps its leftovers at start): eSewa via locally-computed HMAC-SHA256 callback blobs (per the implemented eSewa ePay v2 spec), Khalti/FonePay server-verify APIs monkeypatched (no real network), Stripe via hand-built `t=...,v1=...` headers matching the installed SDK (`WebhookSignature` HMAC over `"%d.%s" % (ts, payload)`, tolerance-aware). Scenarios per gateway: (a) valid, (b) tampered, (c) duplicate, (d) unknown, (e) already-paid, (f) amount mismatch. Baseline probe **proved 6 real money bugs**, then fixes were applied and the probe re-run to **60/60 twice**; fixtures fully deleted afterwards. Pinned by new `backend/tests/test_payment_webhooks.py` **16/16**; required selection `pytest tests/ -q -k "payment or esewa or khalti or fonepay or webhook"` = **23 passed**; adjacent money suites (fees math/summary/discount/student/payment-methods/billing/ops-guards/models) = **33 passed** on the reset `aschool_test` schema (stale create_all-once schema predated `payment_initiations`; drop+recreate of `public` in `aschool_test` — same environmental reset as E15).

| ID | Severity | Finding (baseline runtime-proven) | Fix | Runtime evidence |
|----|----------|-----------------------------------|-----|------------------|
| **E60** | **P0** | **Every initiate route handed the gateway a DEAD callback URL**: `fees.py _initiate_online_payment` built `success_url`/`return_url` as `{base}/api/v1/webhooks/{gateway}/callback`, but `webhooks_bp` is registered at `/webhooks` (no `/api/v1` prefix) — url_map has no `/api/v1/webhooks/*`. So after a parent ACTUALLY PAID at eSewa/Khalti/FonePay, the gateway redirected to a 404 and the money was never recorded. | `fees.py` now builds `f"{base_url}/webhooks/..."` for all three gateways; pinned by `test_initiate_esewa_uses_registered_webhook_path_and_persists` (asserts the success_url ends `/webhooks/esewa/callback` and `/api/v1/webhooks` appears nowhere). | Probe: `initiate esewa success_url is the REGISTERED /webhooks path` PASS; live url_map shows all three `/webhooks/{gateway}/callback` routes. |
| **E61** | **P0** | **No server-side record of what was initiated** and **FonePay callbacks could never resolve the fee collection**: the callback guessed `data.get("purchase_order_id") or data.get("R1")` — a real FonePay callback carries neither (R1 is the remarks string "School Fee - \<name\>"), so a fully-valid HMAC-signed + server-verified FonePay SUCCESS callback left the collection `pending` (probe baseline) — money taken, never marked. | NEW `PaymentInitiation` model (`app/models/fee.py`: collection_id/gateway/gateway_ref/amount/status initiated→completed\|failed/initiated_by/completed_at) + migration `b7e2c9d4a5f3` (chained on head `d4e5f6a7b8c9`, applied in `aschool-flask-1`) persisted BEFORE the redirect in `_initiate_online_payment` for all three gateways (eSewa ref=transaction_uuid, Khalti ref=pidx from the API response — honest 502 with NO row when Khalti initiation fails, FonePay ref=PRN). FonePay callback now resolves the collection ONLY via the PRN→initiation anchor (school-mismatch cross-checked); unknown PRN → honest 404 "payment NOT recorded, contact the school office". Bonus fix: the Khalti `refund` route read a nonexistent `fc.gateway_pidx` column and `fee_config["khalti"]` (credentials actually live in `fee_config["payment_methods"]`), so refund always 422ed — now resolves the pidx from the latest Khalti initiation + real method config. | Probe E2E: initiate → `PaymentInitiation` row exists pre-redirect with amount 9000 → callback with that PRN → collection `paid`, exactly 1 receipt → duplicate PRN idempotent; unknown PRN → 404, nothing recorded. Pinned by `test_fonepay_initiate_persists_prn_then_callback_pays_once` + `test_fonepay_unknown_prn_404_records_nothing`. |
| **E62** | **P0** | **Callbacks were not idempotent**: a duplicate callback (same gateway transaction) created a SECOND, zero-amount `FeeReceipt` (receipt_number inflation, `fee.paid` re-emitted), and — worse — a duplicate callback for a PARTIAL payment re-applied the same transaction (`[partial_paid:5000]` → `[partial_paid:10000]` on a 9000 fee with ONE gateway charge = money counted twice). `FeeReceipt.idempotency_key` (unique, indexed) existed but was never used. | Shared `_finalize_fee_payment()` in `webhooks/__init__.py` now runs every fee callback through one ordered gate: existing receipt with the same `(school, collection, method, transaction_id)` → idempotent 200 `{duplicate: true}` (receipt id echoed); completed initiation → same; failed initiation → 409; applied payments write `idempotency_key = webhook:{gateway}:{collection}:{txn}` (SHA-256-folded if >100 chars) with a flush-race guard; commit exactly once; `fee.paid` emits the RECORDED amount (was the raw gateway amount, double-emitting full amounts on duplicates). | Probe: eSewa duplicate → still 1 receipt, paid-note 9000, `duplicate:true`; duplicate PARTIAL callback (5000/9000) → note stays 5000.0, 1 receipt; second legit partial 4000 settles to paid exactly once; post-settlement different txn → 409. Pinned by `test_esewa_valid_then_duplicate_marks_paid_once`, `test_esewa_second_partial_payment_settles_then_conflict`, fonepay/khalti duplicate assertions. |
| **E63** | **P0** | **Khalti pidx-swap attack accepted**: the callback trusted the query-string `purchase_order_id` to pick the collection and credited it with ANY pidx Khalti reported `Completed` — an attacker paying NPR 1 for their own collection could point that pidx at a victim's 9000 fee (probe baseline: 200, victim credited partial 1.00). | `KhaltiGateway.verify_payment` now returns the lookup response's `purchase_order_id` and the callback REJECTS (400, loud log) any pidx whose gateway-echoed purchase_order_id does not match the resolved collection. | Probe `pidx-swap (attacker pidx → victim collection)` → 400, victim still `pending`, 0 receipts (was 200 + credit). Pinned by `test_khalti_pidx_swap_rejected`. |
| **E64** | **P1** | **Amount mismatches silently accepted**: callback amounts were recorded as `min(amount, outstanding)` — a signed callback for 12000 on a 9000 fee was silently capped+recorded as paid (probe baseline), and any underpay silently became a partial regardless of what was actually charged at initiation. | With an initiation anchor (E61), the callback amount must equal the initiated charge within 0.01 else **409, nothing recorded** (loud error log); with no anchor (legacy in-flight checkouts), an amount ABOVE outstanding is now **409** (impossible from our own initiation) instead of a silent cap; below-outstanding without an anchor still records honestly as partial (documented intent). | Probe: underpay-vs-initiated 5000/9000 → 409 + 0 rows; overpay-no-anchor 12000/9000 → 409 + 0 rows; legacy 5000/9000 → partial 5000.0 (intent); Khalti/FonePay mismatch paths identical. Pinned by `test_esewa_amount_mismatch_vs_initiation_rejected`, `test_esewa_overpay_without_anchor_rejected`. |
| **E65** | **P0** | **Stripe webhook success path could never work**: `stripe_webhook` used `SchoolPlugin` without importing it (NameError → 500 on every valid `checkout.session.completed`), AND the `stripe` package was not in `requirements.txt` (so even with `STRIPE_WEBHOOK_SECRET` set the handler always answered 400 "Stripe integration unavailable" — masking the 500). E5's fix only ever tested the unconfigured path. | `from app.models.plugin import SchoolPlugin` added inside the handler; `stripe>=5.0,<6.0` added to `requirements.txt` and installed in `aschool-flask-1`; signature verified against the installed SDK (hand-built `t,v1` headers, tolerance respected — the probe's first fixed-2023 timestamp was rejected as outside tolerance, which itself proves verification is real). | Probe: valid signed event → 200, `SchoolPlugin` activated `is_trial=False` (E5 semantics), billing_cycle honored; replay + second event → still 1 row, no duplicates; wrong-secret signature → 400; malformed payload → 400; unconfigured secret → 400 (E5 regression held). Pinned by `test_stripe_valid_event_activates_plugin_paid_once` + `test_stripe_bad_signature_rejected`. |
| **E66** | **P2** | Two smaller honesty gaps: (1) a Khalti lookup network failure crashed the callback into a 500 (`resp.json()` unhandled); (2) the cookie-session CSRF guard would 403 any POST webhook arriving with aschool cookies, and webhooks are signature-authenticated, not cookie-authenticated. | (1) `KhaltiGateway.verify_payment` catches request/JSON errors → `{verified:false, network_error:true}` → callback answers honest **502** with nothing recorded (FonePay `_server_verify` likewise flags `network_error` → 502); (2) `csrf_protect_cookie_auth` exempts `request.path.startswith("/webhooks/")` (cookie-origin logic has no meaning for HMAC-authenticated gateway servers). | Probe: patched lookup outage → 502, collection untouched (pinned by `test_khalti_lookup_network_failure_honest_502`); POST fonepay callback with an authenticated session cookie in the jar → no longer 403. |

- **Gateway verdicts**: eSewa ✅ (after E60/E62/E64 — HMAC verified locally, foreign `product_code` now rejected), Khalti ✅ (after E60/E62/E63/E64/E66 — server-side lookup + purchase_order_id cross-check), FonePay ✅ (after E61/E62/E64 — signature + server-verify + PRN anchor; previously the flow NEVER recorded money), Stripe ✅ (after E65 — signature-verified, idempotent activation, honest 400s when unconfigured).
- **FE/parent-app completion hooks verified against url_map**: web collect page `POST /fees/collections/<id>/pay-online` (exists), parent Flutter `POST /fees/initiate-payment` (exists) → WebView/launchUrl of the gateway → gateway redirects to the now-registered `/webhooks/{gateway}/callback` → status picked up by the parent app's fees refetch (no separate verify endpoint by design). Shared `fee_repository.dart` (`/fees/initiate-payment/{id}`, `/fees/pay`) remains dead code (M6 note stands, unused by the parent app).
- **Known residuals (honest)**: eSewa callbacks with the SAME transaction_uuid but a DIFFERENT ref after a completed initiation return idempotent `duplicate:true` without recording — if eSewa ever completed two charges for one uuid the second charge needs manual reconciliation (409 fires only when no completed initiation anchors the uuid, e.g. legacy checkouts); `payment_initiations` rows are not expired by any job (stale `initiated` rows are inert — anchors only, never charged by themselves); Stripe events other than `checkout.session.completed` are acked without action (by design); webhook `fee.paid` emit failures are still swallowed (pre-existing).

## 2. Document generation (G)

| Finding | Status | Evidence | Note |
|---|---|---|---|
| G1 report-card PDF discarded; fake pdf_url | **FIXED-UNCOMMITTED** | `backend/app/tasks/report_generation.py` — `generate_report_card_pdf` now persists the WeasyPrint bytes through the canonical storage helper (`app.utils.file_upload.upload_file` → `LOCAL_UPLOAD_DIR` locally / R2 in prod) and stores the real served URL on `ReportCard.pdf_url` (HTML fallback when WeasyPrint missing). Fix pre-dated this batch on disk but was still marked OPEN; re-verified here. | Runtime ✅ (2026-08-29, aschool-flask-1, throwaway school cleaned up after): task run → `pdf_url=/uploads/reports/<school>/report_card_<student>_<exam>.pdf`; GET that URL → 200, `%PDF-` magic, 9,767 bytes. |
| G2 EMIS CSV built then discarded | **FIXED-UNCOMMITTED** | `report_generation.py export_emis_data` now uploads the CSV (`upload_file`, folder `compliance/<school_id>/`) and stores `file_url` + `export_data["file_key"]`; `api/v1/compliance.py download_emis_export` streams it (send_file locally, presigned redirect on R2). Same provenance as G1. | Runtime ✅ (2026-08-29, platform probe): `export_emis_data.run()` → `file_url` set; gated `GET /compliance/emis/<id>/download` → 200 streaming CSV (`Class,Grade,Total,Male,Female,…`). |

## 3. Mobile (M) — aschool_shared + 5 apps

| Finding | Status | Evidence | Note |
|---|---|---|---|
| M1 NotificationService.init never called (push dead) | **OPEN** | `aschool_shared/lib/services/notification_service.dart:24` defines `init()`; zero references in any of the 5 apps' `lib/` (grep = 0) | FCM/OneSignal tokens still always null; `register-fcm` still no-ops. |
| M2 notification center unrouted | **OPEN** | `aschool_shared/lib/providers/notification_provider.dart`, `widgets/notification_center_screen.dart` exist; no app screen/route references them | Unread-count still unused. |
| M3 HR split-brain paths | **FIXED-UNCOMMITTED** | Verified against live url_map (flask container): blueprint `url_prefix="/hr"` (`hr_payroll.py:21`); GET `/hr/payroll` (:78, role-restricted superadmin/school_admin/accountant), GET `/hr/payroll/<uuid>/payslip` (:216), GET `/hr/leave`+`/hr/leaves` (:378/:394, no role gate), POST `/hr/leave` (:402). No `/hr-payroll/*` and no `/hr/payroll/slips` anywhere in url_map. Fixed: `aschool_shared/lib/repositories/hr_repository.dart` now calls GET `/hr/payroll`, GET `/hr/leaves`, POST `/hr/leave` (envelope `{success,data,error}` + DioException→`ApiException{statusCode}`); `aschool_shared/lib/models/hr.dart` field names aligned to backend `_payroll_dict` (:705) — `allowances_total`/`deductions_total`/`paid_at` (old code read nonexistent `total_allowances`/`total_deductions`/`payment_date`), numeric-string-safe `_num()`, added `staff_name`; `flutter_teacher/lib/features/payroll/payroll_slips_screen.dart` now uses the repo + real fields and renders 403 as an explicit message instead of a blank list | Admin `hr_payroll_screen.dart` (`/hr/payroll`,`/hr/leave`) and teacher `leave_screen.dart` (`/hr/leave` GET+POST, fields incl. `days`) verified already matching url_map — untouched. Note: no teacher-facing "my payslips" route exists server-side, so teacher-role users on the payroll screen get the explicit permission message (the payslip-by-id route is role-open but ids are undiscoverable for them). dart analyze: aschool_shared 0 issues; flutter_teacher 0 issues in touched files. |
| M4 admin marketplace install/uninstall 404s | **FIXED-UNCOMMITTED** | url_map confirms: only POST `/plugins/install` (body `plugin_slug`, returns 201; `plugins.py:133`) and POST `/plugins/uninstall` (body `plugin_slug`; `plugins.py:350`) — no `/{slug}/install`, no DELETE `/{slug}/uninstall` (only `/{slug}/trial|subscribe|config`). Fixed `flutter_admin/lib/features/marketplace/marketplace_screen.dart` to install/uninstall via new shared `aschool_shared/lib/repositories/plugin_repository.dart` (POST body `{plugin_slug, billing_cycle:"monthly"}` / `{plugin_slug}`), success snackbar added, backend `error` message surfaced instead of generic "Operation failed". Also fixed response parsing: backend marketplace serializes `price_monthly`/`price_yearly` via `float()` → `399.0`; `PluginManifest.fromJson`'s `as int?` cast threw on every paid plugin — replaced with num/string-safe `_priceToInt` (`plugin_manifest.dart`) | dart analyze: flutter_admin 0 issues in touched files. |
| M5 AI path split-brain | **FIXED-UNCOMMITTED** | url_map: only `/ai-tools/{question-paper, lesson-plan, timetable, timetable/save, remarks, homework-help, insights/weekly|daily-brief|risk-alerts}` (`ai_tools.py`, prefix `/ai-tools`); no `/ai/{tool}/generate` anywhere. New shared `aschool_shared/lib/repositories/ai_repository.dart` maps each tool to its real route + request fields: question-paper (subject, grade, total_marks, duration_minutes, [topics]), lesson-plan (subject, grade, topic, [duration_minutes]), remarks (student_name, marks, total, percentage → `data.remark`), timetable (academic_year_id), GET insights/weekly. `flutter_admin/lib/features/ai_tools/ai_tools_screen.dart` + `flutter_teacher/lib/features/ai_tools/teacher_ai_screen.dart` rewritten: per-tool structured input forms hit real routes and render actual service shapes (paper `{sections:[{questions:[…]}]}`, plan `{learning_objectives, lesson_structure,…}`, remark string); auto_grader/study_tips (no backend route) now honestly disabled with pointers instead of guaranteed 404; PluginGate slug corrected `ai_insights`→`ai_tools` (matches `plugin_required("ai_tools")` + `manifests/ai_tools.yaml` slug). Student `/ai-tools/homework-help` verified matching (sends `question`, reads `data.response`) — untouched. Also fixed `flutter_admin/lib/features/dashboard/principal_dashboard.dart`: `/ai-tools/daily-brief` → `/ai-tools/insights/daily-brief` (404 before) and brief text now built from the real `{date, events, total_students}` shape (old code read nonexistent `summary`, so the card always showed "Loading…") | dart analyze: flutter_admin + flutter_teacher 0 issues in touched files. |
| M6 parent fee initiate-payment mismatch | **FIXED-COMMITTED** | `flutter_parent/lib/features/fees/fee_payment_screen.dart:79` `POST /fees/initiate-payment` (body `fee_ids`) matches backend `api/v1/fees.py:1344-1355`; payment-methods :30 matches | Commit `3920609` "eSewa checkout now completable from parent app". Note: shared `fee_repository.dart:31,69` (`/fees/pay`, `/fees/initiate-payment/{id}`) still has no backend route — dead code, unused by parent. |
| M7 plugin config plumbing fake | **FIXED-UNCOMMITTED** | Backend needed no change: `GET /plugins/installed` (`plugins.py:126`) already returns `"config": sp.config or {}` per row (also `PUT/GET /plugins/<slug>/config`). Flutter wired: `models/plugin_manifest.dart` — `InstalledPlugin` gains `config` field (M9-safe `is Map` parse, included in `toJson` so it persists in the secure-storage cache); `services/plugin_provider.dart getConfig()` now returns the plugin's real config (null only when plugin unknown; `{}` when install has no config) instead of `const {}`. Real call-site: `flutter_admin/lib/features/marketplace/marketplace_screen.dart:207` — detail sheet reads `pluginProvider.getConfig(slug)` and renders the `last_payment` audit record (the one payload the backend writes to config, E5 subscribe). | ✅ Runtime (flask container): admin JWT for school `70e19451…` → `PUT /plugins/attendance/config` 200 → `GET /plugins/installed` 200, 6 rows, row `attendance` carried `{'quick_actions': ['homework','attendance'], 'banner_text': 'Welcome'}` (config restored to `{}` after). `dart analyze` aschool_shared: No issues; flutter_admin: 0 issues in touched files (16 pre-existing infos elsewhere). |
| M8 offline_sync dead code | **FIXED-UNCOMMITTED (deleted)** | Chose DELETE. Evidence: grep `offline_sync|OfflineSync|offlineSyncProvider` across aschool_shared/lib + 5 apps = 1 hit, the barrel export (`aschool_shared.dart:9`), 0 call sites in any app; docstring claimed "Uses Isar" but `isar` absent from `aschool_shared/pubspec.yaml` (queue was actually in-memory `_queue = []` — no persistence at all, so the service was both dead and dishonest); no offline-sync product requirement anywhere (grep "offline" in README/audits hits only this finding); `connectivity_plus` in shared pubspec was consumed only by this file. `offline_sync.dart` deleted + barrel export removed; repo-wide re-grep = 0 references. Note: `connectivity_plus: ^6.0.3` now unused in `aschool_shared/pubspec.yaml` (left — pubspec outside this fix's file scope; harmless). | dart analyze aschool_shared clean post-delete. |
| M9 unsafe casts | **FIXED-UNCOMMITTED** | Shared helpers live in `aschool_shared/lib/utils/safe_parse.dart`: `safeString/safeInt/safeDouble/safeNum/safeBool` (+ `OrNull` variants), `safeList/safeMapList/safeMap(OrNull)`, `safeDateTime`, envelope unwrappers — ids accept String|int, numerics accept int|double|String, lists/maps degrade to `[]`/`{}`, malformed shape logs via `logJsonShape`, nothing throws. Remaining bare `as (String|int|double|bool|num|List|Map)` in aschool_shared models+repos: 16 → 8, and all 8 are nullable `as String?` in `models/hr.dart` already fixed by the M3 pass (left per scope); `models/user.dart` + `models/timetable_slot.dart` converted. Screen-level raw-map casts in the 5 apps: 235 → 4; the 4 are `as List<Map<String,dynamic>>` / `as List<ClassModel>` on locally-built provider maps whose runtime types are guaranteed by construction (providers build them via `safeMapList` / repo-typed lists), so they cannot throw. 47 screens across all 5 apps converted to tolerant coercion (mechanical sweep + hand-fixed chained-fallback sites: offline_exam subject_results chain, report-card/GPA `??` chains, spread `.map` patterns). | dart analyze: aschool_shared No issues; all 5 apps 0 errors + 0 warnings (remaining findings are pre-existing style infos — verified identical by kind against HEAD `df584be` and none sit on modified lines; flutter_user fully clean). Sample-checked 3 models against real serializers: `Student` vs `models/student.py to_dict` (`id`/`class_id` = str-or-None, `roll_number` int → safeString/safeIntOrNull), `Exam` vs `exams.py _exam_dict` (`subject_ids` list, `total_marks` int, ISO dates), `Fee` vs `fees.py` (`amount` = `float(...)` → double, exactly the shape that made `as int`/`as num` throw) — field types match with tolerant coercion. |
| M10 silent catch swallows | **FIXED-UNCOMMITTED** | 134 `catch (_)` at HEAD → 115 remaining at session start (19 already converted by earlier uncommitted fee/attendance/marks/auth fixes) → **0 remaining now**. All 115 triaged and addressed: **62 class (a) user-facing loads** (log via `debugPrint` + shared `ErrorContainer(errorMessage, onRetry:)` inline UI or error-state/SnackBar; loading never left false on failure) — includes priority flows: admin exam detail-sheet subjects, parent homework, parent chat threads+messages, plus all admin list/tab screens, teacher leave/diary/library/timetable/portfolios/lms/wellbeing, parent health/bus/portfolio/elibrary/pt-slots, student 13 screens, shared holiday/gallery/emergency; **12 class (b) mutating actions**: visitor check-in + parent chat send fully converted (chat rolls back the optimistic bubble + "Message not sent" SnackBar), 10 others got `debugPrint` on top of existing SnackBars (marketplace install/uninstall, inventory add, compliance generate, visitor QR lookup, settings change-password, teacher announcements/lesson/topic uploads, portfolio feedback); **30 class (c) legit fire-and-forget/fallbacks** (token-refresh cleanup, badge refresh, plugin cache reads, banner/date parse fallbacks, bus-location 15s poll, students pagination) → log line only; **11 log-only** on catches that already had error UI (notices/class_sections/class_subjects/ai_tools/teacher payroll/notices/ai) or an empty-state fallback (admin evacuation tab). Plus 2 actions that had **no catch at all** now wrapped with error SnackBar (admin publish-notice, teacher create-teacher — both previously closed their dialog showing success on failure). Spot-read end-to-end: admin exam sheet, parent homework, parent chat send, admin marketplace, teacher payroll (loading → error → visible). | dart analyze: aschool_shared **0 issues**; flutter_admin 16 / flutter_teacher 13 / flutter_parent 11 / flutter_student 11 — identical to pre-session baselines (only line-number shifts of pre-existing infos/warnings; one pre-existing uncommitted-change info in teacher online_exam_screen additionally fixed). Remaining honest gaps: 30 (c) sites intentionally log-only; error text is generic (backend `error` body not surfaced on the 62 (a) loads — message + retry only). |
| M11 coverage gaps | **OPEN** | Stubs remain: `flutter_admin/lib/features/dismissal/dismissal_screen.dart:300` (QR "Coming soon"), `wellbeing_screen.dart:227` (survey creation); no live-bus, teacher conference, parent library/LMS screens found | Commit `14e780b` only relabeled placeholders honestly; no new coverage. |
| M12 pubspec version constraints | **FIXED-COMMITTED** | `flutter_student/pubspec.yaml:7` `sdk: '>=3.2.0 <4.0.0'` now present | Part of `ad29e04` version-unification pass. |

## 4. Uncommitted working-tree changes (git diff HEAD) — what each fixes

| File | Change |
|---|---|
| `backend/config.py` | `JWT_TOKEN_LOCATION = ["headers","cookies"]` + cookie names/Secure/SameSite/CSRF flags — lets web dashboard use httpOnly cookie sessions while mobile keeps Bearer headers. Does **not** add `STRIPE_WEBHOOK_SECRET` (E5 unchanged). |
| `backend/app/__init__.py` | Blocklist loader now rejects tokens with `iat < User.tokens_invalid_before` (global per-user invalidation, covers refresh + other devices); fail-open with rollback on error. |
| `backend/app/models/user.py` | New nullable column `tokens_invalid_before` (pairs with untracked migration `27312847ace7_add_tokens_invalid_before_to_user.py`). |
| `backend/app/api/v1/auth.py` | `change_password` + `logout_all` set `tokens_invalid_before`; registration rewritten: direct token login (no OTP step), `phone_verified=False` (honest state, becomes verified only via send-otp/verify-otp), and plugin grants via tier-based `grant_plan_plugins()` (see E1/E1b); **now also provisions the AI quota at signup via `AITokenHub.ensure_quota_exists()` + documents that SMS needs no provisioning (see E12)**. |
| `backend/app/services/auth_service.py` | Login fallback: `X.aschool.com` emails also match `X.aschool.com.np` (demo-domain rename shims). |
| `backend/app/utils/nepal_grading.py` | `calculate_gpa` now credit-hour-weighted (accepts `credit_hours`, defaults 1.0). Hand-verified 2026-08-28 (Money & Grades audit §1): 45+ in-container assertions — bands vs claims, boundaries ±ε, equal/weighted GPA, null/zero credit, 0/0 marks, no silent subject drop — all match; no code change needed. |
| `backend/app/api/v1/hr_payroll.py` | Payslip gross/net recomputed from allowances/deductions (accepts numeric strings; net = gross − deductions) instead of falling back to basic salary. 2026-08-28 (Money & Grades audit §2): alleged "incorrect default when components aren't stored" NOT present — fallback uses real records (runtime-verified). Fixed real defect: a derived net now uses the gross that will actually be stored (explicit client gross wins over component math) so stored pairs always satisfy net = gross − Σdeductions — applied in `create_payroll`, `update_payroll` and `_payroll_dict`'s net fallback (legacy gross 46000/net-NULL row now reports 44000, matching the payslip). Payslip PDF numbers pdftotext-verified (40000/5000/45000/2000/43000). |
| `backend/app/services/ai/{adaptive_learning,benchmarking_ai,risk_detector}.py` | Model-rename crash fixes: `ExamResult`→`Marks` (`obtained_marks`, group by `subject_id`), `AttendanceRecord`→`Attendance`. |
| `backend/app/tasks/admission_followup.py` | `plugin_slug="admissions"` → `"admission"` (correct slug, task now finds installs). |
| `backend/app/api/v1/iemis_importer.py` | Phone fix only (bulk-cap logic was already done): `_placeholder_phone()` deterministic 9800000xxxx fallback + `permissions={"placeholder_phone": True}` marker for student and staff `User` rows whose source data lacks a phone — `users.phone` is NOT NULL (E10). |
| `backend/app/plugins/listeners.py` | `admission.accepted` auto-enrollment now plan-cap checked via `assert_student_cap()` (E11) + same NOT NULL-phone placeholder fallback for the auto-created user when the application has no parent phone (E10). |
| `backend/app/services/ai/token_hub.py` | Comments only: `_check_quota` / `ensure_quota_exists` docstrings document that a missing quota row BLOCKS (no unlimited default) and that registration provisions eagerly (E12). |
| `backend/tests/test_provisioning_and_caps.py` | New — 6 regression tests: IEMIS phoneless import creates a marked placeholder-phone user; placeholder determinism + collision avoidance; admission auto-enrollment blocked at cap / allowed within cap; registration provisions the AI quota row with env defaults. 6/6 green in `aschool-flask-1`. |
| `backend/app/api/v1/multi_branch.py` + `backend/app/models/school_chain.py` + migration `b5e8f2a9c4d1_add_school_chain_tables.py` (+ merge `e7a1c4f8b2d6`) + registration in `api/v1/__init__.py` (`STATICALLY_MOUNTED_MODULES` + register) + manifest `api_blueprint` repoint | **multi_branch plugin (premium, NPR 2999) implemented** — was an empty module published in the live marketplace. New: `school_chains` (one live chain per parent school) + `school_chain_members` (branch = real School tenant; partial-unique indexes: one chain per owner, one membership per school, code unique per chain). Blueprint mounted under `/schools` (matches the pre-existing frontend pages' calls), every route gated `@jwt_required @school_required @plugin_required("multi_branch") @role_required("superadmin","school_admin")`: GET `/schools/chain/overview` (per-branch student/staff counts + performance + month fee total via grouped aggregate queries), GET `/schools/chain/dashboard` (pooled attendance %, chain revenue), GET `/schools/chain/analytics?period=this_month|this_year|last_year` (period metrics with change vs previous window, branch rankings), GET/POST `/schools/branches` (registry; POST creates a real branch School tenant or links an existing one via `{"school_id"}`; first branch implicitly starts the chain), PATCH/DELETE `/schools/branches/<member_id>` (owner-only; delete unlinks, tenant preserved). Writes transactional with rollback. **Runtime ✅ (aschool-flask-1, dev DB, temp seed cleaned up after): seeded 2 branches (7+4 students, 3+2 staff, paid fees 8000+2000, attendance 5/7+4/4) → overview/dashboard/analytics returned exactly 11 students / 5 staff / Rs. 10,000 / 81.8% attendance, matching direct SQL counts; branch create/patch/delete proven against live url_map (tenant survives unlink); unrelated school with plugin → overview 403 + empty registry + cross-chain link refused; school without plugin → 403; unauthenticated → 401. Imports proven in-container. Pinned by `backend/tests/test_multi_branch.py` — 10/10 green; adjacent `test_plugin_manifests.py` + `test_marketplace_billing.py` 613/613 green.** | Frontend pages (`/dashboard/multi-branch/*`, 4 files) already existed and call exactly these endpoints — no 404 wiring needed. Limitations (honest): performance_score is a derived metric (50% attendance + 50% fee-collection rate — no exam-score aggregation); "branch-switch context" is served by the registry listing (each branch remains its own tenant login, no cross-tenant session switching); mobile admin app has no multi_branch screens (manifest flutter section is aspirational). |
| `backend/app/api/v1/biometric.py` + `backend/app/models/biometric.py` + migration `b10c5d9e2f3a_add_biometric_tables.py` (merged via `e7a1c4f8b2d6`) + registration in `api/v1/__init__.py` (`STATICALLY_MOUNTED_MODULES` + register) + `models/__init__.py` import | **biometric plugin (premium, NPR 1999) implemented** — was an empty module published in the live marketplace (PRIOR_AUDIT_DIFF #82/#110). Three new tables: `biometric_devices` (per-school device registry; per-device API key — only SHA-256 hash stored, plaintext shown once at create/`regenerate-key`), `biometric_punches` (raw punches; DB-level idempotency: partial-unique `(device_id, device_punch_id)` + unique `(device_id, device_user_id, punched_at)`), `biometric_sync_logs` (one row per ingest batch / manual sync). Blueprint mounted at `/attendance/biometric` (matches the pre-existing frontend pages' calls). Admin routes gated `@jwt_required @school_required @plugin_required("biometric") @role_required("school_admin")`: GET `/overview` (fleet health + today's pipeline), GET/POST `/devices`, PATCH/DELETE `/devices/<id>`, POST `/devices/<id>/regenerate-key`, POST `/devices/<id>/sync` (re-maps pending/unmapped punches → attendance + writes a sync log), GET `/logs?search=&status=`, GET `/punches?device_id=&status=&limit=`. Device routes (NO JWT — auth via `X-Device-Key` header, plugin-active check is a direct trial-aware DB query since devices carry no school context): POST `/ingest` (batch, strictly validated atomically — any malformed record → 400 + nothing written; per-punch SAVEPOINT so concurrent replays colliding on the unique indexes count as duplicates; maps device user id → Student by `student_id` code, then `admission_number`, or explicit `student_id`; upserts the daily `attendance` row — first punch = `check_in_time`, later punch = `check_out_time`, device punch overrides manual `absent` → `present`; attendance dates use Nepal time UTC+5:45; unmapped punches are STORED with `status=unmapped` + reason so no device data is lost), POST `/heartbeat` (drives online/offline — 10-min window — and last-seen). Writes transactional with rollback. **Runtime ✅ (aschool-flask-1, dev DB, temp school cleaned up after): imports + 11 routes in url_map; device registered (key shown once, 32 chars); ingest 3 punches → 3 punch rows + 2 attendance rows (in 09:05 NPT / out 16:01, class_id set); exact batch replay → 3 duplicates 0 new, row counts unchanged; malformed batch (1 valid + 1 bad ts) → 400 `invalid_records` + counts unchanged (rollback proven); unknown user id → punch stored unmapped, manual sync after "enrollment" → mapped + attendance row; heartbeat → device `online` + last_seen; overview stats and sync logs correct. Live dev server answers 401 (not 404) on the new routes. Pinned by `backend/tests/test_biometric.py` — 11/11 green twice; `test_plugin_aliases.py` 2/2, `test_models.py` 13/13.** | Frontend pages (`/dashboard/biometric`, `/devices`, `/logs`) already existed and call exactly these endpoints with matching field names — no wiring needed. Limitations (honest): real ZKTeco hardware untestable here — the ingest contract (`X-Device-Key` + batch punches) is designed for ZKTeco ADMS-style push clients but only HTTP clients were proven; teacher punches not mapped (TeacherAttendance exists but device→teacher id matching is out of scope); no late-status cutoff (punches mark `present`, check-in/out times recorded); per-device enrollment count is a proxy (distinct ids the device has reported), not the device's onboard user list. |
| E15 `fee_types` table missing from provisioned DBs (schema drift) — Fee Types page 500s | **FIXED-UNCOMMITTED** | The `FeeType` model (`app/models/fee.py:19`, `__tablename__="fee_types"`) had NO Alembic migration, so every DB built only via `flask db upgrade` lacked the table: `GET /fees/types` silently fell back to DEFAULT_FEE_TYPES while `POST/PUT/DELETE /fees/types` raised UndefinedTable → 500 ("Could not create fee type"). Full table-name diff (148 live tables vs models) confirmed `fee_types` was the ONLY missing table. Fixed: new migration `migrations/versions/d5a9e7c1b3f2_add_fee_types_table.py` (BaseModel columns + school FK + index, chained on head `c9d2e4f6a8b1`), applied via `flask db upgrade` in `aschool-flask-1`. | Runtime ✅ (live HTTP, throwaway enterprise school): POST `/fees/types` 201, GET 200, structure auto-apply → collections, full pay → receipt → `GET /fees/receipts/<id>/pdf` streams application/pdf; overpay → 400 with zero new receipt rows; bogus `initiate-payment` fee_ids → 404 with zero rows created. Phase-2 probe (98 checks) 98/98 green twice. Tests: `test_fees_money_math_verification.py` + `test_fees_summary.py` + `test_fees_discount_stacking.py` + `test_student_fees.py` + `test_hr_payroll_math.py` + `test_gps_pipeline.py` + `test_models.py` + `test_plugin_manifests.py` = 644 passed (after resetting the stale `aschool_test` schema — its `create_all`-once schema predated `students.admission_application_id` and caused 8 unrelated failures that are NOT code regressions). |
| E16 dismissal verify-qr ignored the parent app's QR format; records serializer lacked student_name — web Scan-QR flow dead | **FIXED-UNCOMMITTED** | Contract chain: parent app encodes `aschool:pickup:<parent_user_id>:<student_id>` (`flutter_parent/lib/features/dismissal/dismissal_qr_screen.dart:159`, fed by `GET /parent/dismissal-status` which supplies `parent_user_id`+`student_id`), but backend `verify_qr` required `{pickup_id, student_id}` and ignored `qr_code` → the web page's scan box (`frontend/app/dashboard/dismissal/page.tsx` sends `{qr_code}`) got 400 on EVERY scan. Also `_record_dict` had no `student_name` and the web table read nonexistent `guardian_name`/`time`/`status` fields. Fixed backend (`api/v1/dismissal.py`): `verify_qr` now parses the `aschool:pickup:uid:sid` string (validates student-in-school, resolves the student's active AuthorizedPickup preferring one `authorized_by_id == parent_user_id`; malformed → 400; unknown student → 403), `_record_dict` includes `student_name`, and `GET /dismissal/records` implements the `?search=` param the web sends (Student name ilike). Fixed frontend page to map real fields (`picked_up_by`, `dismissed_at`, derived released badge). **Related, NOT fixed (larger, mobile-side): admin flutter Overview tab calls `GET /dismissal/summary` which does not exist in url_map (routes are only /authorized, /records, /verify-qr) → overview shows its error state; mobile Scan-QR tab is an honest "Coming soon" stub (M11).** | Runtime ✅ (live HTTP, probe): verify-qr via `{pickup_id, student_id}` 201; via parent-QR string 201 with `picked_up_by` from the resolved pickup; malformed qr_code 400; unknown-QR student 403; inactive pickup 403 with zero records written (rollback proven); records list by date returns student_name; `?search=` filters by student name. Pinned by `backend/tests/test_ops_validation_guards.py` (6 tests, green). |
| E17 Unvalidated FK/id inputs caused unhandled IntegrityError → 500 (visitor, inventory, payroll) | **FIXED-UNCOMMITTED** | Three write paths accepted ids they never validated, so a bogus/empty id hit the DB constraint and surfaced as a 500: (1) `POST /visitors/checkin` — `visitors.name` NOT NULL, no check → empty body 500; `visiting_staff_id` FK unchecked → unknown UUID 500; (2) `POST /visitors/appointments` — `staff_id` presence-checked but not FK-checked → unknown UUID 500; (3) `POST /inventory/assets/<id>/audit` — `asset_id` never checked → unknown UUID 500; (4) `POST /hr/payroll` — `user_id` never FK-checked → unknown UUID 500. All four now validate up front (`400 name is required` / `400 … does not match a user at this school` / `404 Asset not found`), matching the pattern already used in dismissal/transport. Also added the negative-money guard to `POST /inventory/assets` (purchase_price/current_value/depreciation_rate must be numbers ≥ 0 — the "negative-stock guard" analog; inventory has NO stock-quantity model by design: it tracks assets/procurement/audit-logs, documented in the frontend page comment too). | Runtime ✅ (live HTTP, probe): each case 4xx with the specific message, and row counts unchanged after the failed writes (rollback). Pinned by `backend/tests/test_ops_validation_guards.py` (6 tests: QR-string verify + malformed/unknown, visitor name+staff, appointment staff, inventory audit 404 + negative price 400, payroll unknown user 400). Suites re-run green: 644 (fees/hr/gps/models/manifests) + 30 (plugins/billing/provisioning/payment-methods). |
| E18 AI quota exhaustion returned an opaque 500 from every AI endpoint except design_studio `/ai/suggest` | **FIXED-UNCOMMITTED** | `AITokenHub.request()` raises `QuotaExceededError`, but only `design_studio.py ai_suggest` caught it — `/ai-tools/{question-paper,lesson-plan,remarks,homework-help,insights/weekly}`, `/assignments/<id>/ai-grade`, `/design-studio/ai/{insights,risk-students,homework-help}` all let it propagate → generic 500 handler. Fixed once globally: new `@app.errorhandler(QuotaExceededError)` in `app/__init__.py` (next to the other handlers) → **429** JSON `{success:false, error:"AI quota exceeded (daily_limit): X/Y tokens used.", quota:{reason,used,limit}}`. Blocked attempts were already quota-logged (`status="quota_exceeded"` row) — unchanged. | Runtime ✅ (aschool-flask-1 test-client probe, patched-hub providers, real quota+logging): daily-limit exhausted → 429 `daily_limit` on `/ai-tools/remarks` AND `/ai-tools/homework-help`; missing/inactive quota row → 429 `inactive`; quota restored → 200 again; `quota_exceeded` usage row persisted. E7 wiring re-verified holding: zero `import anthropic` outside token_hub.py across `app/services/ai/`. Pinned by `tests/test_ai_quota_and_usage.py` (5/5). |
| E19 `GET /ai-tools/insights/risk-alerts` (and `/design-studio/ai/risk-students`) 500-crashed on the first student | **FIXED-UNCOMMITTED** | `SchoolInsightsService.calculate_student_risk_scores` filtered `Incident.students_involved.contains(...)` — that column does not exist (`models/incident.py` has `involved_student_ids` UUID ARRAY) → AttributeError → 500 for any school with ≥1 active student. Fixed to `Incident.involved_student_ids.any(student.id)`. | Runtime ✅ (probe): endpoint 200; incident risk counted via array membership (fixture: absence 40 + overdue fee 20 + incident 15 = risk_score 75, reason "1 recent incident(s)"); multiple incidents stack correctly (2 → +30). Pinned by `tests/test_ai_quota_and_usage.py::test_risk_alerts_counts_incidents_via_involved_student_ids`. |
| E20 `/ai-usage/stats` daily_chart returned the OLDEST 7 days of usage, not the last 7 | **FIXED-UNCOMMITTED** | `api/v1/ai_usage.py get_stats` grouped tokens by day `ORDER BY day ASC LIMIT 7` with NO time filter → the first 7 days ever logged (stale chart on any long-lived school; dev DB already showed wrong window). Fixed: `created_at >= now - 7 days` filter added before ascending ordering (limit dropped). | Runtime ✅ (probe + pinned): a 20-day-old success row (999 tokens) is excluded, today's rows sum matches SQL; `tests/test_ai_quota_and_usage.py::test_daily_chart_covers_last_seven_days_only`. |
| E21 `ai_adaptive_learning` (premium NPR 1499) is published-but-empty on the backend | **FIXED-UNCOMMITTED** → see E23-adaptive-learning (same finding; routes/models/migration/runtime proof live there) | Web pages `frontend/app/dashboard/ai-tools/{learning-paths,progress}/page.tsx` (gated `PluginGate slug="ai_adaptive_learning"`) call `GET/POST /lms/learning-paths`, `POST /lms/learning-paths/generate-ai`, `GET /lms/adaptive-progress` — **none exist** in the Flask url_map (`lms.py` has no such routes; no `LearningPath` model exists in `models/lms.py`); the pages 404 into empty/error states. `AdaptiveLearningAI` service (`adaptive_learning.py`) works — recommend/assess/practice all run through AITokenHub — but has **zero API callers**; manifest `api_blueprint: app.api.v1.lms` promises wiring that was never written. Flutter student/teacher tabs (manifest `adaptive_learning`) have no verified backend either. | Runtime ✅ service-level (in-container, real fixture students): `assess_mastery` on strong student (85→advanced), NULL `obtained_marks` row counted as 0 (no crash), zero-marks student → 0.0/beginner; `recommend_path` tolerates all-None `student_data` + malformed LLM JSON → `{"recommended_topics": [], "raw": …}`; `generate_practice_questions` returns text; all quota-logged. Fixing needs a `learning_paths` model + migration + 4 routes (same effort class as multi_branch/biometric) — deferred, plugin should not be sold meanwhile. |
| E22 AI feature entitlement/slug overlaps (verified; product decision needed) | **OPEN** | (a) **ai_grading bypass**: `POST /assignments/<id>/ai-grade` is gated `plugin_required("assignments")` (starter NPR 299), not `ai_grading` (growth NPR 599) — any assignments school gets AI auto-grading; the web assignments page wires the button with no `ai_grading` gate; web has NO ai_grading gate anywhere and the manifest's sidebar routes (`/dashboard/ai-tools/grade-queue`, `/plagiarism`) do not exist as pages. (b) **insights dual-gate**: the same weekly-report/risk content is served under BOTH `ai_tools` (`/ai-tools/insights/*`, used by the web insights page) and `ai_insights` (`/design-studio/ai/insights`, `/ai/risk-students`) — buying either premium plugin unlocks both surfaces. (c) **manifest metadata stale (cosmetic)**: `ai_grading` lists nonexistent `services.ai.grading_engine`; `ai_tutor` lists `services.ai.tutor` (real: homework_helper) + `api_blueprint: design_studio`; `ai_tools` lists `lesson_planner`/`remark_generator` (real: lesson_plan/report_remarks via question_paper); `ai_insights` `api_blueprint: design_studio` is correct only because the routes live there; loader stores keys verbatim (no runtime validation) so nothing crashes. | Verified by source + url_map + runtime probes (endpoints work; gates behave as described). Flipping (a) or (b) changes what paying customers can reach — needs a deliberate entitlement decision, not a drive-by gate flip. |
| E23-adaptive-learning `ai_adaptive_learning` (premium NPR 1499) had ZERO API routes — the E21 fix: real backend surface for `/lms/learning-paths*` + `/lms/adaptive-progress` | **FIXED-UNCOMMITTED** | New blueprint `app/api/v1/adaptive_learning.py` (registered statically in `api/v1/__init__.py` alongside white_label/multi_branch/biometric + added to `STATICALLY_MOUNTED_MODULES`; models re-exported in `models/__init__.py`), every route gated `@plugin_required("ai_adaptive_learning")` + `@school_required` + `role_required("school_admin","teacher")`, all queries tenant-scoped via `g.school_id`. Routes: `GET/POST /lms/learning-paths` (list w/ `?search&student_id&class_name`; manual create), `POST /lms/learning-paths/generate-ai` (per-student generate/regenerate: LLM via `AdaptiveLearningAI.recommend_path` → AITokenHub with `QuotaExceededError` bubbling to the global 429; on no-provider/provider-error/unparseable output falls back to a DETERMINISTIC rule-based path built from the student's real Marks and stores `source="rule_based_fallback"` + honest `source_note` — never fake LLM output; same-subject regeneration replaces only that subject's plan, whole-student regeneration supersedes all active paths, history kept), `GET /lms/mastery` + `PUT /lms/mastery/<id>` (teacher override, `source="manual"`) + `POST /lms/mastery/assess` (real `AdaptiveLearningAI.assess_mastery`, no LLM/no quota), `GET /lms/adaptive-progress` (per-student rows: avg from real Marks, level from MasteryRecord/Marks thresholds, path counts incl. class-wide paths, latest stored recommendation; no per-row LLM). Storage: NEW models `LearningPath`+`MasteryRecord` (`app/models/adaptive_learning.py`, steps JSONB, completion computed from real step status) + migration `e8a3d5f7c2b4` chained on `d5a9e7c1b3f2`, applied in `aschool-flask-1`; `aschool_test` schema reset so create_all rebuilds include them. Service small fix: `assess_mastery` now actually filters Marks by subject (was silently all-subjects) with a None-safe fallback preserving legacy behavior. Frontend: `ai-tools/learning-paths/page.tsx` rewritten (student-picker generate dialog calling the real endpoint, source badges showing AI/Rule-based-fallback/Manual with the honest note, error+retry states); `ai-tools/progress/page.tsx` mapped to the real shape with error state + honest level badges (tsc --noEmit: 0 errors in touched files, 10 pre-existing elsewhere). | Runtime ✅ (live HTTP on :5003 vs throwaway e23-probe-a/b schools, real fixture Marks 40/85): generate-ai 201 `rule_based_fallback` (placeholder ANTHROPIC key → honest note with provider 401), steps tagged Mathematics/English weakest-first, regenerate deactivates old (active:1 total:2); list/search 200; mastery assess → Mathematics beginner 40.0 / English advanced 85.0 (per-subject — the service fix is visible), PUT override → `manual`, invalid level 400; adaptive-progress real rows (avg 62.5, level intermediate, class-wide path counts for both students); no plugin → 403 on all 5 routes; cross-tenant student → 404; `daily_limit=0` → 429 with quota payload while assess stays 200 (no LLM); `ai_usage_logs` shows feature `service:adaptive_learning` quota_exceeded+error rows (token-hub routing proven). Pinned by `tests/test_adaptive_learning_api.py` (7 tests); `pytest -k "adaptive or lms"` = 31 passed. Limitation: AI generation needs a REAL Groq/Anthropic key to produce `source="ai"` (verified in tests via mock only — the dev env's Anthropic key is a placeholder); step completion tracking (marking steps done) is NOT yet implemented anywhere, so completion_rate stays 0 until a step-progress endpoint exists; manifest still says `api_blueprint: app.api.v1.lms` (harmless — lms routes live there, adaptive routes are statically mounted). |
| E30 `social_ads` (growth NPR 499) is published-but-empty on the backend | **FIXED-UNCOMMITTED** | New blueprint `app/api/v1/social_ads.py` (registered statically in `api/v1/__init__.py`; `models/social.py` + migration `f6a9c2e4b7d1` add the `name`/`content`/`media_url` identity+creative columns `AdCampaign` lacked — everything else reused as-is). All 8 routes mounted under `/social`, each gated `@jwt_required @school_required @plugin_required("social_ads") @role_required("school_admin")`, tenant-scoped to `g.school_id`: GET/POST `/social/campaigns` (list w/ `?status&objective` + honest stats / create), GET `/social/campaigns/preview` (GET+POST — audience estimate for a targeting draft), GET/PATCH/DELETE `/social/campaigns/<id>` (detail incl. estimate / field updates with status-transition map draft→active→paused/completed / soft delete), POST `/<id>/pause`, POST `/<id>/resume`. Targeting validated against REAL school data: `class_ids`/`section_ids` (list or comma string) checked as UUIDs (junk → 400, never a Postgres 500), each must be a live non-deleted Class/Section of THIS school, sections must belong to the selected classes; `audience` enum students_parents/students/parents; free-text note sanitized. Content is bleach-sanitized with the SAME allow-list as notices (`notices.py::_sanitize_html`); `media_url` must be http(s) (javascript:/data: → 400). **Delivery honesty: NO Meta Ads API is wired** — reach/impressions/clicks/spend stay at their real 0 defaults and are never fabricated; the preview/estimate is an honest in-school audience size (matched students + DISTINCT guardians via SQL COUNTs) labeled "Not a Meta impression forecast". Frontend `social-hub/campaigns/page.tsx` rewritten: real list/stats, create dialog with class/section chip multi-select + audience enum + live estimated-audience preview, pause/resume/delete actions, loading + error/retry states, backend error toasts surfaced; tsc clean on the touched file (4 pre-existing errors elsewhere untouched). | Runtime ✅ (aschool-flask-1): imports proven + 8 routes in url_map; `flask db current` = `f6a9c2e4b7d1` (head). Pinned by `tests/test_social_ads.py` — 7/7 green (isolated `aschool_test_e30` DB after a concurrent-agent truncate deadlock on shared `aschool_test`): CRUD roundtrip create→list→get→patch(draft→paused 400)→activate→pause→resume→soft-delete; preview estimate == direct SQL counts (3 students + 6 guardians → 9, audience=parents → 6, live-bump proven); unknown class / foreign-school section / section-not-in-class / bad audience / bad dates → 400 with nothing persisted; `class_ids=["abc"]` → 400 (found + fixed a bare-string return that leaked a 200 HTML page for bad media_url on create AND patch); `<script>`/`onerror` stripped from content, plain-text name fully stripped; rival school WITH plugin → 404 on foreign campaign (tenancy), without plugin → 403 (gate); monkeypatched commit raise → 500, zero rows, next create succeeds (rollback). No-plugin 403 also pinned in `test_comms_plugins.py::test_social_ads_campaigns_endpoints_exist_and_are_gated`. `pytest -k "social or ad"` = 123 passed (4 failures + 3 errors all in adaptive_learning/campus_ops/biometric/simulation — other agents' concurrent areas; social/ads/alias subset re-run green 31/31). | Meta Ads delivery (impressions/reach/clicks/spend) is NOT implemented — no credentials/provider exist; counters are honest zeros and the plugin still advertises "Meta Ads API post boosting" (manifest demotion/wiring is a product decision, same as E22). Manifest `depends_on: [social_hub]` remains unenforced at install (E30/E41 mechanism gap). Mobile flutter admin app has no social_ads screens (manifest aspirational). |
| E31 SMS send path never recorded real outcomes — rows stuck `queued` forever | **FIXED-UNCOMMITTED** | `/sms/send` commits SMSLog rows `queued` and dispatches `send_bulk_sms` — but the task ignored `log_id`, and `send_sms` never touched SMSLog, so every row stayed `queued` forever and `/sms/stats` credits stayed 0 no matter what happened (the API docstring claimed "the Celery task flips it to sent/failed" — it did not). `communications.py` bulk path didn't even send log_ids. Fixed `app/tasks/sms_sender.py`: `send_sms` takes `log_id`, `send_bulk_sms` forwards it, `_update_sms_log()` flips the row to the REAL outcome — provider success → `sent` + cost + provider_message_id + sent_at; missing/placeholder token or `SMS_CONSOLE_MODE` → `failed`, cost 0, provider `console` + loud `logger.warning("[DEV SMS] … NOT delivered")`; provider exception → `failed` + re-raise (no fake success anywhere). `communications.py` now includes `log_id` in its task payload. | Runtime ✅ (probe ×2, 108/108): no-creds → row `failed/console/cost=0`; fake SPARROW token → network/4xx error → row `failed` + task raises; patched fake provider 200 → row `sent`, cost from response, sent_at set; stats `credits_used` counts only sent rows. Pinned by `tests/test_comms_plugins.py` (4 SMS tests). |
| E32 WhatsApp auto-replies silently lost (JSONB in-place append) | **FIXED-UNCOMMITTED** | `POST /whatsapp-bot/auto-replies` did `rules = config.auto_replies or []; rules.append(...); config.auto_replies = rules` — when the config row already existed, `rules` IS the loaded list object, so reassigning it is a no-op for SQLAlchemy change detection: commit persisted nothing, the endpoint replied `total_rules: 2` and the rule vanished (runtime-reproduced: PUT 1 rule + POST → GET returns 1). Fixed: build a NEW list before assignment. Inbound webhook itself verified honest per §4: single enabled bot → inbound stored in `whatsapp_messages` (+ auto-reply only when creds exist, `skipped` otherwise, no fake outbound row); no enabled bot → `{"unhandled": 1, "reason": "no_enabled_bot"}`; two enabled bots → `ambiguous_bot_owner`; status-only payloads → explicit note, never fake success; `X-Hub-Signature-256` verified 403/200 when `WHATSAPP_APP_SECRET` set; attribution requires exactly one enabled bot platform-wide (loud log + unhandled otherwise). Foreign enabled bot configs on the shared dev DB were snapshotted/disabled/restored by the probe. | Runtime ✅ (probe ×2): all webhook scenarios above; rollback on mid-write DB failure (patched commit raise) → per-message except rolls back, row count unchanged, msg counted unhandled. Pinned by `tests/test_comms_plugins.py` (5 WA tests incl. append persistence). |
| E33 Input guards missing on conferences + notices writes (bogus ids / bad dates → 500) + bare-array slots 500 | **FIXED-UNCOMMITTED** | E17-class gaps, runtime-confirmed then fixed: (1) `POST /conferences` with missing title/dates or garbage datetimes → IntegrityError/DataError 500 (title/start_date/end_date NOT NULL) → now 400 with field names; end<start 400. (2) `POST /conferences/<uuid>/slots` on an unknown conference → FK 500 → 404; missing/unparseable slot times → KeyError/DataError 500 → 400; `end<=start` 400; bogus `teacher_id` → FK 500 → 400 "does not match a user at this school" (E17 wording); malformed non-UUID teacher/student ids guarded against PG DataError. (3) Bare top-level array slot bodies (the "accept array or single" claim) raised `AttributeError: 'list' object has no attribute 'get'` → 500 — now accepted (dict / {"slots": [...]}/ [list]). (4) `PUT .../slots/<uuid>/notes` on unknown slot → FK 500 → 404. (5) `book` with bogus `student_id` → FK 500 → 400. (6) `POST/PUT /notices/events` with missing/garbage `start_date` (NOT NULL) → 500 → 400. | Runtime ✅ (probe ×2): each case 4xx with specific message, zero rows written; booking flow end-to-end once (create conf → teacher slots → parent book w/ student → 409 double-book → cancel → notes PUT/GET incl. follow-up). Rollback #2: patched commit raise during 2-slot bulk create → 0 partial rows (fresh-transaction count). Pinned by `tests/test_comms_plugins.py` (6 conference/notices tests). |
| E34 `g.current_user` never resolved for X-School-Slug/subdomain requests → 500 on any endpoint touching it | **FIXED-UNCOMMITTED** | `resolve_school()` returned EARLY after `_set_school_context()` for the subdomain and `X-School-Slug` (mobile) paths — the JWT user resolution only ran in the no-school fallback (web uses cookies, so web worked; mobile Bearer+header calls hit `AttributeError: 'NoneType' object has no attribute 'id'` on `g.current_user.id`). Runtime-confirmed live: `POST /sms/send` (sent_by_id) 500'd for a header-authenticated admin. Fixed centrally in `app/__init__.py`: user resolution extracted to `_resolve_jwt_user()` and run for EVERY request before school resolution (best-effort, fail-open with rollback, unchanged semantics for the web fallback path). 20+ endpoints across social_hub/incidents/wellbeing/gamification/conferences/compliance/library/health_records/sms read `g.current_user`. | Runtime ✅ (probe ×2 + test): header-authenticated `POST /sms/send` now 201 with `sent_by_id` persisted. Pinned by `tests/test_comms_plugins.py::test_current_user_resolved_for_header_auth`. Adjacent suites (auth/cookies/ops-guards/aliases/provisioning/models) 49/49 green. |
| E24-exams-class-id marks entry saved rows with `class_id=None` → invisible in /results and /grade-sheet (renumbered from the prior agent's ID E15, which COLLIDED with the ops-batch E15 fee_types migration row above — ops E15 kept as-is, exams row moved here) | **FIXED-UNCOMMITTED** | `submit_marks` (`backend/app/api/v1/exams.py`) resolved `cls_id = rec.get("class_id") or data.get("class_id")` only — neither the FE marks dialog nor several mobile callers send `class_id`, so every row landed with NULL; `GET /exams/<id>/results?class_id=` (`:852 marks_query.filter_by(class_id=class_id)`) and `/grade-sheet` (`:950`) filter by class_id → freshly entered marks never appeared. Fixed: `exams.py:684-697` resolves rec → request-level default → **the student's own class** (`Student.class_id`) before write; upsert path backfills `existing.class_id` when NULL (`:732-733`); in-range probe (`:234` `GET /exams/<id>/marks`) still returns all rows | Runtime ✅ (live HTTP in `aschool-flask-1`, throwaway school, exam+2 students): `POST /exams/<id>/marks` with NO class_id anywhere in payload → 200 `new:2`, both rows persisted with `class_id = class "Five"`, `GET /exams/<id>/results?class_id=` returns exactly 2 ranked results; resubmit → `new:0, updated:1` (upsert intact). Pinned by `tests/test_exam_attendance_timetable_assignment.py::test_exam_marks_entry_resolves_class_from_student` (also asserts `/results?class_id` finds the rows). |
| E40 `disaster_management` (premium NPR 999) is published-but-partial on the backend | **FIXED-UNCOMMITTED** (2026-08-29 batch 6) | New blueprint `app/api/v1/disaster_management.py` (registered statically in `api/v1/__init__.py` — `STATICALLY_MOUNTED_MODULES` entry + register block; models re-exported in `models/__init__.py`), every route gated `@plugin_required("disaster_management")` + `@school_required`, tenant-scoped to `g.school_id`, mounted under `/emergency` to match the frontend calls. Routes: `GET/POST /emergency/drills` (list with `?status&drill_type&upcoming=true`; create admin-only with title/type/date/duration guards), `GET/PATCH/DELETE /emergency/drills/<id>` (PATCH marks completed/missed/cancelled → `completed_at`; DELETE soft-deletes), `GET/POST /emergency/drills/<id>/participations` (per-class participation: int guards, present≤expected, class/section school-scoped FK guards, UUID[] guard on `missing_student_ids`; POST open to teachers too), `GET /emergency/disaster/overview` (aggregates REAL rows — EvacuationPlan/EmergencyAlert counts from the emergency tier + drill stats — plus `readiness_score` 0-100, formula documented in code: drill recency 0-40 (≤90d=40/≤180d=25/≤365d=10), completed-in-365d frequency 0-30 (≥4=30/3=22/2=15/1=8), active plans 0-20 (≥2=20/1=12), alert hygiene 0-10 (unresolved alerts older than 7d penalize 5 each); also `upcoming_drills` + `recent_alerts` from the seismic source), `GET /emergency/seismic-alerts` (public USGS FDSN feed — NSC publishes no stable JSON API; ≥M4.0 within 200 km of the school's stored coordinates, Kathmandu default flagged `center_default:true`, haversine `distance_km`, 10-min per-school cache; on feed failure returns an EMPTY list + `unavailable:true` + reason — never fabricated events). Storage: NEW models `DisasterDrill`+`DrillParticipation` (`app/models/disaster_management.py`; drills are distinct from EmergencyAlert — planned scheduled→completed/missed/cancelled lifecycle vs the live-alert enum) + migration `c3d4e5f6a7b8` chained on head `f6a9c2e4b7d1`, applied via `flask db upgrade` in `aschool-flask-1`; existing emergency models reused everywhere else. Frontend: `disaster/page.tsx` adds readiness card + last-drill date + upcoming-drills list + honest feed-unreachable note; `drills/page.tsx` error toast on mark-complete; `alerts/page.tsx` honest "Feed Unreachable" badge with reason. `tsc --noEmit` in `aschool-nextjs-1`: 0 errors in touched pages. | Runtime ✅ (live HTTP in `aschool-flask-1`, throwaway growth school, probe `backend/tmp_dm_im_verify.py` kept for reruns): drills CRUD E2E incl. all 400 guards, upcoming filter, complete→`completed_at`, delete→404; participation guards + detail nesting; overview stats matched fixtures (1 plan / 1 active alert / drills) with **readiness hand-computed 70 = 40+8+12+10**; seismic 200 with correct event fields (magnitude/location/time/depth_km/distance_km≤200) or honest unavailable flag; no-plugin → 403 on all routes; unauth → 401; live url_map shows all 9 routes. Pinned by `backend/tests/test_disaster_management_api.py` **6/6** (incl. gate flip with cache invalidation + rollback-on-commit-failure leaving 0 rows); campus-ops 404 pin flipped to 200 (`test_campus_ops_plugins.py` **13/13**). | Limitations (honest): seismic source is USGS (NSC has no public JSON API) — needs internet egress, otherwise the endpoint degrades to an honest empty+unavailable state; `alert_service.send_emergency_alert/mark_student_safe` remain unwired (pre-existing, out of scope — the emergency-tier alert/headcount routes work); drill participation records counts + missing ids, it does not rewrite attendance; manifest `services:` still lists nonexistent `app.services.emergency.earthquake_api` (stale metadata, harmless — loader stores verbatim). |
| E41 `incident_management` (growth NPR 399) has ZERO API surface — its whole web UI is dead | **FIXED-UNCOMMITTED** (2026-08-29 batch 6) | New blueprint `app/api/v1/incident_management.py` (registered statically in `api/v1/__init__.py`; models re-exported in `models/__init__.py`), url_prefix `/incidents/management` — **zero base `/incidents` routes touched or duplicated** (base `incidents.py` untouched; `/<uuid>` converters keep the namespaces disjoint). All gated `@plugin_required("incident_management")` + `@school_required`, tenant-scoped. Routes: `GET /overview` (stats.active = reported+investigating; `pending_escalation` = active cases with NO escalation row; `resolved_this_month` by `resolved_at`; `total_this_year` by `created_at`; `recent_cases` with school-scoped `student_name`), `GET /active?search&severity`, `POST /incidents/management` (create case: friendly labels mapped onto the base enum — behavior→behavioral, violence→fighting, academic→other; student FK guard; `parent_notified`; witness NAMES recorded on the audit trail — WitnessStatement requires user accounts, base-plugin statements still drive `witness_count`), `POST /<id>/assign` (assignee FK-validated at the school; notifies the assignee), `POST /<id>/status` (forward-only reported→investigating→resolved→closed — "open" = the base enum's `reported`; backward/no-op → 400), `POST /<id>/escalate` (severity bump one step or explicit higher via body; at critical → 400; flips reported→investigating with audit; writes `IncidentEscalation`; notifies the target — explicit `escalated_to_id` FK-validated or the school's first active admin — via the EXISTING in-app notification service `notifications.create_notification`; re-escalation allowed until critical), `GET /escalations` (open escalated cases with latest-escalation metadata; resolved cases drop off), `PATCH /<id>/resolve` (resolution + `resolved_at`; closed → 400), `POST /<id>/conference` (escalated cases only → 400 otherwise; stamps escalation + incident), `GET /<id>/audit` (append-only `IncidentWorkflowEvent` trail: created/assign/status_change/escalate/resolve/conference with actor + from→to + notes), `GET /reports?period=this_week|this_month|this_year` (Monday-based week; summary total/resolved/escalated/**avg_resolution_days** = avg(resolved_at−created_at) over real rows, `by_type`+`by_severity` grouped counts, `resolved_cases` with resolution). Storage: NEW models `IncidentEscalation`+`IncidentWorkflowEvent` (`app/models/incident_management.py`) + workflow columns on `incidents` (`assigned_to_id`, `escalated_at`, `escalated_to_id`, `parent_notified`, `conference_scheduled(_at)`) + migration `d4e5f6a7b8c9` chained on `c3d4e5f6a7b8`, applied in `aschool-flask-1`; `aschool_test` schema reset so `create_all` rebuilds include them. Frontend: all 4 pages (`incident-management/{page,active,escalations,reports}`) got error+retry cards (were isLoading-only); escalations conference button now invalidates its list. `tsc --noEmit` in `aschool-nextjs-1`: 0 errors in touched pages. Correction to the prior row: `install_plugin` DOES enforce `depends_on: [incidents]` now (live probe: installing incident_management without incidents → "Dependency not met"), while `grant_plan_plugins` growth tier grants incident_management without incidents — the extension therefore also works standalone (management routes are self-sufficient; they create/manage Incident rows directly). | Runtime ✅ (live HTTP, throwaway schools, `backend/tmp_dm_im_verify.py` **111/111 green** incl. E40): create/type-mapping/student-name; assign+notify; forward-only workflow (skip/no-op/backward → 400); escalate medium→high→critical with notification row persisted (`/notifications?category=incident` non-empty), escalations listing + resolve drops off; conference only after escalation; audit trail complete (created first, resolve last, severity from→to pinned); reports math exact (total 2 / resolved 2 / escalated 2 / avg not null / by_type {fighting,theft}); cross-tenant student → 400, foreign incident invisible (404 + absent from listings); school without plugin → 403; unauth → 401. Pinned by `backend/tests/test_incident_management_api.py` **8/8** (incl. gate flip, cross-tenant isolation, rollback-on-commit-failure leaving 0 incidents AND 0 audit rows); `test_incident_management_only_install_fails_base_gate` still green (base-gate tier split intact). | Limitations (honest): witness names from the create dialog live on the audit trail (not user accounts → no WitnessStatement rows); notifications are in-app only (no SMS/push fan-out on escalation); `escalated_to` defaults to the school's first active admin (no principal role exists); audit events sharing one commit share one `created_at` timestamp (pairwise order between same-commit events is not guaranteed). |
| E42 incidents: unvalidated inputs on the most complex write path (cross-tenant UUID[] leak + FK 500s) | **FIXED-UNCOMMITTED** | `POST /incidents` accepted `involved_student_ids` with valid-UUID ids from ANOTHER school (stored the foreign reference AND the serializer leaked that student's name cross-tenant); raw garbage ids hit the Postgres UUID[] column → DataError 500; statements/actions on a bogus incident id → FK IntegrityError 500; empty `statement` (NOT NULL) → 500; bogus `witness_id`/`student_id` on statements/actions → FK 500; invalid `status` enum on PUT → 500. Fixed in `api/v1/incidents.py`: `_parse_student_ids` UUID coercion + school-scope check (`not at this school` 400), serializer resolves names SCHOOL-SCOPED with id-fallback (no cross-tenant name leak even on legacy rows), per-endpoint 400/404 guards, datetime parse guards. | Runtime ✅ (probe ×2, 145/145): foreign-student id → 400 + zero rows; garbage id → 400; bogus incident → 404; empty statement → 400; bad status → 400; forced commit failure on create → 500 with ZERO partial rows. Pinned by `tests/test_campus_ops_guards.py` (cross-tenant + rollback) and `tests/test_campus_ops_plugins.py`. |
| E43 counselor-note contract: web page wrote notes the backend silently emptied | **FIXED-UNCOMMITTED** | `frontend/app/dashboard/wellbeing/counselor/page.tsx` posted `{note, session_type, action_taken}` but `create_counselor_note` reads only `student_id`/`type`/`content` — every note was saved with `content=""` (NOT NULL satisfied by empty string) and the table read nonexistent fields (`n.note`, `n.session_type`, `n.student?.name`) so rows rendered as em-dashes. Fixed: FE sends `{student_id, type, content}` (action_taken dropped — no backend column), table renders `note_type`/`content`/`student_name`; backend `_note_dict` now includes `student_name` (Student relationship join, same pattern as the other serializers). | Runtime ✅ (probe + pinned `test_counselor_note_persists_content_and_returns_student_name`): note round-trips content/type, serializer carries the student's name. |
| E44 campus-ops web pages: 15 list pages degraded to a silent empty state on API failure + 3 field-contract bugs | **FIXED-UNCOMMITTED** | (a) Error states: library books/catalog/overdue, gamification badges/houses/rewards + dashboard, wellbeing counselor/surveys, health-records records/vaccinations, hostel (3 queries), disaster page/drills/alerts — all had `isLoading` but NO `isError` handling; added `retry: 1` + the established error card with Retry (E25/E26 pattern). (b) `incidents/page.tsx` rendered `i.type` but the serializer returns `incident_type` → Type column was always an empty badge. (c) `health-records/vaccinations/page.tsx` bound the Date-Given `BSDateInput` to nonexistent `form.administered_date` (state is `date_administered`; pre-existing at HEAD) → the picked date never reached the payload. (d) `wellbeing/surveys/page.tsx` dialog had dead Description/Target-Group inputs (backend reads `questions`/`target_class_ids`; no description column) and the table read `s.target_group` (serializer: `target_audience`) — dead inputs removed, payload corrected, honest "targets all students" note added. | `tsc --noEmit`: **0 errors in every touched campus-ops page** (pre-existing errors in unrelated files unchanged). Pages hitting the dead E40/E41 endpoints now show the error card instead of a fake empty state. |
| E25 timetable `POST /timetable/slots` had ZERO conflict detection + accepted garbage ids → silent teacher double-booking and FK/DataError 500s; FE page had no error state | **FIXED-UNCOMMITTED** | `create_slot` (`backend/app/api/v1/timetable.py:102-214`) accepted ANY day/period/teacher combination — two sections could silently share one teacher at the same time (the solver avoids clashes but manual slots bypassed it), and bogus/garbage `class_id`/`teacher_id` died at commit (FK) or in the ORM (DataError) as 500. Fixed: (1) conflict calculation — a slot occupies (day_of_week, period_number) plus its time window; `:117-158` rejects 409 when the same day+period (or an overlapping start/end window) is already taken by the same class/section OR the same teacher (`Slot conflict: the teacher is already booked…` / `…class/section already has a lesson…`); a free slot is accepted, and an overlapping window with a DIFFERENT teacher+section is legitimately allowed; (2) E17-class guards — required-field/`int`/time-parse checks (`:131-152`) + school-scoped FK validation for class/section/subject/teacher (`:156-172`, wording matches E17 `…does not match a … at this school`); `DELETE /slots/<garbage>` → 404 (`:236-245`); new `_parse_time`/`_coerce_uuid` helpers (`:252-273`); (3) FE `frontend/app/dashboard/timetable/page.tsx:45-56,116-131` — classes + slots queries gained `retry:1` + explicit error cards with Retry (previously failure silently rendered nothing under the toolbar) | Runtime ✅ (live HTTP probe ×2, 54/54 checks): free slot 201; **hand-built example — two sections (Five-A, Five-B) same teacher Monday P1 10:00-10:45 → 409 "the teacher is already booked"**; same section different teacher → 409 "class/section already has a lesson"; same teacher other day → 201; same teacher Monday P99 10:30-11:15 (overlapping window, different period) → 409; different teacher+section overlapping → 201; garbage class_id / period_number / start_time → 400; unknown teacher/subject (valid UUID) → 400 (was FK 500); GET filter + serializer names verified. Pinned by `tests/test_exam_attendance_timetable_assignment.py::test_timetable_slot_conflict_detection` (8 timetable assertions). |
| E26 assignments: NOT NULL class/subject wrote straight to commit (IntegrityError 500), model `is_late` column never set, bogus/garbage `student_id` → FK 500; FE page had no error state and read a nonexistent `submission_count` field | **FIXED-UNCOMMITTED** | `backend/app/api/v1/assignments.py`: (1) `create_assignment` `:46-66` validates `g.user_id` + coerced `class_id`/`subject_id`/`section_id` are present AND school-scoped (400, E17 wording) instead of relying on the model's NOT NULLs (`models/assignment.py:34-36`); (2) `submit_assignment` `:160-196` coerces `student_id` (garbage → 400, was DataError 500), verifies the student exists at THIS school (`:177-180` — valid-UUID foreign student used to pass and die at commit), and now computes the model's dormant `is_late` column (`models/assignment.py:assignment_submissions.is_late` Boolean — existed but was never written by any path): tz-safe `submitted_at > due_date` (`:190-196`); `_submission_dict` returns it (`:331`); (3) FE `frontend/app/dashboard/assignments/page.tsx:101,254-266` — assignments query gained `retry:1` + error card with Retry (was silent empty-state on failure); `:46,388` reads the REAL serializer field `submitted_count` (was `submission_count` — the table always showed "View" instead of the count); grade/create/delete mutations already had error toasts | Runtime ✅ (live HTTP probe ×2): missing class_id → 400; bogus class → 400; garbage student_id → 400; unknown assignment → 404; on-time submission → `is_late:false`; submission against a past-due assignment → `is_late:true`; teacher grade → 200 `marks:8.5, status:"graded"`; `submitted_count:1`. Pinned by `tests/test_exam_attendance_timetable_assignment.py::test_assignment_create_guards_and_submit_is_late` + `::test_assignment_submit_rejects_bad_student_without_rows` (zero rows after failed submits). |
| E27 "late" attendance counted inconsistently across rate endpoints (student-level endpoints counted late as attended; class summary, parent app and analytics did not) | **FIXED-UNCOMMITTED** | Uniform rule chosen from the model + majority of sites (incl. the explicit comment at `attendance.py:313-318`): **a late student DID attend → every rate counts present + late; half_day/leave/excused never count toward the rate** (half_day keeps its own counter). Sites made consistent: (1) `attendance.py:289` `/attendance/summary` class-level `attendance_rate` now `(present+late)/total_students` (was present-only → understated by every late); (2) `parent_app.py:162-165` `/parent/dashboard` `attendance_pct` counts `("present","late")` (was `+half_day` — the only half_day-counting site); (3) `parent_app.py:238-268` `/parent/child-attendance` — `present` counts exact "present" (was `+half_day`), NEW `half_day` summary key added, `percentage = (present+late)/total` (was present-only); (4) `analytics.py:84-85,92-96,152-158` `/analytics/overview` today% + 30-day rate + per-class buckets all count `("present","late")`; (5) `reports.py:85-91` `_attendance_summary_data` `attendance_rate` (JSON + PDF) counts `present+late` (per-student rows at `:320` were already correct — now consistent with them). Already-consistent sites left untouched: `attendance.py:189` student summary, `:319/:349` school-overview, `teacher.py:193`, `student_app.py:116-118`, `multi_branch.py:123` | Runtime ✅ (live HTTP probe ×2, late-checkin fixture: s1 = 3 present+2 late+1 absent+1 half_day over 7 days; today s2 late + s3 present): student summary `percentage=71.4` (5/7); class summary `present:1, late:1, attendance_rate=66.7` (2/3 — old rule 33.3); `/parent/child-attendance` `present:3, late:2, half_day:1, percentage=71.4`; `/reports/attendance/summary` `attendance_rate=77.8` school-wide (7/9 — old 44.4) with per-student 71.4 intact; `/analytics/overview` `today=100.0` (2/2) and 30-day `77.8` (old 44.4). Pinned by `tests/test_exam_attendance_timetable_assignment.py::test_attendance_late_counts_in_rates` (also pins the mark upsert). |
| E28 rollback-on-error PROVEN for exams marks entry and assignments submission (was asserted-but-unproven) | **FIXED-UNCOMMITTED (proof + guards)** | Both endpoints commit in ONE transaction (`exams.py:781` single `db.session.commit()` after the whole batch; `assignments.py:199` single-row commit), so any mid-write failure rolls back at session teardown. Proven two ways, plus hardening so the common failures are clean 400s instead of 500s: (1) exams — per-record pre-validation (`exams.py:635-700`) fails the WHOLE batch 400 BEFORE any write on missing/non-UUID/garbage `student_id`/`subject_id`/`class_id` or student/subject/class not in this school; a natural mid-batch commit-time failure (Numeric(6,2) overflow `theory_marks=100000` on record 2 → DataError at record 2's autoflush) → 500 with **zero** partial rows; (2) assignments — forced commit failure (`db.session.commit` monkeypatched to raise) → 500, **zero** rows; bogus/garbage student → 400 with zero rows | Runtime ✅ (probe ×2 + pinned tests): marks `[good, bogus-student]` → 400 + row count unchanged; marks garbage student → 400; marks `[good, overflow]` → 500 + count unchanged (2 before, 2 after); assignment submit with commit monkeypatched to raise → 500 + `AssignmentSubmission` count for that student = 0 while the fixture assignment row survives. Pinned by `tests/test_exam_attendance_timetable_assignment.py::test_exam_marks_entry_guards_fail_batch_before_write`, `::test_exam_marks_entry_mid_batch_failure_leaves_no_partial_rows`, `::test_assignment_submit_rollback_on_commit_failure` (3 rollback tests). |

| E50 alumni web directory: FE/BE contract break made "Add Alumni" fail 100% of the time; search box a no-op; stat cards dead | **FIXED-UNCOMMITTED** | `frontend/app/dashboard/alumni/page.tsx` posted `{name, batch_year, current_occupation, organization, address}` but `POST /alumni` requires `first_name`+`last_name` (400 on EVERY submit — toast "Failed to add alumni" always), and the serializer returns `first_name`/`last_name`/`designation`/`current_organization`/`location`/`batch`/`graduation_year` — none of which the cards read (they rendered `a.name`, `a.current_occupation`, `a.organization`, `a.batch_year`, `a.address` → em-dashes everywhere). The search box sent `?search=` which `list_alumni` never implemented, and `data?.stats` didn't exist (all stat cards fell back to list-length/0). Fixed BOTH sides: backend `list_alumni` now filters `?search=` (ilike first/last name, email, current_organization) and returns `meta.stats {total, this_year(last-2 grad years), active(mentor count), organizations(distinct)}`; FE dialog posts the real contract (splits `name` → first/last, sends `graduation_year`+`batch`, `designation`, `current_organization`, `location`), cards render the serializer fields, stats read `data.meta.stats`. Same class of bug as E16/E43. | Runtime ✅ (platform probe ×3): FE-shape payload → 201 with all fields persisted; `?search=Sarita` → exactly 1, `?search=zzz` → 0; `meta.stats` == SQL hand-count (total 3 / active 1 / organizations 1); per-plugin 403 gate toggle verified. `tsc --noEmit`: 0 errors in touched pages. Pinned by probe checks (tmp_platform_verify.py). |
| E51 designer web page degraded to a silent empty template grid on API failure | **FIXED-UNCOMMITTED** | `frontend/app/dashboard/designer/page.tsx` fetched `/design-studio/templates` with no `isError` handling — a failed request (403/expired session) rendered "no templates" with no way to retry (the last remaining E44-pattern page in the platform batch). Added `retry: 1` + the established destructive error card with Retry button. | `tsc --noEmit`: 0 errors in the touched page (4 pre-existing errors in unrelated `sidebar.tsx`/`sanitize.ts` unchanged). |

## 1f. Phase-4 frontend correctness sweep (2026-08-29, batch 7)

| Finding | Status | Evidence (current source) | Note |
|---|---|---|---|
| E70 Two competing PluginGate components (`lib/plugins.tsx` prop `slug`, used by 112 files, vs `lib/plugin-gate.tsx` prop `plugin`) | **FIXED-UNCOMMITTED** | Canonical = `lib/plugins.tsx` (alias resolver + one-click install + labels live there). `lib/plugin-gate.tsx` deleted; its only remaining export in use, `usePluginEnabled`, moved into `lib/plugins.tsx` (wraps `isPluginInstalled`, which already does single-hop alias expansion — better than the deleted copy's `normalizePluginSlug`-only check). Sole straggler `frontend/app/dashboard/exams/results/page.tsx:7` migrated to `import { PluginGate, usePluginEnabled } from "@/lib/plugins"`. Grep: 0 code references to plugin-gate remain (only a provenance comment); `__tests__/plugins.test.tsx` already mocks `@/lib/plugins` only. | The deleted component's unused extras (`roles` prop, `PluginNavGate`) had zero callers (confirmed by import grep before deletion). `tsc --noEmit`: 0 new errors. |
| E71 18+ gating slugs absent from `PLUGIN_LABELS` (`lib/plugins.tsx`) — gate prompts displayed raw underscored slugs (e.g. "student_portfolio — Not Installed") | **FIXED-UNCOMMITTED** | Added 24 labels (post-alias missing set: admission, assignments, basic_reports, benchmarking, biometric, compliance, conferences, disaster_management, dismissal, elibrary, emergency, health_records, incident_management, incidents, inventory, multi_branch, notices, social_ads, social_hub, student_portfolio, timetable, wellbeing, white_label, ai_adaptive_learning) + `library_management`, each matching the live marketplace `Plugin.name` exactly. **Lockout audit: all 43 distinct `<PluginGate slug>` values (grep of app/, incl. alias resolutions for communications/hr/transport/visitors/library) verified present in the live 55-plugin published catalog (in-container query) — ZERO gating slugs missing from the catalog**, i.e. no install-forever lockout existed; the defect was display-only. | Live catalog snapshot: 55 published rows; frontend gating slugs = 43 distinct; union resolves 100%. |
| E72 Stale fetch paths: frontend `portfolio` page called `GET/POST /portfolio`, `PATCH /portfolio/:id/verify` — NONE exist in the Flask url_map (backend exposes only `/portfolio/students/<uuid>[/items|/credentials]`); page rendered fake data shapes (`category`/`verified`/`student_name` not in serializer) | **FIXED-UNCOMMITTED** | `frontend/app/dashboard/portfolio/page.tsx` rewritten to the real contract: student-selector → `GET /portfolio/students/:id/items`, add-achievement → `POST /portfolio/students/:id/items` (payload `{title, description, item_type}` = real columns), grouped by `item_type`, client-side search, honest empty state; Verify button removed (no backend concept — pinned: PATCH `/portfolio/:id/verify` → 404); `verified`/`achievement_date` fake fields dropped. Second stale path: `POST /ai-tools/letter-writer` had NO backend route (letter page 404'd on every Generate) — new `POST /ai-tools/letter-writer` in `app/api/v1/ai_tools.py` (gated `@jwt_required @school_required @plugin_required("ai_tools") @role_required(...)`, required-subject 400, consistent with sibling routes) + `QuestionPaperService.generate_letter` (AITokenHub `feature="letter-writer:generate"`, fast model) — path diff sweep of 240 distinct frontend paths vs live url_map now: 0 unmatched (remaining 2 "misses" were regex false positives: `/library/books${params}` query concat). | Runtime ✅ (`backend/tmp_phase4_verify.py`, throwaway school, kept for reruns): letter-writer 200 + content, usage row `letter-writer:generate` success, missing-subject 400, no-plugin school 403; portfolio item create 201 → list 200 with real serializer fields, teacher role 200, no-plugin 403, verify route 404; fixtures cleaned to 0 rows. Live curls: `POST /ai-tools/letter-writer` → 401 (was 404), `GET /portfolio/students/<uuid>/items` → 401. |
| E73 33 dashboard/report pages checked `isLoading` but rendered NOTHING (or fake zeros) on API error — silent empty states platform-wide (the last unreached E44-pattern pages) | **FIXED-UNCOMMITTED** | Established error pattern applied (`retry: 1` + destructive "Failed to load …" card with Retry): **money/grades priority** — fees overview (was rendering Rs. 0 KPIs on failure!), fees/defaulters, fees/collect (student ledgers), hr dashboard, super-admin platform dashboard, exams index, exams/grades, exams/marks (students query), exams/results (results + grade-sheet), exams/report-cards, reports/{exam,expense,teacher}, analytics/{overview,academic,financial}; **dashboards** — multi-branch/{index,dashboard,analytics}, benchmarking, biometric{,/logs}, library, communications{,/gallery}, transport/{logs,map}, timetable/teacher, students/reset-password, social-hub, ai-tools/insights, notifications class; **website-builder** — domain/editor/seo/themes (seo's error check placed BEFORE the `isLoading || !form` spinner, which previously spun forever on error). Full-page early-return card where the page is query-driven, inline error branch where loading was inline. | `tsc --noEmit` in `aschool-nextjs-1`: 4 errors total = exactly the 4 pre-existing (`sidebar.tsx`, `sanitize.ts`) — 0 new. Count: 19 batch (standard shape) + 14 hand-edited (fees ×2, hr, insights, social-hub, reports ×2, website-builder ×4, exams ×5 incl. collect) = 33. |
| E74 Data tables clipped on mobile: `overflow-hidden` wrappers prevent horizontal scroll on wide tables | **FIXED-UNCOMMITTED** | 4 wrappers fixed `overflow-hidden` → `overflow-x-auto`: `app/dashboard/sms/page.tsx` (×2: history + credits tables), `app/dashboard/exams/results/page.tsx` (grade-sheet), `app/school/[slug]/results/page.tsx` (public results). `components/ui/table.tsx` Table primitive already wraps in `overflow-auto`; remaining `<table>` pages either use that primitive or have narrow content (bulk-import 3-col guide table — not an offender). | 11 raw-`<table>` pages surveyed; 5 lacked scroll; 4 fixed (designer/writer's tables are print-HTML strings, not UI). |
| E75 Hardcoded hex colors in `app/` where a design-token equivalent exists (`tailwind.config.js` ocean/mint/sun/fog/ink) | **FIXED-UNCOMMITTED** | 16 arbitrary-value classes → tokens, all in `app/(auth)/login/page.tsx` (13) + `app/(auth)/register/page.tsx` (3): `bg-[#c5f4dd]/15|/20`→`bg-mint/*`, `text-[#c5f4dd]`→`text-mint`, `bg-[#f4c25d]`+`/[#f4c25d]/10`→`bg-sun*`, `text-[#f4c25d]`→`text-sun`, `text-[#0d1f14]`→`text-ink`, `from-[#0e3b2e]`→`from-ocean`, `via-[#155a44]`→`via-ocean-light`, `to-[#0e3b2e]`→`to-ocean`. Not replaced (no token equivalent / not classes, noted honestly): `app/page.tsx` hexes are CSS-var DEFINITIONS in a `<style>` block; `app/(auth)/layout.tsx` hexes are plain CSS rules; dashboard pages' hexes (green/amber/red semantics) have no brand-token equivalents. | Zero token-hexes left in the two edited files; no redesign (same colors, token references). |

## 1e. PLATFORM plugins batch — verification log (2026-08-29, Phase-2 batch 6)

Method: throwaway growth-plan fixture school built from direct models in `aschool-flask-1` (admin/teacher/student users, class+section+subject, 2 students with attendance rows, exam+marks, fee collections, 2 alumni, a FOREIGN school's course taught by this school's admin as a cross-tenant fixture), driven over live HTTP (login tokens from `data.access_token`). Probe `backend/tmp_platform_verify.py`: **152/152 checks green, run 3×**; forced-commit-failure rollback probe in-process via `app.test_client()`. Fixture schools fully deleted afterwards (0 rows remain, sweep of 12 leftover schools from earlier runs included). FE type-checked in `aschool-nextjs-1` (`tsc --noEmit`): 0 errors in the two touched pages.

- **slug consistency** ✅ for all 12 — manifest slug = backend `plugin_required` slug = frontend `PluginGate` slug for every gated plugin (website_builder, basic_website, design_studio, lms, alumni, compliance, iemis_importer, file_management, basic_reports ×7 routes incl. the 3 new `/pdf` exports). `settings_core`/`dashboard`/`marketplace_nav` are core, sidebar-driven (no PluginGate, no backend gate — consistent with manifests). design_studio's blueprint also carries intentional shared-surface gates (`digital_content`/`ai_insights`/`ai_tutor` — E22b territory). Live 403-toggle verified per plugin (basic_reports, basic_website, design_studio, alumni, lms, compliance, iemis_importer → 403 with install URL when deactivated, 200 after re-activate).
- **marketplace_nav** ✅ — catalog lists 55 published plugins, lms entry carries manifest price/tier (799/growth), digital_content absent (E14 holds); install/uninstall roundtrip; sidebar `bottom_nav` exposes marketplace+settings.
- **dashboard** ✅ — `/analytics/overview` hand-checked against SQL: attendance 66.7 (2/3 present+late rule), fees collected 5000, collection rate 62.5, active students 2, report-card avg 55.0 — all exact; teacher dashboard 200.
- **basic_reports** ✅ — attendance summary (rate 66.7 = SQL, per-student rows), fees collection (== SQL), exam results (avg 55 == SQL), `/reports/dashboard` totals == SQL; **all three `/pdf` exports stream real `%PDF-` bytes**; role gate (teacher on fees/collection → 403).
- **settings_core** ✅ — `/schools/current` GET/PUT roundtrip persisted, teacher PUT → 403, notification-settings merged defaults + PUT roundtrip persisted.
- **file_management** ✅ — upload → record (size/type) → download bytes match; list search; usage byte-count; presigned URL; PATCH tags; DELETE soft-deletes + unlisted; oversize multi-file batch → 413 with ZERO partial rows.
- **iemis_importer** ✅ — formats(3)/validate dry-run/import E2E (phoneless row → deterministic placeholder `9800000xxxx`, E10 holds); history + detail; re-import idempotent (skipped=2); **rollback: cap-exceeded import → 403, zero partial students, failed log row**.
- **basic_website** ✅ — config PUT sanitizes CSS (url()/`@import`/javascript: dropped, safe declarations kept) on both nested and legacy top-level paths; public endpoint reflects theme+meta+sections; contact form 201 (lands in compliance audit-logs); unpublish hides the public site.
- **website_builder** ✅ — 20 themes, apply theme, page create/section update, publish/unpublish; **workflow proven end-to-end: builder config change (heading v1→v2, meta "Probe Site v2") renders on the public API AND in the live Next.js SSR page (`/school/<slug>` HTML contains "Welcome v2" + meta)**.
- **design_studio** ✅ — templates list (7 built-ins), school override upsert appears in list, single render HTML; **bulk id-cards + bulk marksheets JSON shape verified** (`{count, cards|marksheets: [{student_name, student_id, student_roll, template_id, template_width/height, canvas_json, html}]}`, marksheet embeds the real subject marks 80/30 + rank + percentage); documents CRUD (teacher role) incl. update-via-same-id + delete→404 + name guard.
- **alumni** ✅ after E50 — directory CRUD + batch/mentors filters + search (new), events (title/date guards, create/list/update), donations (bogus alumni_id 400, negative amount 400), per-plugin gate.
- **lms** ✅ — course→lesson(×2)→enroll (bogus student 400, duplicate 409)→progress E2E (0→50→100%, aggregates re-derived, student-app dashboard shows 100%); cross-tenant lesson PUT → 404; **rollback: forced commit failure on record_progress → zero partial StudentProgress rows AND Enrollment aggregates intact (sp 2→2, 100.0→100.0)**.
- **compliance** ✅ — report generate (real student/staff counts), manual draft→submit (submitted_by set), EMIS export record + task-backed CSV persisted and downloadable (G2 ✅), audit-logs cross-plugin check; report-card PDF task proven at runtime (G1 ✅, 9,767-byte `%PDF-`).
- **premium gating sanity** (white_label/biometric/multi_branch/ai_adaptive_learning): all four → 403 with install URL on a growth school that hasn't installed them. Other agents' implementations untouched.
- **Tooling note:** the probe's old per-school cleanup swallowed FK errors (cross-tenant `courses.teacher_id` reference blocked user deletion) and deferred its single commit, silently leaking fixture schools across runs; rewritten as a two-pass FK-safe sweep (expanding IN, verbose failures, 0 leftovers). No product code affected.

Tests: `tests/test_reports_pdf.py + test_plugins.py + test_plugin_aliases.py + test_plugin_manifests.py` = **636 passed** and `test_models.py + test_provisioning_and_caps.py + test_marketplace_billing.py` = 22 passed, run on a DEDICATED test DB (`TEST_DATABASE_URL=…/aschool_test_platform`) — on the shared `aschool_test` DB the same suites deadlocked (TRUNCATE races with other agents' concurrent pytest processes; environmental, not code). One stale pin updated: `test_json_endpoints_keep_shape` now includes the per-student `students` key the attendance summary legitimately returns.

## 1b. Academics batch — verification log (2026-08-29, Phase-2 batch 4)

Method: throwaway fixture school (growth plan) with timetable/assignments/attendance/exams/basic_reports installed, admin/teacher/parent/student users, class "Five" (2 sections) + subject, driven over live HTTP in `aschool-flask-1` (login tokens from `data.access_token`); forced-failure rollback probes run in-process via `app.test_client()` with `db.session.commit` monkeypatched. Probe `backend/tmp_probe_e24_batch.py`: **54/54 checks green twice**; fixture school fully deleted afterwards (0 rows remain, sweep of prior runs included). FE type-checked in `aschool-nextjs-1` (`tsc --noEmit`): 0 errors in the two touched pages (7 pre-existing errors in unrelated files).

- **timetable** ✅ after E25 — conflict detection live (teacher/class double-book → 409, free slot → 201, overlapping-window teacher clash → 409, different teacher+section overlap → 201); GET filters/serializer verified; FE loading+error states.
- **assignments** ✅ after E26/E28 — CRUD + submit + grade end-to-end; due-date logic verified (`status` active/past; `is_late` now persisted and returned; late-checkin fixture proven both ways); FE error state + real `submitted_count`.
- **attendance** ✅ after E27 — late-checkin fixture verified against student summary, class summary, parent dashboard + child-attendance, reports summary, analytics overview (all consistent: present+late; half_day excluded, reported separately).
- **exams** ✅ after E24-exams-class-id/E28 — marks entry persists class (E24), batch guards + rollback proofs; results/grade-sheet class filtering sees the rows.
- Residual (honest): timetable conflict detection is per-day/period + time-window; it does not model room capacity or teacher-absence (no model exists for the latter).

Tests: new `backend/tests/test_exam_attendance_timetable_assignment.py` 8/8; required suite `pytest tests/ -q -k "exam or attendance or timetable or assignment"` = **61 passed**; adjacent suites `test_gpa_endpoint_vs_util + test_biometric + test_multi_branch + test_ops_validation_guards + test_models` = 41/41.

## 1c. COMMUNICATIONS plugins batch — verification log (2026-08-29, Phase-2 batch 3)


Method: throwaway fixture school (growth plan) with the 6 paid/free comms plugins installed + admin/teacher/parent users + student, driven over live HTTP in `aschool-flask-1` (login token from `data.access_token`); webhook scenarios in-process via test_client so config patches apply and foreign enabled-bot rows could be snapshotted/disabled/restored. 108/108 probe checks green twice; fixture schools fully deleted afterwards (0 rows remain).

- **slug consistency** ✅ — manifest slugs = backend `plugin_required` slugs for all 7 (sms_notifications/whatsapp_bot/notices/social_hub/conferences verified by live 403 toggle per route; notifications is core/un-gated by design); frontend `PluginGate` slugs match (sms, whatsapp, notices, social_hub, social_ads-campaigns, conferences added this batch); marketplace listing matches manifest tiers. Caveats: `social_ads` shares the social_hub blueprint but gates NOTHING backend-side (E30); `communications`→`sms_notifications` alias is frontend-only mapping (harmless, no backend gate uses the legacy slug).
- **sms_notifications** ✅ after E31/E34 — send/validate/history/templates/stats all live; no-credential behavior honest (console fallback + `failed` rows, loud log); task with fake provider config → `failed` + raised, never a silent fake success.
- **whatsapp_bot** ✅ after E32 — config/auto-replies CRUD persisted; send + send-bulk degrade to `{"skipped": true, reason: whatsapp_not_configured}`; inbound webhook honesty contract fully verified (processed/store/unhandled/signature/rollback). Note: `tasks/whatsapp_sender.py` still `config["WHATSAPP_ACCESS_TOKEN"]` direct-KeyError if ever invoked without creds — it has no callers today (dead task), left as-is.
- **notices** ✅ — CRUD + filters + XSS bleach-sanitization + BS dates; event guards fixed (E33); `notice.created` event fans out real in-app notifications to all school users (cross-plugin integration with the notification center proven live).
- **social_hub** ✅ — posts/likes(toggle)/comments/groups + delete authorization (author or admin) verified live.
- **social_ads** ✅ — E30 FIXED-UNCOMMITTED (2026-08-29): real `/social/campaigns*` blueprint (plugin-gated, tenant-scoped) + honest audience estimates; delivery counters stay honest zeros (no Meta API).
- **conferences** ✅ after E33 — slots/booking/notes end-to-end once; all guard cases 4xx; rollback on mid-write failure proven.
- **notifications (in-app)** ✅ — list/unread-count/mark-read/mark-all/delete + per-user tenancy (404 cross-user) + per-school isolation; unread-count consumed by the web bell page; service endpoints match backend routes.
- **Frontend**: web notifications page had a silent `catch {}` → added explicit error card + retry; conferences page was the only comms page without `PluginGate` → added; conferences list got `isError` + retry instead of empty-list-on-failure. sms/whatsapp/notices/social pages already have loading+error states. (Flutter sides: M2/M10 covered elsewhere.)

Tests: new `backend/tests/test_comms_plugins.py` 21/21; adjacent suites `test_auth + test_web_session_cookies + test_ops_validation_guards + test_plugin_aliases + test_provisioning_and_caps + test_models` 49/49. (Test-DB deadlocks during runs were caused by a concurrent agent suite, not code — retry-on-setup-error loop used; final runs green attempt-1.) Temp probe kept at `backend/tmp_comms_verify.py` for reruns.

## 1d. CAMPUS OPS plugins batch — verification log (2026-08-29, Phase-2 batch 5)

Method: throwaway growth-plan fixture school registered over live HTTP in `aschool-flask-1` (token from `data.access_token`), premium plugins (emergency/incidents/disaster_management) installed via the marketplace API, growth-tier campus-ops plugins verified present from `grant_plan_plugins`, class + 3 students + teacher user. Probe `backend/tmp_campus_verify.py`: **145/145 checks green** (137/137 on the pre-rollback version, run twice; rollback + counselor section added afterwards). Forced-failure rollback probes run in-process via `app.test_client()` with `db.session.commit` monkeypatched. Fixture school fully deleted afterwards (0 rows remain). FE type-checked in `aschool-nextjs-1` (`tsc --noEmit`): 0 errors in all touched pages.

- **slug consistency** ✅ for the 8 plugins with a live API surface — manifest slugs = backend `plugin_required` slugs = frontend `PluginGate` slugs, with two deliberate alias/canonical notes: backend library routes gate the LEGACY slug `library` while the marketplace/product slug is `library_management` (single-hop alias makes both installs pass; student_app correctly gates the canonical `library_management`; the web pages gate `library` — consistent trio, but **both `library` and `library_management` remain PUBLISHED in the marketplace at the same price** — the same double-listing E14 flagged as a "clean rename"; harmless but a candidate for the E14-style unpublish treatment); `elibrary` manifest `api_blueprint: app.api.v1.library` is stale metadata (routes live in `elibrary.py`, statically mounted — cosmetic only, E22c class). Live 403 toggle verified per-plugin (wellbeing and incidents uninstalled → 403 with install URL → reinstalled → 200).
- **incidents vs incident_management** — confirmed legit base+extension at slug level (no route overlap). Extension backend now REAL (E41 FIXED batch 6 — `/incidents/management/*` workflow/escalation/analytics); disaster drills/seismic/overview real too (E40 FIXED batch 6). Correction: `install_plugin` DOES enforce `depends_on: [incidents]` now (probed: install without incidents → "Dependency not met"), while `grant_plan_plugins` grants incident_management without incidents — the extension therefore also works standalone.
- **library_management** ✅ — issue/return E2E: default due +14d, copies decremented/incremented atomically, exhausted-book 400, double-return 400, `?status&book_id&student_id` filters (drives the web return flow), student/book name denormalization. **Rollback proof**: forced commit failure on issue AND return → zero rows AND `available_copies` NOT desynced (the two-table write is single-transaction).
- **elibrary** ✅ — digital books (stats meta), past papers (title+file_url guards), OER resources (tags list guard) verified.
- **hostel** ✅ — allocate/checkout E2E: context fields (hostel_name/room_number/monthly_fee/roll), full-room 422, duplicate active allocation 422, summary occupancy math (100% → 0 after checkout), bad-date 400, bogus student 400. **Rollback proof** (pinned test). Plugin gates added this batch (previously entirely ungated — routes were reachable without any install).
- **health_records** ✅ — profile upsert via PUT (exists:false→true roundtrip), profiles registry + search, visits + immunizations create/list with student_name; unknown-student 404 / foreign-student 400 guards. FE vaccination date-picker binding bug fixed (E44c).
- **emergency** ✅ — alert trigger (enum + title guards), resolve (status enum), headcount submit/list with integer + UUID[] guards; Socket.IO `emergency.alert_broadcast` emitted on trigger; teacher role 403 on admin-only trigger. Residual folded into E40: `alert_service.send_emergency_alert/mark_student_safe` have zero callers (SMS/push on alert + safe-status confirmation unwired).
- **incidents** ✅ after E42 — report + witness statement + action + resolve E2E; cross-tenant involved_student_ids rejected; search/status/severity/type filters; rollback proof.
- **incident_management** ✅ after E41 (batch 6) — see the E41 row: assign/forward-only-status/escalate(+notification)/conference/resolve/audit/reports over the shared Incident rows; base routes untouched.
- **disaster_management** ✅ after E40 (batch 6) — see the E40 row: drills CRUD + participation + overview w/ hand-computed readiness + honest USGS seismic feed.
- **gamification** ✅ — points award (int guard, bogus-student 400), badge award (badge-existence 400), houses/rewards guards, leaderboard **math hand-checked**: S2 +20, S1 +5+7, S3 −3 → board `[(1, Bibha, 20), (2, Asha, 12), (3, Chandra, −3)]`, `?top=1` alias limits; teacher award reflected immediately. Negative points allowed (behavior deductions — intentional).
- **wellbeing** ✅ after E43 — mood log E2E (admin on-behalf + bogus-student 400 + non-int energy 400 + admin-without-student-profile 400), summary math `{happy:2, sad:1} total 3` hand-checked, counselor notes (contract fixed), surveys CRUD (dead FE inputs removed). Mood self-check-in resolves the logged-in student's profile (previously FK 500).
- **Frontend**: all 10 plugins' pages use live endpoints with loading states; the 15 pages missing error states now have error+retry cards (E44); incidents Type column and vaccination date binding fixed; counselor/surveys contracts aligned (E43/E44d).

Tests: new `backend/tests/test_campus_ops_plugins.py` **13/13** (issue/return math + rollback, hostel occupancy + rollback, incident cross-tenant + serializer scoping + rollback, leaderboard math, emergency guards, mood summary, counselor-note contract, E40/E41 404 pins, incident_management-only-gate coupling pin) + `backend/tests/test_campus_ops_guards.py` **8/8** (prior-agent file fixed this session: flush-before-id assignment, second-room date guard, plugin-cache invalidation after direct DB flips). Committed `tests/test_tenant_isolation_hostel_faq.py` updated for the new hostel plugin gates (installs the plugin so the tests pin isolation, not entitlement). Full affected selection `pytest -k "library or hostel or health or emergency or incident or gamification or wellbeing or plugin"` green after fixes. Temp probe kept at `backend/tmp_campus_verify.py` for reruns.

## 6. AI plugins batch — verification log (2026-08-29, Phase-2 batch 2)

Method: in-container Flask test_client probes with AITokenHub provider functions monkeypatched (fake text, real quota check + real usage logging), fixture school with classes/students/attendance/marks(±NULL)/fees/exam+report-cards/assignment/incident; every non-LLM aggregate independently recomputed in SQL and compared to endpoint output; fixture schools fully deleted afterwards (0 rows remain, 0 orphan usage logs).

- **ai_tools** ✅ — question-paper/lesson-plan/remarks/homework-help/timetable + insights/weekly all 200 with well-shaped payloads; 6 distinct usage-log features (`question-paper:generate`, `lesson-plan:generate`, `question-paper:remark`, `auto-grader:grade`*, `homework-helper:tutor`*, `school-insights:weekly`*; *shared with the plugins below) each carrying tokens=150 + user attribution; quota-exhausted → 429 (E18). Insights weekly embeds real 7-day metrics (attendance 88.9% matched hand count of window rows).
- **ai_grading** 🔧 — `/assignments/<id>/ai-grade` 200 + parsed grading JSON + usage row `auto-grader:grade`; but gating is `assignments`, not `ai_grading` (E22a), and the manifest's frontend routes don't exist.
- **ai_tutor** ✅ — `/design-studio/ai/homework-help` (gate `ai_tutor`) and `/ai-tools/homework-help` (gate `ai_tools`) both 200 Socratic JSON + `homework-helper:tutor` usage rows.
- **ai_insights** ✅ (after E19 fix) — risk-alerts 200 with correct multi-factor scores (attendance %, overdue fees, incidents), daily-brief non-LLM (events + headcount verified), weekly report metrics real; design-studio `ai/insights` + `ai/risk-students` gates honored; usage logged. Web frontend surfaces insights only under ai_tools (E22b).
- **ai_adaptive_learning** ✅ (after E23 fix) — `/lms/learning-paths*` + `/lms/adaptive-progress` + mastery routes live, plugin-gated and runtime-verified; service null-safety preserved (see E23-adaptive-learning).
- **benchmarking** ✅ — `/benchmarking/overview` school block matched hand-math exactly (pass 75.0 / avg 60.5 / attendance 76.2 / ratio 5.0); district+national averages matched an independent per-school SQL recomputation (7.0/8.7) across all dev-DB schools; rankings endpoint works (N+1 per-school `_overview_payload` — noted perf, not fixed).
- **advanced_analytics** ✅ — `/analytics/overview` (attendance 76.2 = 16/21 rows, fees 8000/2000/80.0%, exam avg 60.5/pass 75.0, top subject = SQL max-avg subject), `/analytics/academic` (class-wise hand math: C5 57.5/50%, C6 63.5/100%, at-risk = the 30% student), `/analytics/financial` shape — all real aggregates, hand-verified. Note: analytics routes are role-gated only (no plugin gate — consistent with manifests pointing at core `reports`/no gate).
- **ai_usage** ✅ — quota GET/PUT/init(idempotent)/stats/logs verified; stats `usage.today`, `top_features` counts and (post-E20) `daily_chart` all match SQL exactly.
- E7 re-check: token-hub-only wiring still holds repo-wide (0 direct anthropic imports outside token_hub.py).

Tests: new `backend/tests/test_ai_quota_and_usage.py` 5/5; `test_ai_token_hub.py + test_auth.py + test_plugin_aliases.py + test_provisioning_and_caps.py` 28/28; `test_plugins.py` 16/16.
| `backend/app/api/v1/fees.py` | 2026-08-28 (Money & Grades audit §3): discount stacking confirmed ADDITIVE on the base at the single math site `_collection_payable_total` (cap = base, net floored at 0); partial-payment running balance exact; overpay capped; discount-below-paid edits rejected 400. Fixed real defect: receipt PDF "Outstanding after payment" was recomputed from the collection's CURRENT state, so reprinting an older receipt after later payments printed a wrong due (first receipt of a 9000 settlement printed 0.00 instead of 6000.00) — `_receipt_pdf_html` now sums receipts up to and including that receipt (capped at payable) for a point-in-time figure; pinned by in-container pypdf regression test. Also hoisted `sqlalchemy.func` to module import. |
| `backend/tests/test_gpa_endpoint_vs_util.py` + `backend/tests/test_fees_money_math_verification.py` + `backend/tests/test_hr_payroll_math.py` (fixture made consistent with its own assertions + 3 new tests) | New Money & Grades regression tests (2026-08-28 audit §1-§3): `GET /exams/<id>/results` GPA == util == hand math (3.6 / 1.47; weighted 3.69 / 1.33); fees additive 10%+5% = 1500 on 10000, fine+discount payable 9000, waive clamp 0, running balance 3000/6000→9500 settle, overpay cap 8500, summary 18000/18000; payroll explicit-gross net consistency 44000 and legacy-row fallback. Payroll file 8/8, both new files green in `aschool-flask-1`. |
| `backend/tests/conftest.py` | `_reset_database()` now rebuilds the schema only once and TRUNCATEs all tables between tests (dedicated connection + `engine.dispose()` around both paths). The old drop-schema/create-schema churn per test intermittently failed `CREATE TYPE` with `duplicate key … pg_type_typname_nsp_index` (pre-existing: `test_plugin_aliases.py` errored at setup before this change) and was slow. Suites verified green after: `test_biometric.py` 11/11 (×2), `test_plugin_aliases.py` 2/2 (previously erroring), `test_models.py` 13/13. |

## 5. Tally

- 27 findings audited: **3 FIXED-COMMITTED** (E8, M6, M12), **16 FIXED-UNCOMMITTED** (E1, E1b, E2, E3, E4, E5, E6, E10, E11, E12, M3, M4, M5, M7, M8, M9), **2 PARTIAL** (E7, E9), **6 OPEN** (G1, G2, M1, M2, M10, M11).
- P0 E1-E5 all FIXED-UNCOMMITTED now (commit pending). P1 E10-E12 fixed this session. M3/M4/M5 verified against the live Flask url_map and fixed this session.
- E13/E14 (staff-import `User.settings` crash; digital_content/elibrary pricing dedup) fixed this session — see rows above. E14 duplicate-pair scan fully resolved: BOTH `digital_content`/`elibrary` AND `portfolio`/`student_portfolio` were true same-feature-twice duplicates — both deduped the same way (canonical slug kept published: elibrary 299, student_portfolio 299; duplicate unpublished+deprecated in manifests, seed-synced, gating alias kept for legacy installs, backend gates reconciled to the canonical slug); `incidents` vs `incident_management` is a legitimate base+extension pair, both stay published.
- Plugin-module gap (prior audit §4: `multi_branch` + `biometric` published-but-empty): **both backends now implemented** (see §4 rows) — the premium NPR 1999 biometric and NPR 2999 multi_branch tiers no longer sell empty modules.
- **Phase-2 Money & Operations batch (2026-08-29): E15 (fee_types schema drift), E16 (dismissal verify-qr QR-string contract + records student_name/search), E17 (visitor/inventory/payroll 500-on-bad-input guards + inventory negative-money guard) — all FIXED-UNCOMMITTED, runtime-verified via a 98-check live-HTTP probe (98/98 twice) + new `tests/test_ops_validation_guards.py` (6/6) + 674 pre-existing tests re-run green.** Plugin verdicts: fees ✅ (types/structures/collections/pay/receipt-PDF/defaulters/summary), hr_payroll ✅ (stats/payroll CRUD/approve/payslip-PDF/leaves), admission ✅ (inquiry→application→under_review→interview→accept auto-enrolls 1 Student+User cap-checked→enrolled; re-accept does not duplicate), inventory ✅ (assets/QR-scan/audit/procurement; no stock-move model exists by design), gps_tracking ✅ (routes/buses/stops/realistic ESP32 ingest with lat/lng range + foreign-bus rollback; transport blueprint gated by slug `gps_tracking`, legacy alias `transport`), visitor_management ✅ (check-in/out/search/appointments/approve), dismissal ✅ after E16/E17 fixes. Residuals (not fixed, low sev): admin-flutter `/dismissal/summary` 404 (see E16 note); list-query error UI on some web pages degrades to empty state (M10 pattern).
- **Phase-2 AI plugins batch (2026-08-29): E18 (quota-exhausted 500→global 429 handler), E19 (risk-alerts `Incident.students_involved` AttributeError crash), E20 (`/ai-usage/stats` daily_chart oldest-7-days) — FIXED-UNCOMMITTED + pinned (5/5); E21 (ai_adaptive_learning premium published-but-empty: `/lms/learning-paths*`, `/lms/adaptive-progress` don't exist) FIXED-UNCOMMITTED via E23-adaptive-learning (new gated blueprint + LearningPath/MasteryRecord models + migration, runtime-verified + 7 pinned tests); E22 (ai_grading bypassed by assignments gate; insights dual-gated ai_tools+ai_insights; stale manifest service names) — OPEN, need product/model decisions. E7 token-hub routing verified still holding. Per-plugin verdicts in §6.**
- **Phase-2 COMMUNICATIONS batch (2026-08-29): E30 (social_ads published-but-empty — FIXED-UNCOMMITTED, see row), E31 (SMS queue rows stuck queued forever — FIXED), E32 (WhatsApp auto-reply JSONB append silently lost; inbound webhook honesty verified — FIXED), E33 (conferences/notices 500-on-bad-input guards + bare-array slots — FIXED), E34 (`g.current_user` unresolved for mobile header auth → 500s — FIXED centrally). All fixed rows runtime-verified twice (108/108 probe checks) and pinned by `tests/test_comms_plugins.py` 21/21 + 49/49 adjacent suites. Per-plugin verdicts in §1c.**
- **Phase-2 ACADEMICS batch (2026-08-29): E24-exams-class-id (marks entry `class_id=None` invisible to /results + /grade-sheet — renumbered from the prior agent's colliding "E15" row; ops E15 fee_types kept as-is), E25 (timetable manual slots: zero conflict detection + garbage/FK-id 500s + FE error state), E26 (assignments NOT NULL 500s + dormant `is_late` column now computed + student-id guards + FE error state + `submitted_count` field fix), E27 (attendance "late" rule made uniform across 6 sites in 5 files: present+late counts toward every rate, half_day excluded), E28 (rollback-on-error PROVEN for exams marks entry and assignments submission: forced commit failure + mid-batch DataError → zero partial rows) — all FIXED-UNCOMMITTED, runtime-verified twice (54/54 live-HTTP probe checks, fixture cleaned to 0 rows) and pinned by `tests/test_exam_attendance_timetable_assignment.py` 8/8 + required `-k "exam or attendance or timetable or assignment"` suite 61 passed + 41 adjacent. Plugin verdicts: timetable ✅ (after E25), assignments ✅ (after E26/E28), attendance ✅ (after E27), exams ✅ (after E24/E28). Per-plugin notes in §1b.**
- **Phase-2 CAMPUS OPS batch (2026-08-29): E40 (disaster_management published-but-partial — drills/seismic/overview routes do not exist — OPEN), E41 (incident_management has ZERO API surface; depends_on unenforced — OPEN), E42 (incidents cross-tenant UUID[] leak + FK 500 guards — FIXED), E43 (counselor-note contract: notes were saved with empty content — FIXED), E44 (15 campus-ops web pages lacked error states + 3 field-contract bugs — FIXED). Rollback-on-error PROVEN for library issue/return (counter integrity) and incidents create (+ hostel allocation in tests). Runtime-verified via a 145-check live-HTTP probe; fixture cleaned to 0 rows. Plugin verdicts: library_management ✅, elibrary ✅, hostel ✅, health_records ✅, emergency ✅, incidents ✅ (after E42), gamification ✅, wellbeing ✅ (after E43), disaster_management ⚠️ (E40), incident_management ⚠️ (E41). Per-plugin notes in §1d.** (E40/E41 subsequently FIXED in batch 6 — see next bullet.)
- **UPDATE batch 6 (2026-08-29, later same day): E40 + E41 FIXED — see the flipped rows above. disaster_management got `app/api/v1/disaster_management.py` (drills CRUD+participation, overview with hand-computed readiness score, honest USGS seismic feed; models `DisasterDrill`/`DrillParticipation` + migration `c3d4e5f6a7b8`), incident_management got `app/api/v1/incident_management.py` (assignment, forward-only status workflow w/ audit trail, escalation via the in-app notification service, parent conference, analytics; models `IncidentEscalation`/`IncidentWorkflowEvent` + `incidents` workflow columns + migration `d4e5f6a7b8c9`; zero base-route duplication). Runtime: 111/111 live-HTTP probe checks (both plugins E2E, gating, cross-tenant, rollback via monkeypatched-commit tests), fixtures cleaned to 0 rows. Tests: `test_disaster_management_api.py` 6/6 + `test_incident_management_api.py` 8/8 new; campus-ops 404 pins flipped to 200 (13/13); adjacent suites (aliases/billing/provisioning/models) 25 passed. `depends_on` enforcement CORRECTION: install_plugin DOES enforce it now. Plugin verdicts: disaster_management ✅ (after E40), incident_management ✅ (after E41).**
- **Phase-2 PLATFORM plugins batch (2026-08-29): E50 (alumni web directory FE/BE contract break — Add Alumni always 400, search no-op, dead stat cards — FIXED both sides), E51 (designer page silent empty state on API failure — FIXED). G1/G2 (report-card PDF + EMIS CSV discarded) re-verified FIXED at runtime and their rows flipped from OPEN. Rollback-on-error PROVEN for IEMIS import (cap-exceeded → zero partial students + failed log) and LMS record_progress (forced commit failure → zero partial rows, aggregates intact). Runtime-verified via a 152-check live-HTTP probe run 3×; fixture cleaned to 0 rows incl. 12 leaked by earlier runs. Plugin verdicts: marketplace_nav ✅, dashboard ✅, basic_reports ✅, settings_core ✅, file_management ✅, iemis_importer ✅, basic_website ✅, website_builder ✅ (public SSR render of changed config proven), design_studio ✅, alumni ✅ (after E50), lms ✅, compliance ✅. Per-plugin notes in §1e.**
- **Phase-4 frontend correctness sweep (2026-08-29, batch 7): E70 (PluginGate dedup — plugin-gate.tsx deleted, canonical lib/plugins.tsx), E71 (24 PLUGIN_LABELS added; all 43 gating slugs verified against the live 55-plugin catalog — ZERO lockout slugs), E72 (portfolio page rewired to real /portfolio/students/:id/items contract + new /ai-tools/letter-writer route; 240-path diff vs url_map now clean), E73 (33 pages gained error states — fees/exams/super-admin/analytics/multi-branch incl.), E74 (4 table overflow wrappers), E75 (16 hex→token replacements). tsc: 0 new errors; runtime probe backend/tmp_phase4_verify.py 14/15 (1 self-check expectation, see row) + live curls green.**
- **PAYMENT-GATEWAY callback batch (2026-08-29): E60 (initiate routes handed gateways DEAD `/api/v1/webhooks/*` callback URLs — paid money 404ed and was never recorded), E61 (no server-side initiation record; FonePay success callbacks could NEVER resolve the collection — money taken, never marked), E62 (duplicate callbacks created duplicate/zero receipts and double-counted PARTIAL payments), E63 (Khalti pidx-swap: any Completed pidx credited any collection), E64 (amount mismatches silently capped/accepted), E65 (Stripe webhook NameError on every valid event, masked by missing `stripe` package), E66 (Khalti lookup outage 500; webhooks CSRF-exempted) — all FIXED-UNCOMMITTED + runtime-verified (probe 60/60 twice) and pinned by `tests/test_payment_webhooks.py` 16/16; required `-k "payment or esewa or khalti or fonepay or webhook"` = 23 passed, adjacent money suites 33 passed. Migration `b7e2c9d4a5f3 payment_initiations` applied. Gateway verdicts: eSewa ✅, Khalti ✅, FonePay ✅, Stripe ✅ — see §1f.**

## 7. Slice-1 frontend page verification (2026-08-29) — E90-E98

Scope: the 25 slice-1 routes (landing, auth, super-admin, dashboard root, profile, settings/*(6), marketplace, notifications, notices, sms, bulk-uploads/*(3), users, staff/*(2), parents/*(2), visitors). Per-page ledger: `audits/coverage/FRONTEND_PAGE_VERIFICATION.md` (slice 1 section). Method: live-HTTP probes against `aschool-flask-1` :5003 with a real demo-admin login (token from `data.access_token`), DB verification in `aschool-postgres-1`, url_map dump cross-check, PluginGate slugs checked against the live 55-plugin catalog. All probe fixtures deleted afterwards (0 rows remain).

| Finding | Status | Evidence (current source) | Note |
|---|---|---|---|
| E90 `/dashboard/settings` Advanced Configuration JSON editor was a dead round-trip — read `school.config` and PUT `config`, but the School model has no `config` column (JSONB is `settings`), GET never returned it, and `_populate_school`'s allowlist silently dropped it | **FIXED-UNCOMMITTED** | `frontend/app/dashboard/settings/page.tsx` — interface field is now `settings`, editor hydrates from `school.settings`, save PUTs `settings`. Runtime: PUT `{"settings":{"working_days":[0,1,2,3,4],"periods_per_day":8}}` → persisted + re-read via GET /schools/current | All other settings fields (name/name_nepali/phone/email/district/municipality/logo/banner) verified persisting before and after |
| E91 `/dashboard/bulk-uploads/csv` was a FAKE importer — `handleUpload` ran a 1.5s `setTimeout` and toasted "CSV Import is queued for processing" with no network call (code comment: "backend API is not yet available"); Download Template was a fake toast; `/dashboard/staff/bulk-upload` redirected into it | **FIXED-UNCOMMITTED** | Backend: `iemis_importer.py` gains `_parse_csv` (stdlib csv, utf-8-sig, same FORMAT_MAP header mapping as the Excel path) + `_parse_tabular` dispatch; POST /iemis/import and /validate now accept `.csv`. Frontend: `bulk-uploads/csv/page.tsx` rewired to a real POST /iemis/import with a result summary card (total/imported/failed + error list), real template download built from GET /iemis/formats, and the 3 backend-supported formats (fake "inventory"/"library" options removed). E2E: 2-row CSV → 2 Student rows in DB + `completed` IemisImportLog; unknown extra column warned+ignored | The page's own success toast is now only shown after the backend response arrives |
| E92 `/dashboard/bulk-uploads/history` crashed on real data — page read `log.format/total_processed/successful/failed` but GET /iemis/history serializes `format_code/total_rows/imported_rows/error_rows` → `log.format.replace` TypeError on first render | **FIXED-UNCOMMITTED** | `bulk-uploads/history/page.tsx` — ImportLog interface remapped to the serializer keys, table + details dialog updated, `error_rows`-aware green banner, plus error+retry state (E98 batch) | Verified against the real log row produced by the E91 import |
| E93 `/dashboard/sms` History tab crashed on live rows — serializer returns one row per recipient (`to_phone`) with `status:"queued"` and NO `sent_count`/`failed_count`; the Recipients cell rendered `NaN` and StatusBadge's `{sent,failed,pending}[status]` lookup hit `undefined.cls` for every queued row (i.e. all rows) | **FIXED-UNCOMMITTED** | `sms/page.tsx` — SmsLog interface matches `_sms_dict` (to_phone/provider/cost), Recipients column shows `to_phone`, StatusBadge gained `queued` + unknown-status fallback. Runtime: SMS send E2E → row transitions `queued` → `failed|console` (honest no-credential behavior) | RUNTIME NOTE: E31's lifecycle fix was already correct in source, but the running `aschool-celery-worker-1` held the stale module and left every row queued forever — `docker restart aschool-celery-worker-1` applied it; worker restart is now part of the deploy checklist. 3 legacy queued rows remain in dev DB (pre-restart), frontend no longer crashes on them |
| E94 Login page Phone-OTP mode could never sign in users stored with a `+977-` prefix (incl. seeded demo admin `+977-9800000001`): the form regex only accepts `98XXXXXXXX`, OTP verified against cache, but `User.query.filter_by(phone=…)` is exact-match → "User not found. Contact your school admin." | **FIXED-UNCOMMITTED** | `backend/app/services/auth_service.py` — new `AuthService._phone_lookup_variants()` (raw, dash-stripped, +977/+977–/977/national forms) used by the user lookups in both `send_otp` and `verify_otp`; cache keys still keyed by the typed phone so send/verify stay consistent. E2E: send-otp 9800000001 → verify-otp → access_token + "Demo Admin". `tests/test_auth.py` 16/16 | Password login unaffected (different path, tested working with `98XXXXXXXX` phones) |
| E95 Landing "Book a Free Demo" contact form is a no-op — server-component `<form>` with no action/handler; submit just reloads the page and discards the inquiry | **OPEN** | `frontend/app/page.tsx:533-584` | Needs a product decision + backend target: no public demo-request/contact endpoint exists (public contact routes are per-school website `/website/public/<slug>/contact`). Options: new public `demo_requests` table+route, or mailto |
| E96 No password recovery anywhere — `/reset-password` route does not exist in the frontend (no forgot-password link on /login either) and the backend has no forgot/reset endpoint (only authenticated `POST /auth/change-password`) | **OPEN** | grep clean across `frontend/app|lib|components`; backend `auth.py` has no reset route | Forgotten-password users are fully locked out. Natural implementation: purpose-scoped OTP (existing send-otp/verify-otp) → verify → set-password; deliberately not built inside a verification slice (touches auth flows + tests) |
| E97 `/dashboard/settings/roles` is entirely fake — hardcoded roles with invented counts (45 teachers / 350 parents), "Create Custom Role" / "Edit Permissions" buttons are no-ops, no roles API exists in the backend url_map; `/dashboard/marketplace` SaaS Packages tab checkout is cosmetic — "Pay with X" only toasts, no subscription/order is created (the `/plugins/<slug>/subscribe` routes take a plugin slug, not a package) | **OPEN** | `settings/roles/page.tsx:8-33`; `marketplace/page.tsx:183-239` | REAL RBAC editor + package checkout both need backend product work. FIXED as part of this row: marketplace plugin + package prices displayed as USD `$` although backend prices are NPR (landing says NPR 2,999) — relabelled to NPR in PluginCard and PricingTier |
| E98 Slice-1 pages that rendered NOTHING on fetch failure (E44/E73 pattern residue): settings/backup, users (+ toggle-active had no onError), staff, notices (both queries), bulk-uploads/history | **FIXED-UNCOMMITTED** | Established destructive error card + Retry pattern applied to `settings/backup/page.tsx`, `users/page.tsx`, `staff/page.tsx`, `notices/page.tsx` (notices + events), `bulk-uploads/history/page.tsx` | Users/staff/notices/backup/history pages now degrade to an explicit error card instead of a silent empty state |

Verification summary (slice 1): **25 routes — 19 WORKS, 5 FIXED (E90 settings, E91 csv bulk-upload + staff/bulk-upload redirect target, E92 history, E93 sms history, E97 currency relabel), 1 BROKEN-LOGGED-as-route (E96 /reset-password; roles page logged under E97)**. PluginGate slugs for the slice (`notices`, `sms_notifications`, `visitors`→`visitor_management` alias, `website_builder`) all resolve against the live 55-plugin catalog with matching backend decorators. Backend: `tests/test_auth.py` 16/16 after the E94 change; `iemis_importer.py` imports clean in-container. Frontend: `tsc --noEmit` = 5 errors total — the 4 documented pre-existing (sidebar.tsx, sanitize.ts) + `ai-tools/timetable/page.tsx:134` which belongs to another agent's uncommitted diff (111 lines vs HEAD; not a slice-1 file). All 25 slice-1 routes return 200 through the live Next.js container with an authenticated session.

## 5. Slice-3 frontend deep dive fixes (2026-08-29) — exams/ai-tools/reports/analytics pages

Method: 25 pages (`/dashboard/exams/*`, `/dashboard/ai-tools/*`, `/dashboard/reports/*`, `/dashboard/analytics/*`) exercised against live `aschool-flask-1` via their exact FE payloads on a fresh API-seeded fixture in the demo school (exam fa7d043b, 3 subjects incl. Science FM 125 + credit_hours 4/3/3, 3 students, 9 marks, attendance 2/3, paid+pending fees, 2 expenses). Ledger: `audits/coverage/FRONTEND_PAGE_VERIFICATION.md` §Slice 3.

| ID | Severity | Finding (baseline runtime-proven) | Fix | Runtime evidence |
|----|----------|-----------------------------------|-----|------------------|
| **E110** | **P1** | **Report-card generation diverged from the results page and left its own outputs half-computed**: `report_generation.py` summed full marks from `Subject.full_marks` (default 100) instead of the exam's `Marks.full_marks` (125 for a Science with practical) → report card said 94.67% where the results endpoint said 87.38% for the same student; `overall_grade`/`overall_gpa` were NEVER computed (marksheet modal + report-cards table showed "—" forever) and `rank_in_class` never set; "Generate with AI" never attempted any AI remark (`ai_remarks` always empty) | Task now resolves per-Marks full marks (m.full_marks → subject+practical fallback), computes overall via `nepal_grading.calculate_gpa` (credit-hours aware, identical math to /results), persists `overall_grade/overall_gpa/percentage`, bulk task ranks the class by percentage, and makes a best-effort AI remark through `QuestionPaperService.generate_remark` (token-hub routed, `school_id` passed; ANY failure leaves `ai_remarks` empty — never fabricated) | Regenerated: rank 1 Aasha 87.38/A/3.76, rank 2 Bikash 53.23/C+/2.4, rank 3 Chhaya 28.92/NG/0.48 — matches /exams/<id>/results and the hand-run util; bulk-report-cards PDF %PDF-1.7 15,390 B, pdftotext finds "Aasha Gurung"; remark call logged `question-paper:remark / error (401)` with 0 tokens and remark honestly empty |
| **E111** | P2 | Results-page GPA ignored subject `credit_hours`: `_build_subject_grade` never surfaced them so `calculate_gpa` fell back to equal weighting (Aasha 3.73) while the (fixed) report cards used credit-weighted 3.76 — two official numbers disagreed | `result["credit_hours"] = getattr(subject, "credit_hours", None)` added in `_build_subject_grade` (None → equal weighting, explicit 0 → no weight, unchanged for subjects without credits) | /exams/<id>/results now returns 3.76 for the fixture — identical to the report card and the hand-run util weighted call; `tests/test_exam_attendance_timetable_assignment.py` + `test_reports_pdf.py` 15/15, `test_plugins.py`+`test_plugin_aliases.py` 19/19 |
| **E112** | P1 | **Exam Grades page was doubly dead**: it POSTed `/exams/grade-table` (no such route → 405 on every "Add Grade") and its GET mapping read `name/min_percentage/max_percentage/grade_point` while the backend returns the static NEB table as `{grade, gpa, min_pct, description}` → blank rows even on read | No storage model exists (deliberate static NEB reference), so the page was rewritten as a read-only grading-scale table rendering the live GET (`/dashboard/exams/grades/page.tsx`), dead POST/dialog removed | Page renders the 8 NEB bands (A+ 90 4.0 … NG) from the live endpoint; url_map has only GET /exams/grade-table |
| **E113** | **P0 for AI UX** | **Invalid/missing LLM credentials surfaced as an opaque 500 on every AI endpoint**: token hub re-raised raw provider exceptions (container key is set but invalid → Anthropic 401) so /ai-tools/*, letter-writer, weekly insights all 500ed ("Internal server error") | New `AIProviderError` in `token_hub.py` raised on provider failure/no-provider (after usage logging), plus a global `@app.errorhandler(AIProviderError)` → **502** `{success:false, error}` next to the existing QuotaExceededError 429 handler | With the invalid key: /ai-tools/{question-paper,lesson-plan,remarks,letter-writer} and /ai-tools/insights/weekly all return 502 JSON with the provider message; quota-exhausted path still 429 (daily_limit=0 probe); usage rows logged status=error 0 tokens; `tests/test_ai_quota_and_usage.py` 5/5 |
| **E114** | P1 | `/dashboard/ai-tools/question-paper` could never succeed: it never sent the backend-required `duration_minutes` → 400 "Missing required fields: duration_minutes" on every click | Added a Duration field (default 180) and sends `duration_minutes`; `chapters` → `topics[]` | Endpoint reached with the page payload (502 honest under E113's invalid key; 200-class response shape `{title, sections[]}` renders in the output pane) |
| **E115** | P2 | `/dashboard/ai-tools/lesson-plan` sent `objectives` — a key the backend never reads (`learning_objectives`), so the Learning Objectives textarea silently did nothing | Payload renamed to `learning_objectives` | Backend receives objectives (contract fields verified against ai_tools.py) |
| **E116** | **P0** | **AI Report Remarks page was 100% broken**: sent `{exam_id: parseInt(uuid)→2, class_id: parseInt(uuid)→2, tone}` — garbage ints from UUIDs AND the backend contract is per-student `{student_name, marks, total, percentage}` → 400 on every click; feature never worked | Page rewritten: GET `/exams/<id>/results?class_id=` → per student GET marksheet (real per-subject marks) → POST `/ai-tools/remarks` → table of remarks; per-student failures (429/502) render "not generated" — no fabricated comments; Copy All only copies real remarks | With fixture results the loop issues 3 marksheet GETs + 3 remarks POSTs; with invalid key all three honestly marked not generated (usage rows `question-paper:remark` status=error); a mocked-valid provider returns `{remark}` which renders |
| **E117** | **P0** | **AI Timetable page was 100% broken**: sent `academic_year: "2026"` while the backend requires `academic_year_id` (always 400 "academic_year_id is required") AND rendered a `{class: {day: {period}}}` shape the solver never returns (grid always empty even on success) | Rewritten: Academic Session Select (GET /academics/years) + Periods Per Day; payload `{academic_year_id, periods_per_day}`; grid renders the real solver shape `{classes:[{class_name, section_name, slots:[{day,period,subject_name,teacher_name}]}], conflicts}` | POST /ai-tools/timetable 200 with 4 class-sections × 6 days × 8 periods of real slots (subject+teacher assigned); conflicts array rendered when non-empty |
| **E118** | P1 | `/dashboard/exams/online/questions` (AI Question Generator) had NO plugin gate but its endpoint is `plugin_required("digital_content")` → schools without the e-library got a raw 403 with a misleading "AI limits" toast | Wrapped in `PluginGate slug="elibrary"` (canonical slug per E14; alias keeps legacy installs working) — the gate's one-click install flow handles the rest | Probe: 403 before install → gate renders on page → after POST /plugins/install elibrary the endpoint is reachable (502 honest under invalid key) |
| **E119** | **P0** | **Teacher Performance Reports page could never load**: `GET /design-studio/data-sources/teacher/records` 500ed on every call — `AttributeError: type object 'User' has no attribute 'first_name'` (User has only `full_name`) in the teacher branch of `list_source_records` | Branch rewritten on `User.full_name` (filter/order/label) with first/last split for the template `fields` contract | Live GET → 200 with 3 staff records (name/role/email/phone); page stats + CSV derive from the same rows |

- **Counts (slice 3)**: 25 pages — 17 WORKS, 8 FIXED (E110-E119), 0 BROKEN-LOGGED remaining. PluginGate sweep: all 43 distinct FE gate slugs resolve against the live published catalog via `_acceptable_plugin_slugs` (0 lockouts).

## 8. Slice-4 frontend deep dive fixes (2026-08-29) — fees/hr/communications pages

Method: 25 pages (`/dashboard/fees/*` ×7, `/dashboard/hr/*` ×9, `/dashboard/communications/*` ×9) exercised against live `aschool-flask-1` with the demo-admin token; fixture built through the real API (fee type→structure auto-apply→discounted bill 5000−500+100=4600→partial 2000→settle 2600→2 receipts with pdftotext-checked PDFs; staff payroll with components 40000+8000−3500 → payslip PDF hand-checked; WhatsApp webhook probes for all three rule types). Ledger: `audits/coverage/FRONTEND_PAGE_VERIFICATION.md` §Slice 4. Probe rows labeled Slice4 left in the demo school; WhatsApp bot config restored to pre-probe state.

| ID | Severity | Finding (baseline runtime-proven) | Fix | Runtime evidence |
|----|----------|-----------------------------------|-----|------------------|
| **E120** | **P1** | **`/reports/fees/collection` (JSON + PDF, consumed by `/dashboard/fees/reports`) was discount-blind**: `_fee_paid_amount` treated a `paid` collection as `amount` — the BASE column — ignoring `discount_amount`/`late_fine_amount`, and `_fee_due_amount` did the same. Probe: a fully-paid 5000/−500/+100 bill (real money 4600) was reported as 5000 collected → "Collected In Period" 10000 vs the true 9600 that /fees/summary reports (two official numbers disagreeing); pending dues over/understated the same way. | `reports.py` helpers now delegate to the canonical `fees.py` helpers (`_extract_partial_paid` → payable = base+fine−discount for paid, cumulative `[partial_paid:]` note otherwise; `_collection_payable_total` for dues) — identical math to /fees/summary by construction. | After fix: JSON `{total_collected: 9600.0, total_pending: 13500.0, payments_count: 2, rate 41.6}`; persisted PDF (15,331 B) shows NPR 9,600.00 with the 5000 + 4600 payment rows. Read-only change; `test_reports_pdf.py` + `test_fees_money_math_verification.py` 9 passed. |
| **E121** | **P1** | **WhatsApp auto-reply `match_type:"exact"` fell through to the trailing contains check** — `_wa_match_auto_reply` returned the rule on equality but, on mismatch, continued to `keyword in text`, so "SL4EXACT2" triggered the exact rule "SL4EXACT" (runtime-proven against the live school config). | Exact branch now `continue`s on mismatch; contains/regex semantics unchanged. | In-container matcher assertions 7/7 (contains, exact hit, exact near-miss now None, case-insensitive exact, regex hit/miss, no-match). Pinned by `tests/test_comms_plugins.py::test_wa_auto_reply_exact_match_does_not_fall_through_to_contains` (new). |
| **E122** | **P0 for comms UX** | **Broadcast push/email/WhatsApp channels were fake-success no-ops**: `send_broadcast` returned `{recipients: N, status: "accepted"}` for every non-SMS channel and did NOTHING — a push broadcast created 0 in_app_notifications (DB-proven) while the page toasted "Broadcast sent to 2 recipients!". The task-bar flow "compose+send in-app message delivered to recipient's /notifications" never delivered anything. | Backend: push → one `InAppNotification` per audience user (new `_users_for_audience`/`_guardian_user_ids`, single commit, 500-user cap); email → `EmailService.send_bulk_email` with honest sent/failed (unconfigured SMTP → `status:failed, reason:email_not_configured_or_smtp_error`); WhatsApp → per-phone `WhatsAppCloudService.send_text` degrading to `status:skipped, reason:whatsapp_not_configured` (E32 honesty); class_parents scoped to that class's guardian-linked users ONLY (bad UUID → honest 0); SMS path unchanged. FE: toast now surfaces the real per-channel outcome (delivered/queued/skipped/failed). | Push E2E: all_parents → 2 rows (category `broadcast`, school-scoped), visible in Papa Probe's own `GET /notifications` + unread-count; class_parents for a live class → exactly that class's guardian user (not the other parent); invalid class UUID → `queued:0` + note; WhatsApp → `skipped/whatsapp_not_configured, failed:2`; email → `failed` + SMTP reason; forced commit failure → zero committed rows (in-process + independent DB count). Pinned by 2 new comms tests (push delivery + honest skip/fail). |
| **E123** | **P1** | **A payroll run with components was impossible from the UI**: `generate_payroll` seeds zero-value drafts (basic 0, empty components) and the payroll page had no way to enter basic salary/allowances/deductions — `POST/PUT /hr/payroll` (the only component paths) had ZERO frontend callers, so every UI-only payslip would be NPR 0. Also `mark_paid` silently dropped the `payment_method` the page sends. | Payroll page: new per-row "Edit salary components" dialog (basic salary + add/remove allowance & deduction rows, live gross/net preview) → `PUT /hr/payroll/:id {basic_salary, allowances:{name:amount}, deductions:{name:amount}}` (recomputation already guarded by the earlier math audit); `_payroll_dict` now exposes `allowance_items`/`deduction_items` so the editor hydrates real names; `mark_paid` stores `payment_method`; removed the dishonest no-op Export / Send Payslips buttons; added error+retry + truthful generate feedback. | E2E with the dialog's exact payload: PUT basic 30000 + {Transport:3000} − {PF:1000} → gross 33000/net 32000 with items echoed; approve → pay {payment_method:"bank"} → serializer now shows `method: bank`. Payslip PDF for the sibling record hand-checked: 40000 + 8000 = 48000 − 3500 = 44500. `test_hr_payroll_math.py` 15-passed suite re-run green. Honest residual: `generate_payroll` still doesn't consume `settings.payroll` (persist-only today — verified persisting; the component editor is the real numbers path). |
| **E124** | **P1** | **No leave-apply UI existed anywhere in the web app**: `POST /hr/leave` had zero FE callers (admin pages could only approve/reject), so the bar's leave apply→approve flow could not be performed by any web user (mobile had it). | `/dashboard/hr/leaves` gained an Apply Leave dialog (staff select from GET /staff, leave type, from/to dates with auto days, reason → `POST /hr/leave {user_id, leave_type, start_date, end_date, days, reason}`); both leave pages got error+retry states (E73-pattern residue). | Dialog payload runtime-verified: `POST /hr/leave` → 201 pending row (Ram Bahadur, casual, 2 days, staff_name resolved) → PATCH approve → `approved`; missing start_date → 400 (rollback proven); second request PATCH → `rejected`. |
| **E125** | P2 | **Gallery gate mismatch**: the page gated `notices` but every `/files/*` route is gated `plugin_required("file_management")` — a school with notices but without file_management hit a raw 403 inside the gate (the gate's one-click install would offer the wrong plugin). | `gallery/page.tsx` PluginGate slug → `file_management` (published in the live catalog). | Demo school has file_management → 200 verified; slug present in the 55-plugin catalog so the gate renders for missing installs. |
| **E126** | P2 | `POST /staff` with a missing phone returned an unhandled IntegrityError **500** (`users.phone` NOT NULL) — same E17 family. | Up-front guard → 400 `phone is required`. | Live: `{"error":"phone is required"}` HTTP 400; with phone, create works (slice-1 contract unchanged). |
| **E127** | **P1** | **`/dashboard/communications/announcements` was 100% broken — 500 on every publish AND every list load**: the page hardcoded `notice_type:"announcement"` but `notice_type` is a Postgres enum (general|academic|event|holiday|urgent) → `psycopg2.errors.InvalidTextRepresentation` on create (line notices.py:103) and the identical 500 from `GET /notices?type=announcement` on list. | FE: form sends `notice_type:"general"` (default + reset) and the list fetch drops the broken type param; BE: unknown `?type=` values now return an honest empty list instead of reaching the DB. | Runtime: create with the fixed payload → 201 → listed → delete; `GET /notices?type=announcement` → `[]` HTTP 200; `tsc --noEmit` unchanged (only the 4 documented pre-existing errors in sidebar.tsx/sanitize.ts). |

- **Counts (slice 4)**: 25 pages — 13 WORKS, 7 FIXED (E120 fees/reports, E121 whatsapp rules, E122 broadcast, E123 payroll, E124 leave apply + report page error state, E125 gallery gate, E127 announcements), 0 BROKEN-LOGGED remaining (payroll/settings WORKS with an honest persist-only caveat; defaulters "Send Reminder" noted as a cosmetic no-op). PluginGate sweep: slice-4 gate slugs (`fees`, `hr`→hr_payroll, `communications`→sms_notifications, `notices`, `whatsapp_bot`, `file_management` after E125) all resolve against the live 55-plugin catalog with matching backend decorators — zero lockouts.
- **Tests**: `test_comms_plugins.py` (21 existing + 3 new = 24, incl. E121 pin + E122 push/honesty pins) + `test_models.py` = **37 passed**; `test_reports_pdf.py` + `test_fees_money_math_verification.py` = **9 passed**; `test_hr_payroll_math.py` + `test_ops_validation_guards.py` = **15 passed**. FE `tsc --noEmit`: 0 new errors (4 documented pre-existing). All 25 routes return 200 through the live Next container with an authenticated session.

## 9. Slice-2 frontend deep dive fixes (2026-08-29) — students/teachers/academics/attendance/timetable pages

Method: 27 pages exercised against live `aschool-flask-1` :5003 with the demo-admin token; every fetch checked against the live url_map; core actions runtime-verified into `aschool-postgres-1` (rows, tenant scoping, cap, rollback). Ledger: `audits/coverage/FRONTEND_PAGE_VERIFICATION.md` §Slice 2. All 27 routes 200 through the live `aschool-nextjs-1` container with an authenticated cookie session. FE `tsc --noEmit`: 0 errors in slice-2 files (4 documented pre-existing errors elsewhere unchanged). An earlier crashed slice-2 attempt (15:11-15:58) had left stray probe rows (S2-PROBE-*/S2-TR-1/S2-PR-*/S2-IMP-* students, "Slice2 Teacher" user b736e44f) — left in place; this run's own probes were cleaned up.

| ID | Severity | Finding (baseline runtime-proven) | Fix | Runtime evidence |
|----|----------|-----------------------------------|-----|------------------|
| **E100** | **P0** | **Editing a student from the web 500ed whenever the Phone field was blank — the normal case**: both edit dialogs (list page + student detail) send `phone: null` when the field is empty, and `update_student` wrote `user.phone = data.get("phone") or None` into the NOT NULL `users.phone` column → IntegrityError 500 and the ENTIRE edit (name/class/status/everything) rolled back. Every student enrolled via the Add Student dialog has `phone=""`, so every edit that didn't type a phone crashed (runtime-proven: PUT with the exact dialog payload → 500, DB row unchanged). | `students.py update_student` now stores `""` instead of None (`data.get("phone") or ""`), keeping `email` nullable semantics unchanged; a real phone still persists to the login user. | Same payload → 200 with student row updated (last_name/blood_group/dob_bs) and user.phone left `""`; PUT `{phone:"9812223333"}` → user.phone updated; PUT `{phone:null}` again → back to `""`. |
| **E101** | P2 | **Add-Class dialog's "Initial Section" was silently dropped**: the academics hub collects `initial_section_name`/`initial_section_capacity` and sends them with POST /academics/classes, but `_apply_class_payload` reads neither — the class was created with 0 sections (DB-proven) while the admin believed section A existed; roll-assignment/attendance-by-section then can't see the class. | `create_class` now creates the initial `Section` (name + capacity) in the same transaction when `initial_section_name` is provided; payload keys not sent → behavior unchanged. | POST /classes with `{name:"8 S2", numeric_grade:8, initial_section_name:"A", initial_section_capacity:25}` → 201 with `sections:[{name:"A", capacity:25}]`, Section row in DB; create without the keys still creates no section. |
| **E102** | **P1** | **Timetable clash detection missed class-wide slots**: a manual slot created with section NULL (the Add Slot dialog's "All sections" default) was treated as non-overlapping with sectioned slots, so a manual "All sections" slot + a generated section slot could occupy the SAME (day, period) of the same class — a real double-book the grid hides (it renders only the first slot per period; proven after a scoped solver save left 2 Monday-P1 slots). | `create_slot` class-clash test now treats section-NULL slots as covering the WHOLE class: a new slot conflicts when the existing slot's section matches, OR either side is NULL; teacher-clash and everything else unchanged. | NULL-section slot where a section slot exists (Monday P1) → 409; sectioned slot where a NULL slot exists (Thursday P1) → 409; two DIFFERENT sections at one period (no NULL involved) → 201; teacher double-book other class → 409 (regression held); bogus class uuid → 400 with zero rows. |

- **Counts (slice 2)**: 27 pages — 23 WORKS, 3 FIXED (E100 students-edit 500, E101 initial section, E102 timetable class-wide clash), 0 BROKEN-LOGGED. Two minor no-E-id FE fixes folded into WORKS rows (reset-password display key, teachers page error+retry; academics hub error+retry shipped with E101). PluginGate sweep: `attendance`, `timetable`, `notices` all published + decorator-matched; students/teachers/academics endpoints carry no plugin gates — zero lockouts.

## 9. Slice-6 frontend deep dive fixes (2026-08-29) — E140-E143

Full per-page ledger: `audits/coverage/FRONTEND_PAGE_VERIFICATION.md` § Slice 6. All 27 pages (/dashboard/transport/* 8, disaster 4, incident-management 4, white-label 4, multi-branch 4, biometric 3) verified against live url_map + DB + nextjs :3003 cookie session (all 200 pre/post fixes). E144-E149 intentionally unused.

| Finding | Status | Evidence (current source) | Note |
|---|---|---|---|
| E140 /dashboard/transport ungated + phantom driver_name | **FIXED-UNCOMMITTED** | `frontend/app/dashboard/transport/page.tsx:17-23` wraps TransportContent in `PluginGate slug="gps_tracking"` (import existed, was never used); driver_name input/column/payload key removed (`:275-277`, `:237-241`) — buses.driver_id is a users FK and the API silently dropped the free-text field. Runtime ✅ POST /transport/buses 201 without driver_name; stats match DB. | All sibling transport pages already gated gps_tracking (published, catalog-verified). |
| E141 biometric devices page discarded the one-time device API key | **FIXED-UNCOMMITTED** | `frontend/app/dashboard/biometric/devices/page.tsx:22-56` captures the 201 payload's `api_key` (backend stores only SHA-256, returns it once — `backend/app/api/v1/biometric.py:331-380`); `:91-114` renders a shown-once dialog with copy button. Runtime ✅ E2E: heartbeat→online, ingest by admission_number → punches mapped → Attendance row check_in 22:29:07/check_out 23:09:07 NPT; batch replay → duplicates=1 (no second row); malformed batch 400 atomic; GHOST-99 stays unmapped and manual /sync honestly reports it failed. | `POST /devices/:id/regenerate-key` still has no FE caller (residual). |
| E142 white-label theme/branding PATCH silently persisted only a subset of JSONB fields (7→1 then 7→4 keys across identical requests) | **FIXED-UNCOMMITTED** | Instrumented proof: full `stored` dict logged pre-commit, defaults returned post-commit — JSONB replace-assignment is equality-compared at flush and the UPDATE was skipped. Fix: explicit `flag_modified(school, "settings")` in `save_theme` (`backend/app/services/website/white_label.py:319-325`) and `save_branding` (`:193-199`). Runtime ✅ all 7 theme keys roundtrip and stay stable at +1s/+8s/+28s (API + psql); branding write-through to SchoolWebsite.customizations{colors,white_label} + School.logo_url re-verified, then restored to baseline. | Domain DNS verify is genuinely live (pending for unresolvable fake domain, failed for example.com with found-vs-expected records); no unset-domain API/UI exists (residual). |
| E143 GPS timestamps rendered 5:45h off for Nepal users | **FIXED-UNCOMMITTED** | `backend/app/api/v1/transport.py:317-326` — `_gps_dict` now emits ISO-Z ("2026-08-29T16:51:11.602751Z") instead of naive `str(datetime)` which `new Date()` parsed as browser-local on /transport/map (last-update cards) and /transport/logs. Runtime ✅ serializer output verified. | Socket.IO path (gps_processing.py:101) already emitted ISO — REST was the only offender. |

Environment notes: demo-school plugins installed for verification (emergency, incidents, disaster_management, incident_management, white_label, multi_branch, biometric — all published). Fixtures labeled Slice6 left in demo school (transport route/bus/stop, drill+participations+plan, 4 incidents, 3-branch chain with seeded branch data, biometric device+punches). tsc --noEmit: 4 pre-existing errors in sidebar.tsx/sanitize.ts, zero in slice-6 files.

## 10. Slice-5 frontend deep dive fixes (2026-08-29) — E130-E134

Method: 25 pages (`/dashboard/library/*` ×6, `/dashboard/elibrary/*` ×3, `/dashboard/health-records/*` ×4, `/dashboard/wellbeing/*` ×4, `/dashboard/gamification/*` ×5, `/dashboard/social-hub/*` ×2, `/dashboard/portfolio`) exercised against live `aschool-flask-1` :5003 with the demo-admin token; core actions runtime-verified into `aschool-postgres-1` (library counter integrity 2→0→1 with per-status issue counts, gamification leaderboard hand-computed vs SQL group-by, vaccination dates persisted, mood summary math, campaign lifecycle draft→active→paused→active); every fetch cross-checked against the live url_map; all 25 routes 200 through the live `aschool-nextjs-1` container with an authenticated cookie session. Full per-page ledger: `audits/coverage/FRONTEND_PAGE_VERIFICATION.md` §Slice 5. E135-E139 unused. Backend pins: `tests/test_campus_ops_plugins.py` + `tests/test_campus_ops_guards.py` = **21 passed** after the E130/E131/E134 changes.

| Finding | Status | Evidence (current source) | Note |
|---|---|---|---|
| E130 health-records Allergies registry search dropped EVERY row for any normally-cased query | **FIXED-UNCOMMITTED** | `backend/app/api/v1/health_records.py:26-35` — `list_health_profiles` lowercased the needle but not the haystack (`needle in p.get("student_name")`), so `?search=Aasha` (what the page's search box sends for "Aasha Gurung") filtered all profiles out (runtime-proven: 0 rows for Aasha/aasha/Aash/Slice2/zzz). Fixed: haystack `.lower()`ed too. Runtime ✅ Aasha/aasha/AASHA → 1 row, zzz → 0. | Page: `frontend/app/dashboard/health-records/allergies/page.tsx:41` — the only consumer of `?search=` on profiles. Profile PUT roundtrip (allergies/medical_conditions arrays + height/weight) re-verified. |
| E131 awarding the same badge twice created duplicate StudentBadge rows (no dedup; UI dialog can be submitted repeatedly) | **FIXED-UNCOMMITTED** | `backend/app/api/v1/gamification.py:140-147` — probe awarded badge 59fb1f2b… to Aasha twice → 2 StudentBadge rows (model has no unique constraint). Added an up-front dedup check → 409 "This student already has this badge"; fresh pair still 201. Runtime ✅ duplicate 409, dup row cleaned to 1. | Consumers (student/parent badge lists) previously rendered the badge twice. Points unaffected. |
| E132 social-hub feed offered the moderation delete to teachers, but `delete_post` authorizes only the author or superadmin/school_admin → guaranteed 403 toast on click | **FIXED-UNCOMMITTED** | `frontend/app/dashboard/social-hub/page.tsx:116-124` — new `canDeletePost(post)` (superadmin/school_admin OR `user.id === post.author_id`) gates the trash button; `canModerate` retained for role display logic. Backend authorization (`social_hub.py:53-66`) unchanged and verified: admin delete of another user's post → 204 soft-delete, row `is_deleted=t`, gone from the feed. | Affordance now matches the server rule; the author-teacher case still works (author_id match). |
| E133 ad-campaign lifecycle was dead from the UI: create always yields status "draft" and the page only rendered pause (active) / resume (paused) — no draft→active path existed, so every fresh campaign was stuck in draft and pause/resume always 400ed ("Only active campaigns can be paused (current: draft)", runtime-proven) | **FIXED-UNCOMMITTED** | `frontend/app/dashboard/social-hub/campaigns/page.tsx` — new Start action (Play icon on draft rows) → `PATCH /social/campaigns/:id {status:"active"}` (the backend's STATUS_TRANSITIONS already allow draft→active; the page simply never called PATCH). Runtime ✅ E2E lifecycle with the page's own calls: create draft → start active → pause paused → resume active; invalid active→draft → 400 guard; delete soft-deletes. | Audience preview verified honest (students+guardians counted from the school's own DB; class-targeted estimate == SQL count). Reach/clicks cards stay 0 until a real Meta delivery integration (labeled on-page). |
| E134 portfolio writes for an unknown/foreign student uuid crashed 500 (FK violation at commit): POST items/credentials and PUT portfolio auto-created `StudentPortfolio(student_id=<foreign>)` / `MicroCredential(student_id=<foreign>)` with no existence check | **FIXED-UNCOMMITTED** | `backend/app/api/v1/portfolio.py` — shared `_student_or_none()` guard; `add_item`/`add_credential`/`update_portfolio` now return 404 "Student not found at this school" before any write (E17-family wording); `add_credential` additionally requires a non-empty title (400). Runtime ✅ foreign uuid → 404 on all three writes with ZERO rows (was 500/500/500); real-student happy paths re-verified (item 201, credential 201 with issuer/issued_at). | List routes legitimately return `[]` for a student without a portfolio (no leak, first-achievement flow depends on it). Pinned suites: campus-ops 21/21 green post-change. |

## 10. Slice-7 frontend deep dive fixes (2026-08-29) — E150-E159

Full per-page ledger: `audits/coverage/FRONTEND_PAGE_VERIFICATION.md` § Slice 7 (final). 37 routes (7 website-builder, 5 designer, 9 certificates, 2 iemis-import, 14 public /school/[slug]) verified against live :5003/:3003 with the demo-admin session; all 200 pre/post fixes.

| Finding | Status | Evidence | Note |
|---|---|---|---|
| E150 six public school-site subpages fetched endpoints that never existed (teachers, events, gallery, alumni, results) — every page rendered its permanent empty state despite live DB data (e.g. 2 real teachers, 1 event, 9 marks rows) | FIXED-UNCOMMITTED | New public endpoints in `backend/app/api/v1/website.py`: GET /website/public/<slug>/teachers (User role=teacher), /events (Event table), /gallery (ManagedFile images), /alumni (Alumni rows), /results (symbol_no+DOB → real Marks of latest result_published exam). Runtime: /school/demo/teachers HTML now contains "Sita Sharma"; events show "Parents Day"; results checker S6-DEMO-001+2009-04-15 → Aasha Gurung, 87.38%, GPA 3.73, wrong DOB → honest 404 | news + news/[articleSlug] left honest-empty: no article model/CRUD exists anywhere in the product (residual) |
| E151 design-studio bulk/single document data path broken three ways: (a) built-in certificate templates (character/transfer/merit/participation) had ZERO {tokens} — certificates printed literal underscores, never the student's real name; (b) transfer_certificate/id_card_staff/merit/participation/hall_ticket builders existed but had been DROPPED from the TEMPLATES registry — the registry seed sweep soft-deleted their rows, so the transfer page silently generated a character certificate and bulk certificate_type=transfer 500'd ("Template not found"); (c) `BulkGeneratorService._render_for_student` used `TemplateEngineService` without importing it — bulk certificates AND bulk admit cards 500'd with NameError on EVERY call (latent, never-tested paths) | FIXED-UNCOMMITTED | (a) {name}/{father_name}/{dob}/{class}/{section}/{enrollment_number}/{date}/{leaving_*} tokens added to the 5 static canvas templates + marksheet/report-card/admit-card headers; enrollment/symbol + date/issue_date + leaving_date aliases in `_merge_data`. (b) 5 registry entries restored (template_engine.py TEMPLATES) — seed sweep re-hydrates the DB rows automatically. (c) local import added in `_render_for_student`. Runtime: single character render prints real name+enrollment; staff render prints "Sita Sharma"; bulk transfer/merit 5 rows each with real names (merit correctly landscape 1123×794); bulk admit-cards 5 with real names + exam name; bulk marksheets emit per-student subject rows ("Mathematics 92.0 A+ Pass") via the data-driven writer template | bulk marksheets default stays "marksheet" (registry marksheet IS the writer/data-driven one) |
| E152 public /school/[slug] pages used ISR `revalidate: 300` with no on-demand invalidation — publish/unpublish/section/theme edits could take up to 5 minutes to appear (verified: heading edit stale for minutes) | FIXED-UNCOMMITTED | All 14 public fetches tagged `tags: ["school-<slug>"]`; new route handler `frontend/app/revalidate-site/route.ts` (POST, access_token-cookie-guarded, 401 without session) calls revalidateTag + revalidatePath('/school/[slug]','layout'); `lib/revalidate.ts` helper wired into every website-builder mutation (publish/unpublish, page create/delete/toggle, section add/save/delete/reorder, theme+template apply, SEO save). Runtime: heading v2→v3 visible immediately after the revalidate call | revalidate-site sits outside /api/* so the flask rewrite doesn't shadow it |
| E153 AI-builder "Apply This Design" sent `customizations` to /website-builder/themes/apply which only reads `color_overrides` — the AI-chosen palette/fonts were silently discarded (theme applied with default colors) | FIXED-UNCOMMITTED | frontend/app/dashboard/website-builder/ai-builder/page.tsx:36-45 now posts `theme_slug` + `color_overrides: color_palette`; runtime-verified: applied palette lands in the public config payload | fonts/hero_style were never consumed by any backend surface (honest drop, commented) |
| E154 the website-builder "contact" section rendered a DEAD form on public sites (readOnly inputs + button with no handler) | FIXED-UNCOMMITTED | `ContactFormSection` in frontend/components/website/SectionRenderer.tsx POSTs to /website/public/<slug>/contact (endpoint 201-verified, audit_logs row); public homepage passes school.slug via liveData; editor canvas (no slug) shows an honest disabled "preview" form instead of one that goes nowhere | SectionRenderer is "use client"; form state is local |
| E155 certificate print dialogs used invalid CSS `border: 10px solid #double` — the decorative border never rendered (character + transfer pages) | FIXED-UNCOMMITTED | Replaced with valid `10px double #1e40af` / `#dc2626`; transfer page's Reason/Date-of-Leaving now also included in the render payload (they were collected but never sent) | cosmetic but user-visible on the printed artifact |
| E156 IEMIS importer round-trip gaps: no template download anywhere (the slice bar's "template download" step was impossible), the import page offered only the File-Manager picker (no local-file input), and `getHistory()` read `data.items` while GET /iemis/history returns a bare list — the history table showed "No imports yet." FOREVER, even with completed imports | FIXED-UNCOMMITTED | Backend GET /iemis/template?format=… generates a real .xlsx (openpyxl, exact IEMIS header row per FORMAT_MAP + honest sample row + column widths); FE download buttons + local-file input on the importer page; `iemis.service.getHistory` normalizes list→{items,pagination} + history-page error state. Round-trip runtime-verified: download template (200 xlsx) → validate → import (1 row, completed) → history renders the job + DB student row exists | imported fixture student "S7 Probe Student" (IEMIS-S7-001) left in demo school, labeled |
| E157 debug leftovers in the SSR public homepage: `console.log("[DBG-PAGE] …")` ×2 (printed sections payload per request in the server container) + an unused `DebugClient` import (the component itself renders "debug client works") | FIXED-UNCOMMITTED | frontend/app/school/[slug]/page.tsx: both console.logs and the import removed; live HTML re-verified free of DBG-CLIENT markers | DebugClient.tsx component file itself kept (unused, harmless) |
| E158 slice-7 pages with query-driven lists but NO error state (E44/E73 pattern tail): website-builder/pages, iemis-import/history, certificates character/staff/transfer — a failed fetch rendered a silent "No pages/students/staff found" empty state | FIXED-UNCOMMITTED | Established error pattern applied (`retry: 1` + destructive message + Retry) to all five pages | designer hub + templates + editor + website-builder main/seo/themes already had states (E73 batch) |
| E159 the builder's Unpublish did NOT take the public site offline: GET /website/public/<slug> returned school+sections+notices regardless of SchoolWebsite.is_published, so an "unpublished" site stayed fully reachable with fallback theming (publish state only affected the admin status banner) | FIXED-UNCOMMITTED | `_public_site_guard` in website.py applied to /public/<slug> and all 6 sub-endpoints: row exists AND is_published=False → 404 "Website not published"; schools with NO website row keep the legacy always-on fallback (basic_website-only schools unaffected). Runtime: unpublish + revalidate → SSR shows "School Not Found" and API 404s; republish + revalidate → site live again | same guard also protects teachers/gallery/alumni/results/events/facilities |

Verification summary (slice 7): **37 routes — 20 WORKS, 16 FIXED (E150-E159 + E158 batch), 1 WORKS-with-residual pair logged (news ×2 counted inside the 20)**. All 16 dashboard + 14 public routes return 200 through aschool-nextjs-1 with an authenticated/anonymous session as appropriate. Backend: py_compile clean on the 4 touched files; flask --reload picked up every change. Frontend: tsc --noEmit = 4 pre-existing errors only (sidebar.tsx, sanitize.ts), zero in slice-7 files. Fixtures labeled S7 left in the demo school (builder home page + 2 sections, published; S7 Probe Design doc; IEMIS student IEMIS-S7-001; contact + admission probe rows; Aasha dob_ad=2009-04-15; website_builder trial install).

## 11. Slice-3 backend deep dive fixes (2026-08-30) — E180-E189

Per-file verdicts: `audits/coverage/BACKEND_FILE_VERIFICATION.md` § Slice 3 (fees, hr_payroll, admission, inventory, visitor, dismissal, transport + fee/gps/admission tasks). Every route probed in-container (positive + negative + cross-school); regression tests in `backend/tests/test_slice3_ops_fixes.py`.

| Finding | Status | Evidence | Note |
|---|---|---|---|
| E180 fee_reminders used the RAW base everywhere: `send_fee_reminders` computed pending as base − paid (discounted students got reminders for money they did not owe — base 1000/fine 50/discount 200/paid 300 reminded Rs.1050 vs real Rs.550) and `generate_monthly_fee_report` counted raw base by status (overstated expected/collected, fully-paid partials counted as pending) | **FIXED-UNCOMMITTED** | `app/tasks/fee_reminders.py` — `_fee_payable_total` (base+fine−discount, floor 0) + payable-aware `_fee_paid_amount`; reminder skips zero-due; report sums payable/paid/due per collection and method breakdown from receipts. Runtime ✅ 850/300/550 on the fixture; discounted fixture no longer reminds | flag originated in slice-3 scope notes |
| E181 fees.py 500s on bad input + unvalidated scholarship updates: POST /structures with bad class_id uuid, POST /collections with bad student_id uuid, GET /recent + /outstanding?limit=abc all crashed DataError (500); scholarship PUT accepted percent=400 / -50 / discount_type="half"; POST accepted negative fixed discounts | **FIXED-UNCOMMITTED** | `app/api/v1/fees.py` — `_parse_uuid` + `_coerce_limit` guards (400s), class_id/student_id validated before queries; PUT reuses POST validation (percent 1-100, fixed > 0, type enum). Runtime ✅ all five cases 400 with zero writes | net≥0 invariant already clamped at apply-time; this stops garbage at the source |
| E182 record_payment idempotency lookup was GLOBAL: `FeeReceipt.query.filter_by(idempotency_key=...)` without school filter — school B replaying school A's idempotency key received school A's RECEIPT (cross-tenant leak) and believed its own payment was recorded | **FIXED-UNCOMMITTED** | `app/api/v1/fees.py` record_payment — lookup now `filter_by(idempotency_key=..., school_id=g.school_id)`; the GLOBAL unique index on fee_receipts.idempotency_key (probed) means a key already used by another school would 500 on insert, so the stored key is namespaced `<school_id>:<key>` when a foreign collision exists. Runtime ✅ foreign-key replay at demo school records a normal local payment (receipt stored as `2d2900…:e182-xs-key`), never echoes the foreign receipt, never 500s | unit test creates a real foreign receipt and asserts the local receipt wins |
| E183 webhook payment finalize used the RAW base as the collection total: `_apply_fee_payment`/`_extract_partial_paid` in `app/api/webhooks/__init__.py` ignored discount/late-fine, so a discounted collection fully paid via eSewa/Khalti/FonePay (net 800 on base 1000 − 200) stayed "partial" with a phantom Rs.200 due FOREVER (callback amount == initiated amount, so every check passed) | **FIXED-UNCOMMITTED** | `_apply_fee_payment` now uses the same `_collection_payable_total`/`_extract_partial_paid` as the /fees API (imported, single definition). Runtime ✅ base 1000 − 200 paid 800 → "paid". E60-E66 single-use/amount-cross-check logic confirmed intact | duplicate-callback + amount-mismatch paths re-read paid state via the payable-aware helper |
| E184 auto_generate_monthly_fees crashed on EVERY run: beat task (daily 00:45) called `Student.query.filter_by(is_active=True)` but Student has NO is_active column → InvalidRequestError, swallowed by the per-school except — beat-scheduled monthly billing has NEVER generated a row; it also ignored scholarships entirely | **FIXED-UNCOMMITTED** | `app/tasks/fee_reminders.py` — student scope now `status=="active"` (matches /fees/batch-monthly); `_student_discount_for_item` applies active scholarships additively on base, capped, same rule as the API generator. Runtime ✅ task created 4 collections in demo school (was: exception every run); test asserts created ≥1 with 10% discount | discount windows checked against BS today, lookup failures contained |
| E185 hr_payroll tenant/enum/500 gaps: POST /hr/leave accepted ANY user_id — a REAL user of another school produced a successful cross-tenant leave row (proven: demo-school leave linked to foreign user); bad/foreign user_id also 500'd via FK; same class of bug in create_appraisal (+ non-numeric scores → 500, foreign user accepted); /hr/expenses validated nothing (bad category/uuid/amount/date → 500, negative amounts stored); leave approve/PATCH accepted free-form statuses ("banana"); hr_stats "monthly_payroll" summed net_salary across ALL months ever while the dashboard labels it Monthly Payroll | **FIXED-UNCOMMITTED** | `app/api/v1/hr_payroll.py` — `_school_user_or_none` tenant guard on leave/appraisal user targets; leave dates parsed (400); VALID_LEAVE_STATUSES closed set on approve+PATCH; appraisal scores/overall numeric (400); expense amount>0/numeric, ISO date, category must belong to school (create+update); monthly_payroll filtered to current YYYY-MM. Runtime ✅ all negative cases 400; cross-school leave write now impossible; hr/stats returns current-month sum only | no leave accruals / salary-structure versioning / attendance-linked deductions exist in the product (models absent) — logged as absent, not broken |
| E186 admission cross-tenant + task crash: POST /applications accepted an inquiry_id belonging to ANOTHER SCHOOL (proven: demo application silently linked to foreign CRM row) and bad uuid → 500; PUT /inquiries accepted free-form statuses and unvalidated assigned_to; route rejected "shortlisted" though it is a valid admission_status enum value the pipeline supports; `cleanup_stale_applications` wrote status="archived" which is NOT in the enum → DataError crash on every run, stale apps never cleaned | **FIXED-UNCOMMITTED** | `app/api/v1/admission.py` — inquiry_id must be a uuid of THIS school (400/404); inquiry status closed set + assigned_to tenant check; "shortlisted" added to the transition list; `app/tasks/admission_followup.py` cleanup now parks stale apps as "rejected" + auditable [auto-archived] remark. Runtime ✅ foreign inquiry → 404; shortlisted → 200; cleanup completes and marks the fixture | follow-up SMS task honestly no-ops without Sparrow creds (0/1 sent, status unchanged) — verified with a fixture inquiry |
| E187 inventory update-side gaps + missing audit trail: PUT /assets duplicate asset_code → IntegrityError 500; negative current_value stored on update; assigned_to_id accepted users of OTHER schools (cross-tenant link); assignment/return lifecycle wrote ZERO AssetAuditLog rows (probed: 0 after assignment); POST /procurement without title → 500; negative total_estimated_cost stored | **FIXED-UNCOMMITTED** | `app/api/v1/inventory.py` — update mirrors create-side validation (dup code → 409, money ≥ 0, assignee tenant check); update_asset records an automatic assign/return audit row on assignee change; procurement title required + cost validated. Runtime ✅ dup → 409, foreign assignee → 400, valid assignment → exactly 1 audit row action=assign | |
| E188 visitor guards: checkout could be repeated forever, silently overwriting the original checked_out_at; appointment PUT accepted cross-school staff_id (proven 200 → foreign link) and free-form statuses | **FIXED-UNCOMMITTED** | `app/api/v1/visitor.py` — double checkout → 400 "Visitor is already checked out"; appointment PUT applies the same staff tenant/FK check as POST and a closed status set. Runtime ✅ all three cases verified | |
| E189 transport update-side FK gaps: update_bus/update_stop skipped the route_id validation POST has — bad uuid → 500 (DataError), and a foreign-school route could be linked silently | **FIXED-UNCOMMITTED** | `app/api/v1/transport.py` — PUT bus/stop validate route_id as uuid + school ownership before writing. Runtime ✅ bad uuid → 400, unknown uuid → 400 | device ingest path unchanged: ESP32 devices publish to Firebase; poller → process_gps_data (JWT-gated /gps-logs POST is for manual/admin use) |

## 12. Slice-4 backend deep dive fixes (2026-08-30) — E190-E199

Per-file verdicts: `audits/coverage/BACKEND_FILE_VERIFICATION.md` § Slice 4 (communications, sms, notifications, notices, conferences, social_hub, social_ads, webhooks + comms/social services). Every route probed in-container (aschool-flask-1, demo tenant, positive + negative); regression tests in `backend/tests/test_comms_social_slice4.py`; migration `e5b7c1d9a3f8_social_hub_moderation_and_group_members`.

| Finding | Status | Evidence | Note |
|---|---|---|---|
| E190 chat role matrix was decorative: GET /communications/contacts filtered by the `role_contacts_for` matrix, but POST /communications/send and GET /communications/messages/<id> accepted ANY user of the school — a student could DM an arbitrary parent (proven: student→parent 201), a parent could DM a student (201), by POSTing a raw uuid that never appears in their directory; same hole in the parent app (`parent_app.py` chat send/read) | **FIXED-UNCOMMITTED** | `app/services/chat_service.py` — new `can_message(sender_role, target_role)` + `ChatNotAllowedError`; `send_message` enforces the matrix on EVERY send path (dashboard + parent app both funnel through it); `communications.py` send + thread-read and `parent_app.py` send + thread-read map it to 403. Runtime ✅: student→parent 403, parent→student 403, student thread-read of parent 403; parent→teacher and teacher→parent still 201 (teacher DMing any parent of the school is the BY-DESIGN staff-directory model) | matrix = what the contacts list can show is exactly what can be messaged |
| E191 a delivered chat message was invisible outside the chat page: send_message persisted ChatMessage only — no in-app notification, so the recipient's /notifications inbox and bell badge never knew a message arrived (proven: 0 notifications after send); conversely reading a thread left any message notifications unread forever | **FIXED-UNCOMMITTED** | `chat_service.send_message` writes an `InAppNotification` (category "message", data.thread_user_id = sender, single commit with the message so the inbox can never lag/lose a message); `list_messages(mark_read=True)` clears that thread's message notifications in the same commit as the read receipts. Runtime ✅: send → notification in recipient /notifications; thread read → message-category unread for the thread drops to 0 | additive payload fields only; parent app benefits via the shared service |
| E192 conferences BS-calendar overflow 500: `_conf_dict` called `ad_to_bs` raw — a conference dated outside BS 1975-2100 (AD 1918-2044) 500'd on CREATE (row still committed!) and poisoned GET /conferences for the WHOLE school forever (proven: 2055 conference → list 500 until the row was hand-deleted); out-of-range dates were accepted on write; `meeting_link` accepted any string (javascript: URIs) | **FIXED-UNCOMMITTED** | `conferences.py` — `_bs_in_range` + `_bs_or_none` (same contract as notices): write paths reject out-of-range dates with 400 BEFORE any add/commit (zero partial writes; update rolls back), serializer degrades to null; meeting_link must be http(s) (400). Jitsi honesty documented in-code: public Jitsi rooms are NOT access-controlled — anyone with the URL can join (product limitation, flagged not hidden). Runtime ✅ 2055 → 400, list stays 200, javascript: link → 400 | |
| E193 conference booking/notes integrity: `book_slot` recorded a client-supplied `parent_id` verbatim (a parent could book "as" any other user — proven: slot recorded teacher2's id); `cancel_booking` let ANY user of the school cancel someone else's booking (proven); notes had NO participant model — any unrelated teacher could WRITE another teacher's conference notes and any unrelated parent could READ them (both proven) | **FIXED-UNCOMMITTED** | `conferences.py` — booking attributed to the CALLER; admins may book on behalf of a validated in-school parent; cancel restricted to booking parent / slot teacher / school admins (403 otherwise); notes GET/PUT gated by `_notes_participant` (booked parent, slot's teacher, admins). Double-book same slot still 409 (E33). Runtime ✅: spoofed parent_id ignored for parents, intruder teacher notes-write 403 + notes-read 403, unrelated-parent read/cancel 403, participant read/write 200 | notes are private to the slot's participants |
| E194 social hub had ZERO moderation capability: no hide/unhide endpoint existed (proven 404) and Post had no moderation state — the only "moderation" was author/admin hard DELETE | **FIXED-UNCOMMITTED** | `models/social.py` Post gains `is_hidden` (indexed) + `hidden_by_id`; migration `e5b7c1d9a3f8` adds columns; new routes POST /social/posts/<id>/hide and /unhide (superadmin/school_admin only) flip real state and record who hid it; hidden posts leave every non-admin feed (admins see them with the flag so they can moderate what they hid). Runtime ✅: hide flips state + disappears from parent feed + admin still sees; teacher hide → 403; unhide restores visibility | reversible, auditable — rows are never destroyed |
| E195 group membership was fiction: `hub_groups.member_count` was a dead counter nothing ever incremented, posts had no group_id at all, POST /social/posts silently IGNORED a supplied group_id (201 with no scoping — "group" visibility unenforceable) | **FIXED-UNCOMMITTED** | new `GroupMember` model (unique group_id+user_id) + `hub_group_members` table (same migration); new routes POST /social/groups/<id>/join and /leave (creator auto-membered as moderator, count synced both ways); create_post validates group_id (400 junk / 404 unknown group / 403 non-member) and stamps visibility=group; group posts visible to members + author + admins only, and likes/comments/likes-visibility follow the same gate. Runtime ✅: non-member post 403, join → post 201, outsider can't see/like, leave → count 1 and post invisible to ex-member | |
| E196 social input hardening: empty post content stored (blank feed rows), empty comments stored, junk path UUIDs (`/posts/not-a-uuid/like`, `/comments`, DELETE) reached Postgres UUID casts → DataError 500 (proven), group name nullable violated with "" accepted | **FIXED-UNCOMMITTED** | `social_hub.py` — content-or-media required for posts (400), comment content required (400), `_parse_uuid` guard on ALL id path params → 404 (never DataError), group name required (400). Runtime ✅: all five junk-uuid probes 404; empties 400 | |
| E197 sms template create validated nothing: name="" body="" stored (blank template rows in every picker); channel accepted any string (later rendered as-is) | **FIXED-UNCOMMITTED** | `sms.py create_template` — name + body required (400), channel closed set sms/email/whatsapp/push (400). Runtime ✅ all three probes | |
| E198 webhook holes found by regression probes (E60-E66 payment paths re-verified intact: esewa no-data 400, garbage 400, unknown collection 404, stripe unconfigured 400): (a) WhatsApp GET verify FAILED OPEN when WHATSAPP_VERIFY_TOKEN was unconfigured — empty hub.verify_token matched "" and echoed the challenge to anyone (200); (b) POST /webhooks/whatsapp with malformed JSON → unhandled JSONDecodeError 500 (Meta retry-storm bait); (c) Meta redeliveries (same wa_message_id) created duplicate inbound rows | **FIXED-UNCOMMITTED** | `webhooks/__init__.py` — verify fails CLOSED (403 + logged error) when the token is unconfigured; malformed JSON → 400; duplicate `wa_message_id` (inbound) short-circuits with `duplicates` counter, stored exactly once. Runtime ✅: empty/wrong token 403, bad JSON 400, redelivery → duplicates=1, DB has 1 row | signature-verified tamper rejection (WHATSAPP_APP_SECRET path) untouched |
| E199 service-layer reliability: (a) `EmailService` SMTP had NO timeout — a hung SMTP server hung the synchronous /communications/broadcast email path forever; bulk failures logged nothing; (b) `services/social/social_hub.py` `get_unified_feed` swallowed ALL platform failures with bare `except: pass` (empty feed indistinguishable from expired token); `publish_to_all` accepted tiktok in `platforms` but silently dropped it (read-only wrapper — fake delivery contract); (c) `PostSchedulerService` crashed on every call — referenced nonexistent `SocialPost.content`/`image_url` columns (TypeError) and wrote status "cancelled" which is NOT in the post_status enum (DataError); (d) `SmsGatewayService.send_sms` crashed with raw JSONDecodeError on non-JSON provider responses | **FIXED-UNCOMMITTED** | email_service: SMTP(timeout=MAIL_TIMEOUT default 10s) + per-recipient bulk failure logging with addresses; social_hub service: feed failures logged via logger.exception with school + platform, tiktok target produces a loud honest error entry ("publishing is not supported — read-only integration"); post_scheduler: real columns (content_en/media_urls), cancel → "draft" (valid enum); sms_gateway: non-JSON + request exceptions → structured {"success": False} instead of raw crash. Runtime ✅: scheduler schedule+cancel round-trip in tests; sms_gateway fake non-JSON → structured failure; meta/tiktok/youtube httpx clients all have timeout=30 (verified, sane) | post_scheduler had zero callers — fixed because the scheduler task is one import away |

## 11. Slice-1 backend deep dive fixes (2026-08-30) — E160-E169

| ID Finding | Status | Fix | Residual |
|---|---|---|---|
| E160 privilege escalation + account hygiene: `PUT /users/<id>` mass-assigned ANY `role` string (`_populate_user` allowed set) — a school_admin could set role="superadmin" on themselves (live-verified 200 pre-fix) and the claim takes effect at next login = full platform takeover; `POST /staff` had no duplicate-phone check (silent second account with same phone — login_with_password picks first) and no password-strength check (users.create_user has both) | **FIXED-UNCOMMITTED** | users.update_user: role accepted only if unchanged (no-op payloads like teachers.service `role:"teacher"`) or within the create-path role set; "superadmin"/arbitrary strings → 400. staff.create_staff: dup-phone → 409 (live-verified against an active user's phone), weak password → 400. Runtime ✅: role=superadmin 400, role=wizard 400, no-op 200, legit staff→accountant 200 | school_admin can still set peers to school_admin/parent/student within their school (their station); `permissions` JSON remains admin-settable by design |
| E161 dead superadmin claim locked the platform admin out of schools: `schools.get_school/update_school` checked `claims.get("is_superadmin")` — a claim `AuthService.create_tokens` never sets — so even superadmin@aschool.com.np got 403 on GET/PUT /schools/<id> (live-verified); tenant protection worked only via the g.school_id fallback. Also: tenant admins could mutate routing/billing fields (slug, custom_domain, plan, max_students, is_active) via update_school AND /schools/current | **FIXED-UNCOMMITTED** | superadmin recognised by role claim (role=="superadmin"); protected fields stripped for non-superadmins in update_school + update_current_school. Runtime ✅: SA GET /schools/<demo> 200 (was 403), admin cross-school 403, slug/plan strip | |
| E162 numeric-param 500s + unbounded presign: `GET /files/<id>/presigned?expires=abc` → ValueError 500; negative or huge expires passed through (botocore caps R2 signatures at 7 days → prod 500 on huge); `GET /files/stock-search?...&page=abc` → ValueError 500 | **FIXED-UNCOMMITTED** | presigned: 400 on non-integer + clamp [60s, 7×24h] (runtime ✅ abc→400, -5→60, 99999999→604800); stock-search: 400 on non-integer page/per_page | |
| E163 batch roll numbers: `POST /students/batch-roll-numbers` with roll_number "abc" → unhandled `int()` ValueError 500 (live-verified); partial updates before the crash item were already flushed | **FIXED-UNCOMMITTED** | per-item int validation → 400 naming the offending value, before any write (runtime ✅) | |
| E164 (E96) NO password recovery existed anywhere — no route/endpoint/link; forgotten-password users fully locked out | **FIXED-UNCOMMITTED** | Full honest flow: POST /auth/forgot-password (email → 32-byte urlsafe token stored ONLY as SHA-256 in dedicated redis namespace `pwreset:<hash>`, 30-min TTL, per-email 60s cooldown → 429; unknown email → generic 200 `delivery: "skipped"`; SMTP unconfigured/failed → token logged server-side + `delivery: "unavailable"` — never fake success; sent → `delivery: "sent"`); POST /auth/reset-password (token+new_password → policy-validated, single-use delete-before-use, clears lockout, bumps tokens_invalid_before → every old JWT access+refresh all devices rejected). Tokens live OUTSIDE flask-caching — redis MONITOR during the dive caught unrelated batch DELs sweeping `flask_cache_*` keys (school-plugin invalidations) which silently voided tokens and the cooldown. Frontend: new `/reset-password` page (token-less email-request mode with honest delivery hint + token reset form) + "Forgot password?" link on /login. Runtime ✅ full loop 12/12 steps (forgot→log→weak 400→bad 400→reset 200→replay 400→old pw 401→old JWT 401→new pw 200→new session 200); both page modes verified live; tsc clean | delivery "skipped" vs "unavailable" is a mild account-existence oracle (explicit honesty requirement of the brief); re-login within the same second as a password change hits the iat<cutoff race and fails once (fail-closed, self-heals) |
| E165 three parent-app screens called endpoints that existed NOWHERE: flutter_parent `/parent/child-health?type=records|vaccinations|allergies`, `/parent/portfolio`, `/parent/elibrary` → 404 on every load (permanent error state even with live data). Also: the elibrary + portfolio screens read `res.data['resources']/['entries']` off the raw Dio envelope (never unwrapped `data`) → would always render empty even with a backend | **FIXED-UNCOMMITTED** | New ward-scoped, plugin-gated parent endpoints (health_records/student_portfolio/elibrary gates) emitting exactly the keys the screens parse (child-health: visit/vaccine/allergen fields incl. doctor_name; portfolio: {entries, summary}; elibrary: {books, past_papers, resources} + ?q filter); flutter screens unwrap the envelope. Runtime ✅: real visit/immunization/allergy fixtures visible to the probe parent; elibrary returns the school's real books/papers/resources; frontend container serves both page modes | conference slot booking is check-then-set (no row lock) — concurrent double-book theoretically possible |
| E166 SSRF in stock import: `POST /files/stock-import` fetched ANY user-supplied URL server-side (probed http://169.254.169.254 and https://evil.example.com → both would fetch; timeout 30s) | **FIXED-UNCOMMITTED** | https-only + host allowlist images.unsplash.com / plus.unsplash.com / images.pexels.com / videos.pexels.com → 400 otherwise; frontend files.service sends stock search results' `full_url` (Unsplash/Pexels CDN) — compatible | |
| E167 file upload had a size limit but NO type restriction — any binary (.exe, .svg-with-JS, .html) was stored and served same-origin from /uploads | **FIXED-UNCOMMITTED** | Extension allowlist (env-overridable FILE_ALLOWED_EXTENSIONS; default: images/video/audio/pdf/doc/txt/xls/csv/zip matching FilePicker + bulk-image ZIP flows; svg intentionally excluded as stored-XSS vector) → 415 with the permitted list. Runtime ✅ .exe 415, .png 201 | MIME sniffing not performed (extension-level gate; ClamAV scan still applies when enabled) |
| E168 unified search numeric params: `GET /search?limit=abc` → ValueError 500; `limit=-3` → negative LIMIT SQL error 500 (both live-verified) | **FIXED-UNCOMMITTED** | clamp [1,25] + fallback 8 (runtime ✅ abc/-3 → 200) | |
| E169 files tenant isolation + cross-school probes: all ManagedFile/FileFolder queries filter school_id — proven with a second-school admin (Bright-Star probe admin, file_management temporarily installed then uninstalled): GET/PATCH/DELETE/presign of a demo-school file → 404, list → empty; upload keys embed g.school_id; usage totals match DB. Logged as WORKS (verified, no code change needed) with two notes: bulk-profile-images reads the whole ZIP into memory (no cap) and upload folder_id is not validated to belong to the school (metadata-only linkage) | **VERIFIED** | — | ZIP memory cap + folder_id validation left as hardening backlog |

## 12. Live-stack bugfix batch (2026-08-30) — E175-E179

| ID Finding | Status | Fix | Residual |
|---|---|---|---|
| E175 /dashboard/files listed NOTHING despite uploads existing: browser fetch chain for `GET /api/v1/files/...` ended at `http://flask:5000/...` (ERR_NAME_NOT_RESOLVED). Root cause is a trailing-slash redirect ping-pong, NOT a NEXT_PUBLIC_API_URL misconfig: files.service called `/files/?…` → Next dev 308-normalizes to `/files?…` (same-origin, harmless) → Flask route was declared `@files_bp.route("/")` so `/files` 308'd back to `/files/` with a WERKZEUG-ABSOLUTE Location built from the proxied Host header = `http://flask:5000/api/v1/files/…` → browser follows and dies on the Docker-internal hostname | **FIXED-UNCOMMITTED** | backend `files.py list_files`: `strict_slashes=False` so both `/files` and `/files/` answer 200 with no redirect (docstring explains why it is load-bearing); frontend `files.service.listFiles` now calls `/files?…` (no trailing slash) so no hop at all. Runtime ✅: demo-admin login → upload `e175-verify.txt` via POST :3003/api/v1/files/upload (201) → GET :3003/api/v1/files?per_page=100&folder_id=root returns it (count 7, first item e175-verify.txt); with-slash URL also resolves with exactly 1 same-origin 308, final url stays on localhost:3003; `curl -s http://localhost:3003/dashboard/files \| grep -o flask:5000` = empty (0 hits in HTML and in the page's compiled client chunk) | Werkzeug's absolute-redirect behavior still exists for any OTHER route declared with a trailing slash under a url_prefix — none found today (`grep route\("/"` = files only) |
| E176 client-side API URL builders bypassing the shared axios client (`lib/api.ts` relative `/api/v1` + withCredentials) — each one is a latent Docker-host leak the moment NEXT_PUBLIC_API_URL is ever set to an internal hostname: (a) files.service upload used raw XHR with `NEXT_PUBLIC_API_URL` prefix; (b) ContactForm, (c) AdmissionForm, (d) school results checker, (e) verify-otp (verify + resend), (f) register, (g) certificates/students image-embed prefixed relative /uploads srcs with the env var | **FIXED-UNCOMMITTED** | all seven unified on the shared client / same-origin relative URLs: upload → `api.post("/files/upload", form, { onUploadProgress })` (axios progress replaces XHR, backend error body surfaced); ContactForm/AdmissionForm → `api.post(/website/public/<slug>/contact|admission-inquiry)`; results → `api.get`; verify-otp/register → `api.post("/auth/…")` (both are in api.ts's isAuthEndpoint no-refresh list); certificates embedImages uses root-relative srcs as-is. tsc (container) clean on all touched files | school/[slug] SSR pages + middleware + next.config.js `API_URL \|\| NEXT_PUBLIC_API_URL \|\| "http://flask:5000"` fallbacks are SERVER-ONLY (verified: no "use client", no host string in served client chunks) — that is the intended Docker-internal SSR fetch path, left as-is (see E179) |
| E177 public /school/<slug> of an UNPUBLISHED school rendered the same "School Not Found" title+body as a bogus slug — dishonest UX for the website-builder "Unpublish" state (backend guard returns 404 {"error":"Website not published"} since E159) | **FIXED-UNCOMMITTED** | backend `_public_site_guard` unpublished 404 now carries `data.school_name` (+ slug); new `frontend/lib/public-site.ts` `getPublicSite()` (server-only) distinguishes the two 404s by error body; `school/[slug]/layout.tsx` renders an honest "🚧 <School> — Website Coming Soon / website hasn't been published yet / Website not published" state (and generateMetadata title matches) vs genuine "School Not Found / doesn't exist" for unknown slugs; root `school/[slug]/page.tsx` refactored onto the same loader with matching defensive states. 404 semantics kept (no site content leaks, API still 404s). Runtime ✅: is_published=false + revalidate-site → title "Bright Star Public School — Website Coming Soon" + coming-soon body; fake slug → "School Not Found"; republish + revalidate → normal site title again (DB flip reverted) | ISR data cache still serves the old render for up to 5 min unless revalidate-site is called (pre-existing E152 behavior, mitigated by builder hooks calling it on unpublish) |
| E178 site favicon 404'd on every page (no icon anywhere: no app/icon.*, no public/ dir, root metadata has no icons) | **FIXED-UNCOMMITTED** | `frontend/app/icon.svg` added (Next app-router icon convention → auto `<link rel="icon">`, white "A" mark on primary-blue rounded square). Runtime ✅: GET :3003/icon.svg → 200 image/svg+xml | browsers that hard-request /favicon.ico regardless of link tags would still 404 — none observed in the console evidence; add a real favicon.ico only if it shows up |
| E179 audit conclusion for the "flask:5000 fallback" grep sweep requested with E175: every literal `\|\| "http://flask:5000"` API_URL fallback lives in server-only code — middleware.ts (SSR fetch for custom-domain resolution), next.config.js rewrites (server-side proxy target), super-admin/layout.tsx + all 14 school/[slug] SSR pages (server components, verified no "use client") | **VERIFIED** | — (no code change needed) | these are the intended Docker-internal SSR fetch path; the next.config rewrite is what makes browser-relative /api work. Only rule: never import API_URL into a client module — client code must use lib/api.ts |

## 13. Theme-engine real-design port (2026-08-30) — E170-E174

User request: replace the 20 invented school themes with REAL openly-licensed school/college website designs, remove the old set everywhere (registry, backend, DB rows), and keep the same token mechanism (`--color-*`/`--font-*` + section styling). Sources + licenses: `frontend/themes/THEMES_CREDITS.md`.

| Finding | Status | Evidence (current source) | Note |
|---|---|---|---|
| E170 all 20 themes were invented (no design source, no license trail): `frontend/themes/registry.ts` + `backend theme_engine.py` mirrored each other with made-up palettes | **FIXED-UNCOMMITTED** | 10 themes ported at token level from real GPL WordPress education themes downloaded from wordpress.org (license headers verified in each `style.css`): `collegiate-heritage`←Education Hub (WEN, GPLv3), `university-azure`←University Hub (WEN, GPLv3), `educenter-bright`←Educenter (Sparkle, GPLv2+), `kids-campus-playful`←Kids Campus (Grace, GPLv2+), `blossom-montessori`←Preschool and Kindergarten (Rara, GPLv2+), `community-skyline`←Education Zone (Rara, GPLv2+), `global-elearning`←eLearning (Masteriyo, GPLv3+), `boarding-crimson`←VW School Education (VW, GPLv3+), `institute-industrial`←Education Insight (Ovation, GPLv3+), `campus-warm`←Campus Education (Themesglance, GPLv3). Palettes+font pairings extracted from each theme's real CSS (all Google-Fonts hosted). `frontend/themes/THEMES_CREDITS.md` records source+license+derivation notes; no CSS/JS/images copied — tokens only. `frontend/themes/loader.ts` category map + per-theme hero styles updated | No proprietary code committed; placeholder/stock mechanism unchanged |
| E171 theme switching was cosmetically dead on the public site: `customizations["colors"]` (written once by a page-template apply) permanently overrode the theme palette in the public layout's `generateThemeCSS(theme, colorOverrides)`, and `WhiteLabelService.brand_colors()` counted the builder's own `customizations.primary_color/secondary_color` mirrors as "brand colors" — so every later theme apply inherited the FIRST theme's primary/secondary forever (live-proven: apply boarding-crimson → render kept teal `#0f766e`/`#7C3AED`) | **FIXED-UNCOMMITTED** | `ThemeEngineService.synced_colors()` (theme_engine.py) computes the 5 core tokens from the chosen theme (white-label stored branding + explicit `color_overrides` keep precedence; auxiliary keys like `surface` preserved); `apply_theme` now sends the refreshed `colors`; `WebsiteBuilderService.update_config` merges it (core tokens replaced, aux kept) and also syncs when only `theme_slug` changes; `PUT /website/config` handler (`api/v1/website.py`) — which previously wrote `theme_slug` directly, bypassing the service — runs the same sync unless the payload carries explicit colors; `brand_colors()` now reads ONLY `settings["white_label"]["branding"]` (save_branding still writes through) | Runtime ✅ (live stack): apply boarding-crimson → public render `--color-primary:#002b46` + `data-theme="boarding-crimson"`; bare PUT `theme_slug=educenter-bright` → DB colors.primary `#004a8d` + render `#004a8d`; re-apply → back to `#002b46`. Re-validation via the picker's own `/revalidate-site` flow |
| E172 DB rows + code still referenced removed theme ids: `SchoolWebsite.theme_slug='modern-minimal'`/NULL, hardcoded fallbacks `"modern-minimal"`/`"default"` in website_builder service+API, public layout, editor page; seed wording "20 themes" | **FIXED-UNCOMMITTED** | `backend/scripts/migrate_theme_slugs.py` — idempotent mapper old→closest new (all 20 old ids mapped; NULL/unknown → `global-elearning`), `--dry-run` supported; `DEFAULT_THEME_ID="global-elearning"` exported by the registry and used as the single fallback everywhere (`registry.ts`, both Next layouts/editor, backend service+API); seed_full descriptions updated | Runtime ✅: run 1 migrated 2 rows (`modern-minimal`→global-elearning, `''`→global-elearning); run 2 = no-op; every DB row now on a valid new theme; both public sites render correct signatures |
| E173 AI website designer still prompted with the 20 removed theme ids → guaranteed invalid `theme_slug` suggestions; its fallback variations referenced `modern-minimal`/`nepal-heritage`/`private-classic` palettes | **FIXED-UNCOMMITTED** | `app/services/ai/website_designer.py`: prompt now lists exactly the 10 new ids; `_default_variation` palettes/fonts rebuilt from `global-elearning`/`collegiate-heritage`/`educenter-bright`; docstring example updated | Frontend ai-builder "School Type" select values (government/private/…) are school types, not theme ids — left as-is |
| E174 full verification sweep of the ported theme system | **VERIFIED** | `GET /api/v1/website-builder/themes` → exactly 10 ids (no old id); Google Fonts css2 combined query (11 families incl. Amatic SC/PT Serif) → 200; all 14 public `/school/bright-star-public-school` pages → 200 with boarding-crimson CSS vars + `data-theme`; `/school/demo` renders collegiate-heritage `#294a70`; `pytest -k "theme or white_label or website"` in-container → 33 passed; `tsc --noEmit` in aschool-nextjs-1 → 0 errors in touched files (only the 4 documented pre-existing ones) | Theme picker (dashboard/website-builder/themes) lists only the new themes with the existing CSS-tile gradient previews + CSS modal (no heavy images) |

## 14. WordPress-style plugin architecture batch (2026-08-30) — E160-E166 (plugin-architecture allocation)

NOTE on IDs: the E160-E169 band was DOUBLE-ALLOCATED — §11 "Slice-1 backend deep dive" uses E160-E169 for users/schools/files fixes, while THIS plugin batch was briefed the same band. The rows below are the authoritative reference for the E-tags planted in `backend/app/plugins/billing.py`, `backend/config.py`, `backend/app/api/v1/plugins.py` and `frontend/app/dashboard/marketplace/page.tsx` comments (each tagged "plugin-architecture batch"). User-visible complaint driving the batch (live-browser-verified): paid-style "Start Trial"/"Cancel Subscription" everywhere, no install/activate semantics, no per-plugin settings.

| ID | Finding / requirement | Status | Fix | Runtime evidence (aschool-flask-1 + aschool-nextjs-1, probe `tmp_plugin_arch_verify.py` — 39/39 checks) |
|---|---|---|---|---|
| E160 config-driven install policy: FREE vs PAID install semantics were indistinguishable (install always meant "trial") | **FIXED-UNCOMMITTED** | `config.py` `PLUGIN_TRIAL_DAYS` (env, default 14) + `PLUGIN_FREE_TIERS` (env, default "core,add_on"); `app/plugins/billing.py` `plugin_is_free()` (price_monthly==0 OR is_free OR category in PLUGIN_FREE_TIERS) + `effective_trial_days()` (config supersedes per-plugin catalog trial_days) + `_apply_install_policy()`; free install → `is_trial=False, trial_ends_at=None, active` immediately ("Install", never a trial); paid install → PLUGIN_TRIAL_DAYS trial. Marketplace response per plugin: `price_monthly/price_yearly, tier, is_installed, install_state (not_installed\|active\|inactive), is_trial, trial_days_left, can_subscribe, is_deactivated`. **Trial-clock exploits closed (found + fixed at runtime):** (a) uninstall/deactivate → reinstall RESET the 14-day window (infinite trial) — reinstall now preserves the ORIGINAL trial window; expired-trial reinstall refused ("subscribe to install it again"); (b) subscribed (is_trial=False) rows reinstall as-is with their `config["last_payment"]` proof — never minted a fresh trial; (c) `subscribe` no longer routes through install_plugin (it minted a trial first AND deadlocked expired trials: install refused with "subscribe to install it again" while subscribe itself called install → 409) — it now creates/reactivates the row directly as PAID with dependency checks, and clears `trial_started_at/trial_ends_at` residue on conversion | ✅ free install (academics/basic_website) → 201, is_trial=False, marketplace `trial_days=0, can_subscribe=false`; paid install (library_management 199) → trial, `trial_days_left==14 (PLUGIN_TRIAL_DAYS)`; subscribe on FREE → 400; reinstall after uninstall → trial_ends_at unchanged; expired-trial reinstall → 400 + "already been used"; subscribe after expired trial → 200 (deadlock gone); subscribed uninstall/reinstall → is_trial stays False + last_payment kept; suites `test_plugins + test_marketplace_billing + test_plugin_aliases + test_provisioning_and_caps` = 28 passed |
| E161 no WP-style activate/deactivate — install/uninstall were the only verbs and uninstall ignored deactivated rows | **FIXED-UNCOMMITTED** | POST `/plugins/<slug>/activate` + `/deactivate` (tenant-scoped, superadmin/school_admin, idempotent 409 on repeat, 404 when uninstalled/never installed) flipping `SchoolPlugin.active` while PRESERVING config/trial/billing state (`deactivate_plugin`/`activate_plugin` in billing.py never stamp `uninstalled_at`); `uninstall_plugin` now accepts DEACTIVATED rows too (WP lets you delete a disabled plugin — was a silent 400); the request-path loader `_set_school_context` (app/__init__.py) already filters `active=True` → deactivated plugins vanish from `g.installed_plugins` + `/plugins/installed` + sidebar, and `plugin_required` gates 403 immediately (cache invalidated on every flip) | ✅ deactivate → gated `GET /library/books` 403 + `school:{id}:plugins` cache = list WITHOUT the slug; reactivate → 200 and the gated route 200 again; trial window byte-identical across deactivate/reactivate; uninstall of a deactivated plugin → 200 (was 400), second uninstall → 400 |
| E162 plugin config GET/PUT incomplete + JSONB in-place-edit bug class | **FIXED-UNCOMMITTED** | GET/PUT `/plugins/<slug>/config` exist, tenant-scoped (404 cross-school), readable while active OR deactivated, 404 once uninstalled. PUT validation: JSON-dict only (non-dict → 400), 16KB cap (`PLUGIN_CONFIG_MAX_BYTES`), reserved keys not client-writable (`config["last_payment"]` — the E5 subscribe audit trail). **Real bug fixed:** PUT mutated `sp.config` in place and reassigned the SAME dict object — SQLAlchemy's attribute history compares equal → no UPDATE emitted → every second save on a non-empty config was silently dropped; now merges into a FRESH dict + `flag_modified(sp, "config")` defense-in-depth. Default semantics MERGE; `?replace=1` replaces the whole dict (needed to drop deleted keys — the settings page sends the full dict with it) | ✅ PUT {string, number, bool, nested object} → 200 → GET readback identical; SECOND PUT persists (bug fixed — post-fix readback shows "Changed"); `?replace=1` drops removed keys; merge default restores over stored config; non-dict → 400; reserved key → 400; 20KB blob → 400; cross-school GET/PUT → 404 |
| E163 marketplace/PluginGate copy lied about money (live complaint): paid-style "Start Trial"/"Cancel Subscription" everywhere, "Install Now — Free" on PAID gates, hardcoded 29/99/299 SaaS prices | **FIXED-UNCOMMITTED** | `frontend/app/dashboard/marketplace/page.tsx` card states read ONLY the live catalog response: FREE not installed → **Install** (instant); FREE installed → Deactivate/Activate; PAID not installed → **Start {trial_days}-Day Free Trial** (API `trial_days`); PAID trial → **Trial · N days left — Subscribe** (opens SubscribeDialog which collects the E5 payment proof, 402 without it); PAID subscribed → **Manage** (→ settings page) + Uninstall. **No "Cancel Subscription" anywhere** (repo grep = 0); no local price literals (prices via `formatCurrency(price_monthly)`); SaaS Packages tab prices/features derived from the real catalog tiers (core+add_on+starter/growth/premium aggregates + plugin names — `buildPackages`), not hardcoded 29/99/299; `lib/plugins.tsx` PluginGate button "Install Now — Free" → honest "Install" (paid gates start a trial per backend policy) | ✅ tsc --noEmit clean on the page; grep "Cancel Subscription" across frontend app/components/lib = 0; grep 29/99/299 literals in marketplace page = 0; packages render `NPR {sum} /mo` with "Aggregate monthly price of the N catalog plugins in this plan" + tier plugin names |
| E164 no installed-plugins management surface (WP plugins.php) | **FIXED-UNCOMMITTED** | New `frontend/app/dashboard/plugins/page.tsx` — one row per installed plugin (from the marketplace catalog's `install_state`, which includes DEACTIVATED rows that `/plugins/installed` hides): name+emoji+description, category badge, status badge (Trial · N days left / Free / price), **Active toggle** (Switch → activate/deactivate), Settings link, Uninstall (confirm; "data is preserved"), "Add New (Marketplace)" header link. Sidebar link: new `backend/app/plugins/manifests/plugins_nav.yaml` (bottom_nav, visible_to admins, `published: false` so seed never lists it as an installable product) + `plugins_nav` in `PluginLoader.BOTTOM_NAV_ALWAYS_SLUGS` right after `marketplace_nav` | ✅ page 200 with session via :3003; live `/plugins/sidebar` bottom_nav = `['marketplace_nav', 'plugins_nav', 'settings_core', 'iemis_importer']` (adjacent to Marketplace, before Settings); `plugins_nav` absent from the 55-plugin marketplace catalog (published:false); toggle/uninstall hit the live E161 endpoints |
| E165 no per-plugin settings UI (config was API-only, M7 flutter-only) | **FIXED-UNCOMMITTED** | New `frontend/app/dashboard/plugins/[slug]/settings/page.tsx` — friendly form over the plugin's `SchoolPlugin.config` via GET/PUT config: string → text input, number → number input, boolean → switch, arrays/objects/unknown keys → JSON textarea (parse-validated); add-key row (name + type Text/Number/On-Off/JSON); per-key remove; saves via PUT `?replace=1` (full dict so removed keys actually drop); 404 → honest "not installed" state; save disabled for non-admin roles (server enforces superadmin/school_admin). Linked from marketplace **Manage**, the plugins table **Settings**, and PluginGate stays marketplace-pointed | ✅ page 200 with session (`/dashboard/plugins/library_management/settings`); backend round-trip proven by the E162 runtime checks (persists all four value types, replace drops keys); tsc --noEmit clean on the file |
| E166 full-stack regression sweep of the batch | **VERIFIED** | `tmp_plugin_arch_verify.py` (kept for reruns) = **39/39 checks** across install policy, activate/deactivate gates, config validation/persistence, trial-clock preservation, uninstall semantics, marketplace fields, sidebar; `pytest tests/test_plugins.py tests/test_marketplace_billing.py tests/test_plugin_aliases.py tests/test_provisioning_and_caps.py` = **28 passed**; `tsc --noEmit` in aschool-nextjs-1 → 0 errors in touched files | Throwaway probe school + fixtures fully deleted after (cleanup check green); no git commits made |

## 15. Live-browser UX test fix batch (2026-08-30) — E200-E207

Source: `audits/coverage/LIVE_BROWSER_UX_TEST.md` (live-verified findings). All runtime probes on the live stack (aschool-flask-1 :5003, aschool-nextjs-1 :3003).

| ID | Finding | Status | Fix | Runtime evidence |
|---|---|---|---|---|
| E200 CORS blocks Flutter apps (P0): POST /auth/login from origin http://localhost:8091 failed preflight — flask-cors passed the OPTIONS through to Werkzeug's default handler → 200 + `Allow:` but ZERO Access-Control-* headers (8090/8091 Flutter-web dev ports were not in the origin list); `CORS_ALLOW_ORIGINS` env was not honored at all (only the legacy `CORS_EXTRA_ORIGINS`) | **FIXED-UNCOMMITTED** | `backend/app/__init__.py`: built-in dev defaults now include localhost/127.0.0.1 on 8090+8091 (kept 3000/3001/8080); `CORS_ALLOW_ORIGINS` (canonical, comma-separated) AND legacy `CORS_EXTRA_ORIGINS` both extend the list; production patterns (FRONTEND_URL, https://base, www, `*.base` regex) unchanged; socket.io origins get the same additions. New `normalize_preflight_response` after_request (registered BEFORE cors.init_app so it runs AFTER flask-cors attaches headers) rewrites preflights 200→204 and strips Content-Type/Length | Live ✅: OPTIONS /api/v1/auth/login Origin 8091 → **204** + Allow-Origin:8091, Allow-Headers echo (authorization, content-type), Allow-Credentials:true, Allow-Methods, Max-Age:600; same for 127.0.0.1:8090; POST login Origin 8091 → 200 + Allow-Origin echoed (real demo-admin login success:true). In-container `CORS_ALLOW_ORIGINS=http://custom-origin.example:5555,http://flutter.staging.example` → both 204+echo; unknown origin → still no CORS headers. CSRF guard reuses the same `_cors_origins` list so Flutter Bearer clients stay unaffected |
| E201 Unpublished public site still renders (P1): POST /website-builder/unpublish → public /school/<slug> kept serving the FULL site for minutes — the guard fetch (`lib/public-site.ts getPublicSite`) was ISR-cached (`revalidate:300`) and dashboard revalidation only fires from builder UI, not API callers | **FIXED-UNCOMMITTED** | New `getPublicSiteStatus()` (no-store fetch of the public endpoint) = REQUEST-TIME GUARD; `school/[slug]/layout.tsx` consults it FIRST — unpublished → E177 coming-soon markup, unknown slug → School Not Found, and children (all 12 subpages) never render; `generateMetadata` decides on the guard too. Published sites keep ISR (`revalidate:300` + tags) for heavy content; if guard says published but the ISR cache still holds a pre-publish 404, one no-store re-fetch recovers. `getPublicSite(slug, {noStore})` option added. Backend: publish/unpublish now fire-and-forget POST `{NEXTJS_INTERNAL_URL}/api/revalidate` (new config keys `NEXTJS_INTERNAL_URL` http://nextjs:3000, `ISR_REVALIDATE_SECRET`) for all 13 public pages of the slug; `frontend/app/api/revalidate/route.ts` secret check normalized (`(secret ?? "") !== (env ?? "")`) — the old strict `!==` 401'd every server-side ping when the env was unset (also silently broke `website_live_sync`'s revalidation) | Live ✅: unpublish as demo owner → GET :3003/school/demo shows "Website Coming Soon" within seconds (5 hits in HTML); republish → `<title>Demo School Nepal</title>` + full site again within seconds; direct revalidate probe → 200 `{"revalidated":true}`, wrong secret → 401; flask log "ISR revalidation failed" count = 0 (all pings succeeded). **demo + bright-star-public-school both restored is_published=True** |
| E202 website-builder publish/unpublish accepted a `school_slug` body param and silently IGNORED it (always used caller tenant) — a caller could believe they published/unpublished another school | **FIXED-UNCOMMITTED** | `_resolve_target_school()`: empty/absent slug → own school (unchanged); provided slug ≠ caller's school slug → **403** "school_slug does not match your school" (before any DB write); matching slug → proceeds | Live ✅: unpublish + publish with `school_slug:"bright-star-public-school"` as demo admin → 403; `school_slug:"demo"` → 200; empty body → 200 own school; bright-star publish state untouched |
| E203 Add Student modal: Guardian Name input had `required` but label lacked `*` (First/Last Name show `*`) — native validation silently blocked submit with no feedback | **FIXED-UNCOMMITTED** | `frontend/app/dashboard/students/page.tsx` label → "Guardian Name *" + helper text "Creates the parent login" | tsc clean (file untouched by errors); matches the modal's existing `Label *` convention |
| E204 students table rendered "Class Class 10" — legacy class_name rows already carry the "Class " prefix and the renderer prepended another | **FIXED-UNCOMMITTED** | renderer strips a leading `class ` (case-insensitive) before prepending: `` `Class ${student.class_name.replace(/^\s*class\s+/i, "")}` `` — legacy "Class 10" rows render "Class 10", clean "10" rows render "Class 10" | renderer-only fix per brief; class-name hygiene backlog unchanged |
| E205 fees overview cards showed abbreviated "Rs. 12K" while the progress row showed exact values — inconsistent money | **FIXED-UNCOMMITTED** | `frontend/app/dashboard/fees/page.tsx`: ALL money renderings now use `formatNepaliCurrency` (lib/nepali-utils.ts — exact NPR, Nepali 2-2-3 grouping, रू symbol): 4 KPI cards, progress Collected/Expected, recent payments, collection-by-class, overdue alert. Page grep = 0 remaining `Rs.`/`K` abbreviations | रू symbol now consistent across the whole overview (was mixed Rs./toLocaleString) |
| E206 React hydration error "<button> cannot be a descendant of <button>" on /dashboard/fees/collect — fee-card outer `<button>` contained the `<Button>` Receipt download | **FIXED-UNCOMMITTED** | outer card → `<div role="button" tabIndex={0}>` + Enter/Space keydown handler + cursor-pointer; inner Receipt `<Button>` stays a real button (its stopPropagation preserved). The 3 other raw `<button>`s in the page contain no nested buttons | nesting removed; keyboard operability kept |
| E207 marketplace website-builder card said "20 themes" but the registry has 10 | **FIXED-UNCOMMITTED** | count now derived LIVE from the theme registry in the marketplace catalog endpoint (`plugins.py`: website_builder description rebuilt as "N open-source school themes + custom domain + AI builder + Craft.js editor") so it can never go stale again; both manifest YAMLs + DB row updated to 10-based copy | Live ✅: GET /plugins/marketplace → "10 open-source school themes + custom domain + AI builder + Craft.js editor" |

Batch verification: `pytest tests/ -q -k "cors or website"` = 22 passed; plugin suites (plugins/marketplace_billing/aliases/provisioning) = 28 passed; `tsc --noEmit` in aschool-nextjs-1 = only the 4 documented pre-existing errors (sidebar.tsx, sanitize.ts), 0 in touched files; no git commits.

## 16. Live-browser UX fix batch 2 (2026-08-30) — E210-E219

Source: `audits/coverage/LIVE_BROWSER_UX_TEST.md` (session-2 remaining findings). Runtime probes on the live stack (aschool-flask-1 :5003, aschool-nextjs-1 :3003) with the demo-admin session (`admin@demo.aschool.com.np`, cookie + Bearer). NOTE: demo admin hit the login lockout during probing (naive/aware datetime crash in `_check_lockout` when `locked_until` is set — flagged, not in scope); lockout row cleared in dev DB to proceed.

| ID | Finding | Status | Fix | Runtime evidence |
|---|---|---|---|---|
| E210 WhatsApp sub-pages 404 (P1): sidebar Conversations/AI Settings/Templates/Analytics all 404; only whatsapp/page.tsx existed | **FIXED-UNCOMMITTED** | (a) Conversations: NEW backend routes `GET /whatsapp-bot/conversations` (grouped by inbound from_phone, per-phone in/out counts, latest-inbound preview, derived handled badge) + `GET /whatsapp-bot/conversations/<phone>/messages` (full thread, oldest first); `POST /whatsapp-bot/send` now RECORDS the outbound WhatsAppMessage (skipped/unconfigured sends are not recorded) so manual replies appear in-thread; handled-ness is DERIVED (an inbound is handled when a later-or-equal outbound to the same phone exists — `_inbound_with_reply_exists` EXISTS subquery) since WhatsAppMessage has no handled column; page `frontend/app/dashboard/communications/whatsapp/conversations/page.tsx` (PluginGate whatsapp_bot, list+thread+manual reply box, loading/error states) | Live ✅: GET /conversations → 4 conversations (7in/0out unhandled · 1/1 handled · 1/1 handled · 1/0 unhandled); GET messages for 9779800000042 returns the full 7-message thread with handled flags; totals match SQL. |
| E211 WhatsApp Templates page (P1, part of the 404 batch) | **FIXED-UNCOMMITTED** | Rules = WhatsAppBotConfig.auto_replies JSONB (E32 fix). Backend: PUT `/whatsapp-bot/auto-replies/<int:rule_id>` + DELETE added (index-addressed — the JSONB list has no stable ids; fresh-list assignment so SQLAlchemy change detection fires); create validates non-empty keyword/response; page `templates/page.tsx` full CRUD (add form, inline edit, confirm-delete, empty/loading/error states) | Live ✅: POST → total_rules 2; GET shows both; PUT rule 1 edit → updated rule echoed; DELETE rule 1 → removed; re-GET persists (FEES rule remains). |
| E212 WhatsApp AI Settings page (P1, part of the 404 batch) | **FIXED-UNCOMMITTED** | Persists to the whatsapp_bot PLUGIN config: GET/PUT `/plugins/whatsapp_bot/config` carrying `ai_settings` {auto_reply_enabled, ai_persona, working_hours{enabled,start,end,days,off_hours_message}} — travels with the plugin install like every WP-style plugin setting; page `ai-settings/page.tsx` (switches, persona textarea, time pickers, day chips, tolerance-normalized load, Save) | Live ✅: PUT with full ai_settings → 200 echoes payload; GET → same values back (JSONB persisted). |
| E213 WhatsApp Analytics page (P1, part of the 404 batch) | **FIXED-UNCOMMITTED** | NEW `GET /whatsapp-bot/analytics?days=14` — real SQL counts only: per-day inbound/outbound (last ≤90d), handled % over the window (derived, same EXISTS), all-time totals, top-5 senders with handled coverage; page `analytics/page.tsx` (recharts grouped bars + table; loading/error states; no synthetic numbers) | Live ✅: days=14 → inbound_last_window 10, handled 2 (20.0%), timeline [{2026-08-29: in 10, out 2}], top_senders with handled_count — matches SQL counts. |
| E214 LMS dead end: /dashboard/lms empty state had no Create action | **FIXED-UNCOMMITTED** | `frontend/app/dashboard/lms/page.tsx`: "Create Course" button (header + empty state) opens a dialog POSTing the real `POST /lms/courses` (title required client-side; description, optional class from `/academics/classes`, draft/published); success invalidates `["lms-courses"]`; page already had loading/error states | Live ✅: POST /lms/courses as demo admin → 201 id 06b187e0…; GET /lms/courses lists it ("E214 Verify Course", draft) — renamed in DB to "Class 10 Science — Term 1" and left as real demo content. |
| E215 Middleware hourly-logout (P1): access JWT 1h; hard nav to /dashboard bounced to /login despite the 30-day refresh cookie | **FIXED-UNCOMMITTED** | `frontend/middleware.ts`: "no session" for ROUTING = no usable access token AND no refresh cookie (`hasNoSession = (!token || expired) && !hasRefreshCookie`). With a refresh cookie present the nav is allowed through and the client recovers (lib/api.ts single-flight POST /auth/refresh on first 401, redirect only if refresh fails). Refresh presence is "maybe authenticated" for routing ONLY — `/super-admin` guard unchanged (still requires a VALID unexpired access JWT with role=superadmin) and every API route remains server-verified (signature + blocklist) | Live ✅: hand-built EXPIRED access JWT → GET /dashboard 307→/login with NO refresh cookie; GET /dashboard **200** with expired access + refresh cookie; GET /dashboard/communications/whatsapp/conversations 200 likewise; GET /super-admin with expired access + refresh cookie still **307→/login** (guard not weakened); no cookies → 307. POST /auth/refresh with the cookie jar → 200 new access token (rotation works). |
| E216 Timetable Monday P8 malformed slot ("Subject" + empty) | **FIXED-UNCOMMITTED** | Data: demo row `68d853ae…` (Class 10, Monday P8) had subject_id/teacher_id/times all NULL → renderer showed the literal placeholder. Backfilled subject=Demo-school Mathematics (0b2f905f…), teacher=Sita Sharma, 15:10–15:30 (mirrors the Class 7 P8 slot). Renderer: `timetable/page.tsx` now shows "Unassigned" instead of the misleading "Subject" when a slot has no resolvable subject | Live ✅: GET /timetable?class_id=209ee501… → Monday P8 subject_name "Mathematics", teacher "Sita Sharma", 15:10-15:30; any future unresolvable slot renders "Unassigned". |
| E217 Exam dates rendered raw ISO-BS "2082-06-15 to 2082-06-20" | **FIXED-UNCOMMITTED** | `exams/page.tsx displayExamDate`: stored BS strings go through `formatNepaliDate` ("15 Ashwin 2082" — the same long-form as attendance's "14 Bhadra 2083"); AD-only dates convert via `displayBS`; empty → "—" | Live ✅: demo "First Terminal Exam 2082" (start_date_bs 2082-06-15) renders "15 Ashwin 2082 to 20 Ashwin 2082" (verified formatter output + page 200). |
| E218 demo Class 10 showed "Grade -" | **FIXED-UNCOMMITTED** | Data-only: `classes.numeric_grade` was NULL on the legacy "Class 10" row → UPDATE to 10 (matching the other classes 5/6/7/8) | Live ✅: GET /academics/classes → [('Class 10', 10), ('Class 5', 5), ('Class 6', 6), ('Class 7', 7), ('Class 8', 8)]. |
| E219 Academic year form allowed no dates; seeded 2082 had N/A | **FIXED-UNCOMMITTED** | `academics/page.tsx` AcademicYearsTab: Start/End now REQUIRED (labels "Start Date *"/"End Date *"; submission blocked with a toast until both BS dates are picked; edit dialog seeds from the stored dates; labels + helper note). Data backfill: 2082 → start 2082-01-01 BS (2025-04-14 AD), end 2082-12-30 BS (2026-04-13 AD) — verified against `lib/nepali_date.ts` (bsToAD/adToBS round-trip both directions) | Live ✅: GET /academics/years → 2082 {start_date 2025-04-14, end_date 2026-04-13, start_date_bs 2082-01-01, end_date_bs 2082-12-30, is_current true}; conversions checked in-container node: 2025-04-14 AD = 2082-01-01 BS, 2026-04-13 AD = 2082-12-30 BS. |

Batch verification: `pytest tests/ -q -k "whatsapp or lms or website or academic"` in aschool-flask-1 = **60 passed, 943 deselected**; `tsc --noEmit` (in aschool-nextjs-1) = only the 4 documented pre-existing errors (sidebar.tsx ×1, sanitize.ts ×3), 0 in touched files; all four WhatsApp pages + /dashboard/lms + /dashboard/exams + /dashboard/timetable + /dashboard/academics return 200 on the live Next server; no git commits.
