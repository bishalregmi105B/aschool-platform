# R3 TREE VERIFICATION — 2026-09-05
_Verified against working tree at HEAD `b25e901` + uncommitted changes. Read-only pass; no code modified._
_Ledger commits `461dde5` (B1+B3) and `c4b09af` (C-P1/P2) verified as ancestors of HEAD._

## B1 closes (roadmap §5 B1)

1. **Legacy AI gates canonicalized — ✅ DONE.** Grep of `@plugin_required(...)` across backend/app: 0 occurrences of `ai_tools` / `ai_adaptive_learning` gates; 52× `@plugin_required("ai_suite")` (largest single gate population). Alias table `PLUGIN_SLUG_ALIASES` at `backend/app/plugins/decorators.py:13` maps ai_tools/ai_tutor/ai_adaptive_learning/ai_insights/benchmarking/advanced_analytics → ai_suite; in-flight diff additionally merges each manifest's `aliases:` via `PluginLoader.alias_map()` (decorators.py:60-90). Test: roadmap asked for `test_ai_gate_aliases.py` — it does NOT exist; coverage landed as `tests/test_w0_close.py` (gate census + alias bidirectionality) + `tests/test_plugin_aliases.py` instead.
2. **Row migration + manifest deletion — ✅ DONE.** `ai_adaptive_learning/manifest.yaml` deleted (dir holds only `__init__.py`, `hooks.py`, `routes.py` — routes kept). Row migration `b9f3a6c1d7e2` ("W0-close: migrate legacy plugin rows to canonical slugs") in `backend/migrations/versions/`.
3. **Catalog endpoint + frontend mirror — ⚠️ PARTIAL (deviation, intent met).** NO `GET /plugins/catalog` route exists in `backend/app/api/v1/plugins.py` (routes: marketplace, sidebar, installed, install, trial, subscribe, uninstall, activate, deactivate, config GET/PUT, refresh-registry, config-schema, **/widgets**, **/aliases**, **/registry**, /health). Catalog intent served by `/registry` (plugins.py:992) + `/aliases` (plugins.py:962) + `/widgets` (plugins.py:927). Frontend: hardcoded mirror removed from `frontend/lib/plugins.tsx` (file rewritten, now consumes `lib/plugin-aliases.ts` which hydrates from `GET /plugins/aliases`, keeping literal FALLBACK_PLUGIN_ALIASES as offline fallback — plugin-aliases.ts:19-37).
4. **Dead services/ai deletion — ✅ DONE (with 2 flagged exceptions).** Commit 461dde5 deleted the 10: admission_bot, attendance_ai, content_gen, fee_predictor, plagiarism, report_remarks, sentiment, social_ai, translator, wellbeing_ai. `social_ads/` folder gone. `basic_reports/manifest.yaml` pointer fixed (subitems now only exam/expense/teacher real pages, E236 comment at line 26). Remaining 20 modules all have importers EXCEPT `benchmarking_ai.py` and `risk_detector.py` (0 app importers; referenced only in `tests/test_ai_token_hub.py:54-55` importlib smoke list). 18 genuinely live.
5. **Guardian PATCH/DELETE — ✅ DONE.** `backend/app/api/v1/students.py:980` PATCH `/<student_id>/guardians/<guardian_id>`, `:1021` DELETE same path (soft delete + primary-exclusivity); tests in `tests/test_w0_close.py`.
6. **health_profiles merge — ✅ DONE.** `backend/app/models/health.py` deleted (ls: No such file); 0 refs to `student_health_records` outside validator docstring; migration `c7d2e9f4a8b3` ("W0 dedup: retire the duplicate student_health_records table"). `owns_tables`: only `ai_teacher` and `nepal_curriculum` manifests declare it — **no manifest owns `health_profiles`** (validator.py:552 notes exactly this gap) → ownership half of the item is ⚠️.
7. **log_usage wired — ✅ DONE.** `backend/app/plugins/billing.py:172-173` calls `log_usage(school_id, plugin_slug, "install")` inside install_plugin; `PluginUsageLog` model at `app/models/plugin.py:114-117` (`plugin_usage_logs` table).

## B2 widgets (roadmap §5 B2)

8. **Tier-1 widget components — ⚠️ PARTIAL (9.5 of 12).** `frontend/components/ui/`: data-table.tsx ✅ (495 ln; server sort via `onSortChange` prop data-table.tsx:72,162,193; bulk action bar; CSV export :216-223; pagination; search), confirm-dialog.tsx ✅ (197 ln; `undoableDelete` + undo toast via sonner :18,146-152), empty-state.tsx ✅ (also exports **ErrorState** :103 and LockedState — not a separate file), skeleton.tsx ✅, sheet.tsx ✅, page-header.tsx ✅, filter-bar.tsx ✅ (URL-synced via useRouter/useSearchParams + router.replace :48-69), pagination.tsx ✅, status-pill.tsx ✅. **MISSING: wizard, JSONSchemaForm** (no json-schema-form file; `config_schema` only mentioned in lib/plugin-widgets/types.ts). **config_schema.yaml: 7 of 41 manifests** (ai_suite, ai_teacher, attendance, fees, library_management, website_builder, whatsapp_bot) — 34 lack one, including top plugins exams, lms, hr_payroll, design_studio, notices, assignments, gps_tracking, nepal_curriculum, iemis_importer, admission, gamification… far short of "top-20 have one".
9. **widgets.yaml support — ✅ DONE (backend + 3 plugins).** `backend/app/plugins/widgets.py` (217 ln): parse `widgets.yaml` honoring `ui.widgets_ref`, normalize shorthand slots/sizes, cache; `widgets_for()` server-side gating (installed+active alias-aware, requires_plugins, surface, slot, role, permissions, order) + `default_layout()`; served by `GET /plugins/widgets` (plugins.py:927). Plugins shipping widgets.yaml (untracked, in-flight): attendance (7 widgets), exams (7), fees (9). `backend/tests/test_plugin_widgets.py` exists (185 ln, 17 tests). `exams/ui/` = component-token registry dir with `index.web.json` mapping `marks_entry_grid → exams/MarksGrid`; frontend registry component exists at `frontend/components/plugin-widgets/components/MarksGridWidget.tsx`, consumed by `frontend/app/dashboard/page.tsx` via `frontend/lib/plugin-widgets/usePluginWidgets.ts`.
10. **validator + plugin_doctor + tests — ✅ DONE.** `backend/app/plugins/validator.py` (608 ln): checks identity, relations (deps/conflicts), pointers (api_blueprint/models_module/services/tasks resolve), declared sidecar files, nav (route/icon/labels), widgets legality, cross-plugin ownership (check_ownership :547). `backend/scripts/plugin_doctor.py` exists (153 ln; --errors/--json/--fix-safe) and RUNS: **"All manifests pass the plugin contract. 49 manifests · 0 errors · 0 warnings"**. Tests: `pytest tests/test_plugin_contract.py tests/test_plugin_widgets.py` = **25 passed, 2 errors** — the 2 endpoint tests (`TestWidgetsEndpoint::test_requires_auth`, `test_returns_only_installed_widgets`) error only on DB connect (project Postgres on 5435 is DOWN; 5432 answers but aschool auth fails; config.py:224-231 → TEST_DATABASE_URL or aschool_test on DATABASE_URL host).

## B3 portals (roadmap §5 B3)

11. **Mock /student gone + 18 real routes — ✅ DONE.** `frontend/app/student/page.tsx` is now a real data page (useQuery on real API, TodayClass/PendingHomework types, ErrorState import). Dedicated real pages with useQuery: student results/timetable/library/lms/ai-tutor/elibrary; parent attendance/results/fees/chat/bus/notices/health/wellbeing/conferences (+parent/page.tsx). Re-exports: teacher attendance → `../../dashboard/attendance/page`, marks → `dashboard/exams/marks/page`, timetable → `dashboard/timetable/teacher/page`, notices → `dashboard/notices/page`, plus teacher/ai-tools page. Zero `PortalSectionPage`/coming-soon remnants in app/ (only unrelated `coming_soon` plugin-flag refs in marketplace). `frontend/__tests__/portal-route-meta.test.ts` deleted — **nothing replaced it** (no portal route test in `__tests__/`, which holds 8 other test files).
12. **Leave-requests approval UI — ❌ NOT DONE.** Grep across frontend app/components/lib for `leave-requests|leave_requests|LeaveRequest|approve`: only `/hr/leaves` staff-leave UI (app/dashboard/hr/leaves/report/page.tsx:48). No student-leave approval surface for teachers anywhere. (D5 blocker still open.)

## Phase C — AI Teacher

13. **P1 content spine — ✅ DONE.** `backend/app/models/teaching_content.py` (696 ln) = exactly 12 tables: teaching_sections, section_versions, section_outcomes, notes, examples, misconceptions, formulas, exam_tips, key_terms, media, content_snapshots, content_reviews (line numbers 42-665). Publish workflow: `TeachingSectionVersion.status` draft/review/published (models/teaching_content.py:157), immutable published rows, edit clones version_no+1 (:140), exactly-one-published partial unique index `uq_tsv_one_published` (:13-14), reviewed_by_id (:171). Override chain = school version over platform via versions table (migration `d4e7f1a9c2b8`).
14. **P2 runtime service — ⚠️ PARTIAL (backend plumbing only).** NO `services/ai_teacher/` or `backend/services/ai_teacher/` standalone Flask app (find: only plugin module + frontend page). `backend/app/realtime.py` still 159 ln, 0 `lesson` refs — no lesson rooms. `token_hub.py` has reserve/reconcile (`_reserve_cost` :380, `reconcile_quota_reservation` :352) but **no `stream=True`** (0 hits). Runtime tables exist (migration `e5a8c2d7f3b1` "Phase C P2: AI Teacher runtime tables"). Cost: host-side estimate + ceiling in routes.py:196-204,323-328 — no two-sided service-side accounting. analyzer→planner: nothing wired beyond generic tool_handlers.
15. **P3 web client — ⚠️ v1 only.** `frontend/app/dashboard/ai-teacher/page.tsx` = launcher (sections list, lesson create, history, cost) — `player_url` links out-of-app (:189-195 "player URL appears once the school finishes service setup"). **NO BoardCanvas / CaptionStream / lesson player component.** No Flutter LessonPlayer (0 hits in 5 apps).
16. **P4 guardrails — ⚠️ PARTIAL.** In `app/plugins/modules/ai_teacher/routes.py`: kill switch (`_kill_switch_on` :42-48, checked :216), explicit GuardianAIConsent assert (:254-262), injection scan `detect_injection` on topic, hours window, budget reserve before service call. No eval set of 20 golden lessons: `tests/test_ai_teacher_plugin.py` has 12 tests, 0 "golden" refs, not wired to CI as eval.
17. **P5 lesson PDF export — ❌ NOT DONE.** No pdf/weasy refs in the ai_teacher plugin; no Flutter player.

## Phase D

18. **D-1 nepal_curriculum — ⚠️ PARTIAL.** Manifest `backend/app/plugins/modules/nepal_curriculum/manifest.yaml`: starter plugin NPR 199, `owns_tables:` (line 54) = the 12 teaching_* tables, depends_on academics — but **no `schema_version: 2`** (v1). NO +11 columns migration for `question_bank_items` (only base `a3c8e2f5b7d9_a03_question_bank_tables.py`; no nepal enum/col migration). NO admin entry UI / exercise bank browser / chapter reader in frontend (only dir hit is ai-teacher page); no frontend routes for nepal-curriculum/teaching-content; no Flutter chapter reader.
19. **D-2 catalog + templates — ⚠️ PARTIAL.** Migration `f6b2d8e4c1a9` ("Phase D-2: AI tool catalog columns on ai_tool_registry") adds 10 columns (trigger_phrases, budget, failure_modes, reference_pack, output_document_type, ui_type, icon, sort_order, badge, input_schema_name — roadmap said 9). `workbench_seed.py` TOOLS = **24 entries** (24 tool_key) incl. doc_sections/item_analysis tools (seed lines 265-290). Result templates: 12 distinct `output_schema_name` values (27 usages) — short of 15. **Web AI hub does NOT drive from ui_type** (0 `ui_type` refs in frontend/).
20. **D-3 learning science — ❌ NOT DONE.** No `spaced_rep_cards` / `student_xp_state` / `live_quiz_sessions` in models or migrations (0 hits). SM-2 fields exist only inside ai_teacher mastery model (`app/models/ai_teacher.py:254` ease_factor) — no shared SM-2 service, no gamification screen fill, no adaptive path.
21. **D-4 voice + documents — ⚠️ PARTIAL.** No edge-tts service (0 hits in app/services + requirements). No STT proxy (only unrelated ai_capture). Deck emitter FIXED: `frontend/lib/hooks/useExport.ts:310-317` — `pptx.defineLayout({name:"PAGE",...}); pptx.layout = "PAGE"` (commit b25e901 verified in tree). DOCX writer exists: `backend/app/services/writer_docx.py` (python-docx). **AiResultView used on 4 pages**: ai-tools letter-writer, question-paper, lesson-plan, ai-workbench (`frontend/components/ai/ai-result-view.tsx`).
22. **D-5 elibrary — ❌ NOT DONE.** `OERResource` (app/models/digital_content.py:51-62) has NO licence/attribution/Sketchfab columns (+6 columns missing); no PDF chat/RAG endpoint (0 hits); no model annotator tool.

## Phase E — Nepal modules (one-liners)

① Transcripts + grading scales — ❌ NOT DONE (0 `transcript`/`GradingScale` in api/models).
② Payroll TDS/SSF — ⚠️ PARTIAL: `app/models/hr_payroll.py:16` JSONB deductions {pf, tax, ssf}; no TDS computation.
③ eSewa/FonePay + refunds — ⚠️ PARTIAL: webhook infra (app/api/webhooks/), FeePaymentRefund model `app/models/fee.py:130` (S-13), 'refunded' status f8c2a9d4e1b7; no bank reconciliation.
④ Marksheet print designer — ⚠️ PARTIAL: designer plugin + `app/templates/designer/report_card_writer`; no dedicated print-designer UI for marksheets.
⑤ Transport-fee billing + driver manifest — ❌ NOT DONE (0 hits).
⑥ Smart SMS 2.0 — ❌ NOT DONE (only listeners.py SMS wiring; no merged SMS+push cost receipts).
⑦ IRD billing/VAT — ❌ NOT DONE (hits are false positives in unrelated services).
⑧ Real timetable solver — ❌ STILL GREEDY STUB: `app/services/ai/timetable_solver.py:39` "Simple greedy scheduling with clash detection" (119 ln, docstring claims constraint satisfaction).
⑨ Subject-wise attendance + leave types + approval UI — ⚠️ PARTIAL: `leave_type` column (models/attendance.py:65, hr leave), but no subject-wise attendance, no student leave-requests approval UI (item 12).
⑩ Excel import/export everywhere + migration kit — ⚠️ PARTIAL: IEMIS importer (openpyxl in app/api/v1/iemis_importer.py; iemis_templates/ has real xlsx) but not "everywhere", no Veda/Paathshala/eZone adapters.
⑪ Admissions CRM kanban — ⚠️ PARTIAL: pipeline stages in app/api/v1/admission.py:248-253; NO kanban UI (0 frontend hits).
⑫ Re-evaluation — ❌ NOT DONE (0 hits).
⑬ ZKTeco/RFID — ⚠️ PARTIAL: biometric plugin with ZKTeco device model (app/models/biometric.py:1,34); no RFID/one-card events.
⑭ Canteen POS/hostel mess — ❌/⚠️: no canteen/POS anywhere; hostel API+model exist (app/api/v1/hostel.py, models/hostel.py) but no mess/maintenance features (0 hits).
⑮ Alumni + wellbeing fill-in — ⚠️ PARTIAL: alumni module has manifest only (NO routes.py); wellbeing has mood tracking + counselor notes (app/api/v1/wellbeing.py:2,6,17) but no emotion coach.
⑯ White-label pipeline — ⚠️ PARTIAL: white_label routes (overview/domain/verify, modules/white_label/routes.py:27-101); no app build pipeline.

## Phase F + infra

- FCM deep links — ❌ NOT DONE: no `google-services.json` in any Flutter app; push_notifications.py has no deep-link refs.
- .arb files — ❌ 0 across all 6 Flutter projects.
- Offline outbox — ❌ NOT DONE (0 hits in flutter_user/student/shared lib).
- Pricing page — ⚠️ PARTIAL: pricing section on public landing `frontend/app/page.tsx:90-127` (NPR 0/399/999 — our tiers, not competitors); no /pricing route; frontend/app/school/[slug] is the school public site.
- Dark-mode tokens — ✅ present: `.dark` block with full token set `frontend/app/globals.css:47+` + `darkMode: ["class"]` tailwind.config.js:3.
- deploy.yml — ✅ ordering CORRECT: backend-test → frontend-test → flutter-test → build-push (needs [backend-test, frontend-test]) → deploy (needs build-push); `.github/workflows/deploy.yml:140,176`.
- Observability — ❌ NOT DONE: no /metrics, no prometheus, no structlog/JSON logs (0 hits).
- TIMESTAMPTZ migration — ✅ EXISTS: `b2e7c4a9f1d3` "D-03 completion: convert remaining naive TIMESTAMP columns to TIMESTAMPTZ".
- nginx real-IP — ✅ EXISTS: `nginx/nginx.conf:16-19` real_ip_header CF-Connecting-IP + Cloudflare set_real_ip_from ranges; host conf sets X-Real-IP/X-Forwarded-For.

## Migration chain

`alembic -c migrations/alembic.ini heads` → **SINGLE head: `a7c3e9f1d4b8`** ("Platform RAG chunks: allow school_id NULL"). Newest 4: a7c3e9f1d4b8 ← f6b2d8e4c1a9 (D-2 catalog cols) ← e5a8c2d7f3b1 (C-P2 runtime tables) ← d4e7f1a9c2b8 (C-P1 teaching spine). No multiple heads.

## Tests

- `pytest --collect-only -q` → **606 tests collected** (61 test files).
- Plugin contract + widgets: **25 passed / 2 errors (DB connect only)**. w0_close + plugin_aliases: 4 passed / 5 errors (all DB connect: 5435 down, 5432 auth-fails for aschool). All DB-independent tests green.

## In-flight changes (uncommitted) — one paragraph

The uncommitted work is the plugin v2 contract + widget system plus a gate/role hygiene fix, not IEMIS features: `backend/app/plugins/loader.py` gains `_normalize_manifest` (v1→v2 in-memory adapter: `frontend:`→`ui.nav`, `flutter:`→`mobile`, pointer keys→`capabilities.*`, schema_version int-coercion, ~100 added lines) while `decorators.py` merges manifest-declared `aliases:` into `_acceptable_plugin_slugs` via a new `PluginLoader.alias_map()` (falling back to the hardcoded table on boot-time import cycles); `backend/app/plugins/widgets.py` (new) implements the widgets.yaml contract (parse/normalize/slot-order/server-side gating) served by the new `GET /plugins/widgets` route in `plugins.py`, with `validator.py` (new, 608 ln) enforcing the whole manifest contract (identity/pointers/nav/widgets/cross-plugin ownership), `scripts/plugin_doctor.py` (new) as its CLI (currently "49 manifests · 0 errors · 0 warnings"), and `tests/test_plugin_contract.py` + `test_plugin_widgets.py` (new) as CI gates; the first three data plugins ship `attendance/exams/fees widgets.yaml` (7/7/9 widgets) plus `exams/ui/index.web.json` registering the `marks_entry_grid` component token rendered by the new `frontend/components/plugin-widgets/MarksGridWidget.tsx` on the dashboard via `lib/plugin-widgets/usePluginWidgets.ts` (the frontend `frontend/tmp/aschool-next-dist` and tsconfig/NEXT_DIST_DIR edits are just build-isolation hygiene from 461dde5, and `portal-route-meta.test.ts` was retired with the coming-soon portals); the four `backend/app/prompts/doc_sections_{en,ne}.md` + `item_analysis_{en,ne}.md` files are the per-tool system-prompt files auto-written by `workbench_seed.py:558` for the 13 new D-2 catalog tools; the remaining diffs are a security fix — `iemis_importer.py` `/validate` + `/import` drop the phantom `data_entry` role (not a user_role enum value per `app/models/user.py:26-38`, so it granted nobody and only widened attack surface on paper) with the matching `visible_to` change in `iemis_importer/manifest.yaml`, plus an ai_redteam known-failure-modes note.
