# R2 — CORPUS DEMANDS (consolidated outstanding-work digest)
Date: 2026-09-05 · Full read of `UNIFIED_ROADMAP_2026-09.md` (v3, plan of record), `_digest/D1–D5`, `R1`, `R2_CORPUS_MASTER_LEDGER.md`, `W3/A1,A1a,A1b,A1c,A2`, the 13 reports of 2026-09-04, `audits/IMPLEMENTATION_AUDIT_2026-09-04.md` (IA), the 2026-08-30 wave, `deep2026/00`, and `docs/MASTER_PLAN_2026-09.md` (MP). Nothing verified against the tree; a separate verifier owns ground truth.

Citation shorthand: `v3` = UNIFIED_ROADMAP · `D1/D2/D3/D4/D5` · `R1` · `A1/A1a/A1b/A1c/A2` (W3) · `AT` = ATEACHER_PLUGIN_INTEGRATION · `ATIB` = ATEACHER_INTEGRATION_BLUEPRINT · `PTTA` = PLUGIN_THEME_TEMPLATE_ARCHITECTURE · `TC` = AI_TOOL_CATALOG_AND_SKILLS · `PUSA` = PLUGIN_UI_SPEC_ALL · `ASH` = ASHLYA_AI_DEEPDIVE · `UX/SI/BC` = Sahayatri reports · `WUI/WWA/FWA/MAI/PDA/BCA/CS/CLN` = 2026-09-04 audits · `MP` · `IA` · `RB` = REMAINING_BACKLOG_2026-08-30 · `MPA` = MASTER_PRODUCTION_AUDIT · `deep00` · `SI*` = SAHAYATRI_SPEC_INVENTORY.

**Corpus-integrity finding up front:** `R1` is INCOMPLETE — it ends at `<!--APPEND-A-->` (R1:1003) and its promised §B contradictions, §C and §D verifier lists never exist; the open-work content lives inside its §A precis annotations. `R2_CORPUS_MASTER_LEDGER.md` covers only §0 (corpus map) and stops at line 143. `PUSA` delivers only 4 of 39 promised plugin specs (PUSA:5, ends `<!--APPEND-->` at :395). `AI_TEACHER_INTEGRATION_BLUEPRINT.md` (53 ln) is truncated at §1.2 — anything citing it for protocol/DDL is citing nothing (R1 §A1); the complete file is `ATEACHER_INTEGRATION_BLUEPRINT.md` (one-word filename difference).

---

## §1 LOCKED DECISIONS & ANTI-GOALS (cite file+section)

1. AI Teacher = separate premium plugin `ai_teacher` (NPR 1499/14990), separate service topology with its own DB+Redis; NEVER aliased into ai_suite (`min_plan_tier` is a literal ai_suite membership check, so an alias would unlock it for free) (v3 §3.1; D1 §A.1, §A.10, D-6).
2. No AI whiteboard; every whiteboard-dependent capability SKIPped with named equivalents; solvers survive as workbench tools (v3 §3.2; D3 §A.2 — 7 whole + 3 partial capabilities die, D3 §A.7).
3. No OCR/vision ingestion; all teaching content admin-entered; D1 §B 12-table `teaching_*` schema is the data model; unauthored chapter ⇒ 422 refusal, never hallucination (v3 §3.3; D1 §B.1; D3 constraint 3).
4. Sahayatri lands as ONE new starter plugin `nepal_curriculum` (NPR 199, readable without AI) + extensions to ai_suite / ai_adaptive_learning / gamification / exams / elibrary / wellbeing; only 4 genuinely new tables beyond D1's 12: `spaced_rep_cards`, `student_xp_state`, `live_quiz_sessions`, `exercise_section_links` (v3 §3.4; D3 §B.1, §C.0).
5. Plugin/theme/widget/config v2 with `_normalize_manifest` adapter + `schema_version: 2` ratchet (warnings→errors); public-site parity lock (byte-equal theme CSS + 3-school×3-viewport Playwright diff at threshold 0) exists BEFORE any theme code changes (v3 §3.5; D2 §D.4; PTTA §4.4).
6. Edge-TTS default voice with per-school queue/cache; Whisper STT via token_hub; every AI call incl. TTS/STT metered; 402 semantics (v3 §3.6; D3 §D.5b, A.4 #35–36).
7. Web pricing page publishes OUR tiers only; never competitor prices (v3 §3.7; D4 §A — Veda's own 1300-vs-900 count contradiction is the reason).
8. flutter_user is THE consumer app; flutter_admin stays; stop publishing standalone student/parent/teacher binaries (v3 §3.8; MAI §3).
9. Every widget = data + renderer token; plugins never ship code the host evals; slots are host-owned closed set; module federation REJECTED (D2 §B, §B.3).
10. One dispatcher (`POST /ai/generate/<tool_key>`), one route per tool family: a tool may add a result template, never a route/page (D3 §D.3; D1 §C.1).
11. Anti-goals (v3 §11): no whiteboard; no OCR; no AI outside workbench guardrails; no Sahayatri auth/subscription internals, Redis-only persistence, `run_text_tool()` catch-all, un-gated voice tutor, SSRF-y PDF fetch; no ATeacher session service, in-memory room state, GET-with-text TTS, dead 1,400 lines; never publish claimed competitor pricing; live public-site design does not change.
12. Review rule: any PR adding prompt text or board-grammar parsing to ASchool has crossed the host/service boundary (D1 §A.1; AT §E.2 risk 11).
13. Writing tools must never return rewritten student text — CI gate (e): no `revised_text` field at type level (MP §13.8; D3 §E.16).

---

## §2 CONSOLIDATED OPEN-WORK BACKLOG

Items marked **[DONE per ledger]** are superseded — do not re-do (v3 §1/§4 verify them in-tree): absent-alert listener `listeners.py:69`; leave write-through + `rejection_reason` (`d5c8f2a7b4e1`); benchmarking `/rankings` rewrite; curriculum/NEB read API `academics.py:983+`; social_ads+social_hub deletion; 7 deprecated AI manifests; ai_suite `__init__.py`+config_schema; grant_plan_plugins coming_soon skip (`entitlements.py:63-90`); loader pointer validator + plugin-contract validator (0 errors/1 warning, 48 manifests); Sparrow HTTPS; WeasyPrint+python-docx in requirements; `scripts/migrate.py`; AW-06 tutor red-team PASS (13 cases, `backend/audits/ai_redteam/known_failure_modes.md` — CS §4); `student_health_records`→`health_profiles` merge (migration `c7d2e9f4a8b3` — A1c §4.1; v3 ledger marks it inside done B1 `461dde5`); device rate limit; P-03 deploy ordering (CS §3 marks ✅ — but IA says ❌, see §3); frontend error boundaries (IA P-06 ✅).

### (a) AI Teacher track (phase C remainders)
- **C-P2 separate runtime service** — Flask+SocketIO in `services/ai_teacher/`, own DB (restricted role) + Redis room state; per-school credential provisioning (key_id + sha256 secret, tenant-provision endpoint, 24 h dual-accept rotation); S2S key+HMAC; session brokering per D1 §A.2–A.3 (25 endpoints; gates-before-spend ordering; ASchool UUID canonical; `content_snapshot_id` = version id); JWT player token ≤15 min, single-use `jti`, fragment-only delivery (`?token=` banned); socket `lesson:{id}` rooms are NEW code (`realtime.py` is 159 lines, school rooms only — D1 D-8); analyzer→planner wired + slide guidance wired (ATIB's dead code, D2 §G.8); board-state Redis (no in-process dicts); stream=True in token_hub + quota reserve/reconcile; two-sided cost ledger (`cost_source="estimated"` honesty rule); Celery `reconcile_lessons`/`purge_transcripts`/`rollup_usage`/`compute_due_reviews`/`export_lesson_pdf` (D1 §A.2). Defect to fix at build time: plugin tasks are plain functions, never registered — loader only validates paths (A1 §7.1 T-20).
- **C-P3 in-app player** — web `BoardCanvas` DOM+SVG hybrid (per-char reveal, KaTeX, stroke-dashoffset tracing), CaptionStream, barge-in (voice interrupt detector exists in Ashlya but dead — wire it), mastery panel; server→client event vocabulary + `seq` ack per D1 §A.4/D2 §G.4; step queue pairs ≤260-char speech with commands, next step only after BOTH finish (D2 §G.5). Board grammar ported verbatim: WRITE/DRAW_SVG/NEXT_SLIDE/PAUSE/REMOVE/ANNOTATE, y-cursor (WRITE height 8, gap 2, y=85 → NEXT_SLIDE), viewBox conventions, 10-colour semantics, 14-step `repair_svg`, bracket-holdback stream parser, 5 personas as DB rows with 4 editable prompt columns (D2 §G.1–G.3; prompt-asset table G.3). Do NOT port: `get_active_session` fallback, unauth restore_session, MCPQ target_id bug, `chemistgpt.txt`/`physicsgpt.txt`, "question = INCORRECT_CARELESS" mastery heuristic (D1 §A.14; D2 §G.8).
- **C-P4 guardrails** — explicit consent assert in the plugin create gate (don't rely on registry category string, D1 D-7); injection + moderation on every student turn AND teacher speech before it reaches the student; `detect_injection` at content publish time; pseudonymization (no email crosses boundary); per-lesson cost estimate pre-start; kill switch via plugin deactivate ends live lessons; under-13 consent not disableable; eval set of 20 golden lessons in CI (D1 §A.7, §A.14.1; v3 §6 P4).
- **C-P5 apps** — flutter_student LessonPlayer (WebView + 4 platform hacks, snapshot cache for offline replay), flutter_teacher LiveLessonsMonitor/ClassMasteryGrid, parent ChildLessonsList (summaries only), lesson PDF export (D1 §A.13).
- **153-tool catalog** — 13 seeded (D-2 `9de7cfe`); remaining ~103 NEW + 26 PART; migrate 11 unmounted services + bespoke `ai_tools.py`/`design_studio.py` (7 duplicate `/ai/*` routes, D1 D-9) into registry; seed waves 0–4 per D3 §D.4 (12 ga → ~35 beta → shape-differentiated → document-gated disabled); EN+NE prompt files + nutrition rows + failure_modes + budget per row (D1 §C.15 top-25 ranking; §C.16 claude-skills conventions: trigger_phrases, reference packs, gold sets).
- **Result templates** — 4 NEW (`table_grid`, `slide_deck`, `chart_panel`, `checklist`) per D1 §C.1; D3 §E's 20-template union is the build list; `follow_up_suggestions` in every schema.
- **Emitters** (D-4 remainder) — new `app/services/ai/document_emitters.py`; pipeline step 6.6; registry `output_document_type`; deck dialect (10 typed slides, 1280×720, notes≠slide text) rendered through the designer canvas — NO python-pptx; fix `exportPPTX` native objects + `defineLayout` once (`useExport.ts:314` — D1 D-10); writer block library (`question_block`, `section_header`, `checkbox_list`, `image`, `page_break`, `two_column_flow`, `answer_space`); multi-page Page n of m/[P.T.O.]; `chart_block` server SVG; Nepali typography pack; "Open in Writer/Designer/Deck" bridge (D1 §C.17.2–C.17.4, 12 ranked upgrades).
- **Registry columns** — D1 D-5 found 0 of the new columns; v3 ledger says D-2 added 11 (`9de7cfe`) — verifier must confirm all 11 of D3 §D.1 (`trigger_phrases, budget, failure_modes, reference_pack, output_document_type, ui_type, icon, sort_order, badge, input_schema_name, grounding`).
- **Unabsorbed Ashlya patterns** (ASH §5 top-5, "only partially reflected in D1/D2, appears nowhere in v3" — R2 master ledger §0.3): task-class model router with p95 latency budgets + fallback chain; per-feature credit ledger with pre-action price; prompt-time section selection for long docs; multi-round tool-calling tutor with persisted source chips. Also #13 DB-editable prompt parts, #15 lesson-summary artifact.

### (b) nepal_curriculum / Sahayatri track (phase D remainders)
- **D-1 packaging done** (`e291f4b`) but D3's substance remains: admin entry UI screens 1–11 (`/dashboard/curriculum/**` — §G.1 map), section editor 7 block tabs field-by-field (§G.3), publish gate 9 conditions (§G.5), review queue, fork/override with re-adopt, student preview with "what the AI sees" toggle, coverage dashboard (§G.8), flutter_admin read+approve subset (authoring web-only, §G.9).
- **Bulk entry** — 8 CSV templates + paste-TSV, dry-run-then-commit, rows land as drafts on new versions, import audit rows (D3 §G.6); slice-2 exit: 200-row import + coverage 11/12 (D3 §H).
- **Exercise bank** — widen `question_bank_type` enum +8 Nepal values (own transaction, append-only — R2 one-way-door risk), `question_source` +2; +11 columns on `question_bank_items` (§C.1 migration SQL verbatim); `exercise_section_links` join table; `content_sha256` exact dedupe + Jaccard 0.72 warn; browser + entry form with `exercise_heuristics` Nepali-verb autofill (§G.7).
- **Grounding** — `context_chapter` builder (§D.5) + `tool.grounding` enforcement (422 on unauthored+required); `used_chapter_context` + `context_ref{version_id,sha256}` in response; `ai_generations.citations` naming exact snapshot bytes; grounding config group (excerpt_chars 1500/tutor 4000, include_exam_tips/misconceptions, unauthored_behaviour).
- **Catalog surface** — dynamic route `/dashboard/ai-workbench/[toolKey]` with GeneratedToolForm + RESULT_TEMPLATES map; `GET /ai/tools` gains `role/category/q`; slice-6 exit test: exactly two route files under ai-workbench (D3 §D.3, §H).
- **D-3 learning loop** — `spaced_rep_cards` (SM-2 verbatim, FK students.id, seed only approved questions joined via links); `GET /adaptive/due|review|stats`; per-section ease → `mastery_records` (+section_id, avg_ease_factor, reason template-filled, uniqueness swap §C.7); `student_xp_state` + LEVEL_XP/XP_TABLE constants + streak logic + 6 Badges; XP events via `points_logs` category='academic_ai' (D3 §C.4–C.5, §H slice 4).
- **Live quiz** — `live_quiz_sessions` (6-char codes A-Z2-9, recycled, unique among live only); socket module reusing `realtime.connect` + `_sessions` accessors; rooms keyed by session UUID with school_id embedded, host sub-room; client `is_correct`/`score` discarded; 1 answer per (participant,question) HSETNX; Redis room+sorted-set+code keys, TTL 4h, ended write-through; `lq_state` late-joiner resync; 6 HTTP companions; **deployment gate: Socket.IO Redis message_queue** (D3 §F.1–F.8).
- **D-4 voice** — edge-tts service (ne-NP Sagar/Hemkala, expressive presets, LRU 128, per-school queue, bounded semaphore → 503+Retry-After; true streaming POST not the b"".join flaw), STT `transcribe()` via token_hub; both metered `tts:speak`/`stt:transcribe` (D2 §G.7; D3 A.4 #35–36; config §B.6 voice group).
- **D-5 elibrary** — `oer_resources` +11 columns (title_nepali, provider, provider_uid, licence, attribution, annotations JSONB, section_id, view_count…); curated Sketchfab seed with licence always visible; `model_annotator` tool (node NAMES + context, never images); model-linked quizzes; oEmbed refresh (D3 §C.6, §H slice 6); PDF chat via existing RAG (SSRF allow-list, BC §7); research agent with domain allow-lists.
- **Owner decisions D3 §J** — ① who authors platform content (else fork/override is dead weight); ② nepal_curriculum NPR 199 vs free; ③ platform-authored questions require nullable `question_bank_items.school_id` — tenancy-audit decision BEFORE slice 1; ④ guardian-consent default (strict vs bulk vs no-student-data subset) — R4: every student 403s until consent, catalog must render locked-with-explanation; ⑤ TTS/STT v1 scope; ⑥ live quiz before/after catalog.

### (c) Plugin/widget/config v2 track (B2 remainders)
- **Tier-1 widgets** (v3 §5 B2; WWA §5 Tier-1): `DataTable` (server sort/filter/paginate/bulk/CSV, L), `ConfirmDialog`+5s undo toast (22 `window.confirm` sites), `EmptyState`, `ErrorState`, `Skeleton`+TableSkeleton, `PageHeader`, `FilterBar` (URL-synced+saved views), `Pagination`, `StatusPill`, `Sheet` drawer. Tier-2: CommandPalette (cmdk), Wizard/Stepper, DateRangePicker (BS+AD), TagPicker, KanbanBoard, HeatmapCalendar, MarkdownRenderer+KaTeX, FileDropzone, Currency/Phone/Percentage inputs, Kbd+useHotkeys. Tier-3: TimetableGrid, MarksGrid, ChartKit, ApprovalQueue, PrintFrame, QRCode, VirtualList, MapView (WWA §5).
- **JSONSchemaForm v2 = FormRenderer** — the 18 typed field rows of D2 §C.2 (string, text, int/number, boolean, enum, multi-enum, color, color-map, file, secret [never echoed, Fernet + `plugin_config_audit`], cron, time/date/bs_date, json, entity-picker [tenant-scoped], list, markdown) + attributes (visible_when/enabled_when both-sides evaluation, roles, advanced, write_only) + `validate_config` never-partial-applies + `config_migrations.py` runner (D2 §C.2–C.5). Replaces `plugins/[slug]/settings/page.tsx` bespoke 4-kind form (WWA §2.16).
- **config_schema.yaml coverage** — today only 6 of 53 ship one (attendance, fees, library_management, website_builder, whatsapp_bot, ai_tools deprecated) (WUI §4; D5 admin: "6 of 43"). v3 B2: write for top-20. PTTA Phase 1 rewrites the 5 existing + adds v2 for `notices, exams, assignments, sms_notifications, timetable`. PUSA gives literal field lists: academics 6, attendance 10 (`marking.default_status: unmarked` fixes the silent-present bug at source), exams 11, fees ~20 (incl. gateway secrets as type: secret — currently plaintext in `schools.fee_config` JSONB, A2 §2.9/G34).
- **Top-20 plugins still lacking** (all except the 6 above; priority per PUSA Part-1 build orders + D5 fix order): academics (16 orphan CRUD routes need `academics.structure` grid; per-(class,subject) teacher endpoint is a MUST-BUILD — `teacher_ids[0]` global bug, PUSA §1), attendance (leave-requests queue UI, unmarked state, keyboard 1-4/j/k/⌘S, NPT $today, `GET /attendance/me` for flutter_teacher, move holidays nav to notices, `GET /attendance/report` inside plugin), exams (MarksGrid keyboard+paste+zero-is-a-mark, single grade-scale source — 3-4 divergent NEB copies, strip `correct_answer` from `GET /exams/online/<id>` student response, report-card job status endpoint, attempts endpoint, exam-routine subject×date×time×room model), fees (payment-methods/QR admin screen — online payment unconfigurable today, `/fees/accounts` server aggregation + virtualization, structure dry-run preview, bills ledger, refund UI, keyboard POS). Deep0026/05 S1–S6 settings defects + E1–E5 event-bus defects fold in here.
- **PUSA §0 conventions are normative** for all remaining 35 plugin specs: screen archetypes L/D/C/E/W/G/B/Q/R/S, 7-part screen contract (PageHeader→FilterBar→DataTable→Sheet w/ ←/→→sticky dirty bar ⌘S→states→ConfirmDialog+undo), widget-record grammar as legal `widgets.yaml` entries. **The 35 unwritten specs are an explicit deliverable** (owner mandate: nothing "coming soon").
- **v2 platform work** (PTTA/D2 §A, §F phases 0–5 not in v3 ledger): 6 adoption waves (wave 1 six module plugins, wave 6 retire legacy flat-manifest dir); `/plugins/registry|aliases|health`; `/plugins/widgets` + `dashboard_layouts` table + dashboard edit mode; `mobile.yaml` + bootstrap `widgets[]` + SpecWidget/MOBILE_SCREEN_REGISTRY; TemplatePack unification (17 website_section packs verbatim, ~15 notification packs replacing ~15 inline f-strings in listeners.py, `template_packs` table); notification packs unlock school-editable alerts (D2 §E.6). Golden test: bootstrap visibility per 5 roles identical to current hardcoded output (PTTA Phase 5).

### (d) Nepal competitive modules (phase E ①–⑯, spec details each needs)
① **Transcripts + per-school grading scales** — A2 G15/G16: `student_transcripts` snapshot, `students.symbol_no/registration_no` (grade-sheet template references `{symbol_no}` today with no column — A2 §3.6); `grading_scales`/`grading_scale_bands` threaded through `nepal_grading`; fix B+/fail contradiction (deep00 F-08); wire dead `scheme_grades` (MP N-02). ② **Payroll TDS/SSF/PF** — A2 G10: slab table, marital threshold, SSF 11/20, PF/CIT, e-TDS export, salary certificate; `users.pan_number/ssf_number`; float→Decimal (A2 §2.2; BCA #6). ③ **eSewa/FonePay + partial refunds + bank rec** — A2 G1–G3: refund methods cash/bank/cheque, receipt **reversal** (Σreceipts still counts refunded money today, A2 §2.8), `bank_statement_*` tables, eSewa server-side `check_transaction_status` call (exists, 0 callers). ④ **Marksheet/report-card print designer** (Veda's moat, D4 §C) — G-02 Nepal templates, designer-grade UX; bulk job status (G28). ⑤ **Transport-fee billing + driver manifest** — A2 G6 `transport_fare_rules`/`student_transport_subscriptions`. ⑥ **Smart SMS 2.0** — SMS+push merge + cost receipts; UCS-2 70-char segmentation validator (D2 §E.6); MP N-08 Messenger/Viber adapters (WhatsApp isn't top-10 in Nepal). ⑦ **IRD billing polish + VAT vouchers** — A2 G11–G12: VAT register, credit notes, fiscal-year close, CEHRD fee-cap report (classifier exists, 0 callers). ⑧ **Real timetable solver + seat plans** — A2 G23/G19 + §4.4 input table (periods-per-week requirements, qualification map, availability, rooms, holidays calendar G24, pinned slots, objective weights, populated conflicts[]); solver is a greedy stub that never reads `teacher_map` and returns `conflicts: []` (BCA §1; D5). ⑨ **Subject-wise attendance + leave types + approval UI** — A2 G20 `period_attendance` (structural: day-row UQ can't extend in place), G21 `student_leave_requests`, G22 `leave_policies`/`leave_balances`; two divergent leave systems today, the one with write-through has zero UI (A2 §4.3). ⑩ **Excel import/export everywhere + migration kit** — only Excel write in backend is the IEMIS template (A2 §5.2); CLN migration plan: Veda/Paathshala/eZone/IEMIS adapters, DOB+guardian-phone dedupe, BS↔AD, parallel-run, same-day cutover; fix IEMIS importer before layering (below). ⑪ **Admissions CRM kanban** (WWA #9; CLN gap list). ⑫ **Re-evaluation workflow** — A2 G17 `exam_reevaluations` + fee + regrade/rank rerun. ⑬ **ZKTeco/RFID BYO + one-card events** — MP C-25 `student_credentials`; hardware loop never tested against live devices (CS). ⑭ **Canteen wallet/POS + hostel mess/maintenance** — A2 G7–G8 (0 canteen code exists). ⑮ **Alumni + wellbeing fill-in**. ⑯ **White-label app build pipeline** (last; MAI consolidation prerequisite).
- **A2 G-list extras v3 never picked up** (map into E or B1-like close): G5 scholarship schemes/applications; G9 fee waivers with authority trail; G13 chart of accounts/journal entries; G14 `fee_collections.fee_type_id` FK (free-text string join today); G18 marks lock/result freeze (`submit_marks` works identically after publish — A2 §3.1); G19 seat plans/admit cards as data; G25 co-scholastic grades; G26 TC/character certificate render; G27 section/stream rank (dead `rank_in_section` column); G28 report-card job status; G29 MoE Flash Report I/II (0 callers); G30 EMIS generate route never generates; G31 IEMIS export back to MoE; G32 SEE/NEB board-result import; G33 payment-initiation expiry (`status="failed"` written by nothing); G34 gateway secrets out of plaintext JSONB.
- **IEMIS importer hardening before ⑩** (A2 §6): `.xls` trap → 500; title-band headers break parsing; no-IEMIS-id rows duplicate on every re-import; class label free-text breaks promotion (NULL numeric_grade); BS/AD `_parse_dob` always returns AD=None (nepali_datetime IS installed — comment is false); placeholder `9800000xxxx` phones in the REAL NTC range and `sms_sender` doesn't filter them → SMS blast dials third parties; synthetic `.local` emails defeat password reset.

### (e) Mobile / market (phase F)
- FCM deep links end-to-end: google-services.json in NO app, ONESIGNAL_APP_ID unwired, `setOnTapCallback` never registered, init-before-slug ordering (MAI; D5). Note deep2026/02 corrects "init never called": all 5 main.dart DO call it — push is dead for the other reasons (R2 master ledger §0.4). Intent-filters/assetlinks absent in every AndroidManifest (FWA). Notification-tap deep link is a commented no-op (`notification_center_screen.dart:133-137`).
- Nepali .arb + BS/AD pickers: 0 .arb files, no flutter_localizations anywhere (FWA).
- The 12→15 shared widgets: FWA §6 list (AschoolSearchField, BsAdDatePicker, TimetableGrid, AttendanceGrid, MarksGrid, PaymentMethodSheet+ReceiptView, MapCard, ChatThread v2, RichTextView, AppImageViewer, FilePickUploadButton, OfflineBanner+OutboxScaffold, EmptyState/ErrorState v2, GradeBadge+TrendChart, RoleSwitcherSheet) — shared kit has 30 widgets but ZERO domain widgets; `PaginatedList` used by 0 screens.
- Camera homework submission: `FileUploadService.pickAndUploadFile` calls `pickImage` — non-images impossible (`file_upload_service.dart:82-94`); web submit unwired (D5).
- Offline outbox: no sqflite/hive/drift anywhere; teacher attendance fails offline outright (MAI; FWA).
- Payment receipts in-app; dark-mode token fix (105 `Colors.white` per D5 vs 95 per FWA — see §3); MAI P0 list: admin Assignments STUB with fabricated numbers (the portfolio's only STUB), share_plus/web version conflict, promote screen GET-only.
- flutter_user has NO go_router — EntryStage stage machine blocks deep links (FWA); "Use Admin App" dead end (MAI).
- Market: pricing page + API docs + migration-kit landing (v3 §9); MP Phase-6 Tier-1: published NPR pricing with per-student calculator; D4: never mirror claimed competitor numbers.
- Open: iOS ship-or-remove; dead deps (`speech_to_text`, `mobile_scanner`, `geolocator`, `retrofit`, `freezed`, `riverpod_annotation`); only test per app pumps a SizedBox (MAI §7).

### (f) Infrastructure / deploy / observability leftovers (IA items v3 never picked up)
- **P-04 observability — not started (IA)**: JSON logs (prod-only today — request-ID missing in dev, A1a §1.3), request-ID echo, Prometheus `/metrics` (latency, queue depth, SMS spend, AI tokens, webhook 4xx), `/ready` as compose healthcheck + deploy gate, Sentry alerts on backup failure/queue depth/webhook 4xx/quota exhaustion (MP P-04).
- **S-08 remainder: nginx Cloudflare real-IP** (`real_ip_header CF-Connecting-IP` + `set_real_ip_from`) — else all rate-limit zones key on 127.0.0.1 (MP S-08).
- **D-03 remainder: one TIMESTAMPTZ conversion migration for existing columns + `TZ=Asia/Kathmandu` in compose** — A1c §4.5 counts **~97 naive datetime columns** remaining; worst: `revoked_tokens.expires_at`, `users.otp_expires_at/locked_until/tokens_invalid_before`, `school_plugins.trial_ends_at`, `fee_collections.last_reminder_sent_at` (Celery writes NPT vs web UTC — 5h45m disagreement), `gps_logs.timestamp` (highest-volume table, unindexed).
- **P-03 deploy**: IA says ordering still wrong (migrate AFTER `up -d`, `git reset --hard`, no rollback); CS later marks P-03 done — CONFLICT, verify. deep00 F-25/F-26/F-27/F-28: single-stage Dockerfiles as root, health check curl-only.
- **P-05 entitlement repair** (CS §3.1): reconcile_plan_plugins, trial-consumed flag, next_billing_date beat, SMS credits gate, gate sweep H1-H7, authz sweep (51 bare endpoints, 204 object-level checks, 83 role lists), plugin-cache single invalidator, elibrary→ai_suite repoint verification. Registry `min_plan_tier` alias-chain leak regression test (MP §14.5).
- **P-02**: dump encryption, uploads R2 sync, restore drill (never proven), retention automation.
- **S-10 TLS at deploy** (certbot/Origin-CA, 421 catch-all, CSP) — infra task, still open (IA).
- **D-06 money JSONB cutover** — IA says NOT STARTED, but A1/A1b show `class_subjects`/`section_subject_teachers`/`fee_structure_items` tables EXIST with **0 importers** while `subjects.class_ids/teacher_ids` ARRAY + `fee_structures.fee_items` JSONB remain the live path (and `utils/money.py` Decimal helpers have 0 callers — money is float end-to-end, incl. `int(amount*100)` 1-paisa loss at `fees.py:1862`). The cutover + Decimal sweep is unfinished D-06.
- **D-05 contract phase** (NOT-NULL academic_year_id); **D-07 residue**: `marks_history` + `user_mfa` tables.
- **A-01 residue**: per-user/per-feature quotas, dedicated usage session (usage write commits caller's half-built transaction, deep00 F-17), alert_at firing.
- **A-03 residue**: blueprint marks-sum validation, designer PDF/OMR, frontend blueprint UI rewrite, kill cheap-gated duplicate endpoint (A2 §8.2 — actually done? design_studio dupes per D1 D-9 migration).
- **W-02/W-03/W-04 website** (MP §5): draft/live separation (editor publishes every 1.5s today), undo/redo, menu builder, media library, per-page SEO UI, transactional template apply, sitemap fix (robots points at wrong URL, only 8 static sections), JSON-LD as real script, canonical, public-site i18n + Devanagari theme fonts, 11 missing section types, contact inbox (`ContactMessage` written, no reader — also RB P1 #3), school profile fields.
- **AW residues**: AW-02 (CurriculumTopic, mastery_records extension, ai_generation_id FKs on feature tables), AW-05 content-library CRUD, AW-07 IEP routes/gate, AW-08 photo OCR (awaiting Google/Azure keys), AW-09 AI-literacy lessons, AW-11 LTI 1.3 stub, AW-12 /student + /parent pages + AIQuickAction.
- **Boot/lifecycle defects** (A1a §1.12 — none cited by v3): `wsgi.py` defaults to development (ProductionConfig.validate skipped); `app/extensions.py:3` re-exports `redis_client` by value → permanently None for early importers; request-time DDL in `extensions.py:120-126` + upsert on a non-existent constraint (every POST to `/ai/ext/pd/progress` raises InvalidColumnReference); 4 boot seeds × ~6 processes, read-then-insert races; 18 localhost origins trusted (CORS + CSRF) in production; `User.school_id IS NULL` bypasses cross-tenant 403; no `SchoolIsolationError` handler (surfaces 500); beat schedule not volume-mounted in prod (re-fires on restart).
- **Data-model defects** (A1/A1b/A1c — none cited by v3): `assets.asset_code` GLOBALLY unique (cross-tenant collision); ~20 missing UQs (users phone/email, students student_id/admission_number, report_cards, staff_payroll, fee_collections natural key, alumni_donations.transaction_ref, ai_school_quotas singleton, attendance/teacher_attendance/school_plugins UQs not partial on is_deleted, curriculum UQs with nullable school_id let duplicate platform rows in); `uq_tsv_one_published` exists only in the migration, not the model (create_all test DBs lose the invariant); teaching cascade: deleting a platform `curriculum_units` row hard-deletes every school's teaching content, bypassing is_deleted; faqs/hostel CASCADE on school_id vs everything else NO ACTION; schools↔users FK cycle; `teaching_notes.media_id` ↔ `teaching_media` FK cycle; 7 teaching block tables + `curriculum_units`/`learning_outcomes` have no school_id (derived tenancy, join-or-leak); 58 PG enums with semantic duplication (3 severity enums, 2 gender, 2 book-status); dead tables to drop (book_transactions, mood_checkins, counselor_sessions, timetables/periods/substitutions, admission_leads, weekly_insight_reports/daily_briefs/risk_alerts, student_ai_profiles); `ai_teacher_mastery` UQ omits school_id.
- **Task-layer defects** (A1 §7.1): T-1 `website_live_sync` calls create_app() per task + T-3 UUID/JSON TypeError + T-4 nonexistent Notice columns; T-7 geofence haversine `lon2 - lat1` bug (every distance wrong); T-8/T-6 push targets 3 non-existent roles (`principal`/`admin`); T-9 gamification streak task writes nothing (streaks stay 0 forever) with unbounded while-True loop; T-11 nine beat tasks without locks under acks_late; T-12 admission followup re-SMSes on re-run; R12 benchmarking `_school_metric_averages` per school = worst endpoint; FonePay callback scans every school in Python.
- **RB items in no later plan** (RB): login-lockout 500 (`auth_service.py:166` naive-vs-aware), student/parent mobile login provisioning (auto enrollment IDs + parent accounts + backfill), teacher-delete orphan User rows, backend file-by-file review slices 1/2/4–7, `/uploads/*` unauthenticated platform decision, deep00 F-03 `.env.example` key drift (15 code-read keys missing incl. SOCKET_MESSAGE_QUEUE), F-21 temp scripts, F-22/23 root-owned files + second alembic ini, F-30 vendored PHP ERPs still on disk (legal hazard; S-14 residual per CS).

### (g) Launch blockers flagged by the corpus
- MOCK data: mock `/student` page [DONE — B3 `461dde5`]; MAI's admin Assignments STUB still ships fabricated numbers; CS/D5 rule: any MOCK finding is a release blocker.
- Parity lock before any theme code (v3 §3.5; D2 §D.4) — the public site's pixels are contractually frozen.
- Moderation-review workflow must be STAFFED before tutor go-live (founder decision 2 — operational, not code; CS/MP).
- Parent-consent/privacy flow before commercial launch (CS; deployment.md).
- Student/parent mobile login provisioning (RB P1 #2) — apps untestable without it.
- Red-team: AI Teacher lesson-level red team (injection via barge-in, self-harm escalation, off-curriculum pivot) before C gate; wellbeing privacy copy must be honest — Sahayatri's "never stored" footer is FALSE under ASchool's ledger (D3 R7).
- TLS + real secrets ≥32 bytes + SMS prod config + operator for deploy (CS).
- Content is the product: AI Teacher with empty `teaching_*` is a shell — CDC Science/Math 8-10 authoring is a release gate tracked as "content coverage %" (D1 §A.14.1 risk 1; D3 R1 — don't demo slice 3 before slice 2 bulk entry exists).
- E22a/E22b/E95/E97/A-07 pricing product decisions still open (CS).

---

## §3 CONFLICTS & STALENESS (where documents disagree)
1. **PUSA completeness**: D2 header says PUSA is 35 lines, only §0 conventions, "no per-plugin specs yet"; R1 §A11 says 4 of 39 specs delivered. The current file is 395 lines WITH 4 full specs (academics/attendance/exams/fees) — D2's characterization is stale (PUSA was appended 17 min after D2 was written). Both agree Appendix P + 35 specs are missing.
2. **AI gate split-brain**: PDA P0-1 (403 risk, P0 fix) vs v3 §1 runtime test declaring it stale/bidirectional — PDA's remediation item 1 is superseded; only the 23 cosmetic legacy gates remain (B1 done `461dde5`).
3. **P-03 deploy ordering**: IA ❌ vs CS ✅ — verify in deploy.yml.
4. **Colors.white count**: FWA "41+19+18+8+9 ≈ 95" vs D5 "105" — same defect class, divergent counts.
5. **Portal stubs**: WUI "15 of 19" vs D5 "18 of 24" — both superseded by B3 `461dde5` (18 routes → real, catch-alls 404).
6. **config_schema count**: PTTA "5" vs WUI "6 of 53" vs D5 "6 of 43" — denominator drift across manifest-count versions (53/55/57/59); numerator 5-6 stable.
7. **Test counts**: MP 389 → BCA 507 → v3 "561 collected (baseline invalid: 343 stale-schema errors + 5 real failures)" → old FIX_TRACKER 722. **No valid full-suite result existed as of 2026-09-05**; gate B's fresh-DB run is the first (v3 §1, R1 §A15).
8. **Push dead cause**: MAI/D5/MPA say `NotificationService.init()` never called; deep2026/02 corrects — all 5 main.dart call it; dead for 4 other reasons (no google-services.json, unwired OneSignal ID, no setOnTapCallback, init before slug).
9. **health_records duplicate**: v3 §1 lists the merge as open W0 work, but A1c §4.1 finds migration `c7d2e9f4a8b3` already dropped `student_health_records` (and v3's own ledger marks B1 done). Verifier should confirm, not re-do.
10. **Registry columns**: D1 D-5 "the columns do not exist" vs v3 ledger "D-2 added 11 columns (`9de7cfe`)" — D1 written pre-9de7cfe; D3 §D.1's 11-name list is the checklist.
11. **"13 unmounted services"** (TC §3.14) → actually 11 (curriculum_seed and rag ARE mounted) — D1 D-2; Slice-1 "cheapest wins" = 11.
12. **12 vs 11 teaching tables** (AT §C.8 "eleven") — the DDL + `_owned_models()` list 12; DDL authoritative (D1 B.4/D-12).
13. **teaching_media.file_id** references `managed_files.id`, not `files(id)` — no `files` table exists (D1 D-4); spec DDL says `files(id)`.
14. **ai_suite bundling of 153 tools vs separate ai_teacher bundle** — TC §4.6 Q1 left open vs AT §D.1 pricing; v3 decision 1 settles it (separate premium), but metering of tier-4 tools (batch grading/OCR/voice, Kahoot-style) remains unanswered (D1 §C.17.6).
15. **Pricing recommendations collide**: CLN's Rs.18/35-per-student ladder vs decided flat plugin pricing (ai_suite 399, nepal_curriculum 199, ai_teacher 1499) — decision record wins; CLN's NPR pricing table is superseded for OUR tiers (kept for competitor posture).
16. **D4/D5 line counts** mis-stated in v3 §0 (65 vs 95; 145 vs 124) — cosmetic (R2 master ledger §0.1).
17. **Audience-count drift everywhere**: 680/681 routes, 146–161 models, 196 tables, 51/53/55/57/59 manifests, 4 vs 5 Flutter apps — deep00 and A1/A1b are the measured baselines; older counts are stale.
18. **R1 promises §B/§C/§D that don't exist**; its embedded "see §B-n" forward-references dangle. R2_CORPUS_MASTER_LEDGER §0 is the only completed part.
19. **W3 A1a §1.12 forward-references an `A1e` fix file that does not exist** (R2 master ledger §0.2).
20. **MASTER_PLAN as sequencing is superseded** by v3, but remains the only written acceptance criteria for S-/D-/P-/A-/AW-/W-/G-/M-/N-/I- IDs; ~65-tool AI_TEACHING_ECOSYSTEM catalog was never reconciled against D1's 153 (R2 master ledger §0.1).
21. **deep00 F-15/F-16 (no structured outputs, temp 1.0)** — superseded by IA A-01/A-02 ✅; deep00 F-01 (no error boundaries) — IA P-06 ✅ partially; F-24 CI `flutter test || true` still open (P-03 family).
22. **Scorecard "54/55 ✅, zero ❌"** vs D5's MOCK/blocker findings — the scorecard predates D5; D5 wins for UX-level truth (R2 master ledger §0.4).

---

## §4 TEST / EVAL OBLIGATIONS (every gate the corpus demands)
- **E2E gate B** (v3 §5): pytest green on fresh DB (first valid full-suite run); plugin validator 0 errors; `/plugins/catalog` consumed by web with mirror deleted; all 18 portal routes render real data; mock page gone.
- **E2E gate C** (v3 §6): seeded Grade-8 algebra lesson asserts blueprint/plan events; valid WRITE+SVG per teaching slide; playable audio; captions match speech; barge-in works; SM-2 rows update; summary + cost logged. Red-team: injection via barge-in, self-harm escalation, off-curriculum pivot — all guarded.
- **E2E gate D** (v3 §7): golden-set evals per tool family; catalog renders 153 tools with gating/costs; admin enters a chapter + 10 exercises and publishes; student reads chapter + runs SM-2 review; live quiz with 3 sockets; credit exhaustion → 402 UI.
- **Public-site parity lock** (D2 §D.4): `capture_theme_baseline.py` asserting backend/frontend theme registries agree; backend `test_theme_baseline.py` string-equality ×10 themes; frontend `theme-parity.test.ts` snapshot; Playwright visual diff `/school/<slug>` 3 schools × 3 viewports **threshold 0** — all committed BEFORE any theme change; `surface_overrides.public_site` empty in phase 1.
- **20 golden lessons** eval set in CI for AI Teacher (v3 §6 P4; D1 §A.7).
- **Ecosystem CI gates a–g** (MP §13.8): (a) no tool `status=ga` without `AINutritionFacts` row (enforced as test assertion `test_ai_workbench.py:214`); (b) fixture-tool generic-runner test (zero routes/pages); (c) pseudonymization — no rendered prompt contains a real student name; (d) context-builder tenant isolation; (e) writing-feedback schema has NO `revised_text` at type level; (f) cost-estimate-before-generate reconciles with recorded cost_usd; (g) every seeded prompt has `_en` + `_ne` variants.
- **Category-level golden sets** (~9, one per category) in `backend/tests/ai_evals/` with citation-presence checks for evidence-grounded tools; nightly CI not per-PR (MP §4b; A-02).
- **Per-tool gold sets** (claude-skills convention): `backend/tests/ai_gold/<tool_key>/`, fresh-context judge ("never judge a document you wrote in the same context"), hard per-run dollar cap (D1 §C.16 #5, #7).
- **AW-06 tutor red-team** — DONE (13 adversarial cases vs gpt-oss-120b, report in `backend/audits/ai_redteam/known_failure_modes.md`) [DONE per ledger].
- **D3 slice exit criteria** (§H, six slices): slice 1 publish-gate + one-published-version DB rejection; slice 2 200-row CSV dry-run catching 3 planted faults; slice 3 grounding `context_ref.sha256` match + kill-switch 403 + consent 403-before-provider + zero new Flask routes; slice 4 SM-2 interval math + streak-once + leaderboard <100ms; slice 5 forged `lq_answer` scored server-side + cross-worker delivery + cross-school code rejection; slice 6 licence-visible viewer + no-image-to-provider + route-file count test.
- **MP §13 universal verification**: failure-mode pytest per backend fix; alembic upgrade head on fresh DB in CI; render one of each artifact (question paper, ID card, report card, Nepali notice) to PDF + `pdffonts` Devanagari check; Celery queue-routing + run-twice idempotency tests; RTL/jest per frontend fix; task-behavior tests.
- **PTTA Phase-5 mobile golden test**: bootstrap visibility per 5 roles identical to current hardcoded output before deleting ~300 lines of rules.
- **Content coverage %** as AI-Teacher release gate (D1 §A.14.1); live-quiz Redis message_queue as slice-5 deployment gate (D3 §F.7/R5); two-worker test explicitly in exit criteria.
- **Test-infra rules**: dedicated `TEST_DATABASE_URL` per suite (shared aschool_test TRUNCATE-deadlocks); restart celery worker after task-file edits (E93 proved load-bearing); pypdf present in container (RB; CS §6).

*End of R2_CORPUS_DEMANDS_2026-09-05.md.*
