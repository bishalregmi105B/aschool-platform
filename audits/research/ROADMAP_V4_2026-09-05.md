# UNIFIED ROADMAP v4 — BUILD PLAN (what's left, verified)
_Date: 2026-09-05 · Supersedes v3 (`UNIFIED_ROADMAP_2026-09.md`, whose §4 ledger is now stale) ·
Basis: full corpus re-read digest [`_digest/R2_CORPUS_DEMANDS_2026-09-05.md`] + ground-truth tree
verification [`_digest/R3_TREE_VERIFICATION_2026-09-05.md`] (every claim below carries file:line
evidence in R3). Head of chain: `a7c3e9f1d4b8`, single head. 606 tests collected._

---

## 0. Ground truth as of 2026-09-05 (verified, not remembered)

| Area | Verdict | Evidence highlights |
|---|---|---|
| B1 W0-close | ✅ 7/7 | 0 non-canonical AI gates (52× `@plugin_required("ai_suite")`, aliases `decorators.py:13`); row migration `b9f3a6c1d7e2`; `ai_adaptive_learning/manifest.yaml` deleted, routes kept; 10 dead `services/ai/*` deleted (2 orphans left: `benchmarking_ai.py`, `risk_detector.py`); guardian PATCH `students.py:980` / DELETE `:1021`; `models/health.py` gone (migration `c7d2e9f4a8b3`); `log_usage` wired at install (`plugins/billing.py:172`). **No `/plugins/catalog` route — but `/plugins/registry` + `/plugins/aliases` + `/plugins/widgets` cover the surface and `lib/plugins.tsx` now hydrates from `GET /plugins/aliases`; consider B1 item 3 closed-by-different-design.** |
| B2 widgets | ⚠️ partial | 9/12 Tier-1 primitives exist (`data-table.tsx` 495 ln w/ server sort+bulk+CSV, `confirm-dialog.tsx` +undo toast, `filter-bar.tsx` URL-synced, sheet/page-header/pagination/status-pill/skeleton, ErrorState inside `empty-state.tsx`). **Missing: `Wizard`, `JSONSchemaForm`/FormRenderer.** config_schema.yaml: **7 of 41 manifests** (ai_suite, ai_teacher, attendance, fees, library_management, website_builder, whatsapp_bot). |
| B2 widget system (uncommitted) | ✅ built, needs commit | `plugins/widgets.py` (217 ln, server-side gating + `default_layout`), `GET /plugins/widgets` (`plugins.py:927`), widgets.yaml for attendance(7)/exams(7)/fees(9), `exams/ui/index.web.json` component tokens → `MarksGridWidget.tsx` via `usePluginWidgets.ts`, `validator.py` (608 ln) + `scripts/plugin_doctor.py` → **49 manifests · 0 errors · 0 warnings**, `test_plugin_contract.py` + `test_plugin_widgets.py` (25 passed). `_normalize_manifest` v1→v2 adapter live in `loader.py`. |
| B3 portals | ✅ done, 1 gap | Mock /student killed; 18 former coming-soon routes are real useQuery pages (student results/timetable/library/lms/ai-tutor/elibrary; parent attendance/results/fees/chat/bus/notices/health/wellbeing/conferences; teacher pages re-exports). **Gap: teacher leave-requests approval UI still has zero UI anywhere.** `portal-route-meta.test.ts` deleted with **no replacement**. |
| C-P1 content spine | ✅ | 12 `teaching_*` tables (`models/teaching_content.py:42-665`), draft→review→publish + `uq_tsv_one_published`, override chain, migration `d4e7f1a9c2b8`. |
| C-P2 runtime | ⚠️ backend tables only | Runtime tables migration `e5a8c2d7f3b1` exists; **no separate runtime service, no `lesson:{id}` socket rooms (`realtime.py` 159 ln, school rooms only), token_hub has reserve/reconcile (`:347-381`) but no `stream=True`, no two-sided cost accounting, analyzer→planner not wired; plugin reconcile/purge tasks are plain functions never registered (T-20).** |
| C-P3/P4/P5 | ⚠️ launcher + guardrails only | Launcher `dashboard/ai-teacher/page.tsx` links out via `player_url`; **no BoardCanvas/CaptionStream/player, no Flutter player, no lesson PDF export, no 20-golden-lesson eval set** (12 tests, 0 golden, not in CI). Kill switch, explicit consent assert (`routes.py:254-262`), injection scan, hours window, budget reserve are in. |
| D-1 nepal_curriculum | ⚠️ packaging only | Manifest + `owns_tables` (12 tables) live; **no `schema_version: 2`, no +11-col `question_bank_items` migration, no admin entry UI, no exercise bank browser, no chapter reader (web or flutter).** |
| D-2 catalog | ⚠️ columns + 24 tools | Migration `f6b2d8e4c1a9` (10 catalog columns); `workbench_seed.py` TOOLS = 24 entries; per-tool prompt files written. **No 15 result templates (12 distinct output_schema_name), frontend does not drive from `ui_type` (0 refs), ~129 of 153 tools unseeded.** |
| D-3 learning science | ❌ | No `spaced_rep_cards` / `student_xp_state` / `live_quiz_sessions`; SM-2 only inside ai_teacher mastery (`ai_teacher.py:254`), no shared service; gamification screens unfilled. |
| D-4 voice/documents | ⚠️ | `useExport.ts:310-317` defineLayout fix verified; `writer_docx.py` exists; AiResultView on 4 pages. **No edge-tts service, no STT proxy, no deck/docx/pdf emitters wired to AI outputs.** |
| D-5 elibrary | ❌ | OERResource lacks the +6 licence/attribution columns; no PDF chat; no model annotator. |
| E Nepal modules | ❌/⚠️ 16 items | Only ⚠️s: payroll JSONB deductions but no TDS calc; refunds ledger exists but no eSewa/FonePay/bank-rec/receipt-reversal; designer templates but no marksheet print designer; leave_type column only; IEMIS-only import; admission stage pipeline but no kanban; ZKTeco device model but no RFID one-card; alumni has **no routes.py**; white-label domain-verify only. **Timetable solver STILL the greedy stub (`timetable_solver.py:39`).** |
| F mobile/market | ❌ mostly | 0 google-services.json (push dead in all 5 apps), 0 .arb, no offline outbox, no /pricing route (landing tiers only), no go_router in flutter_user, admin Assignments STUB ships fabricated numbers. Dark-mode tokens ✅ (`globals.css:47`). |
| Infra | ✅ 3 closed, 1 left | deploy.yml ordered correctly; TIMESTAMPTZ migration `b2e7c4a9f1d3`; nginx Cloudflare real-IP in place. **No /metrics + JSON logs (P-04).** |

**Corpus-wide defects v3 never picked up (R2 §2f, now first-class plan items):** D-06 is a
*cutover* not a build (tables exist, 0 importers, JSONB/ARRAY still live, `utils/money.py`
0 callers, `int(amount*100)` 1-paisa loss); ~97 naive datetime columns (reminder timestamps
disagree 5h45m between Celery NPT and web UTC); IEMIS importer defects blocking migration
kit ⑩ (`.xls`→500, no-id rows duplicate every re-import, `_parse_dob` AD always None,
placeholder `9800000xxxx` phones are real NTC numbers and SMS blast doesn't filter them);
receipt reversal Σ leak; gateway secrets in plaintext JSONB; `teacher_ids[0]` global bug
(per-(class,subject) teacher endpoint MUST-BUILD); `GET /attendance/me` missing (flutter_teacher
calls it); `correct_answer` leak in `GET /exams/online/<id>`; marks lock after publish missing;
login-lockout 500; student/parent login provisioning; teacher-delete orphan; contact inbox;
Ashlya top-5 absorptions (task-class router w/ p95 budgets, pre-action credit price, prompt-time
section selection, multi-round tool tutor).

---

## 1. Phase 0 — land the in-flight work (≤ half day, do first)

The working tree holds the complete plugin-v2/widget system (uncommitted, verified green by
validator+doctor and 25 unit tests). Commit it before anything else:

1. Commit `_normalize_manifest`, `alias_map()`, `widgets.py`, `GET /plugins/widgets`,
   `validator.py`, `plugin_doctor.py`, `test_plugin_contract.py`, `test_plugin_widgets.py`,
   attendance/exams/fees `widgets.yaml`, `exams/ui/`, `MarksGridWidget` + `usePluginWidgets`,
   the 4 prompt files, iemis_importer role-hygiene diff, frontend build-hygiene (tsconfig,
   `frontend/tmp/` gitignore), portal-route-meta retirement. Message: `feat(B2): plugin v2
   contract + widget system + validator gate`.
2. Bring the project test stack up (Postgres **5435** / Redis 6383; `aschool_test` fresh) and
   run the **first valid full-suite run** (~606 tests). This is Gate B from v3 — still owed;
   7 DB-connect errors seen this pass were purely "5435 down".
3. Replace the deleted `portal-route-meta.test.ts` with a portal-data smoke test that asserts
   the 18 portal routes fetch real endpoints (no fabricated data), not route metadata.
4. Add `owns_tables` for health_profiles to the health_records manifest; delete or wire the 2
   orphan `services/ai/*` modules (`benchmarking_ai.py`, `risk_detector.py`).
5. Fix `plugin_doctor`/validator into CI (pre-push or GH Actions job) so 49-manifest 0-error
   stays a ratchet, not a memory.

**Gate 0:** full pytest green on fresh DB; validator 0 errors in CI; dirty tree empty.

## 2. Phase B′ — finish B2 + the human gaps B3 left (small, web track)

1. `JSONSchemaForm`/FormRenderer: 18 typed field rows per D2 §C.2 — incl. `secret`
   (never echoed back) and tenant-scoped `entity-picker`; plus the `config_migrations` runner.
2. `Wizard` primitive (multi-step form shell) — needed by admissions ⑪ and bulk import.
3. `config_schema.yaml` wave: next 13 priority plugins — exams, lms, hr_payroll, design_studio,
   notices, assignments, gps_tracking, iemis_importer, admission, nepal_curriculum, gamification,
   elibrary, sms_notifications (7/41 → 20/41; D2 Phase-1 list + the plugins Phase C/D need
   configurable).
4. Teacher **leave-requests approval UI** (endpoints exist; zero UI — D5 BLOCKER).
5. Quick correctness fixes surfaced by R2 (half-day each, batch in one PR):
   `correct_answer` leak in `GET /exams/online/<id>`; `GET /attendance/me` endpoint
   (flutter_teacher calls it); per-(class,subject) teacher endpoint (kills `teacher_ids[0]`
   global bug); exam marks lock after publish.

**Gate B′:** FormRenderer renders ai_teacher + nepal_curriculum config live; leave approval
flow works end-to-end; 4 fixes tested.

## 3. Phase C — AI Teacher runtime, player, guardrails (the differentiator)

Build order P2→P5 (P1 done). Each step E2E-tested live with the Groq key.

- **P2 Runtime service** (per D1 §A.1): standalone Flask app in `backend/services/ai_teacher/`
  (own process/port, restricted Postgres role, Redis room state); provisioning on install
  (per-school credential + HMAC S2S, 24h rotation); session brokering in plugin routes; JWT
  player token ≤15min/jti/fragment-only; `lesson:{id}` socket rooms with school scoping (new
  code — `realtime.py` has none); **analyzer→planner wired** (fix ATeacher's dead-code defect)
  + slide guidance; board grammar + holdback parser + `repair_svg` ported verbatim (D2 §G);
  `stream=True` in token_hub + quota reserve/reconcile per call; two-sided cost accounting
  (host ledger + service task classes); **register the reconcile/purge tasks** (T-20 — they
  exist but are never scheduled).
- **P3 Web player:** `frontend/app/dashboard/ai-teacher/player/` — BoardCanvas (DOM+SVG hybrid,
  WRITE/DRAW_SVG, y-cursor height-8/gap-2), CaptionStream, barge-in, mastery panel; teacher
  monitoring widgets via the widget system; launcher's `player_url` stops being a dead link.
- **P4 Guardrails + evals:** injection + moderation on every student turn (exists — keep),
  pseudonymization, per-lesson cost estimate pre-start; **eval set of 20 golden lessons in CI**
  (per-tool `tests/ai_gold/` pattern, fresh-context judge, dollar cap); red-team additions:
  injection via barge-in, self-harm escalation, off-curriculum pivot.
- **P5 Apps + export:** flutter_student LessonPlayer (snapshot cache for offline replay),
  flutter_teacher monitoring, lesson PDF export (WeasyPrint, Devanagari fonts per MP §13).

**Gate C:** seeded Grade-8 algebra lesson asserts blueprint/plan events, valid WRITE+SVG per
slide, playable audio, captions match speech, barge-in works, SM-2 rows update, summary+cost
logged; red-team all pass; golden set green in CI.

## 4. Phase D — nepal_curriculum + ai_suite surface (D3 spec §A–§J)

- **D-1′ nepal_curriculum real build:** ratchet to `schema_version: 2` (first dogfood);
  admin entry UI — 11 screens (chapters/notes blocks/examples/formulas/terms/misconceptions/
  exam tips, bilingual, Bloom autofill + Jaccard dedupe) + the 9-condition publish gate +
  **8-template CSV/paste-TSV bulk entry BEFORE the AI slice** (D3 slice order); exercise bank
  browser (+11 columns on `question_bank_items` migration, enum widened with the 19 Nepal
  types — never force MCQ, `exercise_section_links` table); `context_chapter` grounding (422
  on unauthored); chapter reader (web + flutter_student). Resolve D3 §J owner decisions first
  (consent default R4: every student 403s until guardian consent — catalog must render
  locked-with-explanation).
- **D-2′ catalog completion:** 15 result templates (D3 §E) w/ TopicSelector/grounding badge/
  FollowUpChips/tokenCostBadge; frontend AI hub drives from `ui_type` (currently 0 refs);
  seed the remaining ~129 tools in waves on doc_sections/item_analysis (per-tool prompt files
  already auto-written by `workbench_seed.py:558`); Flutter hub mirrors.
- **D-3 learning science:** `spaced_rep_cards` + shared SM-2 service (ai_teacher concepts +
  curriculum exercises), `student_xp_state` + XP events via `points_logs`, adaptive path +
  reasons (extends `learning_paths`), `live_quiz_sessions` (join codes, server-authoritative,
  `lq:{code}` rooms on existing socket auth; Redis message_queue = deployment gate),
  gamification plugin screens filled.
- **D-4′ voice + documents:** edge-tts service (ne-NP Sagar/Hemkala, expressive presets, LRU
  cache, per-school queue, 503+Retry-After); STT proxy via token_hub; deck emitter via
  designer canvas dialect (NO python-pptx); docx/pdf writers for every AI output (writer_docx
  exists — wire it); AiResultView + ui_type covers the render side.
- **D-5′ elibrary:** +6 licence/attribution columns on OERResource, Sketchfab/OER viewer,
  model annotator tool, PDF chat via RAG (SSRF-safe), research agent with allow-lists.

**Gate D:** admin enters a chapter + 10 exercises and publishes; student reads chapter and
runs SM-2 review; live quiz with 3 sockets; catalog renders full tool count with gating/costs
and consent-locked states; credit exhaustion → 402 UI; golden-set evals per tool family.

## 5. Phase E — Nepal competitive modules (re-ranked; A2 corrections folded in)

Order reflects money-risk and dependency, not v3's ①–⑯ numbering:

1. **Transcripts + per-school grading scales** (kill the 3-4 divergent NEB scale copies;
   grade-sheet template already references nonexistent `{symbol_no}`).
2. **Payments hardening pack:** eSewa/FonePay + partial/offline refunds + **receipt reversal**
   (Σreceipts still counts refunded money) + bank reconciliation + payment-initiation expiry
   + **gateway secrets out of plaintext JSONB**.
3. **Payroll TDS/SSF/PF** slabs + annual certificate.
4. **Marksheet/report-card print designer** (Veda's moat) — designer canvas + marksheet blocks.
5. **Real timetable solver** (constraint-aware input contract A2 §4.4, rooms, teacher quals;
   replaces greedy stub `timetable_solver.py:39`).
6. **IEMIS importer fixes THEN migration kit ⑩** (`.xls`, re-import dedupe, DOB BS↔AD,
   placeholder-phone SMS filter; adapters Veda/Paathshala/eZone/IEMIS; parallel-run, same-day
   cutover) + Excel import/export everywhere.
7. **Smart SMS 2.0** (merge SMS+push, cost receipts).
8. **IRD-verified billing polish + VAT vouchers.**
9. **Subject-wise attendance + leave types + leave-request UI** (UI already in B′).
10. **Transport-fee billing + driver manifest;** ⑪ admissions CRM kanban; ⑫ re-evaluation
    workflow; ⑬ ZKTeco/RFID BYO + one-card events; ⑭ canteen wallet/POS + hostel mess/
    maintenance; ⑮ alumni (needs routes.py from scratch) + wellbeing honest privacy copy +
    moderation-review staffing (founder decision); ⑯ white-label app build pipeline (last).

**Gate E:** per-module smoke flows; refund math property test incl. reversal; timetable
solver reports conflicts and honors quals.

## 6. Phase F — Mobile + market

FCM deep links end-to-end (google-services.json per app + `setOnTapCallback` wiring + intent
filters), Nepali .arb + BS/AD pickers, the 12 shared widgets, camera homework submission (and
fix `FileUploadService` pickImage-only), offline outbox, payment receipts, kill the admin
Assignments STUB, dead-dep sweep, /pricing route publishing OUR tiers (never competitor
prices — D4), API docs, migration-kit landing, iOS ship-or-remove decision.

## 7. Platform-hygiene track (parallel, one small PR per cluster, never blocks C/D)

- **D-06 cutover:** make the money tables + `utils/money.py` actually used; Decimal sweep;
  kill `int(amount*100)` 1-paisa loss.
- **Datetime sweep:** ~97 naive columns → TIMESTAMPTZ-aware reads; align Celery NPT vs web UTC.
- **P-04 observability:** JSON logs, request-ID, /metrics.
- **RB orphans:** login-lockout 500, student/parent login provisioning, teacher-delete orphan,
  contact inbox (W-04 leftover), file-by-file review slices 1/2/4–7.
- **Ashlya top-5 absorptions:** task-class router w/ p95 budgets, credit ledger w/ pre-action
  price, prompt-time section selection, multi-round tool tutor; decide tier-4 tool metering.
- **Parity lock:** capture theme baseline + 2 unit tests + 3-school×3-viewport threshold-0
  Playwright diff **before any theme code changes** (D2 §D.4).
- **Content-coverage gate:** AI Teacher surfaces degrade honestly when `teaching_*` is empty
  (shell otherwise); parent-consent/privacy flow copy.

## 8. Sequencing

```
Phase 0 (commit+Gate B) ──┬── B′ widgets/gaps ──→ C AI Teacher P2–P5  (AI track)
                          ├── B′ forms ──────────→ D nepal_curriculum D-1′…D-5′ (D-1′ needs B′ FormRenderer)
                          └── E money pack (1–4) can start after Phase 0; E rest after B′
F mobile/market after C+D gates; hygiene track continuous.
```

## 9. Test/eval obligations carried forward

Gate B fresh-DB full suite (Phase 0); plugin validator 0-error CI ratchet; golden-set evals
per tool family + 20 golden lessons in CI; ecosystem gates a–g (MP §13.8); D3 slice exit
criteria + Redis message_queue deployment gate; parity lock before theme work; fresh-DB
alembic CI, PDF+`pdffonts` Devanagari check, Celery queue-routing/idempotency tests (MP §13).

## 10. Anti-goals (unchanged from v3)

No whiteboard. No OCR ingestion. No AI outside the workbench guardrails. No Sahayatri
auth/subscription internals, Redis-only persistence, `run_text_tool()` catch-all, un-gated
voice tutor, SSRF-y PDF fetch, ATeacher session service, in-memory room state, GET-with-text
TTS, dead 1,400 lines. Never publish claimed competitor pricing. Public-site rendered design
does not change (parity lock first).
