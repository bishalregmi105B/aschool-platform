## User

`Image`
 see this student app home screen 0 % is unmanaged student name is not shown Namaste student is shown `Image` and profile icon takes to parent list directly but should also show student details his history overall analysis his activities homework attendence histories ana many more check codebase and eschool app screens and apis learn from it [@eSchool SaaS v1.8.0 Nulled](file:///home/bishal-regmi/Desktop/ASchool/eSchool%20SaaS%20v1.8.0%20Nulled/) `Image` and improve this ui since it stretched to very top of display of mobile `Image` similarly this also `Image` and also gallery screen just listing not working click not viewing or allowing to download `Image` result only shows this but where is all subject marks whee is markshet gradesheet and also similar in teacher apps students subject wise thei subject marks entry check how eschool have `Image` and improve this bottomsheet ui design since its looking too large heavy check eschool ui `Image` and fix this timetable issue [@flutter_parent](file:///home/bishal-regmi/Desktop/ASchool/flutter_parent/) [@flutter_shared](file:///home/bishal-regmi/Desktop/ASchool/flutter_shared/) [@flutter_teacher](file:///home/bishal-regmi/Desktop/ASchool/flutter_teacher/) [@flutter_student](file:///home/bishal-regmi/Desktop/ASchool/flutter_student/) [@app](file:///home/bishal-regmi/Desktop/ASchool/frontend/app/) [@backend](file:///home/bishal-regmi/Desktop/ASchool/backend/) explore all codebase first and then start

**Tool Call: List the `ASchool` directory's contents**
Status: Completed

# Folders:
ASchool/.agents
ASchool/.github
ASchool/.venv
ASchool/Mighty School Pro v1.6
ASchool/backend
ASchool/docs
ASchool/eSchool SaaS v1.8.0 Nulled
ASchool/flutter_admin
ASchool/flutter_parent
ASchool/flutter_shared
ASchool/flutter_student
ASchool/flutter_teacher
ASchool/frontend
ASchool/hardware
ASchool/iemis_templates
ASchool/nginx

# Files:
ASchool/.codex
ASchool/.gitignore
ASchool/ASchool_ULTIMATE_v1.md
ASchool/IMPLEMENTATION_PLAN.md
ASchool/Makefile
ASchool/PLAN_AUDIT_2026-04-25.md
ASchool/README.md
ASchool/backend_route_audit.md
ASchool/docker-compose.prod.yml
ASchool/docker-compose.yml
ASchool/implementation_plan.md
ASchool/task.md
ASchool/url_map.txt
ASchool/walkthrough.md


## Assistant

<thinking>
**Exploring codebase for fixes**

I need to respond by diving into the codebase to identify and implement fixes. The user has pointed out some issues, so I'll start reviewing the eSchool app screens and APIs to learn. Since we have roots in ASchool, I'll need to explore everything cautiously without assuming any paths. There’s a lot to check, including the Flutter project structure, various UI components, and also backend APIs. It might be helpful to delegate parts of this task. Let's start with directory lists and grep to organize our inspection.

**Delegating investigations with agents**

I’m thinking of spawning a few agents for independent investigations. One can look into the student app's architecture and screens, another can focus on the backend APIs and eSchool references, and the third can inspect the shared widgets and UI safe areas. Since this task is large, delegating is useful. Maybe we can have three agents working in parallel: locating student app issues, analyzing backend APIs for data contracts, and checking the eSchool directory. I'll start by gathering file paths using tools like find_path.
</thinking>

**Tool Call: List the `ASchool/flutter_student` directory's contents**
Status: Completed

# Folders:
ASchool/flutter_student/.dart_tool
ASchool/flutter_student/.idea
ASchool/flutter_student/android
ASchool/flutter_student/assets
ASchool/flutter_student/build
ASchool/flutter_student/lib
ASchool/flutter_student/test

# Files:
ASchool/flutter_student/.flutter-plugins-dependencies
ASchool/flutter_student/.gitignore
ASchool/flutter_student/.metadata
ASchool/flutter_student/README.md
ASchool/flutter_student/analysis_options.yaml
ASchool/flutter_student/aschool_student.iml
ASchool/flutter_student/pubspec.lock
ASchool/flutter_student/pubspec.yaml


**Tool Call: List the `ASchool/flutter_shared` directory's contents**
Status: Completed

# Folders:
ASchool/flutter_shared/.dart_tool
ASchool/flutter_shared/assets
ASchool/flutter_shared/build
ASchool/flutter_shared/lib
ASchool/flutter_shared/test

# Files:
ASchool/flutter_shared/.flutter-plugins-dependencies
ASchool/flutter_shared/pubspec.lock
ASchool/flutter_shared/pubspec.yaml


**Tool Call: List the `ASchool/flutter_teacher` directory's contents**
Status: Completed

# Folders:
ASchool/flutter_teacher/.dart_tool
ASchool/flutter_teacher/.idea
ASchool/flutter_teacher/android
ASchool/flutter_teacher/assets
ASchool/flutter_teacher/build
ASchool/flutter_teacher/lib
ASchool/flutter_teacher/test
ASchool/flutter_teacher/web

# Files:
ASchool/flutter_teacher/.flutter-plugins-dependencies
ASchool/flutter_teacher/.gitignore
ASchool/flutter_teacher/.metadata
ASchool/flutter_teacher/README.md
ASchool/flutter_teacher/analysis_options.yaml
ASchool/flutter_teacher/aschool_teacher.iml
ASchool/flutter_teacher/pubspec.lock
ASchool/flutter_teacher/pubspec.yaml


**Tool Call: List the `ASchool/backend` directory's contents**
Status: Completed

# Folders:
ASchool/backend/.pytest_cache
ASchool/backend/.venv
ASchool/backend/__pycache__
ASchool/backend/app
ASchool/backend/migrations
ASchool/backend/scripts
ASchool/backend/tests
ASchool/backend/uploads

# Files:
ASchool/backend/Dockerfile
ASchool/backend/celerybeat-schedule
ASchool/backend/config.py
ASchool/backend/extensions.py
ASchool/backend/migrate_passwords.py
ASchool/backend/pyproject.toml
ASchool/backend/requirements.txt
ASchool/backend/scratch_admin_check.py
ASchool/backend/scratch_admin_reset.py
ASchool/backend/scratch_routes.py
ASchool/backend/seed.py
ASchool/backend/seed_full.py
ASchool/backend/seed_test_data.py
ASchool/backend/sync_student_users.py
ASchool/backend/wsgi.py


**Tool Call: List the `ASchool/eSchool SaaS v1.8.0 Nulled` directory's contents**
Status: Completed

# Folders:
ASchool/eSchool SaaS v1.8.0 Nulled/App code
ASchool/eSchool SaaS v1.8.0 Nulled/PHP_CODE
ASchool/eSchool SaaS v1.8.0 Nulled/Updates
ASchool/eSchool SaaS v1.8.0 Nulled/eSchool-SaaS-Doc-main

# Files:
ASchool/eSchool SaaS v1.8.0 Nulled/Download More Apps Source.html
ASchool/eSchool SaaS v1.8.0 Nulled/PHP_CODE.zip
ASchool/eSchool SaaS v1.8.0 Nulled/eSchool-SaaS-Doc-main.zip


## Assistant

<thinking>
**Exploring the codebase**

I’m thinking it’s important to explore the entire codebase before starting with any specific task. Inspecting everything first should give a clearer picture. I’m considering using agents to help with this process since they can provide insights or assist in navigating through the code. It feels like the right approach to ensure that I have a comprehensive understanding before diving in. Let's see what I can uncover!
</thinking>

**Tool Call: Investigate student app issues**
Status: Completed

I did not edit any files. Here’s the concise investigation summary.

## Key findings by issue

| Issue | Relevant paths / classes / APIs | Likely root cause | Recommendations |
|---|---|---|---|
| Home says `Namaste, Student!` | `ASchool/flutter_student/lib/features/dashboard/student_dashboard.dart` → `StudentDashboard`; `ASchool/flutter_shared/lib/services/auth_service.dart` → `AuthNotifier`; `ASchool/flutter_shared/lib/models/user.dart` → `User`; backend `ASchool/backend/app/models/user.py` → `User.to_dict()`; backend `ASchool/backend/app/services/auth_service.py` → `login_student()` | Backend returns `full_name`, but Flutter `User.fromJson` only reads `first_name`/`last_name`. Student login also returns `student_profile`, but Flutter ignores it. | Update `User.fromJson` to handle `full_name`, or create/cache a current `Student` profile from `student_profile` / `/students?user_id=...`. Use that for greeting, drawer, avatar, class, etc. |
| Home attendance shows `0%` | `ASchool/backend/app/api/v1/student_app.py` → `student_dashboard()` returns `"attendance": {"percentage": 0}`; real endpoint exists in `ASchool/backend/app/api/v1/attendance.py` → `/attendance/student/<student_id>/summary`; Flutter `ASchool/flutter_shared/lib/providers/attendance_provider.dart` incorrectly uses `user.id` as `studentId` | Dashboard attendance is hard-coded to `0`. Shared attendance provider would also use the auth user UUID, not the student UUID. | Calculate attendance in `/student/dashboard` from `Attendance`, or have Flutter resolve current student id and call `/attendance/student/{studentId}/summary`. Add a shared `currentStudentProvider`. |
| Profile icon opens guardians directly | `ASchool/flutter_student/lib/features/dashboard/student_dashboard.dart` profile `IconButton` routes to `/dashboard/guardians`; `ASchool/flutter_student/lib/router.dart` maps `/profile` → `/dashboard/guardians`; `ASchool/flutter_student/lib/features/guardians/guardian_details_screen.dart` only lists guardians | There is no real student profile/details route. The profile icon is intentionally wired to guardian list. | Add `features/profile/student_profile_screen.dart` and route `/dashboard/profile`. Point dashboard icon and legacy `/profile` redirect there. Include tabs/cards for profile info, guardians, attendance, homework, results, portfolio, achievements/history/activity. |
| Existing profile-like screens have data issues | `ASchool/flutter_student/lib/features/portfolio/portfolio_screen.dart`; `ASchool/flutter_student/lib/features/achievements/achievements_screen.dart`; backend `ASchool/backend/app/api/v1/student_app.py` → `/student/portfolio`, `/student/achievements`, `/student/wellbeing` | Several screens assign `_data = res.data`, but backend responses are wrapped as `{success, data}`. UI then reads top-level fields that are actually under `data`. | Normalize response extraction to `res.data['data']`. Reuse these screens/data in the richer student profile. |
| Gallery lists but click/view/download does nothing | `ASchool/flutter_student/lib/features/gallery/gallery_screen.dart` → `_photoTile()` only renders `Image.network`; no `InkWell`, preview, open, or download action. Backend file API: `ASchool/backend/app/api/v1/files.py` → `GET /files/`, `GET /files/<id>`, `GET /files/<id>/presigned`; model `ASchool/backend/app/models/file.py` → `ManagedFile.to_dict()` | Gallery UI has no interaction handlers. Also private/school-only files may require presigned URLs. Shared `GalleryRepository` calls `/files/gallery`, but backend has no `/files/gallery` endpoint and the screen bypasses the repository. | Wrap tiles with tap actions. Add preview/detail screen and download/open buttons using `url_launcher` or Dio download. Use `/files/{id}/presigned` when `is_public != public`. Fix or remove `GalleryRepository` `/files/gallery` mismatch. |
| Result screen lacks subject marks / marksheet / gradesheet | Flutter `ASchool/flutter_student/lib/features/results/student_results.dart` can render `ExamResult.subjects`; repo `ASchool/flutter_shared/lib/repositories/exam_repository.dart` calls `/student/results`; model `ASchool/flutter_shared/lib/models/exam.dart` → `ExamResult`; backend `ASchool/backend/app/api/v1/student_app.py` → `/student/results` returns `subjects: []`; richer backend exists in `ASchool/backend/app/api/v1/exams.py` → `/exams/results`, `/exams/<exam_id>/marksheet/<student_id>`, `/exams/<exam_id>/report-cards/<student_id>` | UI has subject table support, but the endpoint it uses sends empty subjects and omits useful identifiers/links. Marksheet endpoints exist but are not called from Flutter. | Either make `/student/results` use the richer exam serializers, or switch Flutter to `/exams/results`. Include `id`, `exam_id`, `subjects`, `marks_obtained`, `total_marks`, `remarks`, `pdf_url` where available. Add result-detail/marksheet screen and download/open actions. |
| Assignment bottom sheet too large/heavy | `ASchool/flutter_student/lib/features/homework/homework_screen.dart` → `_showDetail()` uses `height: screen * 0.85`; `_AssignmentDetailView` uses large padding/typography and many full-width blocks; shared `ASchool/flutter_shared/lib/widgets/custom_bottom_sheet.dart` defaults to `0.7` height with large header | Not a data bug. The modal is designed almost full-screen and content is dense. | Either make it a full detail page, or compact the bottom sheet: 60–70% height, smaller padding, concise header, collapsible instructions/attachments, sticky submit button, fewer dividers/large cards. |
| Timetable throws `type 'String' is not subtype of Map<dynamic,dynamic>` | `ASchool/flutter_student/lib/features/timetable/student_timetable.dart`; provider `ASchool/flutter_shared/lib/providers/timetable_provider.dart`; repo `ASchool/flutter_shared/lib/repositories/timetable_repository.dart`; model `ASchool/flutter_shared/lib/models/timetable_slot.dart`; backend generic `ASchool/backend/app/api/v1/timetable.py`; backend student endpoint `ASchool/backend/app/api/v1/student_app.py` → `/student/timetable` | `TimetableSlot.fromJson` assumes `json['subject']` is a map and calls `Map.from(json['subject'])`, but backend returns `subject` as a string. Repo also calls generic `/timetable?class_id=&section_id=` with empty IDs, returning all slots, not current student timetable. Student endpoint returns `{periods: [...]}`, which repo does not handle. | Harden `TimetableSlot.fromJson`: only parse `subject` if it is a map; if string, use it as `subjectName`. Do same for `teacher`. Parse `period_number` and `is_break`. Prefer `/student/timetable?day=...` or resolve current student class/section before calling `/timetable`. Adapt repo to handle both list and `{periods}` payloads. |

## Shared data models involved

- `ASchool/flutter_shared/lib/models/user.dart` → `User`
- `ASchool/flutter_shared/lib/models/student.dart` → `Student`
- `ASchool/flutter_shared/lib/models/guardian.dart` → `Guardian`
- `ASchool/flutter_shared/lib/models/attendance.dart` → `AttendanceRecord`, `AttendanceSummary`
- `ASchool/flutter_shared/lib/models/assignment.dart` → `Assignment`, `AssignmentSubmission`
- `ASchool/flutter_shared/lib/models/exam.dart` → `Exam`, `ExamResult`
- `ASchool/flutter_shared/lib/models/timetable_slot.dart` → `TimetableSlot`
- `ASchool/backend/app/models/student.py` → `Student.to_dict()`
- `ASchool/backend/app/models/user.py` → `User.to_dict()`
- `ASchool/backend/app/models/file.py` → `ManagedFile.to_dict()`

## Highest-impact fixes

1. Add a shared `currentStudentProvider` that resolves `/students?user_id={authUser.id}` once and exposes real `student.id`, name, class, section, photo.
2. Fix `User.fromJson` to consume backend `full_name`.
3. Replace dashboard hard-coded attendance with real attendance summary.
4. Add `/dashboard/profile` and stop routing profile to guardians.
5. Fix timetable JSON parsing for string `subject`/`teacher` and use the student-specific timetable API.
6. Make `/student/results` return subject rows or switch Flutter to `/exams/results`.
7. Implement gallery preview/open/download using existing `/files` and `/files/{id}/presigned` APIs.

**Tool Call: Investigate backend/mobile APIs**
Status: Failed

cloud language model request failed with status 403 Forbidden: {"code":"token_spend_limit_reached","message":"Student plan credits consumed."}

**Tool Call: Inspect eSchool reference app**
Status: Completed

Inspected the reference apps under `ASchool/eSchool SaaS v1.8.0 Nulled/App code`. I did **not** edit any files.

I’ll use these shorthand roots below:

- Student/parent app root: `ASchool/eSchool SaaS v1.8.0 Nulled/App code/e-school-saas-student-parent/e-school-saas`
- Staff app root: `ASchool/eSchool SaaS v1.8.0 Nulled/App code/eschool-saas-staff/eschool-saas-staff`

## Cross-cutting API/UI pattern to port

Relevant paths:

- Student API wrapper: `.../e-school-saas/lib/utils/api.dart`
- Staff API wrapper: `.../eschool-saas-staff/lib/utils/api.dart`
- Student constants/base URL: `.../e-school-saas/lib/utils/constants.dart`
- Student routes: `.../e-school-saas/lib/app/routes.dart`
- Staff routes: `.../eschool-saas-staff/lib/app/routes.dart`

Ideas to port:

- API calls use `Dio`.
- Auth headers are:
  - `Authorization: Bearer <jwt>`
  - `school-code: <schoolCode>`
  - Student app also sends `Accept: application/json`.
- POST requests are sent as `FormData.fromMap(..., ListFormat.multiCompatible)`, useful for arrays/files.
- State pattern is consistently: `Repository -> Cubit -> UI`.
- Navigation uses GetX named routes.
- Reusable UI states:
  - shimmer/loading widgets
  - `ErrorContainer` with retry
  - `NoDataContainer`
  - custom bottom sheets via `Utils.showBottomSheet(...)`
  - module-gated menus using school configuration/module IDs.

---

## 1. Student home/profile

### Key paths

Student home and dashboard:

- `.../e-school-saas/lib/ui/screens/home/homeScreen.dart`
- `.../e-school-saas/lib/ui/screens/home/widgets/homeContainer.dart`
- `.../e-school-saas/lib/ui/screens/home/widgets/homeContainerTopProfileContainer.dart`
- `.../e-school-saas/lib/ui/screens/home/widgets/moreMenuBottomsheetContainer.dart`
- `.../e-school-saas/lib/utils/homeBottomsheetMenu.dart`

Profile:

- `.../e-school-saas/lib/ui/screens/studentProfileScreen.dart`
- `.../e-school-saas/lib/cubits/studentProfileCubit.dart`
- `.../e-school-saas/lib/cubits/studentAllProfileDetailsCubit.dart`
- `.../e-school-saas/lib/data/repositories/studentRepository.dart`
- `.../e-school-saas/lib/data/repositories/authRepository.dart`

### API patterns

Student endpoints from `utils/api.dart` and `StudentRepository`:

- `GET student/get-profile-data`
- `GET parent/get-child-profile-data`
- `GET student/school-settings`
- `GET parent/school-settings`
- `GET student/subjects`
- `GET student/sliders`
- `GET parent/sliders`

Profile method:

- `StudentRepository.fetchStudentFullProfileDetails({useParentApi, childId})`
  - student mode calls `student/get-profile-data`
  - parent mode calls `parent/get-child-profile-data?child_id=...`

### UI ideas to port

- Top profile hero card:
  - avatar
  - full name
  - class/section
  - roll number
  - optional chat icon if chat module enabled
- Home content:
  - sliders/carousel
  - subjects grid/list
  - latest notices
  - gallery preview if module enabled
- Dynamic bottom nav:
  - shows `Home`
  - shows `Assignments` only if assignment module is enabled
  - `Menu` opens animated bottom sheet.
- More menu grid is module-gated and includes:
  - attendance
  - timetable
  - notice board
  - exams
  - results
  - reports
  - guardian details
  - holidays
  - gallery
  - diary
  - transportation
  - settings
- Profile screen UI includes:
  - avatar + name
  - GR/admission number
  - school name/code
  - class, semester, stream, medium, shift
  - roll number, DOB, addresses
  - extra/custom profile fields from API.

Useful porting idea: keep `StudentProfileCubit`-style cached profile fallback. It first emits cached Hive data, then refreshes from API and updates local storage.

---

## 2. Timetable

### Key paths

- `.../e-school-saas/lib/ui/widgets/timetableContainer.dart`
- `.../e-school-saas/lib/ui/screens/childTimeTableScreen.dart`
- `.../e-school-saas/lib/cubits/timeTableCubit.dart`
- `.../e-school-saas/lib/data/models/timeTableSlot.dart`
- `.../e-school-saas/lib/data/repositories/studentRepository.dart`

### API patterns

From `Api` and `StudentRepository.fetchTimeTable(...)`:

- Student:
  - `GET student/timetable`
- Parent:
  - `GET parent/timetable?child_id=<childId>`

Model fields:

- `start_time`
- `end_time`
- `day`
- `note`
- `subject`
- `teacher_first_name`
- `teacher_last_name`

### UI ideas to port

- Default selected day is today: `DateTime.now().weekday - 1`.
- Horizontal weekday selector.
- Filter slots client-side by full day name.
- Header shows class name in a card/pill below app bar.
- Timetable cards show:
  - subject image
  - time range
  - subject name
  - teacher name
- If subject is empty, it treats the slot as break/lunch and shows a lunch icon plus `note`.
- Has shimmer loading, retry error, and `No lectures` empty state.

---

## 3. Gallery, resources, and downloads

### School gallery paths

- `.../e-school-saas/lib/ui/screens/schoolGalleryScreen.dart`
- `.../e-school-saas/lib/ui/widgets/schoolGalleryWithSessionYearFilterContainer.dart`
- `.../e-school-saas/lib/ui/screens/galleryDetailsScreen.dart`
- `.../e-school-saas/lib/ui/screens/galleryImagesScreen.dart`
- `.../e-school-saas/lib/cubits/schoolGalleryCubit.dart`
- `.../e-school-saas/lib/cubits/schoolSessionYearsCubit.dart`
- `.../e-school-saas/lib/data/repositories/schoolRepository.dart`
- `.../e-school-saas/lib/data/models/gallery.dart`
- `.../e-school-saas/lib/data/models/galleryFile.dart`

### Study material/resources paths

- `.../e-school-saas/lib/ui/screens/topicDetailsScreen.dart`
- `.../e-school-saas/lib/ui/widgets/filesContainer.dart`
- `.../e-school-saas/lib/ui/widgets/videosContainer.dart`
- `.../e-school-saas/lib/ui/widgets/otherLinksContainer.dart`
- `.../e-school-saas/lib/ui/widgets/studyMaterialWithDownloadButtonContainer.dart`
- `.../e-school-saas/lib/ui/widgets/downloadFileButton.dart`
- `.../e-school-saas/lib/ui/widgets/downloadFileBottomsheetContainer.dart`
- `.../e-school-saas/lib/cubits/topicStudyMaterialCubit.dart`
- `.../e-school-saas/lib/cubits/downloadFileCubit.dart`
- `.../e-school-saas/lib/data/repositories/subjectRepository.dart`
- `.../e-school-saas/lib/data/models/studyMaterial.dart`

### API patterns

Gallery:

- `GET session-years`
- `GET gallery?session_year_id=<id>&child_id=<optional>&gallery_id=<optional>`

Study material:

- Student:
  - `GET student/lesson-topics?lesson_id=<lessonId>&topic_id=<topicId>`
- Parent:
  - `GET parent/lesson-topics?lesson_id=<lessonId>&topic_id=<topicId>&child_id=<childId>`

Download:

- No special download API for files.
- Uses the direct `file_url` with `Dio.download(...)`.

### UI ideas to port

Gallery:

- Session year dropdown at top.
- Default session year selected from `isDefault == 1`.
- Gallery cards:
  - thumbnail
  - title
  - photo/video counts
  - SVG thumbnail support
- Details screen:
  - thumbnail hero image
  - title + read-more description
  - animated tabs: Photos / Videos
- Photos:
  - grid of images
  - tap opens `GalleryImagesScreen`
  - `PageView` with `PinchZoom`
  - download button at top.
- Videos:
  - YouTube preview using `YoutubePlayer`
  - tap routes to app video player.

Study resources:

- Topic details uses 3 tabs:
  - Files
  - Videos
  - Other Links
- File card:
  - filename + extension
  - round download/check button
  - tap opens/downloads file.
- Videos:
  - thumbnail card
  - routes to player with current video and related videos.
- Other links:
  - thumbnail or link icon
  - external browser via `url_launcher`.

Download UX:

- Before downloading, checks whether file exists in:
  - external/app document storage
  - temporary directory
- If file exists, opens via `OpenFile.open(...)`.
- If not, opens bottom sheet:
  - progress bar
  - percentage
  - cancel button
- Uses storage permission checks and `path_provider`.
- Good pattern to reuse for gallery images, study materials, submitted assignment files, and result PDFs.

---

## 4. Results / marksheet

### Key paths

Result list/detail:

- `.../e-school-saas/lib/ui/widgets/resultsContainer.dart`
- `.../e-school-saas/lib/ui/screens/resultScreen.dart`
- `.../e-school-saas/lib/ui/screens/childResultsScreen.dart`
- `.../e-school-saas/lib/cubits/resultsCubit.dart`
- `.../e-school-saas/lib/data/repositories/studentRepository.dart`
- `.../e-school-saas/lib/data/models/result.dart`
- `.../e-school-saas/lib/data/models/subjectMark.dart`

PDF download:

- `.../e-school-saas/lib/cubits/downloadStudentResultCubit.dart`
- `.../e-school-saas/lib/data/repositories/resultRepository.dart`

Online result:

- `.../e-school-saas/lib/ui/screens/resultOnline/resultOnlineScreen.dart`
- `.../e-school-saas/lib/cubits/resultsOnlineCubit.dart`
- `.../e-school-saas/lib/cubits/resultOnlineCubit.dart`
- `.../e-school-saas/lib/data/models/resultOnline.dart`
- `.../e-school-saas/lib/data/models/resultOnlineDetails.dart`

### API patterns

Offline result list:

- Student:
  - `GET student/exam-marks?page=<optional>`
- Parent:
  - `GET parent/exam-marks?child_id=<childId>&page=<optional>`

PDF marksheet download:

- `GET student-exan-result-pdf?exam_id=<examId>&student_id=<studentId>`
- Returns base64 under `pdf`.

Online result list/detail:

- Student:
  - `GET student/get-online-exam-result-list`
  - `GET student/get-online-exam-result`
- Parent:
  - `GET parent/get-online-exam-result-list`
  - `GET parent/get-online-exam-result`

### UI ideas to port

Results list:

- Tab bar: Offline / Online.
- Offline list supports pagination/infinite scroll.
- Offline card shows:
  - exam name
  - exam date
  - grade
  - percentage
- Online list supports subject filter and pagination.

Result detail / marksheet:

- Header:
  - back button
  - title
  - download icon
  - exam name card below app bar
  - parent mode also shows student name.
- Marks table columns:
  - Subject
  - Marks
  - Total
  - Grade
- Summary:
  - obtained marks / total marks
  - grade
  - percentage
  - pass/fail badge based on subject passing marks.
- Download dialog:
  - shows progress spinner
  - writes PDF to app documents under `Results/<studentId>-<examId>.pdf`
  - opens with `OpenFile.open(...)`.

Porting note: the endpoint name is misspelled in the reference as `student-exan-result-pdf`. If ASchool backend has a cleaner route, keep our API naming clean but preserve the same client flow.

Also verify which identifier our backend expects for `student_id`. The reference sometimes passes the student profile `id`, and in parent mode it derives `userId`.

---

## 5. Assignment details and submission bottom sheets

### Key paths

Assignments list and detail:

- `.../e-school-saas/lib/ui/widgets/assignmentsContainer.dart`
- `.../e-school-saas/lib/ui/widgets/assignmentListContainer.dart`
- `.../e-school-saas/lib/ui/widgets/assignmentFilterBottomsheetContainer.dart`
- `.../e-school-saas/lib/ui/screens/assignment/assignmentScreen.dart`
- `.../e-school-saas/lib/data/models/assignment.dart`
- `.../e-school-saas/lib/cubits/assignmentsCubit.dart`
- `.../e-school-saas/lib/data/repositories/assignmentRepository.dart`

Upload/undo bottom sheets:

- `.../e-school-saas/lib/ui/screens/assignment/widgets/uploadAssignmentFilesBottomsheetContainer.dart`
- `.../e-school-saas/lib/ui/screens/assignment/widgets/undoAssignmentBottomsheetContainer.dart`
- `.../e-school-saas/lib/cubits/uploadAssignmentCubit.dart`
- `.../e-school-saas/lib/cubits/undoAssignmentSubmissionCubit.dart`

### API patterns

Assignment list:

- Student:
  - `GET student/assignments`
- Parent:
  - `GET parent/assignments`

Query parameters:

- `assignment_id`
- `class_subject_id`
- `page`
- `is_submitted`
- `child_id` for parent mode

Submit assignment:

- `POST student/submit-assignment`
- multipart body:
  - `assignment_id`
  - `files`

Undo/delete submission:

- `POST student/delete-assignment-submission`
- body:
  - `assignment_submission_id`

### UI ideas to port

Important: the reference app does **not** implement assignment details as a details bottom sheet. It navigates to full `AssignmentScreen`. It does, however, use bottom sheets for upload and undo. If we want “assignment details bottom sheet” in ASchool, we can port the same detail widgets into a bottom sheet layout.

Assignment list:

- Assigned/submitted separation is client-side based on `assignmentSubmission.id`.
- Cards show:
  - subject image
  - assignment name
  - created date or status chip
  - instructions preview
  - subject name if no subject filter selected
  - due date
- Sort/filter options:
  - due date latest/oldest
  - assigned date latest/oldest
- Status mapping:
  - `0` => in review
  - `1` => accepted
  - `2` => rejected
  - `3` => resubmitted

Assignment detail content worth porting:

- assignment name
- subject name
- due date
- resubmission extra days note
- instructions
- reference materials with download buttons
- submitted files / “My work”
- possible points or earned points
- teacher remarks/feedback

Submission button logic worth porting:

- Parents cannot submit.
- Accepted/in-review/resubmitted submissions hide upload.
- In-review + due date not passed allows undo/reupload.
- Rejected submission allows reupload only if `resubmission == 1` and within `extra_days_for_resubmission`.
- If due date passed and no resubmission window, hide upload.

Upload bottom sheet:

- multi-file picker
- dotted “Add files” area
- selected file list with remove button
- submit button shows upload percentage
- cancel upload on close/pop.

Undo bottom sheet:

- confirmation warning
- blocks close while request is in progress
- returns result to parent screen and can reopen upload sheet.

---

## 6. Teacher mark entry — present in staff app

### Key paths

Teacher result/mark entry:

- `.../eschool-saas-staff/lib/ui/screens/teacherAcademics/teacherExamResultScreen.dart`
- `.../eschool-saas-staff/lib/cubits/teacherAcademics/exam/submitExamMarksCubit.dart`
- `.../eschool-saas-staff/lib/cubits/teacherAcademics/exam/examCubit.dart`
- `.../eschool-saas-staff/lib/cubits/studentsByClassSectionCubit.dart`
- `.../eschool-saas-staff/lib/data/repositories/studentRepository.dart`
- `.../eschool-saas-staff/lib/data/models/exam.dart`
- `.../eschool-saas-staff/lib/data/models/studentDetails.dart`
- `.../eschool-saas-staff/lib/utils/api.dart`
- `.../eschool-saas-staff/lib/app/routes.dart`

Related staff offline result viewing/download:

- `.../eschool-saas-staff/lib/ui/screens/offlineResult/offlineResultScreen.dart`
- `.../eschool-saas-staff/lib/ui/screens/offlineResult/widgets/studentOfflineResultContainer.dart`
- `.../eschool-saas-staff/lib/ui/screens/offlineResult/widgets/studentResultSubjectMarksBottomsheet.dart`
- `.../eschool-saas-staff/lib/ui/screens/offlineResult/widgets/downloadStudentResultDialog.dart`

Teacher assignment review, also useful:

- `.../eschool-saas-staff/lib/ui/screens/teacherAcademics/teacherManageAssignmentScreen.dart`
- `.../eschool-saas-staff/lib/ui/screens/teacherAcademics/teacherManageAssignmentSubmissionScreen.dart`
- `.../eschool-saas-staff/lib/ui/screens/teacherAcademics/teacherEditAssignmentSubmission.dart`
- `.../eschool-saas-staff/lib/data/repositories/assignmentSubmissionsRepository.dart`

### API patterns

Teacher mark entry endpoints from staff `Api` and `StudentRepository`:

Fetch completed/unpublished exams:

- `GET teacher/get-exam-list?status=2&publish=0&class_section_id=<classSectionId>`

Fetch students for mark entry:

- `GET teacher/student-list?paginate=0&status=1&class_section_id=<id>&class_subject_id=<id>&exam_id=<examId>`

Submit marks:

- `POST teacher/submit-exam-marks/subject?exam_id=<examId>&class_subject_id=<classSubjectId>`

Body:

- `marks_data`
  - list of `{ student_id, obtained_marks }`

Assignment review APIs:

- `GET teacher/get-assignment-submission?assignment_id=<assignmentId>`
- `POST teacher/update-assignment-submission`
  - `assignment_submission_id`
  - `status`
  - optional `points`
  - optional `feedback`

### Teacher mark entry UI ideas to port

- Top app bar + filter background.
- Filters use bottom sheets:
  - class/section
  - exam
  - subject/exam timetable entry
- Defaults:
  - first class
  - first exam
  - first subject in exam timetable
- Fetch students only after all required filters are selected.
- Student list table:
  - serial number
  - student name
  - marks input
- Marks input:
  - decimal-capable numeric formatter
  - prefilled from existing `student.examMarks` matching selected exam timetable ID.
- Validation before submit:
  - every student must have marks
  - marks must be numeric
  - marks cannot exceed subject total marks
- Sticky bottom submit button.
- Submit shows spinner and snackbars for success/failure.
- Only uses completed exams with `publish=0`, which is a good safety rule to prevent editing published results.

---

## Highest-value ideas to port first

1. **Download service pattern**  
   Reuse `DownloadFileCubit` + bottom sheet style for resources, gallery files, assignment files, and PDFs.

2. **Result marksheet UX**  
   Port the offline result detail table plus PDF download/open flow.

3. **Assignment detail model**  
   Keep full assignment state: due date original, formatted due date, points, resubmission, extra resubmission days, submission status, feedback, submitted files.

4. **Teacher mark entry flow**  
   Port class/exam/subject filters, prefilled marks, validation, and `marks_data` bulk submit.

5. **Module-gated student home**  
   Use school configuration/modules to show/hide dashboard cards and menu items instead of hardcoding features.

6. **Profile refresh/cache behavior**  
   Emit cached profile immediately, refresh in background, update local storage.

