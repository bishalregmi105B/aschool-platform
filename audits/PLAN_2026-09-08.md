# IMPLEMENTATION PLAN — 2026-09-08
**Basis:** `audits/AUDIT_2026-09-08_VERIFICATION.md` (this pass) + `W5/W5_B`, `W5/W5_C` defect registers + `research/ROADMAP_V5_2026-09-05.md` (phases unchanged, still in force).
**Principle:** stop the active breakage first, then the two live P0s, then correctness P1s, then the carried V5 roadmap. Nothing in Waves R/F is new scope — it is all pre-identified, evidence-located work.

Sequencing overview:

```
R green-CI + P0s  ──►  F correctness P1s  ──►  Q quality P2/P3  ──►  I infra/deploy  ──►  V5 carried roadmap (A′→T→W→D/E/F)
   (1–2 d)              (2–3 d)                  (2–3 d)               (1 d + owner)        (unchanged V5 order)
```

## Wave R execution ledger (2026-09-08) — ALL 4 ITEMS DONE ✅

| ID | Status | What landed |
|---|---|---|
| R1 | ✅ | **WIP finished (decision: complete, not park).** `models/__init__.py` imports textbook/curriculum_graph (+QuestionSubpart/RubricStep) — `metadata.sorted_tables` clean. New migration `b8f2c7d1e6a3` (10 tables + 10 question_bank_items columns + FKs; folds in 3 pre-existing drifts: qb subject/class/created_by FKs, `document_chunks.source_type` enum→String, `teaching_misconceptions` index name). Verified: fresh-DB up (57 revs) → down → up all exit 0; `flask db check` shows ZERO diff on new objects (remaining diffs are the known D-03 naive-timestamp backlog = Q20). Hygiene: CurriculumContextBuilder school-scoped (A19), Preeti duplicate `;I` entries removed, ingest script `--corpus-dir` arg. Preeti tests 6/6. |
| R2 | ✅ | A1 P0 fixed: `moderate()` unpacked correctly `(severity, category)`; flag now carries `source_type="ai_teacher_message"`, real `source_id` (flush before use), `student_id`, real severity/category. Regression test `test_webhook_self_harm_flag_created` passes. |
| R3 | ✅ | B1 P0 fixed: `list_students`/`get_student` → `role_required(superadmin,school_admin,teacher,staff)`; `default_password_hint` removed from `Student.to_dict()`; new admin-only `POST /students/<id>/reveal-default-password`; student detail page shows reveal-once button instead of the hint; dead `PW:` displays removed from parents/users pages. 5 tests in `test_r_wave_p0_fixes.py` pass. |
| R4 | ✅ | B2 fixed fail-closed: live-JWT-with-missing-user-row now 403 (was pass-through); NULL-school tenant users still denied; non-UUID `school_id` claim ("reports") UUID-guarded — no more 500. 3 tests pass. |

**Verification status:** plugin_doctor 49/0/0 ✅ · frontend tsc clean ✅ · jest 47/47 ✅ · full backend suite run recorded in the R-wave commit message.

---

## Wave R — Release blockers (do first, ~1–2 days)

| ID | Item | Evidence | Fix direction | Accept |
|---|---|---|---|---|
| R1 | **Green CI: complete-or-park the uncommitted curriculum-ingestion WIP** | `models/__init__.py:92-93` lacks textbook/curriculum_graph imports; `question_bank.py:45-46,198` FKs → `NoReferencedTableError` on `metadata.sorted_tables` (repro'd) | Decide (owner, see §Decisions): (a) finish — import both models in `models/__init__.py`, add migrations for all 10 unmigrated tables + 12 question_bank columns, add tenancy scoping to `curriculum_context_builder.py`, dedupe Preeti keys, config-ize corpus path; or (b) park — move WIP to a branch, revert `question_bank.py` FKs. Either way CI must be green again | `pytest --co` clean AND fresh `db.create_all()` in a scratch DB succeeds AND `alembic revision --autogenerate` emits empty diff |
| R2 | **A1 P0: self-harm moderation dead** | `ai_teacher/routes.py:700` (`category, _ = moderate(...)` — swapped), `:705-711` (`ModerationFlag(tool_key=…)` non-existent column, missing NOT NULL `source_type/source_id`, no `student_id`) | Unpack `(severity, category)`; construct flag with `source_type="ai_teacher_message"`, `source_id=message.id`, `student_id`; keep webhook from 500ing on flag write | Regression test: a "kill myself" student utterance through `apply_event` produces a `ModerationFlag` row with severity critical and correct student; webhook returns 200 |
| R3 | **B1 P0: student roster + credential leak** | `api/v1/students.py:27-29` (no `role_required`), `models/student.py:192` (`default_password_hint` in `to_dict`) | Add `role_required("school_admin","teacher","staff")` (align with sibling endpoints); drop hint from `to_dict`; expose only via an explicit admin-only reset-password action | Any student/parent token on `GET /students` → 403; `to_dict()` output contains no password material (unit test) |
| R4 | **B2 P1: NULL-school tenant bypass** | `app/__init__.py:434-447` — `school_id IS NULL` non-superadmin passes | Deny when `user.school_id is None and role != superadmin` before the match branch | Test: NULL-school non-superadmin + any subdomain/header → 403 |

## Wave F — Correctness P1s (~2–3 days)

| ID | Item | Evidence | Fix direction |
|---|---|---|---|
| F1 | A2 curriculum context crash — 16/41 tools 500 with a seeded framework | `tool_handlers.py:27-33` uses helper fn `CurriculumUnitActive` as model | Import real `CurriculumUnit`; test with a seeded framework row |
| F2 | A3/A6 migration completeness (the D-02-style debt) | 10 tables migration-less; `question_bank_items` missing `asset_id/textbook_section_id` vs model | One migration chain: textbook ×5, curriculum ×3, subparts/rubric_steps, ALTER question_bank_items, stamp; plugin `hooks.activate` becomes additive-only |
| F3 | A4 catalog seed drops 8 columns | `workbench_seed.py:890-902` | Persist `trigger_phrases/ui_type/icon/badge/budget/grounding/sort_order/output_document_type`; verify prod rows update |
| F4 | A5 lesson stop never reaches the service | `routes.py:497-517` | Call `service_client.stop_lesson` best-effort before local flip; test asserts the HTTP stop call |
| F5 | B3+B13 GPS: Haversine typo + dead push roles | `gps_processing.py:150`, `:176-182` | `lon2 - lon1`; map roles to enum (`school_admin`, `staff`); unit-test distance vs known coords |
| F6 | B4 compliance report enum crash | `report_generation.py:379` (also :301) | Use real enum members; guard invalid values |
| F7 | B5 per-task `create_app()` | `website_live_sync.py:38-41,251-254` | Reuse worker app context (`ContextTask`) |
| F8 | B6 float money in fees | `fees.py:2425-2433,2798-2800`; `utils/money.py` dead | Route scholarship + IRD VAT split through `money.py` Decimal helpers (prereq for D-06) |
| F9 | B7/B8 tenancy integrity | no composite FKs anywhere | Add composite FKs `(school_id, parent_id)` on hot tables (receipts, attendance, students) — expand-phase only |
| F10 | A9+A10 webhook tenancy + idempotency | `routes.py:641-644`, `apply_event :652+` | Derive school from the authenticated key and compare; pre-check `(lesson_id, event_id)` before applying |

## Wave Q — Quality / security P2–P3 (~2–3 days, parallelizable)

AI: **Q1** consent `scope` enforced (A7) · **Q2** `embed()`/`transcribe()` through `_check_quota` (A11) · **Q3** hub returns metadata → `prompt_sha256`/`fallback_used` populated (A12) · **Q4** tutor turn/close restricted to session's student (A14) · **Q5** homework-help role+consent; IEP pseudonymization + guardian consent (A15 remainder) · **Q6** unknown `context_builder` raises at seed/startup, add `context_attendance` (A17) · **Q7** `trigger_phrases`/`failure_modes` Text→JSONB migration (A18/B20) · **Q8** PD seed single-write (A25) · **Q9** `finish_reason="length"` handling (A20).

Backend: **Q10** `date.today()` → Asia/Kathmandu school-date helper (35 sites, B11) · **Q11** attendance bulk prefetch (B14) · **Q12** `permissions.py` wire-in or delete (B15) · **Q13** orphan services wire-or-delete (B16) · **Q14** faq/hostel → `BaseModel`/`SchoolModel` (B17) · **Q15** `UniqueConstraint(school_id, student_id)` (B19) · **Q16** timetable paginate (B21) · **Q17** request-ID middleware all-envs (B22) · **Q18** drop module-level celery context push (B23) · **Q19** last 5 silent `except: pass` (B24) · **Q20** naive `DateTime` → TIMESTAMPTZ sweep (B10 = D-03 contract phase).

## Wave I — Infra / deploy (1 day + owner actions)

- **I1** `deploy.yml` ordering: migrate BEFORE `up -d`, drop `git reset --hard` (P-03, carried) — currently the next deploy can re-create the drift this audit found.
- **I2** Real TLS (S-10): certbot or Cloudflare Origin CA; today prod serves a **self-signed** `brighternepal.com` cert (fine only while fronted by CF Full-mode — verify the SSL mode).
- **I3** Backups remainder (P-02): encryption at rest, uploads sync, one restore drill.
- **I4** Observability remainder (P-04): `/metrics`, wire orphan request-ID middleware everywhere (pairs Q17).
- **I5** Entitlement repair (P-05): close B1 self-register-enterprise path, `reconcile_plan_plugins` task.
- **Owner:** ① rotate the shared Milan Groq key to a dedicated key; ② decide R1 finish-vs-park; ③ confirm CF SSL mode for I2; ④ unrelated: `vexel` stack on the same VPS is crash-looping.

## Wave V — Carried V5 roadmap (order unchanged from `ROADMAP_V5` §6)

Nothing here was started; pick up exactly where the ledger left off:
1. **C″ gate** — full install→lesson→webhook→reconcile E2E against the real ATeacher service (needs its MySQL/eventlet env) + red-team pass (AW-06, founder launch blocker).
2. **T wave-3** (unblocked: writer blocks landed) → deck engine/PPTX native export → wave-4; Sahayatri enum widening + live quiz + Jaccard dedupe.
3. **A′ remainder** — 12 remaining native `confirm()`, fees POS shortcuts, student homework submit, pagination kit ×13, error states ×68, real RBAC editor, exams `[id]` 404, `/pricing` route.
4. **W** — `dashboard_layouts` + dnd edit mode, per-widget `config_schema` popovers, W-02/03/04 website builds, template packs, `events.yaml` contract, `mobile.yaml`.
5. **D/E/F** — nepal_curriculum build, Nepal money pack (after F8), mobile release blockers (FCM/.arb/offline), flutter_admin stub kill.
6. **A-01 remainder** — cost-based atomic quota reservation, per-user quotas (with Q2); **A-03 remainder** — blueprint marks-sum validation, designer PDF/OMR, question-bank UI; **AW-05** library CRUD, **AW-07** IEP routes, **AW-12** student/parent pages, golden-set evals.

---

## Plugin-ecosystem waves (from `audits/PLUGINS_ECOSYSTEM_2026-09-08.md`)

### Wave S — security (DONE 2026-09-08 ✅)
All 9 findings fixed + 15/15 regression tests green. See the ecosystem doc §3 for the full ledger. Remaining security debt (not urgent): Fernet migration for secret envelopes (itsdangerous signing → real encryption), webhook nonce store.

### Wave P — plugin-surface completion (next, ~3–4 days) — makes the ecosystem actually usable
| ID | Item | Why first |
|---|---|---|
| P1 | **Fix the 4 broken widget bindings** (W2/W3/W4 server keys; W1 exams deep-link → `/dashboard/exams/results?exam=$.id`) + W9 role alignment | Users see broken cards today |
| P2 | **Render row_actions/bulk_actions in TablePanel/List renderers** (confirm dialog → POST → refetch) + W6 filter bar + server pagination consumer | Approval queues have backend, zero UI |
| P3 | **Mount `student_profile.tab` WidgetSlot** on students/[id] with student context + mount `plugin_page.main`/`.header` generically | 3 shipped widgets dead on web |
| P4 | **Per-school widget config merge**: endpoint merges `SchoolPlugin.config` → widget `config`; pass `config` prop through PluginWidgetHost; consume `default_layout` (hide/reorder) | The "effective ecosystem" ask |
| P5 | Engine correctness batch: E2 wire `register_plugin_events` from manifests + emit school filter; E3 pass school_id to hooks; E4 enforce `requires_permissions`; E6 `is_deleted` on gate+install; E7 catch-all on blueprint mount; E8 widget-cache reset | False contracts + latent bugs |
| P6 | Install `billing_cycle` validation + no-trial-for-free (E5); config-write AuditLog (E10); `schema_for_role(installed=…)` (E11) | 500s + audit gap |

### Wave M — module correctness (after P, ~2–3 days)
M2 whatsapp send-bulk recipient validation + persistence · M3 payroll status-transition whitelist · M4 fees paid-so-far = SUM(receipts) · M5 attendance partial unique index + upsert · M6 conference slot guarded UPDATE · M8 signed pickup QR · M9 GPS device-key realm · M10 exams GPA weighting + explicit skipped-count · M11 strip or emit the 20 dead `events.emits` claims · wellbeing mood enum. (M1 = A9 webhook tenancy, already in F-wave.)

### Wave F — frontend UX completion (parallel with M)
W10 bs_date + i18n chrome · W11 form/drawer renderer or validator rejection · W12 multi-series charts · W13 config popovers · W14 core-actions always render · W15 sidebar retry+fallback · W16 error states for 51 pages, DataTable adoption.

## Bookkeeping

- Commit `audits/` (this folder) and resolve the `audits/ → audits_old/` deletion staging in git.
- Doc drift to correct in the V5 ledger next revision: backend test is `test_theme_parity.py` (not `test_theme_baseline.py`); "23 widgets" → 22 manifest entries (W5-D); ~12 native confirm() remain (not 17).
