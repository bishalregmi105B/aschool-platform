"use client";
// Registry launch target for /dashboard/academics/class-sections (see
// AOSRouteTable "academics__classes"/"academics__class-sections"): the hub
// reads the window path and opens the Classes & Sections tab (plan 33:
// subroutes deep-link INTO a tab). Wave A removed the standalone page.
import AcademicsPage from "../page";
export default AcademicsPage;
