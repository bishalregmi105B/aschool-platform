"use client";
// Registry launch target for /dashboard/academics/subjects (see
// AOSRouteTable "academics__subjects"): the hub reads the window path and
// opens the Subjects tab (plan 33). Wave A removed the standalone page.
import AcademicsPage from "../page";
export default AcademicsPage;
