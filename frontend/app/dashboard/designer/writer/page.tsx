"use client";

/**
 * /dashboard/designer/writer — now the TipTap v2 writer.
 * The legacy contentEditable writer is retired; old {type:"writer", html}
 * documents are auto-upgraded on open by the v2 editor.
 */
export { default } from "../writer2/page";
